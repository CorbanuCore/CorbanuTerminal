# Isolated execution — rootless engineering assignment

September12 22:30UTC. Routine manager infrastructure inside the existing delivery
control workstream, not a fourth initiative or a qualified functional executor.
Product: **Internal delivery control — TO BUILD**, manager-owned exact allocation
and continuation. [Policy](../../code-blind-functional/isolated-execution.md)
remains unchanged; Linux tests do not bypass denied native Mac interactions.

Manager read the complete pilot audit and actual RTX machine handoff. Ubuntu
host has installed bwrap0.11.1, Python3.14.4 and delegated user cgroup metadata.
Actual synthetic sandbox process and child passed read-only packet, separately
attested namespace invisibility, host-loopback denial, opaque fixture IPC and PTY
controls. No Linux VM, separate service identity, seccomp or complete cgroup
cleanup is yet established. System Python has no observed Slack SDK distribution.
These are implementer-run engineering observations, not policy1.7 qualification.

## Concrete next assignment

Manager accepts rootless RTX as an engineering-only alternative to the unverified
VM assumption. This does NOT accept equivalence to a clean VM/service separation
or permit user-ready claims. Existing worker James owns five private runner files:
guest-policy.sh, probe.py, fixtures.json, test_boundary.py and README.md under the
already-proposed isolated-execution-runner-v1 private coordinator directory.
Target525/hard650 added+deleted lines, including documentation/tests, excluding
verbatim generated evidence. The prior nine-file proposal is not also allocated.

Implement and prove fixed minimal mounts/empty environment, no inherited host
descriptors/capabilities, new namespaces, no-new-privileges, validated fail-closed
seccomp and unique owned user-run cgroup CPU/memory/PID/time limits. Test actual
timeout and detached-child cleanup, cross-run/source/fd/socket escapes and actual
positive PTY/packet/opaque synthetic IPC controls. Controller attests negative
targets exist; namespace invisibility is not mislabeled EACCES or firewall proof.
Do not mount product source to manufacture a denial. Retain all failed attempts.

Remote fixtures use a new mktemp directory under the existing user's writable
temporary/cache area, recorded with exact path/UID before mutation. Only a NEW
owned delegated user cgroup may be changed/stopped, never the user slice, parent
cgroup, PF13 source/lease/workload, media service or an unowned PID. If ownership,
controllers, seccomp or cleanup cannot be enforced, fail before workload and
report the concrete manager prerequisite. No sudo, host sysctl/AppArmor/nft edit,
package install, VM start, credential access or security-denial workaround.

No run.py/service.py/receipts.py/service-profile.json, model client/new inference
broker, real Slack/native product cases, shared source/checker/CI or PF81 changes.
Return frozen five-file diff/hashes, effective policies and literal engineering
proof before manager review. Manager authorizes one independent boundary review
and one necessary corrective pass, preserving prior Slack reviews01–08 separately.

## Remaining manager gate

Accepted product/runtime packaging, opaque service separation and an actually
confined fresh reasoning executor/tool-and-inference route remain separate
prerequisites. An unrestricted agent instructed to use SSH is not that executor.
No product acceptance run starts until those controls are enforced and checked
by the actual independent executor/children and separate evidence reviewer.
Slack authentication/phone/native ACK/overnight reception remain unqualified.
No new human product decision is required for this engineering increment.
