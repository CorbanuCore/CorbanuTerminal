# PF-83 native exit evidence — corrected by pf83-exit-61

This supersedes the earlier ten-line F10/F11 summary, including its overbroad packaged-binary prerequisite for picker/route navigation. Historical returns remain unchanged. Scope: source-informed native supporting evidence at `8ad587541e45e519dc0b5181ac5be36ecaf90b13`, not independent frozen-case acceptance or permission to retire any case. The frozen original `/Volumes/CorbanuDrive/Corbanu/.codex-work/permission-transition.eocbhB/frozen-functional-cases.md` is unchanged (SHA-256 `c97bf701cd7aff2e26f08884f35dbc9a4fc33b1eef14eee6328d2ea1f1411726`).

## Picker and route navigation: what is native today

The TUI native fixtures can open permission/model pickers, render labels/current markers, send `KeyEvent` arrows and Enter, follow consent/reasoning popups, and inspect the emitted exact provider/model and permission events. Those checks do **not** require a packaged binary or real inference. Existing fixtures below cover them; this revision reruns them in a focused lane instead of adding duplicate tests. Synthetic route identities qualify local UI identity/event plumbing only, not actual supported-route authority.

- `chatwidget::tests::permissions::permissions_selection_requests_full_access_after_consent` and `permissions_selection_requests_restriction_without_optimistic_mutation`: widget key events traverse the permission picker/consent, emit the intended confirmation request and leave current permission config unchanged. `permissions_selection_requests_change_without_optimistic_history`, `permissions_selection_requests_confirmation_when_current_is_selected`, and `permissions_full_access_consent_does_not_claim_application` cover adjacent selection paths.
- `chatwidget::tests::popups_and_settings::model_picker_dismisses_after_selecting_openrouter_model_without_effort_choices`: Right/Down/Enter navigation, selected model event, reasoning-popup path and dismissal. `model_picker_same_slug_marks_only_exact_provider_current`: only the exact provider/model pair is labeled current.
- `chatwidget::tests::custom_model_identity::custom_model_routes_are_distinguishable_before_selection`: two synthetic same-slug routes render distinct provider labels; Down/Enter selects `qa-b` and emits exact `OpenReasoningPopup`, `UpdateModelSelection` and `PersistModelSelection` provider/model values. Emitting the persist event is not proof of writing or restoring a real route.
- `app::permission_confirmation::tests` exercises embedded request/outcome correlation, refusal during pending confirmation, failure/uncertainty, saved-default nonmutation and stale completion handling. These are separate fixtures, not a single picker-to-real-route-to-command workflow.

A combined **synthetic** model-switch-while-permission-pending test is also expressible through these native widget/app events. It is not added here: existing tests already establish the local navigation claim under correction, while another synthetic combination would not satisfy F11's actual-route F03/F04/F05 requirement. That combined native race remains an admitted test gap, not a packaged-binary limitation. Likewise, native Escape/cancel traversal and a safe user-visible failure/retry sequence can be added; this revision does not claim them from success-path or injected-outcome tests.

Actual endpoint identity, successful inference/tool effects per exposed supported route, pending-switch behavior on those routes, and the complete human-observable workflow still need mediated inference and route receipts. They are not intrinsically impossible in a native integration harness: the current fixtures supply scripted inference, not that mediation. Independent exact-package real-key execution and evidence review remain the functional acceptance gate; a packaged binary alone supplies neither route mediation nor a pre-application barrier.

## Eleven-case native record

Each row names both the qualified subset and the residual against the original human case. “Partially qualified” is not a full case pass. Historical F01/F02 packaged results are preserved in the sprint ledger and are not relabeled as execution of this frozen tree.

- **F01 — Partially qualified:** idle next-turn upgrade runs a real outside-workspace probe with zero approvals (`thread_settings_confirmation_next_turn_tightening_and_loosening`); native picker consent/request and explicit outcome tests support the UI distinction. Residual: one end-to-end current-package real-key run joining selection, pending display, confirmed state and actual probe, including an understandable basis for any independent prompt.
- **F02 — Partially qualified:** the same native probe fixture gates/declines a fresh restricted turn with zero protected effects; embedded TUI confirmation defers submission while pending without changing config optimistically. Residual: a single current-package workflow proving pending-window submission and post-application effects together; native UI deferral and backend file effects are separate tests, not a proved in-flight cross-layer race.
- **F03 — Partially qualified, narrower closure:** native retained-authority/continuation fixtures and the confirmed-input hint establish old-turn retention and latest-authority deferral; `/status` tests pin an unconditional `Next turn:` label. The earlier display-truthfulness closure removed the false implication that next-turn Full Access described running work; it did not add a captured-running-authority display or equality/mismatch signal. Residual: full active-turn user-visible continuation on the package; conditional mismatch visibility lacks server-grounded captured authority/lifecycle input and is a separately authorized follow-up, not proven by a busy flag.
- **F04 — Partially qualified, post-Applied form:** `thread_settings_confirmation_f04_restricts_work_steered_after_applied` proves no unapproved write after Applied, allowing gating or refused/deferred admission; the inject-items and Core admission fixtures cover further paths. The implementation now defers newly admitted work to a turn with latest authority, preserving already-running work. Residual: original selection/pending-application interval and pre-requested-but-not-started admission ordering, plus current-package keys/display; the native F04 test's historical “pending product decision” comment predates the recorded re-binding ruling and is not the present decision status.
- **F05 — Partially qualified:** separate retained-approval accept/decline fixtures hold the original request through Applied and a later round trip; selection writes zero, decline writes zero, explicit accept writes once, distinct next work writes once without approval, with request/effect counts against replay. Residual: picker/approval-overlay interaction, communicated approval scope and understandable disposition in one workflow; the narrower native form covers retained explicit decision, not every cancellation/replacement option allowed by the original.
- **F06 — Partially qualified:** both real started/finished barriers preserve the once-only in-flight operation and its old-authority continuation while a separate new turn uses the selected authority (`thread_settings_f06_f09_inflight_*`). Residual: original separately requested probe during the transition connected to the UI and intelligible partial effects/continuation; continuation is the tested product branch, not proof of every permitted interruption branch or retroactive revocation.
- **F07 — Partially qualified:** both rapid RPC orders end with a matching real probe; separate TUI tests visibly refuse conflicts and reject an old completion while a newer accepted selection is pending. Residual: one integrated real-key sequence including an open confirmation, UI-settled state and probe; reverse buffered RPC consumption plus injected stale completion is composition, not a live transport reorder. The vacuous post-settlement block removed in round 57 is not claimed as an independent stale-ID proof.
- **F08 — Partially qualified:** native terminal failure/timeout/disconnect correlation, explicit failed/uncertain/unsupported messages, and unchanged config/overrides are tested; an embedded missing-profile request is rejected while prior fresh-session authority survives. Residual: offered cancellation from each effective level and a benign failure reached through exposed user controls, each followed by a real probe and understandable retry/recovery. Escape/cancel navigation is natively expressible but not executed here; injected errors or a programmatically missing profile do not close that original exposed-behavior case.
- **F09 — Partially qualified:** bidirectional F06/F09 fixtures prove once-only completed work, old continuation and new-turn authority; native Core/TUI fixtures additionally preserve deferred input/schema and disclose holding until completion. Residual: visible completed context and continuation/stop/new-session navigation as one workflow, and resistance to conversation wording replacing chosen permissions. The earlier closure was command-count/authority continuity, not all user-visible context or prompt-driven behavior.
- **F10 — Partially qualified:** actual server shutdown/new process/same-profile thread resume covers settled restricted, settled Full Access and unanswered command approval; settled restricted resume must equal the exact pre-restart approval/sandbox pair, Full Access may truthfully reset, and no automatic probe/replay is inferred. The unanswered-approval branch checks non-Never policy and a declined/zero-effect probe, not equality of the whole resumed authority pair; partial policy/network escalation in that branch is an additional native assertion gap. Residual: original (c), accepted changes pending before Core application in both directions, needs an observable pre-application barrier plus controlled interruption/resume of that same thread; current fixtures lack that seam. Ordinary TUI restart/recovery explanation and exposed interruption paths remain unexecuted. The renamed TUI fixture proves request-time nonoptimistic application/persistence only; withholding Applied delivery and fresh-config reload do not prove pending-change recovery.
- **F11 — Partially qualified for local navigation only; route neutrality unqualified:** native picker tests cover route labels/current markers/key navigation and exact selection events. No F03/F04/F05 repetition against actual exposed supported routes is built in these scripted-inference fixtures; the missing capability is credential-isolated mediated inference with verifiable route receipts and real effects for each available route (at least two if available), plus supported switching during pending permission change. Combined synthetic switching remains expressible but untested as disclosed above; neither mock labels nor successful navigation qualify actual-route neutrality.

## Exit and freeze

A legitimate F10 native increment must establish the barrier in both directions, preserve pre-change effect/request counts, restart the actual server, inspect reported authority and run a fresh protected-work probe without inferred completion, approval or replay. Full F11 native integration is possible once actual-route mediation exists; complete independent functional acceptance still uses the exact package. No missing requirement is waived, no new executor refusal is inferred, and no case is retired by this record.

The [production freeze](pf83-exit-61-production-freeze.md) lists every PF-83-touched production/contract file with its current digest and per-file final-for-sprint verdict, derived from Git rather than prior returns. The [return record](pf83-exit-61-return.md) gives current commands, nonzero counts, raw logs, limitations and changed lines. The manager must link these records and retain the remaining independent review/functional gates; this worker cannot edit the sprint outside its write scope.
