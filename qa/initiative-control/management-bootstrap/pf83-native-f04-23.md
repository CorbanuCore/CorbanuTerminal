# pf83-native-f04-23 — native confirmation fixtures

- Allocation digest: `732a50ba8de7f9e200772c73c4020334bd540133a1bdf938123f9f558ac615ef`; claim `cef826cd-c211-45d5-840b-f9f8e461a175`.
- Brief SHA-256 verified: `9e6c8677686e248ac2674eb8fe9862072fdd2ee808285c5483dd702866867f80`.
- Base: `1b1ea6912ef2a8ff2d005c202fba33c9256f04a6`; worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-def015-20260916`.
- Classification: routine test-only allocation under PF-83-S01 (in_progress); no permission semantics change.
- Product heading: **Permission selection confirmation — TO BUILD**. Excerpt: “Existing active-turn approval/sandbox snapshots and pending approvals are not retroactively changed.”
- The stronger F04 assertion is explicitly requested by this allocation, not claimed to match that existing next-turn contract. This fixture tests the expectation implied by an unqualified Applied message.
- Independent functional qualification, real-key TUI, live repositories, human sign-off, release and benchmarks are not performed by this test-only allocation. The existing independent PF-83 gate remains open; no readiness claim.

## Fixtures

1. `thread_settings_confirmation_f04_restricts_work_steered_after_applied`: real app-server/Core/shell turn waits on a disposable file barrier; a restriction receives Confirmed/Applied, steering is acknowledged against the same turn ID, then the barrier is released. Scripted local inference produces a real Python write outside the workspace, inside the disposable fixture. Restricted baseline and fresh-turn replay both decline the same command and assert marker absence. The final assertion requires `(approvals, wrote) == (1, false)`. The second model request must contain the steered user input. No live inference or simulated confirmation/authorization.
2. `thread_settings_confirmation_next_turn_tightening_and_loosening`: confirmed Never/full access → UnlessTrusted/read-only → Never/full access; real writes must respectively execute without approval, remain absent after decline, and execute without approval.
3. `thread_settings_confirmation_leaves_mcp_refresh_equivalent_to_legacy`: two isolated lanes use identical settings with confirm=false/true, then reload a real local stdio MCP server from echo-only to sync-only. Compare complete before/after status responses across lanes; assert each expected tool inventory and wire outcome.
4. `thread_settings_confirmation_preserves_pending_approval`: real pending command approval survives no-op and loosening confirmations without resolution/completion or a marker write; the original request is declined and does not execute, then the next turn runs under full access. Preservation is designed by the cited product requirement. The brief's first two pending-approval descriptions name the same temporal ordering; the fixture covers unchanged and changed selections in that ordering.
5. Existing `settings_confirmation_terminal_reply_cleans_up_and_never_replies_twice` now also completes after a listener-generation change. It must emit one Uncertain result, clear the entry and ignore late completion. Uncertain-on-lifecycle-loss is designed by the same heading: “Error, disconnect and restart recovery must not claim success or retry an uncertain change blindly.” Exact map removal/drop is current implementation. Existing stale-generation/disconnect/teardown tests retained.

## Validation

- **F04 FAILS at the intended assertion**, both focused-4 and the full app-server run: actual `(0, true)`, expected `(1, false)`; zero approval requests and an actual outside-workspace marker write after Applied. Both restricted controls pass. Exact assertion: `assert_eq!((approvals, wrote), (1, false), "F04: work steered into the running turn after Applied must require approval and must not write after decline");`
- **Items 2–4 PASS** in the final full app-server run: both next-turn directions, complete MCP response equivalence, pending approval preservation, all four confirmation lifecycle tests. All six unchanged app-server MCP refresh tests also pass. Designed/current distinctions are recorded above.
- Commands below run from `codex-rs`, with `INSTA_UPDATE=no` and common suffix `--locked --offline --retries 0`. Ordinary tests use guarded `just test`, disposable profiles and native-keyring denial. No native credential prompt observed.
- Full app-server: `just test -p codex-app-server --test-threads 2` plus common suffix; exit 100, **1094 run: 1058 passed, 32 failed, 4 timed out; 1 skipped**. Log: `pf83-native-f04-23-app-server.log`. Includes intentional F04 failure; 31 other failures and 4 timeouts were not baseline-attributed or repaired. They occur in unchanged paths.
- TUI: `just test -p codex-tui permission --test-threads 2` plus common suffix; exit 100, **91 run: 88 passed, 3 failed; 4071 skipped**. All nine permission-confirmation tests pass. Log: `pf83-native-f04-23-tui.log`. Failures: Guardian snapshot gains “via ambient”; status snapshot version 0.1.38 → 0.1.42; provider-convergence integration cannot find `codex`. Snapshots unchanged. The missing-binary failure is resolved by the separate replay below; the two snapshot failures remain.
- TUI prerequisite/replay: `cargo build -p codex-cli --bin codex --locked --offline` passed (exit 0, `pf83-native-f04-23-cli-build.log`). `INSTA_UPDATE=no just test -p codex-tui --locked --offline --retries 0 --test-threads 1 -E 'test(tmux_permission_reload_preserves_active_runtime_over_saved_default)'` then passed **1/1**, 4161 skipped, exit 0 (`pf83-native-f04-23-tui-replay.log`). The first 91-test run is retained unchanged; no claim that it was green.
- Core: `just test -p codex-core --test-threads 1 -E 'test(overrides_turn_context_but_keeps_cached_prefix_and_key_constant) | test(refreshed_mcp_binding_captures_current_approval_authority) | test(mcp_elicitation_reviewer_uses_latest_runtime_authority) | test(refresh_mcp_servers_uses_latest_state_for_existing_turns)'` plus common suffix; exit 0, **4 run/4 passed; 3648 skipped**. Log: `pf83-native-f04-23-core.log`.
- Retained initial attempts: `focused.log` (filter `test(thread_settings_confirmation)`, default threads): 5 run/0 passed/5 setup failures, 1090 skipped; `focused-2.log` (filter `test(settings_confirmation)`, one thread): 9 run/7 passed/2 initialization timeouts, 1086 skipped; `focused-3.log` (filter `test(thread_settings_confirmation_f04) | test(thread_settings_confirmation_leaves_mcp)`, one thread): 2 run/0 passed/2 initialization timeouts, 1093 skipped. All filenames have prefix `pf83-native-f04-23-`, command prefix `just test -p codex-app-server`, common suffix above; exit 100 each.
- Missing stdio helper resolved by `cargo build -p codex-rmcp-client --bin test_stdio_server --locked --offline` (exit 0, `pf83-native-f04-23-helper-build.log`). New fixtures' startup allowance increased from 10 to 60 seconds; behavioral deadlines/assertions unchanged. Final focused-4 uses focused-3's command: **2 run/1 passed/1 intended F04 failure**, 1093 skipped, exit 100. Both setup failures and real failures are preserved separately.
- Formatting: `rustfmt --edition 2024 app-server/src/settings_confirmation_tests.rs app-server/tests/suite/v2/thread_settings_update.rs` before final tests; exit 0, stable-toolchain imports-granularity warning only. `git diff --check` passes. Final scope check shows only those test files and allocated QA evidence. The TUI replay generated four files under `codex-rs/tui/target/tmux-artifacts/pf55-activeruntimepermissions`; preserved at `qa/initiative-control/management-bootstrap/pf83-native-f04-23-tui-artifacts/`, and its empty generated directories removed.
- **Production/non-test Rust changed: 0 lines.** Test diff: +362/−1, all existing tests retained. This evidence record documents the allocation/results; no other non-test source edits. No commit, push, install, semantic fix, ignored test or relaxed F04 assertion.
- Brief clarification: its first two pending-approval cases describe the same ordering. Also, the stronger F04 expectation intentionally exceeds the current product's next-turn authorization contract; this is native proof of the reported expectation mismatch, not authorization to change Core semantics.
