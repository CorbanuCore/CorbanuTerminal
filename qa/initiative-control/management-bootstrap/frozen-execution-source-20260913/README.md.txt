# Frozen functional execution — supervised qualification

PF-80-S01, parent-owned private execution allocation. User explicitly requested
dispatching Fable's decisions and trying qualification now, September 13.
Receiving candidate c77123a7e9d4540a5b3ff19a5a9dc0f12848c923, not yet accepted.
This root owns run.py, fixture.py, denials.py (unchanged prior probe),
ipc_guest.py (450-second peer lifetime and in-container probe mode) and this record, plus generated
artifacts. No edits to original cases, canonical runtime or prior evidence.

First actual case is DEC-001. Preserve all 26 DEC and 20 SLK cases; starting one
does not reduce scope or pass the rest. A per-case fresh Astra High model-only
executor sees its original case, current screenshot/rendered text and same-case
action history only. It cannot call host tools or read source/auth/prior results.
The owner mediator uses the existing pinned Corbanu model transport. Actual
browser UI runs in a nonroot, network-none, read-only, seccomp-constrained Docker
container with separate PID/IPC; source/history/auth and child negative probes
must pass before inference. A separate live synthetic peer has positive liveness
checks before and after this case; cross-run IPC denial is checked before
inference only. Peer is not claimed to be a native Codex agent.

Use exact generated dashboard assets from the real export/activation/publication
path; only service installation calls are stubbed. No source/manager-state mounts.
Only owned named/labeled containers may be removed after their state is recorded.
No daemon, new dependency, public network or live Slack/Task Node effects.

Raw verdicts, failed setup/actions/timeouts, original fixture provenance and
cleanup must remain. One independent evidence review is authorized under the
existing integrator delegation, preserving prior review usage. A raw executor
pass is provisional until that review checks exact candidate and isolation.
Dynamic input cases DEC-013/015 and full Slack service/recovery qualification
need their real prescribed transitions, not replacement static smoke tests.
Off-Mac dashboard access alone is deferred by the user's earlier scope amendment.

Retained setup attempts: fixture `normal` failed on obsolete unused `freeze`
import; `normal_02` uses the same helper with that import removed and current
candidate pin. First confined attempt `attempt-doj8h9bj` stopped before inference
with `browser_denied:request_denied`: generated `health.json` lives beside the
generation, not inside it. The corrected packet includes that exact real-publisher
output. No request/network restriction was relaxed. Both owned containers were
removed; original response, packet and frozen operator remain intact.

Second attempt `attempt-h4ljv1yu` rendered the real candidate and passed the
source/auth/child denials, but its IPC helper unnecessarily launched a second
browser and hit the unchanged 128-PID limit (`EAGAIN`). No inference occurred.
The next version runs the same IPC parent/fork-child observations inside the
already-running browser container; the actual candidate screenshot and browser
process checks provide the UI positive. No resource or isolation limit increased.
