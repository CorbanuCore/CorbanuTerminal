# PF83 round 82 — evidence capture before execution

Routine internal QA preparation supporting **Permission selection confirmation —
TO BUILD**, “A submitted selection is not a confirmed change,” and “Existing
active-turn approval/sandbox snapshots and pending approvals are not retroactively
changed.” Plan `p0-security-levels.md`, sprint `PF-83-S01`; no product change.
Independent design/execution is not performed or replaced here. Internal-stage
N/A acceptance by the integrator remains pending; the later gate is admitted
independent exact-package execution and independent evidence review.

This coordinator-only supplement governs captures for the existing round-70 queue
and round-73 staging sequence. It grants no execution, guest contact, staging,
harness mutation or new scope permission. The frozen human cases are unchanged.
Do not give this source-informed document/schema to the blind executor.

## Before any case

1. Require the existing owner authorization, admitted executor/mediator, independent
   reviewer, identity, package verification and actual-executor/child controls.
   The current preflight-only harness is insufficient. A host collector is not an
   alternative executor, and a successful staging preflight is not case admission.
2. Freeze `evidence-contract.schema.json` SHA-256 with the original case hash,
   exact queue row/variant, branch, profile/repository, package/asset identities,
   admitted driver/collector hashes and attempt ID. Use a new attempt directory.
   Original case SHA-256 remains
   `c97bf701cd7aff2e26f08884f35dbc9a4fc33b1eef14eee6328d2ea1f1411726`.
3. Before keys, have the admitted collector demonstrate continuous fixture
   observation and positive liveness using synthetic witnesses. Establish the
   exact probe in restricted and Full Access calibration runs. Missing calibration
   makes authority claims inconclusive. Record a finite observation window,
   deadlines, clock origin/mapping and uncertainty, final quiescence method and
   every expected label. If timing cannot distinguish the claimed order, do not
   infer it from nearby frames.
4. Initialize `capture-identity.json` with all bindings named by the schema.
   Start raw PTY/key and independent fixture capture before pre-state observation.
   Record actual data in `capture.json` against the schema, referencing sealed
   source files by digest plus exact record/byte/frame locator. A valid reference
   is an observation, never copied expected text. The five structured sections
   are pre_state, observable, post_state, negative_control, refuting_artifact.
5. Each case has one indispensable JSONL timeline below. Its required columns
   are listed in the schema's `x-required-timeline-columns`; unavailable values
   are explicit null with a reason in the raw source, never guessed. Each row
   joins key/UI/fixture observations via `raw_source_refs` containing relative
   artifact, SHA-256 and exact locator. Preserve raw collector output and gaps.
   The timeline is derived evidence: UI entries must cite raw PTY bytes and effect
   entries must cite independent `observer.raw` bytes. Do not manufacture a
   missing event from the expected outcome.

## Case capture checkpoints

| Case | Changed capture sequence | Indispensable artifact |
| --- | --- | --- |
| F05 | Capture pending approval ID/scope and zero baseline, then every key and effect while approval is withheld. Separate fresh decline/accept or disclosed replacement attempts. Observe original-label effects through quiescence and distinct later work. A reissued operation must remain traceable, not disappear behind a new label. | `approval-effects.jsonl` |
| F06 | Observe real started marker before selection and finished/interrupted marker afterward. Submit separate probe promptly after selection while slow work is still underway; record visible queue/refusal if the UI cannot accept it. Preserve ordering uncertainty, partial effects and the stated boundary; do not wait for completion just to fit a normalized packet. | `operation-boundary.jsonl` |
| F07 | Timestamp every actual selection/disposition in both three-selection orders and the open-confirmation branch. Retain raw evidence after settlement through the frozen dwell/turn boundary for late reversion. Explicit serialization is a limitation, not an overlapping-selection pass. | `selection-order.jsonl` |
| F08 | Record the offered cancellation or exposed benign failure route before using it. Capture resolution, remaining authority and a fresh probe, then offered recovery. Separate each starting level and cancel/failure branch. Missing exposed routes are not exercised; injected G08 outcomes cannot close them. | `resolution-authority.jsonl` |
| F09 | Inventory completed labels, counts, output hashes and visible context before selection; monitor them through actual continuation/stop/finish controls, then a distinct new turn/probe. Compare old-label counts and retained context after quiescence, including deferred-request disposition. | `continuation-effects.jsonl` |

Each artifact contains the observations that could refute its claim; the schema
states the specific counterexample per case. Absence makes that case result
unusable even when generic PTY/screenshots exist. Other required evidence remains
mandatory too. A negative control needs both a positive working observer/probe
and an interval with no corresponding effect; an isolated zero snapshot is not
proof of non-execution.

## After each attempt

Stop on the existing refusal/credential/isolation/observation/budget conditions;
do not route around a refusal. Capture final independent fixture state only after
owned processes are quiescent under the admitted launcher. Preserve incomplete
attempts, exact last checkpoint, collector gaps and errors.

Seal all raw files and the five-case timeline using the existing exact-byte
inventory algorithm. Schema-check `capture.json` with a Draft 2020-12 validator;
check each timeline's listed columns, source locators/digests, interval coverage,
clock ordering, observer liveness and refuting condition against raw bytes.
Schema validation alone cannot validate a result. The independent reviewer must
do this comparison and map every original branch/queue variant to evidence or an
explicit disposition with any required product-authority acceptance.

Missing pre-state, transition, post-state, negative control or indispensable
artifact means blocked/inconclusive, never passed. Put blocked/not-exercised
attempts in `results.json` with missing artifact, exact prerequisite and coverage
gap; do not invent evidence references to satisfy the usable-capture schema.
Keep failures and repaired replays in separate directories. No whole-case pass
while an original branch remains missing without an accepted disposition.

## Current capability assessment

**All five cases cannot currently produce admitted refuting evidence.** Read-only
inspection on this round confirms `cases.json:native_dispatch=false`,
`macos_boundary.py:serve` requires `op == "preflight"`, and
`macos_preflight.py` exposes no launch/action/case admission operations.
No approved case/mediator/capture seam is supplied. These are blockers, not
permission to use historical SSH/tmux drivers.

After that shared blocker is resolved, F05 still needs actual pending-approval
navigation; F06 needs observable overlap; F07 needs an actual pending/conflict
opportunity or a disclosed limitation; F08 needs exposed cancel/benign-failure
routes; F09 needs observable continuation/new-turn context and effects.
Availability is unproven, not asserted impossible. The schema records all five
blockers. No case, guest contact or staging was performed in round 82.
