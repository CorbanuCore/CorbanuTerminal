# RETURN — pf83-rebind-45

Allocation `pf83-rebind-45`; digest `0de7bfbe1e4cba763af283a6eddab330bf75ccf070730ccfd42aa0e9ba1db88a`; claim `da068438-1ace-4f8a-9dff-2b647eb21ffd`; runtime `gpt-6-astra`, effort `high`. Frozen brief SHA-256 verified before other reads: `a0e213f31e8164617e8700acfb65fd773709a2d225c5419eac9464d9819c0f68`. Clean initial HEAD matched `44bbf0db17ac35adb00637b84a2f1300fe467eb8`; worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-rebind-20260916`, branch `bootstrap/pf83-rebind-20260916`.
Product-initiative revision under active `docs/plans/active/p0-security-levels.md`, PF-83-S01 (`in_progress`). Product heading **Permission selection confirmation — TO BUILD**, excerpt “Existing active-turn approval/sandbox snapshots and pending approvals are not retroactively changed”; the sprint's September 16 Travis ruling authorizes latest-authority admission of new work. This frozen expanded allocation governs this revision; older canonical coordinates/exclusions remain manager-owned reconciliation outside writable scope.

Public-route repair: `CodexThread::inject_if_running` delegates to existing extension admission, leaving internal code-mode output untouched. `session::tests::authorization_changed_public_injection_waits_for_later_model_turn` holds a real model stream, waits for the changed policy acknowledgement, calls the public bridge, and checks all two superseded-turn model bodies exclude the text while the third, later-turn body includes it.
Natural-completion repair: the terminal InputQueue drain now retains `(TurnInput, Option<Value>)`; `on_task_finished` carries the schema through the follow-up vector; `submit_follow_up_input` passes it into `Op::UserInput`. `session::tests::authorization_changed_natural_completion_preserves_model_output_schema` proves automatic replay after ordinary completion, old/new body separation, and exact schema equality in the later model request's `text.format.schema`.
Discriminating negative controls: temporarily restored the ungated public bridge and the replay's `None`, then ran both new tests: **2 run / 0 pass / 2 fail**, exit 100, run `e0d93a96-e4d1-45e3-a038-53356eeb3c3e`. Public-route test failed “new work entered the superseded turn”; schema test failed with actual Null versus the supplied object. Restored both fixes and formatted before final builds/tests. Earlier positive focused run: **2/2 pass**, `701568a4-3a32-4372-b176-5a522b8a44b3`. These are behavioral model-request tests within the allocated Core session test module, not only queue-shape assertions.
F04's entire containing file is byte-identical to the frozen base, SHA-256 `9552fb85f33630a83b34d7462be11c8a4491aa6d92937a9882bcca297940c62a`; no body or assertion changed. Disclosed limit unchanged: `on_task_finished`'s non-abort `Err(err)` arm records deferred input without executing it. The prior receipt remains unchanged.

Admission inventory: production callers of every InputQueue writer/drain, public CodexThread mutator, submission operation, and direct history writer were inspected. “Gated” below means latest-authority admission for new main-model work (or the stated fresh-task gate); “deliberately ungated” identifies existing-work output or a separate user/service operation, not an unexamined public injection bypass. Authority equality compares approval policy, materialized permission profile/workspace roots, approval reviewer and Windows sandbox level under active-turn/session-state locks; admission is symmetric for tightening and loosening.
| Entry path | Gate / verdict / reason |
| --- | --- |
| Human input: TUI, app-server turn/start, public submit / submit_with_trace / submit_with_id / submit_user_input_with_client_user_message_id, Op::UserInput | **Gated**: user_input_or_turn_inner → steer_input requires nonempty input, Regular task, optional expected-turn match, and authority equality. Mismatch/non-Regular defers; idle starts current context. Additional-context fragments use the same gate. |
| app-server turn/steer; public CodexThread::steer_input | **Gated** by steer_input above. Authority mismatch returns authorizationChanged with input unadmitted; no silent old-turn delivery. |
| Deferral/completion race | **Gated**: defer_user_input_until_active_turn_finished locks active turn; if task vanished, handler rebuilds current context and carries schema into a fresh task. |
| Realtime handoff and transcript-tail flush | **Gated**: realtime_conversation fanout → route_realtime_text_input → user_input_or_turn_inner; same user admission despite bypassing the submission queue. |
| Natural-completion deferred user/communication follow-up | **Gated**: terminal drain → submit_follow_up_input → Op::UserInput (now carrying schema/client id) or native communication op. ResponseItem fragments record after completion and await later inference; they do not start a turn. |
| Interrupted deferred FIFO | **Gated**: submission-loop consumer re-admits users with schema through Op::UserInput, response items through extension admission (idle records history), communications through native operation. Startup drain never splices this FIFO into the old task. |
| Peer send_message/followup_task; public send_agent_message; V2 terminal results; durable/persisted deliveries; direct Op::InterAgentCommunication | **Gated**: recipient enqueue_mailbox_communication_for_session compares authority, pins mismatched mail to old turn, and excludes it from that turn's drains, sleep and wait_agent subscriptions. Restoring old settings does not release already-deferred mail. Separate delivery-phase rules apply; mail intentionally has no Regular-only steer restriction. |
| Idle mail, WakePendingWork, capacity-resume, terminal-boundary wake | **Gated**: scheduler checks pending mail/trigger or outstanding durable sleep, idle slot and execution capacity; builds current context. Task-start drains only eligible mail. Queue-only mail normally cannot initiate idle inference; durable sleep remains the existing exception. |
| Goal extension; public inject_extension_if_running | **Gated**: InjectionSource::Extension requires Regular and authority equality for installed tasks; otherwise defers. Idle returns original items. |
| Public CodexThread::inject_if_running | **Gated (fixed here)**: now delegates to inject_extension_if_running; same Regular/equality admission. Idle returns original items. |
| Extension automatic start; public try_start_turn_if_idle | **Gated** for fresh work: rejects pending trigger mail, busy/reserved slots and Plan mode; reserves/rechecks and starts RegularTask with current default context. |
| app-server thread/injectItems; public inject_response_items | **Gated**: fresh context → inject_no_new_turn → InjectionSource::Context; distinct pointer forces Regular/equality admission. Idle records without inference. Initial context setup is generated context, not caller-item admission. |
| inject_no_new_turn with the exact captured task context | **Deliberately ungated** by pointer identity for existing-work output: active user-shell output and saved exec-prefix/network-rule messages. Fresh contexts take the gate in the previous row. |
| Internal Session::inject_if_running / RunningTurn (code-mode notify delegate) | **Deliberately ungated**: results from already-running code-mode execution remain consumable. Session and thread.session are crate-private; the public bridge no longer reaches this exemption. |
| inject_no_new_turn without context: legacy child terminal-status prefix, Guardian follow-up reminder, saved policy fragments after original task disappears | **Deliberately ungated** RunningTurn context/status paths; these carry generated status for existing work, not arbitrary public item injection. V2 terminal-result mail instead takes mailbox admission above. |
| Op::ApproveGuardianDeniedAction | **Deliberately ungated** exact-action approval message → no-context inject_no_new_turn; requires Denied status and communicates the user's explicit approval in original context. Does not implicitly approve similar actions. |
| Existing model continuation; tool/code-mode/dynamic-tool/elicitation results; UserInputAnswer, RequestPermissionsResponse, ExecApproval, PatchApproval | **Deliberately ungated** by new-work comparison: correlated captured-task response channels and normal tool approval/sandbox checks apply. Existing work and granted approvals are not retroactively revoked. |
| Hooks; plugin/skill/extension prompt contributions; context diffs; time/token/rollout reminders; assistant/tool history; review/compaction output; resumed/forked history | **Deliberately ungated** generated or loaded context for admitted work. Private record/history helpers do not re-admit each fragment; they are not public arbitrary-input routes. |
| Op::Review and Op::Compact | **Gated by a fresh task boundary**, not steered into a superseded task: handlers create current default context and spawn their task; automatic compaction is existing-task continuation. |
| Op::RunUserShellCommand | **Deliberately ungated** explicit user full-access escape hatch: SandboxType::None and disabled permission profile. Active auxiliary execution uses captured context; idle command creates standalone current context. It is separate from model-work admission. |
| Raw realtime audio/text/speech transport | **Deliberately ungated by the main-turn comparison**: separate realtime service with its own per-operation source admission. Any resulting main-model handoff takes the gated realtime row above. |
| Public call_mcp_tool / read_mcp_resource | **Deliberately ungated by main-turn admission**: direct shared-service operation via latest MCP runtime after refresh; result returned to caller, not inserted into running model history. Shared-service policy is unchanged. |
| Settings/runtime/MCP refresh, metadata/memory updates, append_rollout_items; rollback; stop/cleanup; prompt-debug builder | **Deliberately ungated / not running-turn input**: configuration/store/control operations. Rollback rejects an active turn; append_rollout_items writes persistence, not live model history. Prompt-debug builds a separate ephemeral thread and shuts it down. Later ordinary work takes its admission path above. |
| Internal reserved-slot / start_task pending-input transfer | **Gated upstream**: only prior admitted turn-local items and ready mail move into startup. Empty reservation has no captured running task to supersede; deferred/interrupted work is not moved directly into it. |
| Test-only enqueue_mailbox_communication / drain_mailbox_input_items | **Deliberately ungated test fixtures**, cfg(test), absent from production; not a shipped admission path. |

All Rust tests used the guarded `just test` from `codex-rs`, one shared dedicated `CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/.codex-work/targets/pf83-rebind-31`, `INSTA_UPDATE=no`, and `--locked --offline --retries 0 --test-threads 1`. Read test-isolation guidance first; disposable fixtures only, no live profile or native credential prompt observed. Both prerequisite builds exited 0; final `cargo build --locked --offline -p codex-cli --bin codex -p codex-rmcp-client --bins -p codex-code-mode-host --bin codex-code-mode-host` ran after restoration/formatting and before the six final lanes. Logs are local ignored artifacts beside this receipt, prefixed `pf83-rebind-45-`; common suffixes below identify exact files.

| Lane after `just test` (before common flags) | Run / pass / fail / skipped | Exit | Log suffix / run ID |
| --- | --- | --- | --- |
| `-p codex-app-server settings` | 25 / 25 / 0 / 1074 | 0 | `settings.log`; `93244eae-73e9-47d0-a7be-1a015a756315` |
| `-p codex-app-server turn_steer` | 5 / 5 / 0 / 1094 | 0 | `turn-steer.log`; `29cb0721-2f57-41f2-a135-1ec0bc77ef9e` |
| `-p codex-app-server thread_inject_items` | 2 / 0 / 2 / 1097 | 100 | `thread-inject-items.log`; `bead6dea-03c6-4c97-8f39-c673fa33d318` |
| `-p codex-tui permission` | 91 / 91 / 0 / 4074 | 0 | `tui-permission.log`; `06cb0552-74ca-483c-8725-ac112881d23b` |
| `-p codex-core --lib 'session::'` | 345 / 345 / 0 / 2141 | 0 | `core-session-final.log`; `9417034d-7a75-40f5-b266-da06b1ee9156` |
| `-p codex-core --test all -E 'test(suite::pending_input::)'` | 11 / 11 / 0 / 1167 | 0 | `pending-input.log`; `77b61ca9-28ce-4490-b4ca-8ac7d79555d2` |

Both named failures still appear: `suite::v2::thread_inject_items::thread_inject_items_adds_raw_response_items_after_a_turn` and `suite::v2::thread_inject_items::thread_inject_items_adds_raw_response_items_to_thread_history`. No retry, fixture change, investigation or attribution was performed. F04 and the existing settings injection regression pass.
Preserved earlier attempts: `regressions.log` exited 101 before tests (E0308, distinct Core config types); `regressions-corrected.log` exited 101 before tests (E0433, dependency crate name unavailable). Type inference fixed the fixture configuration. `regressions-inferred.log` ran 2 / 0 pass / 2 fail / 2484 skipped (run `5aa5852d-192a-43bc-8474-34b9bb267228`); both new named regressions timed out. `regression-timeout-trace.log` ran 1 / 0 pass / 1 fail / 2485 skipped (run `bd02d751-cbec-4e20-8092-c056e6117f0c`), public-route test only. The trace located the fixture deadlock: TestCodex::submit_turn waits for completion while the test intentionally holds the model stream. Replaced it with asynchronous submissions and matched the changed permission acknowledgement, then both passed in `regressions-async-submit.log`. Negative-control evidence remains in `negative-controls.log`; no old attempt was overwritten.
Final checks: only the four changed Rust files were formatted using `rustfmt --edition 2024 --config skip_children=true`; no workspace formatter/fix. Source hashes were unchanged through the final six lanes. `git diff --check`, writable-scope audit, plan checker (3/3 active), and sprint checker (115 current, 127 archived) pass. No pre-existing test was removed, ignored or weakened; three queue-drain expectations were adapted to the schema-preserving tuple.
Rust changed lines: **34 outside tests** (+17/−17), **200 inside tests** (+194/−6), **234 total**. This receipt adds **56 artifact lines**: **90 outside tests including documentation**, **290 total changed lines**, within the 110-line target and 180-line hard limit. Generated ignored test logs are retained separately.
Brief corrections: none found in this allocation; both previously excluded repair files are now allocated. The earlier admission inventory's broad realtime summary omitted its main-model handoff route; that explicit gated route and direct MCP service calls are included above.
Independent code/functional review, exact-package PTY/live-repository qualification, evidence review and human acceptance remain with the integrator. No commit, push, install, release or human-test-readiness claim; no additional agent dispatched.
