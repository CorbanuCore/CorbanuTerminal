# Management bootstrap — actual startup evidence

PF-80-S01, September 13. Parent owns live setup under the explicit five-part
bootstrap authority. These are bounded engineering/operator attempts, not the
independently isolated functional acceptance gate or full sprint completion.

## Fresh Fable diagnostic

Launcher candidate `627dd1f0841ac76384687de4844d6bbe0a8b9f6e`, installed Corbanu
0.1.41 from the recorded macos-candidate-final9 package. Private evidence root
`/private/tmp/cfable.msxBbt` contains credential material: never publish/copy that
directory wholesale. The three per-run homes, session records and receipts remain
private. No actual credential values are included in this record.

1. Run `f-5kgjgt1d`: 120-second timeout; session
   `01a09951-6d69-70b1-b8b4-bd20b19157ec`; no submitted model turn. Clean, unforced
   process/session shutdown. Failed evidence retained.
2. Run `f-g0pg4qy9`: 90-second timeout; session
   `01a09954-afd3-7201-8648-d321523008b5`; actual pane says provider unavailable or
   inactive. Clean, unforced shutdown. Do not repeat unchanged initialization.
3. Run `f-6lvdp9vl`: parent-operated diagnostic repaired authentication through
   the supported TUI, then resubmitted the bounded brief. Actual fresh session
   `01a0995a-da26-7d23-b340-241ccabd8883`, turn
   `01a0995c-e64b-7520-8da6-7b000d57a252`, provider response
   `msg_011Cezw2VfcGxJA1ptUWPjBk`. Full JSON decision contains three explicit wait
   actions, exact `claude-fable-5-1-plan` / `claude-plan` / `high`, with clean
   unforced shutdown at06:03:23Z. This manually assisted run does not qualify the
   automatic launcher; two corrected automatic runs remain required.

Cause: TUI provider metadata requires explicit authentication-source selection;
legacy environment-first CLI resolution alone does not configure a fresh TUI.
The source and actual UI agree. Supported sequence: `/providers`, choose Claude
Account, `r`, confirm recovery, select long-lived subscription token, supply the
already-authorized token to the verified masked field through a private stdin
TMUX buffer, confirm. Provider becomes “Enabled · configured · current”. Escape
returns to the composer. No external login, new token or Rust change was needed.
Auth entry uses separate text/paste and Enter, not token-bearing command args.
Native storage exists only in the fresh private run home; no old conversation
or user configuration was copied. Parent scanned42 regular run files afterward:
zero exact-token occurrences; this is not a claim of general secret detection.

Kepler returned27 offline tests and closed. Fresh Astra High Goodall
`01a0995e-2e26-7923-b692-38f09c53f48b` now owns the same three launcher files in
the same declared worker checkout, implementing the proven startup sequence,
failure-pane capture and review01's ordinary-vocabulary redaction correction.
No real credential access or live inference is delegated. Launcher review01
has one accepted finding, retained at
`.codex-work/bootstrap-launcher-review.u4wHuc/review01.md`; review02 is authorized
for the changed candidate. The parent owns corrected real runs and integration.

## Canonical three-workstream seed

The owner created the initially disabled coordinator store at
`.codex-work/initiative-control.oGQGyA/state/coordinator`. Source reconciliation
seeded all three workstreams and21 actual sprint/dependency records. Current
security/accounting lifecycle remains blocked for the intentional pause; delivery
is in_progress for bootstrap, but all three coordinator dispatch modes and the
global dispatch gate remain paused until qualification. No allocations, manager
cycles or native dispatch receipts were invented in that seed.

Fresh Git checks: security checkout clean899041c91, accounting clean08eaae600,
owned-ingress clean9f692f653. PF13 task “Set up and build PF13 branch” is idle.
Receiving commits855ab3382, f4507cb50 andc48dab1002 are all ancestors of current
integration HEAD1c0df3cb8. The parent checkout's untracked bootstrap work is
explicitly distinguished from those accepted product checkpoints. Existing
canonical human feed remains its own authoritative decision subsystem, referenced
at revision24/digest rather than overwritten or silently reset.

## Human/private-route prerequisite

Publisher is online; existing services preserved. HTTPS10443 attempt found Serve
disabled on the publisher tailnet. This Mac uses a different tailnet and its peer
inventory does not include productionrpc; DNS lookup failed. The browser requires
administrator authentication. Both the local SSH command and its surviving
remote Serve process were stopped; a fresh probe found no Serve configuration.

Canonical decision `bootstrap-private-route-admin` at feed24 holds only private
remote-link qualification, not engineering. Administrative Slack setup request
was delivered to the approved private channelC0C0X2ELFKR, timestamp
1789278370.283259; actual receipt is
`.codex-work/bootstrap-link-alert.Znrbsk/receipt.json`. It includes the sprint
hyperlink, background, stopped/continuing work and an answerable administrator
setup request. Remote dashboard publication of the record is pending. This is
not evidence for the full automated question/reply/decision/native-ACK chain.

## Receiving checkpoint and follow-up

The real single-writer integration job received launcher `bf1db6008` into `60d0f1ade`.
Its complete 334-test run failed 9 launcher fixture cases before application launch:
the receiving checkout path exceeded the TMUX socket limit. The failed receiving
receipt/marker remains retained; the merge is not yet verified. Parent corrects
only the fixture temp root to resolved short system temp and moves the pure
Checking regression out of the TMUX-only class, also resolving review03's P3.
No production launcher code changed; final live runs remain candidate-applicable.
One scoped test-only review04 is authorized under integrator discretion, retaining
all earlier bootstrap reviews and the failed receiving receipt.

Luna publication succeeded at `84f16ccee`, generation `build-7ldrmop1`, feed24 and
12 reference documents. Facilities HTTP200 and private Serve/web status were checked;
the publish timer is disabled/inactive after the one-off update. Full wrapper
and second source verification passed; publication does not resume product work.

Actual human Slack reply `1789281109.743799` in thread `1789278370.283259` reports the
tailnet switch. Authenticated bot API read confirms author `U0758QY0MEZ`; the Mac's
current shared-peer inventory now includes publisher `100.85.116.26`. Its shared
device has no DNSName, so name-only discovery missed it initially. Recorded as
durable coordinator event `slack-human-1789281109.743799` at revision2. Serve has
no configured route yet. Reply reading was manual; no active periodic poll or
supervised listener is claimed. Existing paused automations stay paused.

Fixture correction: 32 focused tests pass in64.685s from the receiving checkout.
Review04 confirms the code repair, with only a nonblocking evidence-spacing note
corrected here. Its original exit1 remains retained, not relabeled as clean.
No production-code or expectation change; both final actual Fable runs still
apply. The parent accepts this test-fixture correction and will reconcile the
original failed receiving marker before a new explicit combined-verification job.
