# RETURN — pf83-rebind-44

Allocation `pf83-rebind-44`; digest `77ab25ec5b4e5c1f4b0ffaf53fda31c00fc26761d64d6d982b1d3771b48b4cd9`; claim `09fa5f4c-a1dc-4c47-abff-a190cedddaf6`; runtime `gpt-6-astra`, effort `high`. Frozen brief SHA-256 verified first: `1de05ef302d46d9640c38688912d8285aaeace7b4cafb00416e0af647a6f747f`. Clean starting HEAD equals base `a802683f4b1d2c25a5770ecbd942ba663291d3bf`; worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-rebind-20260916`.
Product-initiative revision under active `docs/plans/active/p0-security-levels.md`, PF-83-S01 (`in_progress`). Product heading **Permission selection confirmation — TO BUILD**, excerpt “Existing active-turn approval/sandbox snapshots and pending approvals are not retroactively changed”; the sprint's September 16 Travis ruling authorizes latest-authority admission of new work. This frozen allocation governs this revision; older canonical coordinates/exclusions remain manager-owned reconciliation outside writable scope.
Implemented: deferred mail retains a weak reference identifying the turn state that cannot consume it. Both new subscriptions and existing turn-local waiters ignore that mail. Session-level pending visibility and eligibility for later turns remain. `deferred_mail_does_not_interrupt_turn_local_sleep` covers trigger/queue-only mail, unchanged deferral after authority restoration, no false notification, real SleepHandler completion, another-turn visibility, consumable-mail interruption, and deferred delivery after the boundary. Sleep and wait_agent share the repaired subscription.
Natural-completion schema remains a precisely identified limitation: `take_pending_input_for_turn_state` strips each deferred tuple's schema; `tasks/mod.rs::submit_follow_up_input` constructs `Op::UserInput` with `final_output_json_schema: None`. The accepted message automatically replays under current permissions, but its request schema is absent from the new TurnContext/model output-format request; the caller receives no schema-loss error and structured-output conformance is not enforced. Correct repair carries `(TurnInput, Option<Value>)` through the terminal drain, natural-completion follow-up vector and submit helper, while destructuring only the item for the unchanged non-abort error-recording branch. That requires `codex-rs/core/src/tasks/mod.rs`, outside this allocation; no hidden content marker, client-id side channel or change to failure replay was introduced.
Public route remains OPEN: `CodexThread::inject_if_running` is public and forwards to ungated `Session::inject_if_running / RunningTurn`; downstream crates holding a thread can call it. The pending-input integration suite itself exercises that API. It is not internal-only. Its one-line repair is prepared in `pf83-rebind-44-public-bridge.patch` and passes `git apply --check`, but is NOT applied: `codex-rs/core/src/codex_thread.rs` is outside scope. It redirects only the public bridge to existing extension admission; changing the shared Session method would also defer already-running code-mode notify output. Manager action: allocate the bridge file and terminal-replay file, apply/test the bridge patch and schema tuple repair, then re-review the complete admission claim.
F04's entire file is byte-identical to base. `codex-rs/core/src/tasks/mod.rs` is byte-identical to base. Disclosed limit unchanged: `on_task_finished`'s non-abort `Err(err)` arm records deferred input without executing it.

Complete model-input admission-path inventory for this tree (rows explicitly distinguish new admission, existing-work output, and adjacent APIs; the open public bridge prevents claiming complete coverage). `authorization_matches` compares approval policy, materialized permission profile/workspace roots, approval reviewer and Windows sandbox level symmetrically under active-turn/session-state locks.

| Entry / callers | Gate and disposition |
| --- | --- |
| `Op::UserInput`; thread submit/submit_with_trace/submit_with_id/submit_user_input_with_client_user_message_id; app-server turn/start and ordinary TUI input | `user_input_or_turn_inner → steer_input`: nonempty input, Regular task, expected-turn check when supplied, and authority equality. Mismatch/non-Regular defers; idle starts with current settings. Additional-context fragments follow that same admission. |
| Explicit app-server turn/steer; direct `CodexThread::steer_input` | Same Regular/expected-turn/authority gate; mismatch returns `authorizationChanged` with work unadmitted. |
| Deferral versus completion race | `defer_user_input_until_active_turn_finished` holds the active-turn lock; vanished task returns NoActiveTurn and handler builds a fresh context, carrying request schema. |
| Natural-completion user follow-up | Terminal drain → `submit_follow_up_input → Op::UserInput`; re-enters ordinary admission. Schema limitation above. Response-item fragments are recorded at completion rather than separately starting inference. |
| Interrupted deferred FIFO | Submission-loop consumer re-admits users through Op::UserInput with schema, response items through extension admission or idle history recording, and communications through their native operation. Task startup never splices this FIFO directly into the old task. |
| Peer send_message/followup_task, terminal results, persisted/durable mail, public send_agent_message, direct Op::InterAgentCommunication | All recipient delivery reaches `enqueue_mailbox_communication_for_session`; captured/latest authority mismatch binds mail to the old turn. Turn drains and turn-local waiters exclude it. Restoring old settings does not release already-deferred mail. Mail does not use the Regular-only user-steer gate; its separate delivery phase still applies. |
| Idle mail / WakePendingWork / capacity-resume / task-boundary wake | Pending-work scheduler requires mail plus trigger work or an outstanding durable sleep, an idle task slot and execution capacity; builds current default context and task-start drain admits ready mail. Queue-only mail keeps its readiness boundary and normally cannot trigger idle inference; outstanding durable sleep is the existing exception. |
| Goal extension / direct public inject_extension_if_running | `InjectionSource::Extension`: Regular plus authority equality; mismatch/non-Regular defers response items. Idle returns items unchanged. |
| Extension automatic idle start / public try_start_turn_if_idle | Pending-trigger, busy/reservation and Plan-mode gates; fresh current context; reserves and rechecks before starting RegularTask. |
| app-server thread/injectItems / public inject_response_items | Builds fresh context; `inject_no_new_turn → Context` cannot match captured task pointer, so Regular/equality admission applies. Idle records history without starting inference. Initial context setup is generated session context. |
| **Public CodexThread::inject_if_running** | **OPEN: no admission gate**, forwards to RunningTurn. Proposed bridge-only repair is unapplied. |
| Internal code-mode notify | Session::inject_if_running / RunningTurn: existing execution output intentionally remains consumable; this is the same internal method exposed by the open bridge above. |
| User-shell output; saved exec-prefix and network-rule context fragments | `inject_no_new_turn` supplied captured context bypasses new-work admission only by pointer identity; missing captured context uses the internal no-context path. |
| Generated child terminal-status session prefixes; Guardian reminders; explicitly approved Guardian-denial messages | Crate-private no-context `inject_no_new_turn / RunningTurn`; no authority comparison. Guardian approval operation validates denied status and conveys the explicit user's exact-action approval. These are existing-action/status paths, not arbitrary public item injection. |
| Existing model continuation, tool/dynamic-tool/elicitation responses, request-user-input/permission/exec/patch approval replies | Existing captured task/response channel and normal tool approval/sandbox checks; not fresh top-level input admission. Running tasks and previously granted approvals are not retroactively revoked. |
| Hooks, plugin/skill/extension prompt contributions, context diffs, budgets/time reminders, review/compaction output, resumed/forked history | Generated/loaded context belonging to admitted work; record/history helpers do not separately perform authority comparison. Review/compact operations establish their own task boundary with current default context. |
| Explicit Op::RunUserShellCommand | Explicit full-access user-shell escape hatch (`SandboxType::None`, disabled permission profile); it does not use model-work admission. Active-turn auxiliary execution uses captured context; idle execution creates a current standalone turn. |
| Realtime audio/text/speech; append_rollout_items; settings/runtime/MCP updates | Separate realtime service or persistent store/settings operations, not new main model input through InputQueue. A subsequent ordinary model turn goes through the corresponding admission above. |

All tests run from `codex-rs` via guarded `just test`, `CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/.codex-work/targets/pf83-rebind-31`, `INSTA_UPDATE=no`, `--locked --offline --retries 0 --test-threads 1`. Test-isolation guidance read first; no native credential prompt observed. Prerequisites passed before tests: `cargo build --locked --offline -p codex-cli --bin codex -p codex-rmcp-client --bins -p codex-code-mode-host --bin codex-code-mode-host` (exit 0). Logs are ignored local artifacts beside this receipt, prefixed `pf83-rebind-44-`.

| Lane (command after `just test`, before common flags) | Run / pass / fail / skipped | Exit / log suffix |
| --- | --- | --- |
| `-p codex-core --lib 'session::'` | 343 / 343 / 0 / 2141 | 0 / `core-session-final.log` |
| `-p codex-app-server settings` | 25 / 25 / 0 / 1074 | 0 / `settings.log` |
| `-p codex-app-server turn_steer` | 5 / 5 / 0 / 1094 | 0 / `turn-steer.log` |
| `-p codex-app-server thread_inject_items` | 2 / 0 / 2 / 1097 | 100 / `thread-inject-items.log` |
| `-p codex-tui permission` | 91 / 91 / 0 / 4074 | 0 / `tui-permission.log` |
| `-p codex-core --test all -E 'test(suite::pending_input::)'` | 11 / 11 / 0 / 1167 | 0 / `pending-input.log` |

Exact failures: `suite::v2::thread_inject_items::thread_inject_items_adds_raw_response_items_after_a_turn` and `suite::v2::thread_inject_items::thread_inject_items_adds_raw_response_items_to_thread_history`. Both stop at initialize at approximately 10 seconds; the former's teardown reports two expected model requests but zero received, the latter reports “deadline has elapsed”. No fixture edit/retry or baseline-cause claim. F04 and `thread_settings_inject_items_waits_for_latest_authorization` pass in settings. Initial Core attempt (`core-session.log`) exited 101 before tests: E0433, unqualified `TurnState` in the new test; corrected to `crate::state::TurnState`, formatted and rerun, with the original log retained.
Only changed Rust files were formatted (`rustfmt --edition 2024 --config skip_children=true`); no workspace fix/formatter. `git diff --check`, scope audit and sprint checker pass (115 current, 127 archived). Rust changed lines: **20 outside tests** (+18/−2), **111 inside tests** (+111/−0, including one test-only mailbox constructor line), **131 total**. This 46-line receipt and 12-line unapplied patch add 58 artifact lines: **78 outside tests including artifacts**, **189 total**, within the 90-line target/150-line hard limit. All pre-existing tests retained; no new ignore or relaxed assertion. Brief correction: the public bridge and natural-completion replay require files omitted from writable scope; the bridge is not internal-only. The requested exact `thread_inject_items` filter selects two cases, whereas the prior `inject_items` filter also selected the new settings regression.
No commit, push, install, release, live profile or human-test-readiness claim. Independent functional/PTY/live-repository/evidence-review/human-acceptance gates remain with the integrator; no additional agent was dispatched.
