# RETURN — pf83-rebind-42

Allocation digest: `06d00976da95534233fbbee353af89e053e4c11995ba6da4689f1a1fbbd7f5f3`; claim `0cd44355-8110-4002-927d-fcd6924dd19b`; runtime `gpt-6-astra`, effort `high`.
Frozen brief SHA-256 verified first: `b8df0f688b1cce62c34eba82c5428c6e0994570737c86e6277ec33be81f6719d`. Base remains `52af24987eb7bb27ce7a661f6c58ceb4e4536551`; worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-rebind-20260916`.
Product-initiative revision: active plan `docs/plans/active/p0-security-levels.md`, feature PF-83, sprint PF-83-S01 (`in_progress`). Product heading **Permission selection confirmation — TO BUILD**, excerpt “Existing active-turn approval/sandbox snapshots and pending approvals are not retroactively changed.” The sprint's September 16 Travis decision authorizes re-binding newly admitted work. This explicit frozen allocation governs this revision; reconciling older canonical plan/sprint coordinates and exclusions remains manager-owned and outside writable scope.
No commit, push, installation, release, live-profile use or human-acceptance claim. Independent code-blind execution/review, exact-package affected-flow PTY proof, applicable live-repository proof and release qualification remain integrator gates.

- RPC repair: `inject_no_new_turn` now passes a supplied context into `inject_running_items`. Under the active-turn/state locks, only pointer identity with the task's captured context exempts supplied-context items from admission; the fresh context constructed by `CodexThread::inject_response_items` cannot satisfy that identity. Thus the existing RPC and direct Core API both defer on authorization mismatch/non-Regular task. Internal no-context output callers remain trusted. No caller content can forge that identity.
- Wait repair: turn-local `has_user_input` considers consumable items only; subscriptions with a turn state exclude the interrupted session FIFO. Abort notifies the submission-loop consumer without sending a false Steer activity event. `has_pending_input` and no-turn subscription inspection still expose interrupted storage. The new test exercises real SleepHandler completion in both queue states, checks an existing subscriber gets no false wake, and proves consumable input still wakes it. Sleep and wait_agent share this subscription implementation.
- Schema repair: each deferred entry retains its request schema; abort carries it into the session FIFO; re-admission supplies it to normal `Op::UserInput`. The existing fresh-admission test now submits a schema through the actual input handler and asserts it on the newly running context after interrupt.
- Disclosed limit unchanged: `on_task_finished`'s non-abort `Err(err)` arm records deferred input without executing it. Its entire source file is byte-identical. The separate pre-existing normal-completion follow-up representation still drops per-request schema/metadata; this revision repairs interrupt replay only.
- F04's body is byte-identical across `b28882afe`, `23915e5c6`, `52af24987` and this tree: SHA-256 `0d2667d9cfecc5660c30084b34c44289a72a61044246cdfd8ba9c2ce9914e98a`. Existing tests retained; the prior Review test now correctly expects no consumable Steer activity.

| Model-input admission path / current callers | Gate |
| --- | --- |
| Ordinary submitted input / turn-start / natural-completion user follow-up | `user_input_or_turn_inner → steer_input`: Regular task plus symmetric `authorization_matches`; defer on mismatch/non-steerability; no-active-turn startup captures latest configuration. |
| Explicit `turn/steer → CodexThread::steer_input` | Same `steer_input` check; rejected work is returned with `authorizationChanged`, not admitted. |
| Peer mail / `Op::InterAgentCommunication` | `enqueue_mailbox_communication_for_session` compares latest/captured authority atomically and binds deferred mail to that turn; drains exclude it until a turn boundary. Idle trigger/queue-only delivery keeps existing readiness and fresh-turn rules. |
| Goal extension / external extension input | `inject_extension_if_running → InjectionSource::Extension`: Regular plus authority equality, otherwise deferred. Idle extension startup uses `try_start_turn_if_idle` (busy/pending-trigger/Plan gates, fresh context). |
| `thread/injectItems → CodexThread::inject_response_items` | Fresh-context provenance at `inject_no_new_turn → InjectionSource::Context`, then the same Regular/equality gate. Idle items are recorded without starting a turn and enter a later freshly authorized turn. |
| Interrupted deferred FIFO | Submission loop replays user input through normal user admission with its schema; response items use extension admission or idle recording; communication uses its native operation. No task-start queue splice. |
| Existing work's outputs / context fragments | Deliberately ungated: code-mode notify uses `inject_if_running / RunningTurn`; user-shell output and saved-prefix/network-rule fragments supply the captured context; Guardian-approved messages, Guardian reminders and generated child terminal-status prefixes use internal no-context `inject_no_new_turn`. These are existing-work outputs, not new client/extension admission. |

All Rust tests ran from `codex-rs` via guarded `just test`, with `CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/.codex-work/targets/pf83-rebind-31`, `INSTA_UPDATE=no`, `--locked --offline --retries 0 --test-threads 1`. Test-isolation guidance was read first; no native credential prompt observed. Logs below are local ignored artifacts under this directory with prefix `pf83-rebind-42-`.
Prerequisites all exit 0: `cargo build --locked --offline -p codex-cli --bin codex` (negative-control build and restored final build separately logged), `cargo build --locked --offline -p codex-rmcp-client --bins`, and `cargo build --locked --offline -p codex-code-mode-host --bin codex-code-mode-host`. All three preceded tests; CLI rebuilt after restoring fixes.

| Final-tree command after common flags | Run / pass / fail / skipped | Exit / log suffix |
| --- | --- | --- |
| `just test -p codex-app-server settings` | 25 / 25 / 0 / 1074 | 0 / `settings.log` |
| `just test -p codex-app-server turn_steer` | 5 / 5 / 0 / 1094 | 0 / `turn-steer.log` |
| `just test -p codex-tui permission` | 91 / 91 / 0 / 4074 | 0 / `tui-permission.log` |
| `just test -p codex-core --lib 'session::'` | 342 / 342 / 0 / 2141 | 0 / `core-session.log` |
| `just test -p codex-core --test all -E 'test(suite::pending_input::)'` | 11 / 11 / 0 / 1167 | 0 / `pending-input.log` |
| `just test -p codex-core -E 'test(authorization_changed_turn_receives_code_mode_notify_and_user_shell_output) \| test(code_mode_notify_injects_additional_exec_tool_output_into_active_context)'` | 2 / 2 / 0 / 3663 | 0 / `injections-replay.log` |
| `just test -p codex-app-server inject_items` (initial and identical replay) | Each 3 / 1 / 2 / 1096 | Both 100 / `inject-items.log`, `inject-items-replay.log` |

Final-tree failures, both runs: `suite::v2::thread_inject_items::thread_inject_items_adds_raw_response_items_after_a_turn` and `suite::v2::thread_inject_items::thread_inject_items_adds_raw_response_items_to_thread_history`. Both stop during the existing 10-second initialize handshake before any model request or injection; the former also panics in mock teardown (expected two requests, received zero), the latter reports “deadline has elapsed”. Cause beyond initialization is unproven; no baseline attribution is claimed. Their fixture file is outside allocation. The new boundary regression passes in settings and both injection runs. This lane remains failing, not qualified.
Negative controls temporarily disabled the new RPC source gate, restored deferred-user visibility to the waiter predicate, and restored replay's hard-coded None, without removing tests. RPC command `just test -p codex-app-server thread_settings_inject_items_waits_for_latest_authorization`: 1 run / 0 passed / 1 failed / 1098 skipped, exit 100 (`negative-inject-items.log`). Core command `just test -p codex-core --lib -E 'test(deferred_input_does_not_interrupt_turn_local_sleep) | test(authorization_changed_interrupted_input_wakes_fresh_admission)'`: 2 run / 0 passed / 2 failed / 2481 skipped, exit 100 (`negative-core.log`).
Exact negative failures: `suite::v2::thread_settings_update::thread_settings_inject_items_waits_for_latest_authorization` (injected marker in old model request); `session::tests::deferred_input_does_not_interrupt_turn_local_sleep` (Some(Steer) instead of None); `session::tests::authorization_changed_interrupted_input_wakes_fresh_admission` (None instead of the supplied schema). All pass with fixes restored. No final-source code changes followed those passing lanes.

Brief corrections: schema loss is real, but `SessionConfiguration` does not persist this schema. It lives in the per-request `TurnContext`, initialized to None and populated from `SessionSettingsUpdate`; leaving an update absent would still lose it, so the queue carries it explicitly. Also, the turn-local deferred vector and interrupted session FIFO are distinct. `has_pending_input` drives same-turn inference as well as pending checks; turn-local deferred items must stay excluded so the old turn can finish, while interrupted FIFO visibility is retained.
Only the six allocated Rust files were formatted with `rustfmt --edition 2024 --config skip_children=true`; no workspace formatter/fix ran. Stable imports-granularity warnings only. Scope checks and `git diff --check` pass; sprint checker passes (115 current, 127 archived). No tests removed or ignored. Rust changed lines: **73 outside tests** (+52/−21), **190 inside tests** (+178/−12), **263 total**. This receipt adds **42 non-test documentation lines**: **115 outside tests including documentation**, **305 total changed lines**, below the 120-line target and 200-line hard cap outside tests.
