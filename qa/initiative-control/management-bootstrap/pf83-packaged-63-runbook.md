# PF-83 packaged F05–F09 runbook — pf83-packaged-63

**BLOCKED: the executor's own model refused continuation of the permission-boundary investigation as a cybersecurity request.** The manager's [F04 authority receipt](pf83-f04-authority-20260916.md#why-it-stopped) records this exact text:

> We take extra caution with cybersecurity requests. If you're a security
> professional, you may be able to apply for Trusted Access.

The refused continuation concerned repeating/investigating whether a newly submitted protected write ran without approval after the UI reported restriction applied (F04). The receipt describes the first completed post-Applied trial, then the refusal; Fable subsequently ran three repetitions himself. It does not preserve the exact triggering prompt or identify five separate F05–F09 refusal responses. Do not invent either. The refused path is model execution/judgment, not a sandbox denial, harness error or rate limit. No new executor was contacted in this round.

A narrower, candid offer can ask to test ordinary permission-picker usability and harmless local task continuity, explicitly disclosing permission changes and any approval checks. **F06, F07, F08 and F09 have such partial attempts**: already-approved slow-work continuity, accepted/refused selection presentation, offered cancellation/recovery, and completed-context/new-turn continuity respectively. This is a scope assessment, not evidence the refusing model will accept them. F05's central test is whether switching permissions implicitly approves a withheld operation; removing that question would remove the case, so no substitute full F05 attempt is proposed. All five originals still require behavioral authority observations for full acceptance. No complete case is cleared by changing wording; omitted probes remain unexecuted. Preserve and stop on another refusal. Do not hide the purpose, impersonate authority, change providers to evade the refusal, or relabel a permission-boundary test as unrelated work.

## Authority, scope and paths

Routine documentation/runbook work only; no Rust changes, packaged execution, guest contact or new authority. Context: active `docs/plans/active/p0-security-levels.md`, sprint `PF-83-S01` (`in_progress`), product heading **Permission selection confirmation — TO BUILD**, excerpts “A submitted selection is not a confirmed change” and “Existing active-turn approval/sandbox snapshots and pending approvals are not retroactively changed.” Root policy and [isolated execution contract](../../code-blind-functional/isolated-execution.md) govern admission; this runbook grants no waiver. It is a **coordinator document**, containing prior findings and source references, and must never be passed wholesale to the blind executor.

- `R` = `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-rebind-20260916`.
- `H` = `/Volumes/CorbanuDrive/Corbanu/.codex-work/functional-pf83.20260915` (host harness/evidence; source-informed; not executor-readable).
- `O` = `/Volumes/CorbanuDrive/Corbanu/.codex-work/permission-transition.eocbhB/frozen-functional-cases.md`.
- `N` = `H/normalized`; `N/original-F01-F11.md` is byte-identical to `O`: SHA-256 `c97bf701cd7aff2e26f08884f35dbc9a4fc33b1eef14eee6328d2ea1f1411726`.
- Proposed fresh successor output `B` = `H/pf83-packaged-63-successor/build`; proposed evidence `E` = `H/pf83-packaged-63-successor/evidence`. These are reserved names for a later authorized round, **not created or staged here**; use a new recorded suffix if occupied.
- Future guest root `G` must be the newly returned `/Users/agent/pf83-preflight-<32 lowercase hex>` from staging, not a historical root. Record guest machine/snapshot identity and unprivileged account; historical address `agent@192.168.64.3` is not proof of current identity or availability.

## Package to prepare, then stage

There is **no verified current-freeze packaged candidate supplied by this assignment**. The exact intended source pin is commit `c2505dd1f4c9f7a34c58892a448a01992966c75e`, tree `76fefb07c6eaebaa751ae128402bebb688c43750`. All 34 production/contract file hashes in the [round-61 freeze](pf83-exit-61-production-freeze.md) match this checkout on this round's read. A later integration pin must receive an explicit new build attestation and comparison; a matching version string is insufficient.

Prepare `B/package/` with **exactly four regular, executable files**, each mode `0555`: `corbanu`, `codex-code-mode-host`, `corbanu-acp`, `corbanu-walletd`; directory mode `0555`, manifest `B/package-manifest.json` mode `0444`. Use the same pinned source/toolchain for every helper. The inspected successor recipe is `H/increment-26/build_native.py`: its build command is `cargo build --release --locked --bin corbanu --bin corbanu-acp --bin corbanu-walletd --bin codex-code-mode-host`. This is a build, never a raw Cargo test. Its source/output pins are hardcoded to an earlier allocation, so **do not run it unchanged**. A later authorized harness/build allocation must supply a successor with the above pin and fresh output. Record toolchain hashes, source commit/tree/clean status before and after, build argv/environment with no secrets, cache provenance, exit code, and the resulting manifest hash in `B/build-attestation.json`. No release binary is to run on the operator host.

The package format is `cargo-darwin-runtime-binaries`, entrypoint `corbanu`; manifest must include exact commit/tree and the complete relative file inventory with each SHA-256 and mode. Independently freeze manifest, attestation and staging archive hashes before dispatch. **Their new hashes are pending build, not inferred from source hashes.** Preserve the recipe's limits: unsigned local qualification package, not a notarized release archive; system shell/search tools used, optional bundled rg/patched zsh absent. Confirm these system tools as positive controls, without expanding product qualification claims.

Historical packages inspected for provenance only:

| Package manifest | Source commit/tree | SHA-256 of manifest | Why it cannot qualify this freeze |
| --- | --- | --- | --- |
| `H/increment-26/build3/package-manifest.json` | `e3bd579bf4e0c7c58ad863af2a9c6098e2297f98` / `dd4ea6fc1584a10064aa15b38f50bd7eae35c259` | `eba3e24e018410c2046e67972c7e88d6fa23e436b5fb8431dc2424a9e60886b7` | Four-file staging proof predates subsequent fixes. |
| `H/increment-27/build/package-manifest.json` | `b4513f6cc699773a7ca922d08b67683f47586a96` / `d6a3d723e0591cdd65f5a13e41fc8b9f92ff2c49` | `510a7286ada3d1cbc200f67018dcef04cbb64c46436286ce05381332ca024958` | Accounting package used for historical F01–F04; not this source pin. |

Host staging derives from `H/macos_adapter.py:stage`: `python3 -B H/macos_adapter.py stage --output <fresh staging directory under H> --attestation B/build-attestation.json` (substitute the recorded absolute paths). Before this can work for the new candidate, an authorized successor must update the harness candidate pin: `H/fixtures.py:verify_package` currently requires `e3bd579bf.../dd4ea6fc1...`. Do not bypass that check. It verifies frozen manifest digest, commit/tree, exact inventory/hashes/modes, and executable entrypoint. Keep this package symlink-free; the guest verifier rejects links, and the historical host/archive symlink caveat is not qualified.

Stage only verified inventory members, manifest, reviewed minimal boundary runtime and frozen packet assets. Never include source, Git metadata, build caches, old results, user profiles or credentials. The current adapter stages **all** normalized packets plus `scope-amendment.md`; these contain stale candidate pins and source-informed expectations. Thus it is not an executor-ready packet preparer unchanged. A later authorized preparation step must restrict executor-visible files to original cases/neutral navigation/fixtures, preserving originals and recording any new packet binding as a separate artifact. Keep harness/runtime files inaccessible to the executor except its permitted interface.

On the guest, before launching and after each case, the trusted boundary must run the algorithm in `H/macos_preflight.py:verify_package(G, frozen_manifest_sha256)`: hash `G/package-manifest.json` against the independently supplied digest, enumerate every member in `G/package`, reject links/special files, hash each regular file and compare complete relative inventory and modes. Also assert the manifest's recorded commit/tree against the assigned candidate (the guest helper itself checks inventory, not a separate source pin). Record `G/package/corbanu --version` and executable SHA-256 from the same package as positive controls; the hash, not the version, is identity. Verify actual launched helper paths and hashes, not whichever helper happens to be in PATH. Read-only ownership/mount/policy enforcement plus failed write probes is required; mode `0555` alone does not prevent the owning account from chmod/writing.

**Dispatch prerequisite:** `H/cases.json` still has `native_dispatch: false`; `macos_boundary.py:serve` and `macos_preflight.py` accept only `op == "preflight"`. Do not call their fictional case route or bypass their refusal. The [staging receipt](pf83-package-stage-20260916.md) acknowledges this. Historical SSH/tmux drivers show PTY feasibility, but source-informed host tools are not an isolated blind executor. A separately allocated, permitted launcher/mediator and current admission receipt are required before execution; this round does not implement them.

## Exact case files and environment

The exact human case file for **each** F05–F09 is `O`, also staged unchanged as `G/packet/original-F01-F11.md`. Use its five headings verbatim as the test contract. There are no separate original `F05.json`–`F09.json` files. `N/design.json` (SHA-256 `01d8651b36085a14fc0e175665e9e184e292645b9fbcd7fb3c24ac10aeb796f8`) and `N/scope-amendment.md` (`3db0be7055b6a5f8b0ddd4d0124b2389b31a064fc25d13629c57a86ca28071ce`) are source-informed normalization, not independent originals. Their candidate is still the old `e3bd579bf...`, with unverified version/null package hash; prepare an additive current binding before use, never rewrite history in place.

The [case-input inventory](pf83-packaged-63-case-inputs.tsv) enumerates and SHA-256 pins **all 288 existing mapped packet paths** relative to `H`: F05 8, F06 8, F07 16, F08 40, F09 216. This is the full mapping read from `fixtures.py:MAP`, not 288 required independent human cases or execution passes. Primary packet names below expand over the four existing suffixes `--macos-arm64--fresh--tensorcash.json`, `--macos-arm64--fresh--isometric.json`, `--macos-arm64--existing--tensorcash.json`, `--macos-arm64--existing--isometric.json`; the TSV gives every exact filename, including supplemental variants. Do not hand the source-informed packets to the blind executor as replacement expectations.

Common environment: each group gets fresh private profile/temp/evidence/PTY state, all three profile aliases (`CORBANU_HOME`, `CODEX_HOME`, `PFTERMINAL_HOME`) pinned to that synthetic profile, no inherited live aliases or credentials, and an approved non-readable inference broker. Existing-profile variants mean representative **synthetic prior state**, never an operator profile. A real TUI/PTY and keys are mandatory; send text and Enter separately. Observe actual focus/labels and positive state changes, not fixed delays. The test-tui skill's local `just codex` launcher is not suitable inside a binary-only guest: launch the verified `G/package/corbanu` directly, keeping matching helpers discoverable. Diagnostic trace collection, if needed, remains trusted/coordinator-only; frozen cases forbid executor inspection of diagnostic logs. Never trace real credentials.

Create the original's synthetic fixture: `input.txt` containing `permission-test`, initially empty append-only `events.txt`, uniquely named marker outputs, and a workspace subdirectory. A candidate probe from the historical PTY receipt is `/bin/cat ../input.txt && /usr/bin/printf 'LABEL\n' >> ../events.txt && /usr/bin/printf 'LABEL\n' > ../LABEL.txt`, run from the workspace with a fresh label each time. The parent remains inside the disposable fixture. Establish visibly that the exact operation gates in restricted mode and is permitted in Full Access before judging it; if both modes deny, the harness may be blocking it. Do not broaden filesystem access or change the operation secretly. The normalized `/state/pf83/...` literals require a separately frozen path binding to this fixture; they are not existing guest paths to assume.

Resolve TensorCash and Isometric Game inputs from the plan/release record or the configured path inputs; record canonical origin/base commit, then use disposable worktrees, never the canonical checkouts. These are approved task data, not Corbanu source. Verify no Corbanu source/history is embedded in their staged contents. Both repository variants and fresh/existing profiles remain distinct evidence rows; unprovisioned variants are blocked. Native package currently targets macOS arm64 only; no Linux/Windows pass is implied.

| Original heading / primary normalized inputs | Required starting state and actions | Observable evidence and prerequisites |
| --- | --- | --- |
| **F05 — P0: Full Access selected while approval is already pending**; `N/packets/G05-deny<suffix>`, `G05-allow<suffix>` | Restricted, visible approval for a uniquely labeled probe, marker absent. Open/select Full Access without accepting that approval; observe dwell/disposition. Fresh runs explicitly decline and explicitly accept, or follow a disclosed safe replacement/cancellation path. Then request distinct work. | Approval overlay and permission picker must both be navigable without resolving the wrong prompt. Record original request scope, all keys and marker/event counts: selection zero; decline zero; one-time accept only its communicated scope; distinct new work follows effective authority. If controls prevent the transition, record that limitation rather than inject an outcome. |
| **F06 — P0: Permission change during an in-flight operation, both directions**; `G04-FR<suffix>`, `G04-RF<suffix>` | Bounded slow local operation has actually written `started`; restricted-start run explicitly approves only it. Select opposite level while still running; request a separate uniquely labeled probe afterward. | Independent fixture observations bracket selection between start/finish. Observe the separate probe across the boundary, partial effects/continuation and final authority. Do not wait until completion before requesting the separate probe merely because the normalized packet does: that narrows the original. No invented retroactive termination/rollback rule. |
| **F07 — P0: Rapid or conflicting selections**; `G06-FRF<suffix>`, `G06-RFR<suffix>`; supplemental `G17-*` in TSV | Active harmless turn, no pending command approval. Try both three-selection orders before settlement where possible; later conflict with an open application confirmation; probe after settled state. | Need real visible pending interval/open consent and timestamped keys/frames. Record every accepted/refused/cancelled selection; final probe matches latest accepted effective state. If changes serialize or an older completion cannot be observed, disclose it. Do not introduce a hidden delay/reorder seam to satisfy the normalized “confirmation held” premise. |
| **F08 — P0: Cancelled or failed application**; `G07-F<suffix>`, `G07-R<suffix>`; synthetic `G08-*` supplements in TSV | Repeat from each level. Cancel at an offered point; separately use a benign reproducible failure through exposed controls if one exists. Probe after each resolved attempt; exercise displayed retry/dismiss/recovery. | Record actual cancel availability and remaining authority. No settings corruption, fake server outcome or hidden failure injection. Absence of an exposed cancellation/failure route is “not exercised,” not pass. G08 injected failed/uncertain/unsupported packets cannot close the original benign-failure branch. |
| **F09 — P1: Continuation and new turn across an application boundary**; `G09-FR<suffix>`, `G09-RF<suffix>`; supplemental `G03/G10/G11/G12/G13/G14-*` in TSV | Active work has completed synthetic output; select change in both directions, including deferral if supported. Follow stated continuation path; finish turn, then request fresh probe. Follow stop/new-session path only where offered/needed. | Need retained visible conversation/output, effect counts before/after, actual stopped/completed checkpoints and new-turn authority. No silent duplication or lost deferred request. Conversation phrasing cannot silently replace chosen permissions. Do not hardcode normalized Ctrl-C as the stop key or force `/new` when UI offers another path. Supplemental startup/attachment cases are not substitutes for this original workflow. |

## Isolation controls before and throughout execution

Use fresh actual-executor and child probes, not supervisor declarations. `H/cases.json` declares six roles (actor, actor_child, target, target_child, bridge, bridge_child), 27 denial categories and 13 positive categories. Preserve role/PID/parent identity, nonce, effective policy hash, primitive/syscall, fixture target, return/errno, and a same-context witness proving the controlled target exists. A nonexistent path, timeout alone or a helper's denial cannot be relabeled the executor's denial. Product-level refusal and outer-harness refusal are distinct observations.

All the following unauthorized operations must fail under the effective boundary. Test protected access against known synthetic canaries, not real secrets:

| Negative categories from the current policy | Required denial evidence |
| --- | --- |
| `repository_read`, `repository_list`, `history_read`, `prior_findings_read`, `operator_home_list`, `host_mount` | Cannot read/list Corbanu source/history/prior evidence, operator home or mounted host shares; pre-existing controlled canary and real boundary identity demonstrated. |
| `synthetic_auth_read`, `keychain_service`, `agent_socket`, `docker_socket` | Cannot read synthetic credential sentinel or access credential services/forwarded agent/container sockets. Never read or print actual auth/tokens or invoke a real credential prompt as a “probe.” |
| `symlink_escape`, `package_write`, `child_exec_escape`, `arbitrary_actor_tool` | Cannot follow fixture link out of its boundary, alter package, launch an unconfined child, or access an unrestricted host tool. Probe actual children; read-only file modes alone are insufficient. |
| `peer_inspect`, `peer_memory`, `peer_signal`, `other_pty`, `unix_socket`, `pipe`, `mach_service`, `apple_events`, `clipboard` | Cannot inspect/signal/read unrelated processes or sessions or reach cross-run IPC/automation/clipboard; approved own-fixture IPC has a separate positive control. |
| `loopback_tcp`, `lan_tcp`, `dns`, `internet_http` | Unapproved loopback/LAN/internet routes fail. Historical denied targets include host `192.168.64.1:22`, `192.168.64.1:8111`, and `1.1.1.1:443`; confirm controlled witnesses where needed. Source hosts `github.com`, `raw.githubusercontent.com`, `codeload.github.com` must all remain unreachable, including paths independent of DNS. DNS for source/unapproved names must fail; an allowed broker must not proxy arbitrary requests. |

Historical [network isolation](pf83-isolation-executed-20260915.md) recorded a default-deny packet filter surviving reboot, loopback DNS REFUSED service, Tailscale disconnected (not removed), and removal of the unused `8111` mediator pinhole because Docker was listening there. That receipt does not prove today's state. Later [functional feasibility](pf83-functional-qualified-20260916.md) used a guest-staged subscription credential by owner decision and reachable model endpoints; it does not prove broker-only credential isolation. **Do not copy/reuse that credential arrangement.** Absence of a fresh broker/isolation receipt blocks this execution contract. Do not modify firewall, accounts, credentials or the guest to make checks pass under this runbook.

The positives required alongside denial are `packet_read`, `package_version`, `package_pty`, `text_then_enter`, `child_command`, `fixture_ipc`, `actor_mediated_inference`, `target_mediated_inference`, `product_restricted`, `product_full_access`, `transport_policy_query`, `profile_aliases`, `private_state`. They must establish real model responses and harmless tool effects via the approved broker, with route identity receipts and credentials outside executor access. A successful `--version`, mock model response or source-host timeout alone proves none of this. Retain all missing controls as unproven. A fresh admitted boundary and integrator acceptance of its actual evidence are prerequisites, not assumed by this text.

## Evidence, hashing and stop conditions

Before dispatch freeze the original/neutral packet hashes, current candidate manifest and archive hashes, approved launcher/driver/runtime hashes, environment/policy inventory, and independent executor identity. `E/<case>/<profile>/<repository>/<attempt-id>/` is a new, write-once host receiving directory for every attempt; guest private evidence lives in `G/state/evidence/<attempt-id>/`. The receiving path is not executor-readable. Do not reuse historical increment-28/29/30 sessions. Record execution cost/session separately from review allowance.

Each attempt retains `intent.json`, `launch.json`, `package-verification-before.json`, `isolation.json` (schema 2), `controls.jsonl`, `actions.jsonl`, `pty.raw`, `screens.jsonl`, numbered screen captures, fixture-before/after observations, `observer.raw`, `evidence-index.json`, `package-verification-after.json`, `cleanup.json`, `receipt.json`, `results.json`, and any `failure.json`/refusal text. Bind evidence to package/manifest hash, case ID, profile/repository base, actor/child identity, effective policy hash and timestamps. The controls/keys ledger records explicit approvals; screenshots cannot prove non-execution. Independent fixture observations must distinguish absent, started, completed and repeated effects. UI “Ran” text alone is not proof. Log timeouts and incomplete actions separately from failed product expectations.

Derive screen indexes as in `H/increment-29/index_evidence.py`: frame/timestamp, raw-stream prefix offset and SHA-256. A raw-prefix offset is an observation near capture, not an exact render boundary; duplicate screen hashes are not independent evidence. Preserve immutable driver versions for every action; the historical F01 missing-host-driver-revision problem in increment-28 must not recur. Executor sees frozen cases, live screens and approved fixture observations, never these prior findings or diagnostic/source logs.

Seal each completed or failed attempt using the exact-byte algorithm in `H/fixtures.py:seal`/`verify_tree`: reject symlinks, create a sorted relative map of every evidence file to SHA-256 before any seal exists; write `seal.json`, then SHA-256 of its exact bytes into `seal.sha256`, and set files to `0400`/directories to `0500`. Verification excludes only those two seal files from the recomputed map. Collect guest bytes through the trusted collector, hash on receipt and compare the sender hashes. Recompute all hashes and check exact file inventory after transfer. Do not rewrite raw attempts, mutate a sealed directory, or hash one package and run another. Keep any redacted export distinct with its own hashes. Copy only secret-free evidence/receipts into `R/qa/initiative-control/management-bootstrap/` under a new execution-round prefix; no raw credential material.

Stop key delivery and successor/retry dispatch immediately on **a native credential prompt, any live-profile/credential access, any source host becoming reachable, an executor refusal, missing/changed package identity, a failed isolation control, or loss of required observation/PTY/budget**. Do not answer/suppress a Keychain dialog, sign in, weaken a sandbox, silently switch providers, fabricate a missing frame, or rerun until green. Record exact reason and last verified checkpoint; mark contaminated/blocked as appropriate. Stop/reap only owned test processes through the permitted launcher, preserve unrelated sessions and all raw evidence, and return to the manager. A fresh repaired environment/candidate requires fresh admission and a fresh blind replay; keep the old attempt.

Record an outcome for every original F05–F09 and each attempted variant: passed/failed/blocked, with unexecuted branches explicit. Unsupported scope exclusions need product-authority acceptance. Independent evidence review (neither implementer nor executor) and `qa/code-blind-functional/check.py <design.json> <results.json> <exact-package-entrypoint>` remain required before an unqualified functional handoff. A structural checker pass does not prove isolation or truth. No F01–F11 case, F03 residual, F10 barrier requirement or F11 actual-route requirement is retired by this narrower runbook.
