# RETURN — pf83-display-49

Allocation digest `b5c603d6354a875ddb8c3b5c53b28564b882300be1158c9610cc64f312fcb305`; claim `dd5a12a8-5fed-4401-a6bb-16b7e2bcf2aa`; runtime `gpt-6-astra high`. Brief SHA-256 verified first: `c3ab0ffbcca1690f76654382f6a98f7ff8a13c38f2ecb3765b5920c3ff08fa01`. Initial clean HEAD matched `7995a073cbfdce1a6a70759bc28d76afe47b9f49`; worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-rebind-20260916`.
Bounded display/test revision under product heading **Permission selection confirmation — TO BUILD**, excerpt “This is confirmation plumbing and truthful UI”; owning active plan `docs/plans/active/p0-security-levels.md`, PF-83-S01 `in_progress`. Frozen allocation supplies this worktree/scope; historical plan/sprint coordinates remain manager-owned reconciliation.

**Partial implementation: two frozen-scope/input limitations remain.** Deleted orphan snapshots: **none**. Both obsolete app snapshots below are unreferenced, but `codex-rs/tui/src/app/snapshots/` is absent from the explicit writable scope, so deleting them would violate it:

- `codex-rs/tui/src/app/snapshots/codex_tui__app__permission_confirmation__tests__permission_confirmation_outcomes_are_explicit.snap`
- `codex-rs/tui/src/app/snapshots/codex_tui__app__permission_confirmation__tests__permission_confirmation_superseded.snap`
Tracked-source search found only the outcomes test declaration; its assertion is inline. The superseded assertion is also inline in `permission_confirmation_orders_profiles_and_newer_observations`. Neither external snapshot is loaded. The false text remains in those files; manager deletion/scope correction is still required. No other tracked TUI snapshots contain those two retired statements.

Added `app::permission_confirmation::tests::permission_confirmation_hint_matches_retained_steer_and_latest_submission`: renders the real Applied confirmation, sends Enter during a running turn, injects the authorization-change steer error into `note_thread_steer_failure`, checks the prompt remains queued without submission, changes permissions again, delivers natural completion, and asserts the original payload submits exactly once under the latest Read Only / OnRequest settings with an empty queue afterward. A single tuple assertion couples the rendered load-bearing phrase to held input and resubmission. Removing retention, completion draining, latest-settings binding, or the phrase breaks this test. This exercises the TUI error-to-retention path; it does not claim a new end-to-end server admission test or a mutation-test run.

Removed only the unconditional status paragraph (4 production lines); kept `Next turn:` so session settings are never presented as captured running authority. Conditional disclosure on actual authority inequality is **not implemented**: `chatwidget/status_controls.rs::add_status_output` passes session config, and the status renderer/protocol turn notification has no captured authority or comparison signal. Inventing equality would be false; lifecycle tracking/caller plumbing requires additional scope. No authorization, admission, deferral or gate behavior changed.
Measured existing rendered permission blocks, before → after: **42 columns: 19 → 6 lines; 46 columns: 14 → 4; 70 columns: 6 → 2**. These are custom workspace profiles. New Full Access cases at widths 42/46/70/100 use 2/1/1/1 lines; Read Only uses 3/2/1/1. Width assertions and a snapshot cover all eight outputs. Updated 22 status snapshots; 21 existing general snapshots match outside the permissions block after frame/whitespace normalization (including overlay filler rows).

Read test isolation first; all Rust tests use guarded `just test`, disposable profiles, no live-profile reads or native prompt observed. From `codex-rs`, shared dedicated `CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/.codex-work/targets/pf83-rebind-31`. Prerequisites both exit 0 before tests: `cargo build --locked --offline -p codex-cli --bin codex`; `cargo build --locked --offline -p codex-rmcp-client --bins`.
All four test runs exited 0; failed/timed-out counts are zero in every lane; exact failure names: none. All test commands add `--locked --offline --retries 0 --test-threads 1`. Snapshot regeneration uses `INSTA_UPDATE=always`; final lanes use `INSTA_UPDATE=no`. Ignored raw logs are adjacent, prefixed `pf83-display-49-`.
| Lane | Run / pass / fail / skipped | Exit | Log |
| --- | --- | --- | --- |
| `just test -p codex-tui -E 'test(/^status::tests::/)'` (regenerate) | 41 / 41 / 0 / 4130 | 0 | `status-snapshots.log` |
| `just test -p codex-tui permission` | 93 / 93 / 0 / 4078 | 0 | `permission.log` |
| `just test -p codex-tui status` | 391 / 391 / 0 / 3780 | 0 | `status.log` |
| `just test -p codex-app-server settings` | 25 / 25 / 0 / 1074 | 0 | `settings.log` |

Scoped `rustfmt --edition 2024 --config skip_children=true` ran only on the three changed Rust files before tests; no workspace formatter/fix. Final source hashes stayed unchanged across final lanes. Scope audit (26 files), `git diff --check`, and sprint checker (115 current / 127 archived) pass. Changed lines: **387 inside tests** (172 test source + 215 snapshots), **31 outside tests** (4 production deletions + 27 receipt lines), **418 total**. Under the 60-line target and 110-line hard cap; ignored raw logs excluded.
Independent functional design/execution/evidence review, exact-package true-PTY/live-repository qualification and human acceptance remain with the receiving integrator. No unqualified human-test handoff, release, install, commit, push or extra-agent dispatch.
