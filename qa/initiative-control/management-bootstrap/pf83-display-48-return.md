# RETURN — pf83-display-48

Allocation digest `2ce2d27eef10458880aa4f16995282c74f51732f9d1d1f4131b6f96930904bd6`; claim `6c384000-204d-4845-844e-7053d6da68eb`; runtime `gpt-6-astra`, effort `high`. Brief SHA-256 verified first: `7c2fd8099eae93199853a636699d0f0870c278f9c2c836c6fc52e398ef0585f0`. Initial clean HEAD matched `daab3225e37207ce37784337009b184821da1f93`; worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-rebind-20260916`.
Bounded display fix under product heading **Permission selection confirmation — TO BUILD**, excerpt “This is confirmation plumbing and truthful UI”; active owning plan `docs/plans/active/p0-security-levels.md`, PF-83-S01 `in_progress`. Frozen allocation supplies this worktree/scope; historical plan/sprint coordinates remain manager-owned reconciliation.

| Branch | Old message and hint | New message and hint |
| --- | --- | --- |
| Applied | “Permissions applied for NEXT TURN: {label}.” / “Running command authority and pending approvals are unchanged. Let the current turn finish, or stop it with Esc, then send a new prompt. Shared services keep their existing refresh behavior.” | “Permissions confirmed for new turns: {label}.” / “If a running turn has different permissions, a prompt sent here is held until it finishes, then runs with the latest permissions. Running work, granted approvals and pending approvals are unchanged. Shared services keep their existing refresh behavior.” |
| Applied, newer settings observed | “Permission request applied: {label}. Newer settings are now active.” / “Check current permissions before sending a new prompt. Held initial input was restored without submission.” | “Permission request applied: {label}. Newer settings govern new turns.” / Same continuation hint as above, followed by “Held initial input was restored without submission. Check /status for the latest next-turn settings.” |

The false implication in the first old hint is that a user must wait or stop the current turn **before sending**: new TUI input is retained when authority differs. The second branch did not literally say wait/Esc; its defect was unqualified “now active” plus omission of the held-prompt path. The restored initial-draft statement remains true and is preserved.
Read the complete `pf83-rebind-45-return.md` admission table. “A prompt sent here” limits the claim to the TUI: `app/thread_routing.rs` routes active input through `turn/steer`, whose authorization mismatch falls through `note_thread_steer_failure` to `enqueue_rejected_steer`. This retains input for a later turn. Core `session/handlers.rs::user_input_or_turn_inner` similarly defers UserInput on mismatch; `session/mod.rs::steer_input` compares current session authority with captured task authority under locks. Unchanged authority may steer immediately, hence the conditional.
No blanket promise about all admission rows: direct app-server/public steering returns `authorizationChanged` without admission; mail retains its delivery/wake rules, idle injected context need not initiate inference, running outputs/approval responses remain correlated to existing work, explicit user shell/direct MCP/realtime services retain their separate behavior. The shared-service qualification is retained.

F03 is real: `chatwidget/status_controls.rs::add_status_output` passes `&self.config`; `status/card.rs::StatusHistoryCell::new` derives its label from that config's approval policy, effective permission profile, active profile, reviewer and workspace roots. `chatwidget/settings.rs::apply_thread_settings` replaces those session values after settings notifications; Core retains the active task's `TurnContext`. Deferral does not make the two authorities equal.
Fix: the permissions value is now **“Next turn: {label}”**, with the wrapped continuation **“A running turn keeps its captured permissions (not reported here) and granted approvals. Shared services keep their existing refresh behavior.”** The protocol `TurnStartedNotification/Turn` contains no captured authority, and the status renderer receives no captured-turn snapshot. This fix explicitly discloses that limitation rather than inventing the running policy or claiming it equals the next one; it does not add exact running-policy reporting.

Coverage: existing confirmation-outcomes and superseded-observation tests retained, now with inline snapshots because app snapshot paths are outside this allocation. Existing external snapshot files remain untouched. Every existing status permission assertion now requires the “Next turn: ” prefix; its helper uses a wide render to preserve full profile/root labels. A new status snapshot checks both Full Access and Read Only plus the captured-authority disclosure. Status-card snapshots cover wrapped output and narrow layouts.
Authorization boundary, admission, deferral and gates are untouched: production edits are solely confirmation strings/shared hint and status rendering. No existing test was removed or ignored.

All commands ran from `codex-rs` with shared dedicated `CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/.codex-work/targets/pf83-rebind-31`. Both prerequisite builds passed (exit 0): `cargo build --locked --offline -p codex-cli --bin codex` and `cargo build --locked --offline -p codex-rmcp-client --bins`. Read test isolation before testing; all test runs use guarded `just test`, disposable profiles, `--locked --offline --retries 0 --test-threads 1`; no native credential prompt observed. Logs below are ignored local artifacts beside this receipt, prefixed `pf83-display-48-`.
| Lane | Run / pass / fail / skipped | Exit | Log suffix |
| --- | --- | --- | --- |
| Snapshot regeneration: `INSTA_UPDATE=always just test -p codex-tui -E 'test(/^status::tests::/)'` | 41 / 41 / 0 / 4129 | 0 | `status-snapshots.log` |
| Initial `INSTA_UPDATE=no just test -p codex-tui permission` | 92 / 91 / 1 / 4078 | 100 | `permission.log` |
| Final `INSTA_UPDATE=no just test -p codex-tui permission` | 92 / 92 / 0 / 4078 | 0 | `permission-final.log` |
| Final `INSTA_UPDATE=no just test -p codex-tui status` | 391 / 391 / 0 / 3779 | 0 | `status.log` |
| Final `INSTA_UPDATE=no just test -p codex-app-server settings` | 25 / 25 / 0 / 1074 | 0 | `settings.log` |

The one initial failure was exactly `app::permission_confirmation::tests::permission_confirmation_orders_profiles_and_newer_observations`: Insta rejected a looped inline snapshot without `allow_duplicates!`. Added that wrapper without changing expectations, formatted the changed test file, and reran the entire permission lane successfully. Original attempt preserved; no “pre-existing” attribution or retry hidden. No production changes after prerequisite builds.
Scoped `rustfmt --edition 2024 --config skip_children=true` only; no workspace formatter/fix. Existing status snapshots were regenerated and reviewed: outside the permissions section their content is identical after frame/whitespace normalization. Mechanical frame-width and wrapped-line changes explain the larger snapshot diff.
Final checks: source hashes unchanged through all three final lanes; writable-scope audit and `git diff --check` pass; sprint checker passes (115 current / 127 archived). Changed lines: **35 production Rust** (+30/−5), **75 test-source lines**, **708 snapshot lines**, **33 evidence lines**: **783 inside tests; 68 outside tests including this receipt; 851 total**. Outside-test budget is under the 80-line target and 140-line hard cap; snapshot growth is mechanical wrapping/frame output. Exactly 27 changed/new files, all allocated; ignored build/test logs excluded from changed-line counts. Brief qualifications: the second old hint did not literally instruct wait/Esc, and active authority cannot be read from the existing status input/protocol; both are disclosed above. No boundary defect requiring behavior changes was found.
Independent code/functional design, isolated exact-package execution, evidence review, true-PTY/live-repository qualification and human acceptance remain with the receiving integrator. No human-test readiness, release, install, commit or push claim; no additional agents dispatched.
