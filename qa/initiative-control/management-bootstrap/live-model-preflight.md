# Native model connection — bounded operator preflight

PF-80-S01, **Internal delivery control — TO BUILD**, “Use sequential sprints per
initiative”. This is internal bootstrap evidence, not independent functional
acceptance, an inference mediator or a deployed executor.

The owner ran the fixed nonsensitive text request through pinned Corbanu0.1.41,
Astra High and the supported ChatGPT authentication path. A fresh private native
home links only to the pre-authorized read-only auth file. No credential copying,
refresh, host-home writes, model fallback or executable model tools were used.
The fixed private provider alias has zero request/stream retries and no base-URL
override. Native auth selects the ChatGPT endpoint.

Preserved attempts under `.codex-work/transport-live.ZBeaq9/`:

| Attempt | Actual result |
| --- | --- |
| attempt-rk_95g3z | Initialization denied with native state in the read-only real auth home; no session. |
| attempt-a5uobjaw | Fresh home/session; HTTPS request failed. |
| attempt-zii29ueb | Exact macOS DNS socket allowed; HTTPS still failed. |
| attempt-6r5tj87e | Exact system CA bundle through supported SSL_CERT_FILE; expected text, completed turn, exit0 in2.515s, process group gone. |

A separate unauthenticated DNS diagnostic first failed resolution; after exact
resolver-socket access it reached the endpoint and received HTTP401, as expected
without auth. Shared HTTP-client source supports SSL_CERT_FILE and Rustls; the
final attempt used the existing root-owned public system CA bundle, preserving
normal certificate/hostname checks without granting user Keychain access.

Final native session: `01a09a10-8ed4-7211-a089-ee6d89e6c282`.
Receipt: `attempt-6r5tj87e/receipt.json`; actual policy/argv/environment and input
are retained in `launch.json`, with private raw stdout/stderr and hashes.
The exact requested text was returned. Two exact known web-search deprecation
warnings are separately counted; no runtime error or unknown error item passed.
Source auth, catalog and system CA hashes were unchanged. The one operator
script is119 lines, SHA256
`c0a31a475b204b6c601253415c6296f5190c782dc958f0b525b93862b500f6b2`.

The OS network boundary is HTTPS443 plus the exact system DNS Unix socket,
**not hostname-specific filtering**. The trusted fixed native endpoint and TLS
provide the complementary application restriction. The future action executor
must receive neither this network authority nor the auth link.90s wall/256KiB
output bounds are not RAM/CPU/disk quotas. Frozen transport B and its offline
review remain unchanged; this evidence is a separately scoped live correction.

Remaining: reusable fixed transport, bounded request mediation, actual confined
actions/negative probes, independent frozen-case execution/evidence check, real
Slack decision-to-agent recovery and private off-Mac HTTPS. Recurring operation
and product work remain paused. No sprint, human or release acceptance is claimed.
