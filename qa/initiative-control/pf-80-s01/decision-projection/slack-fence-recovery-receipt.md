# PF-80-S01 local fence-loss recovery — frozen candidate, OFF

Corbanu Terminal development skill, root AGENTS, current initiative-delivery-control plan, PF-80-S01 sprint and full slack-fence-recovery-allocation.md read before implementation. No nested AGENTS applies. Product: **Internal delivery control — TO BUILD**, contextual two-way Slack questions and verified identity/revision-aware manager handoff. Skill-directed shared bookkeeping remains parent-owned; no sprint completion claim.
Worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/tasknode-slack-recovery-20260912`, branch `workstream/tasknode-slack-recovery-20260912`; unchanged launch/HEAD `efb0f95f83cb19b876c6cec5c35a973415c6de99`. Accepted code ancestor `b9e671e60416b952cc20d7b7260d3678080aebd0` already contains transport and shared status: the private proposal's contrary baseline assertion was stale. No checkout movement or private-proposal edit.
Review01–05 failures, clean combined06, previous2675/1024 worker and3131/1314 combined dispositions, all older limits and the receiving243-pass/source-drift-failure/exact-rerun history remain in the unchanged prior receipts/allocation. Parent owns new07 and scoped corrective08; no worker review/child/tool-agent call or reset of review history occurred.
Old tasknode-pf80-s01 seven-file candidate remains untouched: ordered SHA manifest rechecked as `6550f885e4f94883577ee7e2d1add3370f2f4914919f76cc61fb8dc0d96f2564`.

## Implemented local seam and recovery contract

`slack_transport.inspect_fence_loss(store, binding)` returns `{case_digest,retained_ingress,watermark,affected_alert_count,state:"held"}` without mutation. The digest binds absolute private root, exact qualified identity, alerts, replies and complete transport snapshot. An existing prepared incident returns its original case identity; no regenerated resolution or transport attempt.
`slack_transport.recover_missing_fence(store, binding, *, expected_digest, evidence, quiesce_listener=None, now, checkpoint=lambda _:None)` returns `{loss_id,state:"held",retired_count}`. `checkpoint` is deterministic fault injection, not an authority input. Missing/noncallable hook refuses before journal/fence/alert mutation; a boolean return also refuses. The trusted injected hook must own and actually stop/join the runtime and callbacks outside all locks, returning None. A caller-supplied assertion or PID is not that integration contract.
`decision_manager.main(..., quiesce_listener=None)` explicitly registers `inspect-fence-loss` and `recover-missing-fence`, with `--store` and no `--live` requirement. One owner-controlled bounded JSON stdin line carries binding; repair additionally case_digest/evidence. Output is the existing `{type:"result",result:...}` frame. These branches precede credentials, Transport construction and native-owner observation. OFF status has no I/O; enabled status remains a private read with no recovery or SDK import. Redacted status schema is unchanged.
Supported integration sequence: real supervisor stops/joins owned runtime; capture preview after shutdown; invoke repair with the same trusted hook and exact case/evidence. If shutdown changed the preview snapshot, repair refuses without preparing an incident; capture a new preview. After a crash, resume the retained prepared ID/evidence. A restored incident is idempotent while its fence exists; a subsequent disappearance requires a new preview/incident, never replay of the old repair.
Locks are store → transport → nonblocking existing owner flock, held through incident/cancellation/publication/final receipt. The owner inode/device must match the accepted lifecycle journal; an active competing process, missing/replaced lock or snapshot/binding drift denies. The hook runs before these locks; no lock-held runtime join, live I/O or feed lock is introduced.
Optional schema2 `fence_losses` records retain immutable digested intent plus prepared/restored phase. Intent includes original root/binding/lifecycle, ingress/watermark, transport/replies digests, evidence/time, prior hold, barrier digest/size and every prior alert digest/intent digest/cancellation value/reason. A durable prepared record and fence-repair-pending hold precede every cancellation or publication. Initialize, Session startup, qualification and the shared observe_session_locked predicate cannot clear a prepared incident.
Only existing private alert rows' cancelled/reason fields become true/cancelled. Canonical decisions, questions, answers, requests, uncertain attempts, receipts, events, intents, native submissions, ACKs and consumed permits are not rewritten by repair. Qualification positively validates all retired rows under store→transport before and after authentication. No global decision-ID blacklist, canonical cancellation, old-answer copy, automatic reissue or old-permit replay is added.
The barrier is the retained ingress-length prefix plus a nonzero incident-bound loss marker, strictly larger than retained ingress and below MAX_BYTES. It is NOT recovered events or proof of absence. Repair never advances ingress/watermark. Owner-only same-directory staging is fully fsynced, then hard-linked without replacement; exact contents/inode identify an interrupted two-name publication. Only the duplicate staging name is unlinked, leaving the complete final barrier; single-link checks and directory fsync precede restored persistence. Unpublished partial staging remains untouched. Wrong/symlink/unrelated artifacts deny without overwrite.
Restored means bookkeeping complete, not qualification: outage-gap remains, and normal listener/session/epoch, exact binding/watermark/ingress reviewed-gap, drained ingress and uncertainty gates still apply. Every historical alert revision remains refused even with a late exact ACK or previously consumed permit. Only an explicitly new canonical question/revision and new Slack thread can complete normal authenticated reply, manager interpretation, CAS and exact native-ACK gates for one new permit. Later ordinary ACK reconciliation may record its existing audit transition; repair itself does not perform it.
Limits: schema1, never-qualified/missing-binding stores, missing/replaced owner lock, capacity exhaustion or an unrecognized artifact remain held, with no reset/migration/archive route. Interrupted publication scans at most256 directory entries and refuses excess; no pruning is implemented. The production quiescence/supervisor hook is NOT wired or qualified, so default CLI repair deliberately refuses. This is a parent integration prerequisite, not a credential or new human-product decision. No historical lost envelope or already executed work is reconstructed or undone.

## Executed tests and retained failures

All Python invocations ran in this new worktree with the exact prefix below and the table's suffix; no source edits occurred during either full export/source-guard run.

```sh
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/Volumes/CorbanuDrive/Corbanu/.codex-work/initiative-control.oGQGyA/venv/lib/python3.14/site-packages /Volumes/CorbanuDrive/Corbanu/.codex-work/slack-sdk-test.Ob3i5O/venv/bin/python -B -m unittest discover -s scripts/initiative_control
```

| Suffix / exact standalone command | Actual result |
| --- | --- |
| `-p 'test_slack_transport.py' -k repair` initial | Exit1,3tests/3.279s,2failures+6errors: retained publication hard link violated the preexisting single-link safety check. Corrected exact-inode publication cleanup; rerun exit0,3tests/2.648s. |
| `-p 'test_*transport.py' -k repair`; later `-p 'test_slack_transport.py' -k repair` | Exit0,4tests/3.058s; exit0,4tests/3.165s; final after compatibility correction exit0,4tests/3.054s. |
| `-p 'test_decision_manager.py' -k fence` initial | Exit1,7tests/10.406s,1failure: test compared repair's unchanged replies after deliberately invoking ordinary late-ACK reconciliation. Moved exact-byte comparison before that existing audit transition; no runtime bypass. |
| `-p 'test_decision_manager.py' -k fence` subsequent | Exit0,7tests/11.518s and exit0,7tests/11.532s with added cancellation-tamper qualification and canonical-byte checks. |
| `-p 'test_*.py'` first full | Exit1,251tests/144.367s,1error: store-lock error normalization changed the baseline missing-fence qualification FileNotFoundError to Invalid. Preserved the original exception with read-only preflight; authoritative locked checks remain. Baseline assertion unchanged. |
| `-p 'test_slack_transport.py' -k missing_fence_has_no_reset_route` | Exit0,1test/1.540s after compatibility correction. |
| `-p 'test_*.py'` final frozen Python | Exit0,251tests/143.337s. All244 baseline methods/assertions retained; seven added recovery methods. SDK fixture HTTPError500/503/429 cleanup ResourceWarnings remain disclosed, not live errors. |
| `node scripts/initiative_control/test_facilities_js.cjs` | Exit0, Facilities UI regression passed,7 assertions. |
| `python3 docs/plans/check.py`; `python3 docs/sprints/check.py` | Both exit0:3 active plans;115 current/126 archived sprints. |
| `git diff --check`; `git rev-parse HEAD`; `git merge-base --is-ancestor b9e671e60416b952cc20d7b7260d3678080aebd0 HEAD` | All exit0; HEAD/ancestor as above. Final scope/whitespace/Facilities/governance rerun after receipt only. |

Actual spawned-process exits61 at prepared, cancelled, partial, staged, linked, synced and restored boundaries prove reload/re-entry; old open-descriptor callback process is stopped/joined and stale preview rejected. Another actual owner process denies recovery before mutation. Tests cover nonempty lost fence with uncovered bytes, disk-write/link/fsync failure, inode/binding/snapshot/capacity drift, hostile artifacts, default/boolean hook refusal, immutable old journal/alert/feed evidence and prepared admission refusal. Manager tests use real pipes, native-evidence fixture subprocess crash34 and exact assistant ACK frames: late historical ACK and consumed permit cannot dispatch; each fresh explicit revision reaches exactly one new permit through controlled-loopback SDK POST, authenticated callback, CAS and native-stdio evidence. Fixtures are not a production supervisor or live agent qualification.
Existing disposable dependencies were reused without install or shared-environment changes. `importlib.metadata.version` through the pinned Python verified slack-sdk3.44.1/markdown-it-py3.0.0/mdurl0.1.2 (exit0). `shasum -a 256 /Volumes/CorbanuDrive/Corbanu/.codex-work/slack-sdk-test.Ob3i5O/slack_sdk-3.44.1-py2.py3-none-any.whl` exit0: `d6f20a0fbe3fecf9cac955c99d686301b48a645b7045472c3a0cdd186d7c42b2`.

## Frozen paths and next parent gate

Incremental counts use accepted b9e671e60, not cumulative history. Python totals469 changed lines/185 non-test; this57-line receipt yields526 changed lines (519 additions/7 deletions),242 non-test. Original target450/205 is exceeded by76/37; STOP600/260 retains74/18 headroom. Target overage was reported before receipt expansion; no proof compressed or additional source path used.

| Literal path | Added/deleted | SHA256 |
| --- | --- | --- |
| scripts/initiative_control/slack_transport.py | 170/3 | 8fbb5b52c8d7016b97d2b7070e9e524d121570249e2e4c0ebbac148c9f929906 |
| scripts/initiative_control/decision_manager.py | 9/3 | 89d666327e4dbe24efc11306c33350c43d5405f856b27636308ef19ab56f8bc8 |
| scripts/initiative_control/test_slack_transport.py | 188/1 | f2d54880fa758f868594b19020a306195e73c6783e362d0315fb76f0dbb7858c |
| scripts/initiative_control/test_decision_manager.py | 95/0 | 0b404824c3275daf8a133dba1883dc5832964d2fe0df2d9598b1b87ad7f903b3 |

Fifth and only other write: `qa/initiative-control/pf-80-s01/decision-projection/slack-fence-recovery-receipt.md`. Hashes/manifest use `shasum -a 256` in the four-row order followed by this receipt; pipe that output into `shasum -a 256` for the manifest. `git diff --numstat`, `git status --short` and `wc -l` on this receipt establish exact scope/counts. `git diff --no-index --check /dev/null qa/initiative-control/pf-80-s01/decision-projection/slack-fence-recovery-receipt.md` exits1 with zero diagnostics (new file), while tracked `git diff --check` exits0. Receipt hash is returned separately to avoid self-reference.
Parent next: exact frozen-manifest review07, classify any finding before scoped08, then combined receiving proof and separately wire/qualify the real supervisor hook. No worker commit/review/child, shared source/state/plan/old-candidate edit, credentials, live Slack/Task Node/native-agent call, UI, main/push/release or production action occurred. Feature remains OFF. Actual connection/phone/HTTPS/login-return/restart/native ACK, overnight and three-initiative gates remain unrun; this receipt is local recovery proof, not live or whole-sprint acceptance.
