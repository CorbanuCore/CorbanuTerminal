# PF Terminal 0.1.27 Hands-On Acceptance Ledger

Status: **OPERATOR-AUTHORIZED RELEASE CANDIDATE — residual open cells retained below**

Release authorization update (2026-08-03): the operator explicitly directed that 0.1.27 be
packaged, pushed, and released after confirming Task Node recovery/PFTerminal integration and a
successful end-to-end `/spawn` orchestration run. Final local qualification passed the complete
`codex-tui` suite (3,809/3,809; 9 intentionally skipped), the package-builder suite (18/18), and
the installer suite (17/17). This authorization accepts the remaining open matrix cells as
documented residual risk; it does not relabel them as completed acceptance evidence.

Canonical acceptance specification:
`docs/current-sprint/PFTERMINAL-0.1.27-SPEC.md`, section 13.

## Candidate baseline

- Branch: `release/0.1.27-reconstruction`
- HEAD at qualification start: `85f250727b698066ace5e03546e4cd608dc99a53`
- Worktree state: dirty; existing reconstruction work preserved
- Start dirty-state hash (`git status --porcelain=v1 -z`):
  `32dac7563249c8d0a810b075c8e610f31b12eda092a86cfcadffa614a6941227`
- Host: `productionrpc`, Linux x86_64, kernel `6.8.0-49-generic`
- CPU: 48 logical CPUs
- Memory at start: 184 GiB total, 167 GiB available
- Disk at start: 902 GiB total, 555 GiB used, 311 GiB available
- `target/debug/pfterminal` SHA-256:
  `8b7185ccb199ccecfdcbcc4d99b8f0bdeb2e2495bb6655272b1cd7af2fa6fb7e`
- `target/debug/pfterminal-debug` SHA-256:
  - Qualification-start artifact:
    `adf8c3ec55c6edbde8b7d59be52cc4d1b025677426d40c7c1d648bb930726660`
  - `/spawn` permission-repair artifact:
    `b2b6c75c259570aa14f67e96fddf4fa8a40d4868ea69006d42aa58f9f103960e`
  - `/spawn` direct-task and cold-restore repair artifact:
    `dae5d47e8233a4bea1b0a41a9a16bd1f16dfe97d9be4bb7d2d26403d8decff94`
  - Native Core mailbox/provider-wire repair artifact:
    `67ea3a87cf5423efd53ecaf219f8841643b5e389facecb5f8c2873b974a317da`
  - Native Core manager loop-control repair artifact:
    `c660fd4679f74202973d89eef7cd2b386a31f4d0312eeb2279eb68ebdbf86202`
  - Native Core durable-graph and bound-parent repair artifact:
    `7756237732155f49292c9e48bdca5928ba4b4c0349a060ba94817452a35da14c`
  - PFTQA-013 schema-compatible layout and restored-role projection artifact:
    `920e4a117714f8d30e7920cd74f6fc2f411c38477c3a461836f560e770a2252a`
  - PFTQA-014 restored-Nazgul picker-search artifact:
    `1f57870a08d3e34e6ccaa9d33b032c21a867a3806e588c56b7fa18d550fd6080`
  - PFTQA-015 unavailable-pane isolation artifact:
    `c81adcbff7f0b7998e5cdd35733dacc28b219fe92f95979005719a963f20b2a9`
  - PFTQA-016 idle archive cleanup artifact:
    `588130c2281439de5f320bf6a4e8f862106440d71f8efcd0c28d01ad5debbc56`
  - Post-branding archive artifact:
    `42e4fdda7f10e48f7345809f08b014bdcf3eedf5e82173b27f3f1fb85cb36c6a`
  - PFTQA-017 active-archive artifact:
    `12d4b1736a3d24c35815b08f886fd316d4b768a1aba6817720fc350b45d97238`
  - PFTQA-018 active-delete artifact:
    `8a4b5ec474c90120b3fc0c981a64cc46950fa45f647d4581d6c6f915f0971422`
  - PFTQA-019 lifecycle-ownership artifact:
    `017a95b6223efa3089b1f99cc3918e2f47f60a12fcdbe3b476cac2733ff92a1f`
  - PFTQA-020 idle Claude-delete artifact:
    `4c704d4146fe3010c44c76345bc46c4ce476247ac0aeabe4742e5cb049ba5e36`
  - PFTQA-020 active Claude-delete and routing artifact:
    `d43e96dbdf33e41a4dffce60bbb4ef4be60a7bdfe042c6b7f421127f8c966647`
  - Current PFTQA-021 whole-crew lifecycle and empty-projection artifact:
    `2cbab6519397eb344f6435574dbc2608139ccd14892183b75914e07b8a66827e`
  - Current PFTQA-022 wallet and safety-confirmation artifact:
    `f850c7a30c501422cadf3070c5270479575c7ba8560559130acbad7eef896ff9`
  - Current PFTQA-023 vault secure-action and truthful-completion artifact:
    `b162fff7bb9f108109e4d2a16da7ce86b53a9cdd1ba635320127032359677867`
  - Current PFTQA-024 provider-limit and real-route qualification artifact:
    `c9af50fed3747051eead43c390c784bcc08a83a7260f83ca71a28c34641a5a91`
  - Current PFTQA-025 OpenAI reserved-schema and encrypted-follow-up artifact:
    `3e6c5c3727477403b25102ddcdc3107e80a113db852484fb9da9c3b1aa0960f8`
  - Current PFTQA-026 OpenAI interrupt and cold-runtime restoration artifact:
    `45cb31e3822bce7b224ef91c4f807591922ab80daaf9a1933b83bf0b223e7c6d`
  - Current PFTQA-027 rapid slash-command admission artifact:
    `b8d47fe89c99859ef5809c1f5cc5a08db1cdc8a74ad383a4beb59eb8ca818af5`
  - Current PFTQA-028 fresh manual DeepSeek/YOLO crew artifact:
    `b8d47fe89c99859ef5809c1f5cc5a08db1cdc8a74ad383a4beb59eb8ca818af5`
  - Current PFTQA-029 standard-crew mixed-provider adapter artifact:
    `9f7e758a55c526a877f8ef5ba2c8b4c02f9c7779282a14df431404a0d1ac6614`
  - Current PFTQA-030 pane-local provider-projection artifact:
    `8f8e27d698aad0d15a3caa1ecb1fb507ce3b56584849e5ff38bf93d97dad9b4f`
  - Current PFTQA-031 workspace-write permission-resume artifact:
    `db00c6bdd3d94a699b162f4d889498043f1af64a8b9de029f5979d2b318430ef`
  - Current PFTQA-032 on-request and non-OpenAI context-authority artifact:
    `d136f81ef970752c8d2d66ffb5b3020381ecacd170d15e41984f45e4898ed0a7`
  - Current PFTQA-033 read-only permission-contract artifact:
    `d136f81ef970752c8d2d66ffb5b3020381ecacd170d15e41984f45e4898ed0a7`
  - Current PFTQA-034 provider-valid auto-review and Full Access artifact:
    `5e11eaf561f3c7233a42146673b37e2879a40f2c66d3d9ca198c4eeaa350ed65`
  - Current PFTQA-035 fresh YOLO user-pane compound-command artifact:
    `5e11eaf561f3c7233a42146673b37e2879a40f2c66d3d9ca198c4eeaa350ed65`
  - Current PFTQA-043 retained Troll/busy/cold-reuse artifact:
    `7b6574b7fff8ea780f61ce1b0f956d0c91237c803844fdbcd103603019522af0`
  - Current PFTQA-044/PFTQA-045 docs, vault, panes, and Task Node artifact:
    `01f5ab3f9d9ca7af88f1cb31f06f315545cccc87fb8d62d13c574b35da0f4bd9`
  - Current PFTQA-046 ordinary-resume runtime-authority artifact:
    `ae0e967c3d81969b8769781b2b0ebb4d943c86902327219a2f2e6ae5e863523e`
  - Current PFTQA-047 all-advertised-pane packaged-docs artifact:
    `ae0e967c3d81969b8769781b2b0ebb4d943c86902327219a2f2e6ae5e863523e`
  - Current PFTQA-048 central operator-doc rendering artifact:
    `ae0e967c3d81969b8769781b2b0ebb4d943c86902327219a2f2e6ae5e863523e`
  - Current PFTQA-049 slash control-plane argument artifact:
    `05451548f4f4ec7da78a80fdf91a6cd6ba1f603c80e7a92a2fb3128cf4e6545a`
  - Current PFTQA-050 vault storage-safety artifact:
    `942f608981bbb4a42257e7d876d407840a5cd06ddcf228c4693685f32527174f`
  - Current PFTQA-051 local-command and approval-routing artifact:
    `7d25e1d8e2fb64c31a379671e8d797bb4a8a2ea8426a59aa4cdc9c3b2dba94b2`
  - Current PFTQA-052 CLI completion-identity artifact:
    `13708c53ba2f50db1268c12cfabf3cab2afb9c1d87a7d304a2c715f69b9d4440`
  - Current PFTQA-053 wallet inline no-state artifact:
    `3fcf2685e3c11c9ec103729c0754936bfd98d70799bb8a7ab608a56491846834`
  - Current PFTQA-054 retained-spawn provider-recovery artifact:
    `3fcf2685e3c11c9ec103729c0754936bfd98d70799bb8a7ab608a56491846834`
  - Current PFTQA-055 CLI doctor and Bash-completion artifact:
    `1233ac0e1f4447a91f9c080bc4df673f7669f4e3643e9fc28db3f3669472e65c`
  - Current PFTQA-056 provider-search and local-settings artifact:
    `bcf0a0878d7ec79c9a8e972c751ad35c44543fa8524b365503bc9366c0a3094c`
  - Current PFTQA-058 CLI, account-status, permissions, compaction, and Plan-mode artifact:
    `189bd714b18c1e125dedf27ea1eb8d186a52079311f899acc6c7a34f55e193b2`
  - Current PFTQA-059 sandbox, lifecycle-error, and plugin-restart artifact:
    `fb3c40bcfa445cc45546e534826febcfbbc16df1bab444b145e6915d399c5b1a`
  - Current PFTQA-060 native-orchestration and single-audit artifact:
    `87ff1ba7302e03d831d78f9fd0332bced0ce3c5c689a95775419ac1d1d6bd044`
  - Current PFTQA-061 managed-crew active-pane switching artifact:
    `87ff1ba7302e03d831d78f9fd0332bced0ce3c5c689a95775419ac1d1d6bd044`
  - Current PFTQA-062 managed-pane result-ownership artifact:
    `27efd8d403e18c568dab9f515e28da75eb1d9bf978dab4b06209e3af7d6d0d59`
  - Current PFTQA-063 Troll-switching and per-pane model-projection artifact:
    `5c731c3fd6f770ffd31db326e987abed8aae01dda959a2cad4f464c3bd509db5`
  - Current PFTQA-064 Nazgul-switching and managed-root state-projection artifact:
    `a9bc78485af7924956daf8af8fc3235f95d5642fa181f919405d4381b76fc740`
  - Current PFTQA-065 native-Claude-Plan active-pane switching artifact:
    `a9bc78485af7924956daf8af8fc3235f95d5642fa181f919405d4381b76fc740`
  - Current PFTQA-066 native background-terminal stop artifact:
    `0150070f2e8fec0f671db6926f6fdddf8e635bb71a6ff966977947885a127abd`
  - Current PFTQA-067 native pane-name creation/restoration artifact:
    `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`
  - Current PFTQA-068 multi-job `/clean` alias artifact:
    `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`
  - Current PFTQA-069 idle `/quit` and `/exit` artifact:
    `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`
  - Current PFTQA-070 Task Node request-resilience artifact:
    `e85c474f8880ff93bcc01cd0c04566ecd4e4dbfe52baf4471fb3c29593ae5e61`
  - PFTQA-071 large user-run `/spawn` crew artifact:
    `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`
- `target/debug/pfterminal` onboarding and PF user-pane cold-restoration artifact:
  `e1eb0066c4dde6ce5888f22065c1cfc7c462071116d581fe80a5742349a024af`
- `target/debug/pfterminal` `/docs` navigation and packaged-offline artifact:
  `c86d1c6b91b9b7bf82a1f6bf40f56ddf8fa0a12b33013249c388657715cb9acf`
- `target/debug/pfterminal` PFTQA-013 artifact:
  `41bfa2bd4986811c224bf9bebf6508e4233a57e2e78a16c3a49a2c46407691dc`
- `target/debug/pfterminal` PFTQA-014 artifact:
  `9df2651ca9a1e806ada88ebc7477a4dcbc6f2ae7d6a4ea5c42f02930a1962bef`
- Current `target/debug/pfterminal` artifact (not rebuilt for PFTQA-023):
  `9807f38414661753b8f8eb8b7451120f7453e298bfa2efcbaa928b33a37f0953`
- `target/debug/pfterminal-walletd` SHA-256:
  `f8f5263bae77d3e861018682cbb90554be350b7e9629e8b24b5314f95c35b9d9`
- Qualification start: `2026-08-01T23:08:34Z`

This dirty worktree cannot become the final immutable release candidate. It is the active repair
candidate used to find and repair product failures before the clean-artifact gate.

## Spend ledger

Authorized cumulative ceiling: **USD 500.00**.

| Timestamp UTC | System | Action | Resource/request | Estimated USD | Actual USD | Cleanup/receipt | Running total |
| --- | --- | --- | --- | ---: | ---: | --- | ---: |
| 2026-08-01T23:08:34Z | Baseline | Qualification opened | None | 0.00 | 0.00 | N/A | 0.00 |
| 2026-08-01T23:30:28Z | DeepSeek direct | `/spawn` Main/Nazgul diagnosis and clean repair retest | `deepseek-v4-flash`; 82,490 input tokens, including 43,648 cached; 1,695 output tokens across four QA threads | 0.01 | 0.0061 | No retained remote resource; local isolated transcripts retained | 0.0061 |
| 2026-08-02T00:09:23Z | DeepSeek direct | Full Nazgul/Troll/Orc hierarchy, task UI, repeated cold restart, failure diagnosis, and repaired live retests | Cumulative qualification usage: 1,643,104 input tokens, including 1,400,960 cached; 87,806 output tokens. Increment since prior row: 1,560,614 input, 1,357,312 cached, 86,111 output. | 0.06 | 0.0563 incremental; 0.0624 cumulative | No remote resource retained. Runaway Troll verification turn interrupted; isolated local transcripts retained. | 0.0624 |
| 2026-08-02T02:10:26Z | DeepSeek direct | Native Core `spawn_agent`, same-member `followup_task`, provider-wire visibility, and cold-process resume diagnosis/retest | `deepseek-v4-flash`; 1,395,054 input tokens, including 1,230,592 cached; 17,034 output tokens across three isolated QA homes. Includes the intentionally stopped recursive failure run and the final clean run. | 0.04 | 0.0312 incremental; 0.0936 cumulative | No remote resource retained. Final evidence home `/tmp/pfterminal-native-spawn-wire-qa.MOwGzT` retained locally; runaway diagnostic process stopped. | 0.0936 |
| 2026-08-02T03:31:34Z | DeepSeek direct | Core-owned manager loop ceiling, fresh-input recovery, durable no-task hierarchy, cold-result quarantine, and cold-manager recovery | `deepseek-v4-flash`; 505,940 input tokens, including 417,664 cached; 3,145 output tokens across the isolated loop and final hierarchy homes. | 0.02 | 0.0144 incremental; 0.1080 cumulative | No remote resource retained. Evidence homes `/tmp/pfterminal-native-loop-qa.0cHV55` and `/tmp/pfterminal-core-hierarchy-qa.yEXOHF` retained locally; all QA processes stopped. | 0.1080 |
| 2026-08-02T04:40:55Z | DeepSeek direct | Fresh onboarding, provider-save/event handoff, operator-created pane, tool turn, full process exit, exact child restoration, and second tool turn | Earlier onboarding turn: 8,388 input, 9,216 cached, 62 output. Final cold-pane session: 528 input, 34,944 cached, 114 output. | <0.01 | 0.0014 incremental; 0.1094 cumulative | No remote resource retained. Isolated home `/tmp/pfterminal-onboarding-handoff-fixed-qa.FymZYL` retained locally; QA process exited cleanly with status 0. | 0.1094 |
| 2026-08-02T05:07:34Z | DeepSeek direct | `/docs` and `/panes` during two real active Main shell-tool turns | 118,594 input tokens, including 94,976 cached; 285 output tokens. | <0.01 | 0.0037 incremental; 0.1131 cumulative | QA process stopped. Thread `019fc0dd-2395-7940-b076-fe0b1f95d756` was retained in the stable PF home because the harness supplied the debug-home variable to the stable binary; see isolation note below. | 0.1131 |
| 2026-08-02T05:24:04Z | DeepSeek direct | Active PF user-pane switching and return | `deepseek-v4-flash`; 26,843 input tokens, including 26,496 cached; 202 output tokens on isolated thread `019fc0e7-82ff-7f60-af12-5c14fe95db71`. | <0.01 | 0.0002 incremental; 0.1133 cumulative | No remote resource retained. The active turn completed, and the isolated local transcript remains under `/tmp/pfterminal-onboarding-handoff-fixed-qa.FymZYL`. | 0.1133 |
| 2026-08-02T06:41:34Z | DeepSeek direct | Active operator-pane archive admission, interruption, persistence, and cold-restart absence | `deepseek-v4-flash`; 96,436 input tokens, including 76,544 cached; 741 output tokens across completed diagnostic turns and the final interrupted live turn on isolated thread `019fc129-7ad2-7092-ae3d-729962d89f4b`. | <0.01 | 0.0032 incremental; 0.1165 cumulative | Final 240-second tool aborted after 17.1s. Archived rollout and isolated evidence home `/tmp/pfterminal-pane-archive-fresh-qa.5iKrG1` retained; no remote resource retained. | 0.1165 |
| 2026-08-02T06:49:52Z | DeepSeek direct | Active operator-pane permanent delete, process cleanup, and cold-restart absence | `deepseek-v4-flash`; 9,037 input tokens, including 8,960 cached; 150 output tokens on deleted isolated thread `019fc13a-335d-7200-bed0-33cbce9c0973`. | <0.01 | 0.000078 incremental; 0.1166 cumulative | The 240-second tool and thread were shut down; rollout/state were intentionally and irreversibly deleted after explicit confirmation. Isolated owner home retained; no remote resource retained. | 0.1166 |
| 2026-08-02T06:58:32Z | No inference | Claude-pane/native-thread lifecycle ownership guard | Zero-turn Opus 5 Claude Plan pane; confirmed `/archive` and `/delete` without submitting a model prompt. | 0.00 | 0.00 incremental; 0.1166 cumulative | Main remained in normal sessions. Disposable 93 MiB cloned QA home removed after evidence extraction; no remote resource created. | 0.1166 |
| 2026-08-02T07:15:22Z | Claude Plan | Idle/active operator-created Claude-pane permanent delete and external-input routing | Opus 5 Claude Plan. One completed routing probe reported 2,501 input, 8,684 cache-creation input, 14,670 cache-read input, and 2 output tokens; the final active review turn was canceled by confirmed `/delete` before completion and its artifacts were deleted by contract. | Subscription usage | 0.00 metered incremental; 0.1166 cumulative tracked API spend | Idle and active pane directories were permanently removed. The active Claude child was terminated; layout membership became empty; both cold-restart name searches returned no match. Disposable cloned QA home removed after evidence extraction; no remote resource retained. | 0.1166 |
| 2026-08-02T07:47:32Z | No inference | Whole managed-crew cancel/confirm removal, durable cleanup, empty UI projection, and cold restart | Existing native no-task crew; slash/UI and read-only state checks only. Historical token totals shown by the resumed Main did not change. | 0.00 | 0.00 incremental; 0.1166 cumulative tracked API spend | Confirm-cancel retained all members. Confirmed retry removed the three native members, graph/layout ownership, and rollouts while preserving Main. The 92 MiB disposable clone was retained only through documentation and then removed; no remote resource created. | 0.1166 |
| 2026-08-02T08:15:15Z | No inference | Disposable wallet create/restore/lock/backup/balance/plan-list coverage and destructive-confirmation repair/retest | Local wallet daemon plus read-only Solana RPC checks; zero-balance disposable wallets; no model request and no transaction submitted. | 0.00 | 0.00 incremental; 0.1166 cumulative tracked API spend | Exact-artifact retest proved digits cannot commit wallet removal, Esc preserves it, and explicit Down+Enter removes it. Disposable wallet files and test processes were removed; no remote resource or funds retained. | 0.1166 |
| 2026-08-02T08:58:05Z | DeepSeek direct rejected authentication before inference | PFTQA-023 disposable vault add/delete harness error | The harness entered a second disposable marker after the one-entry masked add modal had closed. It became an ordinary prompt, but the deliberately invalid placeholder API key returned 401 before inference. | 0.00 | 0.00 incremental; 0.1166 cumulative tracked API spend | No valid credential, tokens, or remote resource used. The polluted disposable home and tmux session were removed after the truthful-delete retest. | 0.1166 |
| 2026-08-02T09:08:20Z | DeepSeek direct | Provider-harness discovery diagnosis | One real `deepseek-v4-flash` response: 15,313 input tokens, including 512 cached, and 2 output tokens. The harness then failed to discover the completed rollout and submitted no second turn. | <0.01 | 0.0021 incremental; 0.1187 cumulative | No remote resource retained. The initialized isolated home was reused for bounded provider qualification. | 0.1187 |
| 2026-08-02T09:11:57Z | DeepSeek direct | Two-turn response and shell-tool qualification | `deepseek-v4-flash`; 46,127 input tokens, including 45,824 cached, and 67 output tokens. Both turns completed in 7.38 seconds. | <0.01 | 0.0002 incremental; 0.1189 cumulative | Exact provider/model events and the successful marker tool call are retained in the isolated rollout. | 0.1189 |
| 2026-08-02T09:13:56Z | OpenRouter | Pinned DeepSeek Flash response and shell-tool qualification | Exact route `openrouter` / `deepseek/deepseek-v4-flash-0731`; 42,176 input tokens, including 13,952 cached, and 82 output tokens. Both turns completed in 10.98 seconds. | <0.01 | 0.0040 incremental; 0.1229 cumulative | Exact provider/model events and the successful marker tool call are retained in the isolated rollout; no fallback route was configured. | 0.1229 |
| 2026-08-02T09:15:20Z | No inference | Vercel GLM metadata and harness false-positive diagnosis | The selectable `vercel-anthropic-fast` / `zai/glm-5.2-fast` route terminated locally because `max_output_tokens` was absent. The old benchmark incorrectly counted two local `task_complete` failures as successful turns. | 0.00 | 0.00 incremental; 0.1229 cumulative | The harness now requires a provider-response event paired with terminal completion. The same failed rollout evaluates to zero completed turns. | 0.1229 |
| 2026-08-02T09:20:12Z | Vercel rejected authentication before inference | Rebuilt GLM 5.2 Fast serialization probe | Artifact `c9af50fed3747051eead43c390c784bcc08a83a7260f83ca71a28c34641a5a91` used the provider-published 128K output limit and reached `https://ai-gateway.vercel.sh/v1/messages`; the resident credential returned 401. | 0.00 | 0.00 incremental; 0.1229 cumulative | The missing/invented-limit defect is repaired, but Vercel remains unaccepted until a valid credential completes real response and tool turns. | 0.1229 |
| 2026-08-02T09:21:20Z | Anthropic direct | Opus 5 auth/output-limit probe plus two-turn shell-tool qualification | Probe: 17,998 input/cache-write tokens and 4 output. Final session: 58,211 total input tokens, including 38,672 cache reads and 19,533 cache writes, plus 111 output; two turns completed in 9.04 seconds. | <0.30 | 0.2568 estimated incremental; 0.3797 estimated cumulative | Exact `anthropic` / `claude-opus-5` events and the successful marker tool call are retained. Estimate includes Anthropic cache-write premium; provider invoice not yet reconciled. | 0.3797 |
| 2026-08-02T09:22:24Z | Claude Plan | Opus 5 Plan auth probe plus two-turn shell-tool qualification | Existing released OAuth helper authenticated `claude-plan` / `claude-opus-5-plan`. Final session recorded 58,308 total input tokens, including 38,730 cache reads and 19,572 cache writes, plus 117 output; two turns completed in 8.75 seconds. | Subscription usage | 0.00 metered incremental; 0.3797 cumulative tracked API spend | Exact plan provider/model events and successful marker tool call retained; this consumes plan quota rather than API-key billing. | 0.3797 |
| 2026-08-02T09:26:33Z | DeepSeek direct | Current-artifact strict tool-call retest | Artifact `c9af50fed3747051eead43c390c784bcc08a83a7260f83ca71a28c34641a5a91`; 46,145 total input tokens, including 45,824 cached, and 82 output. Two turns and exactly one persisted function call completed in 7.60 seconds. | <0.01 | 0.0002 incremental; 0.3799 cumulative | Strict benchmark passed with exact `deepseek` / `deepseek-v4-flash` identity. | 0.3799 |
| 2026-08-02T09:26:45Z | OpenRouter | Current-artifact strict pinned-route retest | Artifact `c9af50fed3747051eead43c390c784bcc08a83a7260f83ca71a28c34641a5a91`; exact `openrouter` / `deepseek/deepseek-v4-flash-0731`; 42,194 total input tokens, including 28,160 cached, and 80 output. Two turns and exactly one persisted function call completed in 9.28 seconds. | <0.01 | 0.0021 incremental; 0.3820 cumulative | Strict benchmark passed with no fallback configured. | 0.3820 |
| 2026-08-02T09:44:49Z | OpenAI API | Luna reserved collaboration schema and same-child native follow-up diagnosis/retest | Failed schema probes reached HTTP 400 before inference and incurred no recorded tokens. Successful runs used 46,664 uncached input, 167,344 cached input, and 425 output tokens across the normal two-turn probe, the diagnostic native lifecycle, and the repaired native lifecycle. | <0.07 | 0.0659 estimated incremental; 0.4479 estimated cumulative | Exact artifact `3e6c5c3727477403b25102ddcdc3107e80a113db852484fb9da9c3b1aa0960f8` completed `spawn_agent`/`wait_agent`, then encrypted `followup_task`/`wait_agent` on the same retained child with distinct `FIRST_OK` and `SECOND_OK` results. Estimate uses the bundled Luna API-key rates; provider invoice not yet reconciled. | 0.4479 |
| 2026-08-02T10:09:58Z | OpenAI API | Running-child steering, exact target-only interrupt, same-child recovery, and cold-process continuation diagnosis/retest | Estimated 85,070 uncached input, 793,026 cached input, and 2,399 output tokens across control, interrupt, failed cold-resume diagnosis, and repaired cold-resume runs. | <0.18 | 0.1788 estimated incremental; 0.6267 estimated cumulative | Artifact `45cb31e3822bce7b224ef91c4f807591922ab80daaf9a1933b83bf0b223e7c6d` resumed exact parent `019fc1ea-0659-7cf1-8d1d-a7106ca5a3ae` in a new process and appended `COLD_FIXED_ONE` and `COLD_FIXED_TWO` to the original child `019fc1ea-0c53-77c3-83cc-835ffe318493`; one parent spawn total, no remote resource retained. Estimate uses bundled Luna API-key rates; invoice not yet reconciled. | 0.6267 |
| 2026-08-02T10:10:44Z | DeepSeek direct | Current-code retained-role direct input and Nazgul -> Troll -> Orc native-follow-up qualification | Incremental 227,318 input tokens, including 175,104 cached, and 1,468 output tokens across Angmar, Burzum, and Snaga. | <0.01 | 0.0082 estimated incremental; 0.6349 estimated cumulative | Direct Snaga input returned `CURRENT_ORC_DIRECT_OK`; Burzum reused Snaga and returned `TROLL_MANAGER_OK`; Angmar reused Burzum, which reused Snaga, and returned all three Nazgul-chain markers. No agent was spawned, replaced, closed, or shut down. DeepSeek prices use the operator-supplied Flash rates; invoice not yet reconciled. | 0.6349 |
| 2026-08-02T10:19:58Z | No inference | PFTQA-027 rapid slash-command diagnosis and final formatted-artifact live retest | Focused rapid-key-stream regression plus the `burst_enter` set; live `/panes`, bare `/docs`, and configured `/docs` dispatch. | 0.00 | 0.00 incremental; 0.6349 cumulative tracked API spend | Final formatted artifact `b8d47fe89c99859ef5809c1f5cc5a08db1cdc8a74ad383a4beb59eb8ca818af5` opened `/panes` with one Enter, dispatched bare `/docs` with one Enter, and opened the repository's 50-page terminal docs viewer with one Enter. The disposable tmux session was stopped; no matching QA process remained. | 0.6349 |
| 2026-08-02T10:30:12Z | DeepSeek direct | PFTQA-028 fresh manual DeepSeek/YOLO crew creation, role-local input, native hierarchy reuse, and cold restart | 181,371 input tokens, including 126,464 cached, and 1,605 output tokens across Main, Angmar, Burzum, and Snaga. | <0.01 | 0.0085 estimated incremental; 0.6434 estimated cumulative | Exact artifact `b8d47fe89c99859ef5809c1f5cc5a08db1cdc8a74ad383a4beb59eb8ca818af5`. Three initial `NEW_*` inputs landed on Main and are not counted as role evidence. Exact role panes returned all `ACTUAL_*` and `FRESH_*` markers; a new process reused original Snaga `019fc201-bdf5-7c82-94f5-5a0b669c4de8` for `FRESH_COLD_ORC_OK`. No agent was replaced, closed, or shut down. The isolated home is retained; both QA processes exited. | 0.6434 |
| 2026-08-02T11:24:20Z | Claude Plan, OpenAI API, and OpenRouter | PFTQA-029 standard-crew role input, mixed-provider delegation diagnosis, typed-adapter repair, and final retained-crew retest | Cumulative isolated-home usage: Claude Plan/Fable 466,815 input (414,510 cache reads, 52,261 cache writes) and 6,908 output; OpenAI/Sol 451,100 input (409,125 cached) and 3,343 output; OpenAI/Luna 40,465 input (18,994 cached) and 74 output; OpenAI/Terra 40,417 input (18,994 cached) and 39 output; OpenRouter/Grok 29,804 input (19,840 cached) and 113 output. | Provider invoices pending | Plan quota plus OpenAI/OpenRouter invoice reconciliation pending; not added to the numeric cumulative total | Exact artifact `9f7e758a55c526a877f8ef5ba2c8b4c02f9c7779282a14df431404a0d1ac6614`. Final chain earned all `ADAPTER2_*` markers on the original five role threads. The retained tmux session and isolated home remain available for follow-up QA; no remote compute resource was created. | 0.6434 plus unreconciled mixed-provider usage |
| 2026-08-02T11:37:47Z | No inference | PFTQA-030 retained mixed-provider pane-local `/status` repair and cold-process retest | Focused TUI regression plus status-only inspection of original Claude Plan Angmar and OpenRouter Krimp threads. | 0.00 | 0.00 incremental | Artifact `8f8e27d698aad0d15a3caa1ecb1fb507ce3b56584849e5ff38bf93d97dad9b4f`. Angmar reported `Claude Plan - https://api.anthropic.com/v1`; Krimp reported `OpenRouter - https://openrouter.ai/api/v1`. Original IDs, roles, transcripts, completed results, and reusable idle state remained intact. The isolated tmux session remains available; no provider request or remote resource was created. | 0.6434 plus unreconciled mixed-provider usage |
| 2026-08-02T12:08:14Z | DeepSeek direct | PFTQA-031 workspace-write inheritance, managed-chain enforcement, explicit reseed, and flagless cold restoration | `deepseek-v4-flash`; cumulative isolated-home usage 463,176 input tokens, including 393,088 cached, and 7,919 output tokens across Main, Angmar, Burzum, and Snaga. | <0.02 | 0.0131 estimated incremental; 0.6565 numeric cumulative | Artifact `db00c6bdd3d94a699b162f4d889498043f1af64a8b9de029f5979d2b318430ef`. Every role and the managed chain wrote inside the workspace and received exit 1 outside it. A flagless new process restored workspace/never on exact Main and exact Snaga, repeated both checks, and retained original Burzum/Snaga IDs without replacement or shutdown. All outside artifacts are absent; isolated home and tmux evidence remain local. | 0.6565 plus unreconciled mixed-provider usage |
| 2026-08-02T12:29:09Z | DeepSeek direct | PFTQA-032 explicit on-request propagation, stale-context diagnosis, managed native follow-up, approval rejection, and flagless cold restoration | Incremental 666,946 input tokens, including 488,704 cached, and 9,143 output tokens across the retained Main, Angmar, Burzum, and Snaga; cumulative isolated-home totals are 1,130,122 input, 881,792 cached, and 17,062 output. | <0.04 | 0.0289 estimated incremental; 0.6854 numeric cumulative | Artifact `d136f81ef970752c8d2d66ffb5b3020381ecacd170d15e41984f45e4898ed0a7`. All four roles raised real approval dialogs for exact outside writes; each dialog was rejected and all targets are absent. Angmar -> existing Burzum -> existing Snaga returned and independently verified `ONREQ_CHAIN_OK`. A flagless new process restored on-request on exact Main and Snaga and repeated rejected approval. Same IDs/hierarchy remain live; isolated home and tmux evidence remain local. | 0.6854 plus unreconciled mixed-provider usage |
| 2026-08-02T12:36:50Z | DeepSeek direct | PFTQA-033 read-only role inheritance, rejected workspace writes, managed native read verification, and flagless cold restoration | Incremental 570,047 input tokens, including 412,288 cached, and 5,313 output tokens across retained Main, Angmar, Burzum, and Snaga; cumulative isolated-home totals are 1,700,169 input, 1,294,080 cached, and 22,375 output. | <0.03 | 0.0247 estimated incremental; 0.7101 numeric cumulative | Same exact artifact `d136f81ef970752c8d2d66ffb5b3020381ecacd170d15e41984f45e4898ed0a7`. Every role reported Read Only/Ask, completed compound and file reads, raised approval for a workspace write, survived rejection, and left its target absent. Existing Angmar -> Burzum -> Snaga independently verified exit 0 and `[workspace]`. Flagless cold Main/Snaga restored Read Only/Ask and repeated rejected writes; all six explicit/cold targets are absent and the same hierarchy remains live. | 0.7101 plus unreconciled mixed-provider usage |
| 2026-08-02T12:56:13Z | DeepSeek direct | PFTQA-034 provider-valid automatic approval review, Full Access confirmation/direct execution, cold restoration, and safe-profile reset | Incremental 208,691 input tokens, including 138,368 cached, and 3,188 output tokens across the retained hierarchy and one Guardian reviewer; cumulative isolated-home totals are 1,908,860 input, 1,432,448 cached, and 25,563 output. | <0.02 | 0.0111 estimated incremental; 0.7212 numeric cumulative | Artifact `5e11eaf561f3c7233a42146673b37e2879a40f2c66d3d9ca198c4eeaa350ed65`. Exact Snaga used Approve for me to run an outside-workspace command: the DeepSeek-bound Guardian returned low-risk allow and the command completed without a human dialog or `codex-auto-review` route error. Full Access defaulted its warning to Cancel; cancel preserved the prior profile, explicit confirmation enabled Full Access, an outside command ran without approval, and a flagless new process restored Full Access on the exact Snaga. QA then reset the live hierarchy to Workspace/Ask and removed both markers. No member was spawned, replaced, interrupted, closed, or shut down. | 0.7212 plus unreconciled mixed-provider usage |
| 2026-08-02T13:00:47Z | DeepSeek direct | PFTQA-035 exact `--yolo` fresh user-pane compound-command acceptance | Incremental 17,780 input tokens, including 10,240 cached, and 206 output tokens on new persistent pane `019fc28f-d89f-7b41-92d1-b6939536ba02`; cumulative isolated-home totals are 1,926,640 input, 1,442,688 cached, and 25,769 output. | <0.01 | 0.0011 estimated incremental; 0.7223 numeric cumulative | Artifact `5e11eaf561f3c7233a42146673b37e2879a40f2c66d3d9ca198c4eeaa350ed65` launched with literal `--yolo`, created and switched to a fresh DeepSeek direct/high PFTerminal pane with no task, and reported YOLO/Full Access. Its one outside-write + read + Git compound command exited 0 without approval or bubblewrap. The marker was removed; a safe explicit restart restored Workspace/Ask on Main, this same new pane, and exact retained Snaga. The pane and managed crew remain persistent/reusable. | 0.7223 plus unreconciled mixed-provider usage |
| 2026-08-02T13:34:58Z | DeepSeek direct | PFTQA-037 generic vault provider-key add/replace/delete cache-coherence diagnosis and repaired live retest | Old-process cumulative successful usage was 29,501 input, including 19,968 cached, and 40 output; repaired-process successful usage was 19,693 input, including 19,456 cached, and 48 output. The invalid-key and missing-key failures produced no model output. | <0.01 | 0.0015 estimated incremental across old and repaired successful turns; 0.7238 numeric cumulative | Artifact `a4aa83739b953b87aa011f3eb6c8bbe2d7189c55d92d764321fcdf78432ab026`. Pre-repair delete incorrectly retained cached auth. Repaired same-process sequence returned `VAULT_CACHE_FIXED_REPLACE_OK`, failed after confirmed delete with missing `DEEPSEEK_API_KEY`, then returned `VAULT_CACHE_FIXED_ADD_OK` after confirmed generic add. The run also exposed that callers can race an async mutation before its completion toast; that UX row remains open. | 0.7238 plus unreconciled mixed-provider usage |
| 2026-08-02T13:43:00Z | No inference | PFTQA-038 current GPU catalogue, pre-charge cancellation, and local billing-state reconciliation | Current artifact and read-only state inspection; no provider request through PFTerminal, inference, or rental. | 0.00 | 0.00 incremental; 0.7238 numeric cumulative | Artifact `a4aa83739b953b87aa011f3eb6c8bbe2d7189c55d92d764321fcdf78432ab026` rendered the qualified official DeepSeek 0731 TP2 recipe at current r3, GLM, and two explicitly experimental GGUF routes. Esc cancelled at the first price-limit step. All 33 historical local rentals are terminated-confirmed or failed, with zero runtime providers. Standalone Vast/RunPod key files returned 403/401, so independent provider inventory remains open. | 0.7238 plus unreconciled mixed-provider usage |
| 2026-08-02T13:46:16Z | DeepSeek direct plus no-inference read-only diagnostics | PFTQA-039/PFTQA-040 Telegram, Task Node, doctor, goals, memories, MCP, hooks, session lifecycle, and local command-surface QA | Persisted main/fork rollouts recorded 965,436 input tokens, including 850,560 cached, and 7,905 output tokens. Ephemeral side turns and the memory worker are not fully represented in that rollout sum. | <0.03 recorded plus unreconciled ephemeral work | 0.0207 estimated recorded incremental; 0.7445 numeric cumulative | Current artifact `a4aa83739b953b87aa011f3eb6c8bbe2d7189c55d92d764321fcdf78432ab026`. Real evidence includes goal pause/edit/restart/clear, completed memory consolidation, approved nested MCP result `MCP_NESTED_OK`, trusted hook execution on first turn, isolated side/btw replies, exact session IDs across new/resume/fork, and observable local UI persistence. No Telegram message, Task Node mutation, plugin install, app write, GPU rental, or other remote resource was created. | 0.7445 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T14:38:00Z | DeepSeek direct plus no-inference command QA | PFTQA-041 import routing/identity repair and remaining core-command slice | Disposable-home cumulative persisted usage reached 1,159,481 input tokens, including 1,003,904 cached, and 13,345 output tokens. This is 194,045 input, including 153,344 cached, and 5,440 output beyond the prior recorded checkpoint. | <0.01 recorded incremental plus previously unreconciled ephemeral work | 0.0077 estimated recorded incremental; 0.7522 numeric cumulative | Exact artifact `7b6574b7fff8ea780f61ce1b0f956d0c91237c803844fdbcd103603019522af0`. Real `/init`, `/diff`, `/review`, `/compact`, Plan/Default, `/model`, `/providers`, `/orchestrate`, `/clear`, and `/logout` workflows completed. `/import` pre-repair rendered but accepted no input; after the shared event-boundary repair, live navigation, explicit Cancel, and Escape all worked. Import execution was intentionally cancelled and created no imported data or remote resource. After evidence capture, the 95 MiB disposable home, workspace, launcher, token scratch file, and generated `.snap.new` files were removed; no matching process remained. | 0.7522 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T14:44:00Z | DeepSeek direct plus local plugin operations | PFTQA-042 plugin install/disable/enable/discovery/uninstall | One accidental read-only prompt recorded 21,835 input tokens, including 11,264 cached, and 235 output tokens; plugin operations themselves used no inference. | <0.01 | 0.0016 estimated incremental; 0.7538 numeric cumulative | Exact artifact `7b6574b7fff8ea780f61ce1b0f956d0c91237c803844fdbcd103603019522af0`. Build Web Apps moved available → installed → disabled → enabled → uninstalled, and `/skills` exposed its plugin result while installed. No app authenticated, no remote marketplace was added, and the accidental prompt was interrupted after a read-only listing. The 95 MiB home, workspace, launcher, and empty stale `/tmp/.git` marker were removed; no process remained. | 0.7538 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T15:00:00Z | DeepSeek direct | PFTQA-043 role-correct Troll UI, busy native delivery, and cold reuse | Retained-crew totals rose by 438,410 input tokens, including 373,376 cached, and 2,565 output tokens beyond the prior checkpoint. | <0.02 | 0.0109 estimated incremental; 0.7647 numeric cumulative | Artifact `7b6574b7fff8ea780f61ce1b0f956d0c91237c803844fdbcd103603019522af0`; exact Burzum/Snaga IDs retained, new cold work completed, and no member or remote resource was removed or replaced. | 0.7647 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T16:05:00Z | DeepSeek direct plus no-inference local feature QA | PFTQA-044 through PFTQA-046 docs/vault/panes/Task Node/session work | Persisted unique provider work is approximately 38,580 input tokens, including 20,352 cached, and 166 output tokens across the original pane turn, named fork divergence, and post-resume response. Ephemeral `/side` and `/btw` use is not fully represented. | <0.01 recorded plus unreconciled ephemeral work | 0.0027 estimated recorded incremental; 0.7674 numeric cumulative | Final artifact `ae0e967c3d81969b8769781b2b0ebb4d943c86902327219a2f2e6ae5e863523e`. No wallet funds, Telegram bot, Task Node account, GPU rental, or other remote resource was created. The deterministic Task Node stall listener was stopped. | 0.7674 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T16:30:00Z | No inference | PFTQA-047 packaged docs in every advertised pane class and QA-state cleanup | No provider turn was started; all actions were local TUI navigation and lifecycle operations. | 0.00 | 0.00 incremental; 0.7674 numeric cumulative | Final artifact `ae0e967c3d81969b8769781b2b0ebb4d943c86902327219a2f2e6ae5e863523e` opened bare or targeted docs from Main, PF user, Nazgul, Troll, Orc, and a disposable Opus 5 Claude Plan pane. The Claude pane was deleted after the check. Two isolated homes containing copied credentials (184 MiB total), their shared workspace, a small empty fixture, and the stale detached Telegram QA daemon were removed. The 107 MiB retained crew home remains for continuing crew QA. | 0.7674 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T17:05:00Z | No inference | PFTQA-048/PFTQA-049 operator-doc accuracy and slash control-plane admission | No provider turn was started; edited pages were rendered locally and live slash verification retained exactly ten source turn contexts before and after. | 0.00 | 0.00 incremental; 0.7674 numeric cumulative | Central operator docs now include direct/pinned DeepSeek, Kimi, Meta, current Claude/OpenAI examples, and preserved PF product commands. Final artifact `05451548f4f4ec7da78a80fdf91a6cd6ba1f603c80e7a92a2fb3128cf4e6545a` kept `/panes Burzum` and `/pane` local: the former warned and opened bare `/panes`; the latter retained the draft and suggested `/panes`. | 0.7674 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T16:44:38Z | No inference | PFTQA-050 vault concurrent access, wrong-key, and corruption safety | Local encrypted-vault integration tests only; no provider request or remote mutation. | 0.00 | 0.00 incremental; 0.7674 numeric cumulative | Pre-repair synchronized writers reproduced unrecoverable decrypt failures twice. The storage-transaction lock repaired the boundary; the serial suite passed 17/17, including ciphertext-preserving wrong-key failure and reopened corrupt-store rejection. Final artifact `942f608981bbb4a42257e7d876d407840a5cd06ddcf228c4693685f32527174f` built successfully. | 0.7674 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T16:57:41Z | No inference | PFTQA-051 feedback/IDE/pets/Vim/personality/rollout/ps and debug approval | Local TUI navigation and focused tests only; no provider request, feedback upload, background process, or remote mutation. | 0.00 | 0.00 incremental; 0.7674 numeric cumulative | The live pre-repair debug approval ended with `Not available in TUI yet`; the explicit response-destination repair passed three focused regressions and artifact `7d25e1d8e2fb64c31a379671e8d797bb4a8a2ea8426a59aa4cdc9c3b2dba94b2` returned `Local approval resolved: cancelled.` The cold-restarted retained standard crew preserved exact Angmar/Burzum/Snaga/Ghash identities. | 0.7674 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T17:02:57Z | No inference | PFTQA-052 CLI help and completion identity | Local help/completion generation and focused tests only; no provider request or state mutation. | 0.00 | 0.00 incremental; 0.7674 numeric cumulative | All five pre-repair scripts registered Codex. The all-shell regression passed in three binary targets; artifact `13708c53ba2f50db1268c12cfabf3cab2afb9c1d87a7d304a2c715f69b9d4440` emits PFTerminal registrations for Bash/Zsh/Fish/PowerShell/Elvish and root help points to `$PFTERMINAL_HOME`. | 0.7674 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T17:11:10Z | No inference | PFTQA-053 wallet inline no-state parity | Local TUI and daemon-state checks only; no wallet, secret, funds, plan, inference, or remote mutation. | 0.00 | 0.00 incremental; 0.7674 numeric cumulative | Pre-repair unlock solicited an unnecessary passcode and lock claimed a nonexistent transition. The focused set passed 7/7. Artifact `3fcf2685e3c11c9ec103729c0754936bfd98d70799bb8a7ab608a56491846834` live-tested status/create/restore/unlock/lock; create/restore cancelled masked, unlock requested no secret, and lock reported nothing existed. | 0.7674 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T17:15:12Z | DeepSeek direct | PFTQA-054 retained `/spawn` provider failure and recovery | One invalid-key request failed before token use; one valid recovery turn used 20,012 input tokens (18,432 cached) and 175 output tokens. | <0.01 | 0.0003 estimated incremental; 0.7677 numeric cumulative | Artifact `3fcf2685e3c11c9ec103729c0754936bfd98d70799bb8a7ab608a56491846834`. Exact Snaga `019fc248-0e17-7281-a37c-741db6f6b1d3` remained addressable after the visible 401, cold-resumed with the same ID, returned `SPAWN_RECOVERY_OK`, and left the persisted crew `ready`. No agent was removed, replaced, interrupted, or shut down. | 0.7677 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T17:22:00Z | No inference | PFTQA-055 CLI doctor identity and completion execution | Local CLI generation, diagnostic probes, compile, and tests only. | 0.00 | 0.00 incremental; 0.7677 numeric cumulative | Artifact `1233ac0e1f4447a91f9c080bc4df673f7669f4e3643e9fc28db3f3669472e65c`. Clean Bash sourcing registered and exercised `pfterminal`; `doctor --json` returned `overallStatus: ok`, `pfterminalVersion`, `PFTERMINAL_HOME`, correct branch, and no `codexVersion`. CLI run: 848/849 passed; one unrelated cloud-sandbox integration test failed after retry. Other supported shells are absent on this host. | 0.7677 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T17:45:00Z | DeepSeek direct plus local UI | PFTQA-056 provider search and local settings/extension breadth | Most actions were local. One unsupported-command draft was accidentally combined with the next slash command and admitted as a DeepSeek turn; it was interrupted. Telemetry recorded about 15,664 uncached input, 82,688 cached input, and 1,564 output tokens across its tool loop. | <0.01 | 0.0029 estimated incremental; 0.7706 numeric cumulative | Artifact `bcf0a0878d7ec79c9a8e972c751ad35c44543fa8524b365503bc9366c0a3094c`. OpenRouter search repaired and live-qualified; Direct DeepSeek remained visible. Status/config/diff/keymap/raw/theme/title/statusline/experimental/memories/skills/hooks/MCP/apps/plugins surfaces were used without credential, plugin, hook, MCP, or connector mutation. Features 33/33; full TUI 3,778/3,803 with 25 failures retained as release work. | 0.7706 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T17:47:50Z | DeepSeek direct plus local UI | PFTQA-057 rebuilt runtime, retained crew, side turns, file mention, and local service admission | Session telemetry grew from about 36.5K to 61.1K total tokens during the batch: approximately 22.5K additional input and 2.0K output tokens, including goal continuation, response/tool, side-turn, init, and attachment turns. | <0.01 | 0.0037 conservative estimated incremental; 0.7743 numeric cumulative | Exact live process artifact `9d90437015b51c831af2e402e065ac21df7e3fb1e858e8d84cf2555a08d8b47f`. Cold resume preserved Main and all five managed crew IDs. Direct DeepSeek response/tool, copy, `/btw`, `/side`, `/init`, text-file `/mention`, import preview/cancel, usage/process/rollout, goal lifecycle, GPU catalogue, Telegram unlinked, Task Node unlinked, pets/personality compatibility, updated Claude descriptions, and OpenRouter provider search were exercised. No import, GPU rental, Telegram connection, Task Node login, credential, wallet, or plugin mutation occurred. | 0.7743 plus unreconciled mixed-provider and ephemeral usage |
| 2026-08-02T18:50:31Z | Direct DeepSeek, OpenAI API, Claude Plan, and local UI/CLI | PFTQA-058 provider-aware status, permissions identity, CLI execution/review, compaction, and Plan mode | Direct DeepSeek CLI/TUI work and two OpenAI Luna API turns were billable; the observed Luna turns used 10,402 and 10,586 input tokens with small outputs. Claude Plan used subscription quota. Exact invoice cost is not yet reconciled. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `189bd714b18c1e125dedf27ea1eb8d186a52079311f899acc6c7a34f55e193b2`. A persistent Opus 5 Claude Plan pane completed response and shell-tool turns, cold-restored its exact transcript, and now reports command-backed account auth rather than a nonexistent API key. CLI `exec` help, `$PFTERMINAL_HOME`, and visible assistant labels now identify PFTerminal. Direct DeepSeek completed human, JSON, stdin, and tool executions; OpenAI Luna completed response and tool executions; real `review --uncommitted` found the deliberately introduced behavioral regression in a disposable fixture, which was removed. Shared permission descriptions now identify PFTerminal and pass four focused popup tests; live `/permissions` confirmed the rebuilt copy after cold resume. `/compact` completed and a subsequent turn returned `COMPACT_CONTINUE_OK`. `/plan` proposed and revised without tools, explicit “stay in Plan” retained Plan mode, and explicit approval switched to Default and executed only the approved read-only commands. One QA-harness modal-navigation mistake selected “clear context and implement” and created a disposable probe; the probe was removed and the correctly controlled rerun passed. No release promotion or remote resource mutation occurred. | 0.7743 plus unreconciled mixed-provider, OpenAI, and ephemeral usage |
| 2026-08-02T18:57:37Z | Local CLI plus one Direct DeepSeek plugin-skill turn | PFTQA-059 sandbox enforcement, actionable lifecycle errors, and plugin restart persistence | Sandbox and lifecycle checks used no inference. One selected plugin skill returned the exact requested marker on Direct DeepSeek; its invoice cost is not separately reconciled. | <0.01 | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `fb3c40bcfa445cc45546e534826febcfbbc16df1bab444b145e6915d399c5b1a`. Read-only sandbox could read but not write or resolve network; workspace mode wrote inside the workspace and could not write into the repository; Full Access reached the network; all exit codes propagated. Missing archive/unarchive targets returned actionable errors. Missing forced delete originally collapsed its app-server cause; the shared report-chain repair passed in all three CLI binaries and live output now includes the failed RPC, missing rollout, and exact thread ID. The local `build-web-apps` curated plugin installed, survived process restart, exposed its plugin row and `frontend-app-builder` skill, completed a selected-skill turn, then was removed; after another restart neither the plugin nor its skills remained. All disposable sandbox/helper artifacts were removed. No plugin, marketplace, credential, or remote resource remains. | 0.7743 plus unreconciled mixed-provider, OpenAI, and ephemeral usage |
| 2026-08-02T19:24:39Z | Claude Plan, OpenAI API, OpenRouter failure path, and local orchestration | PFTQA-060 native Multi-Agent v2 orchestration dispatch, exact assignment state, Core result handoff, and duplicate-audit removal | Claude Fable/Opus Plan used subscription quota; retained OpenAI workers performed bounded probe turns; one retained OpenRouter worker failed before inference because its key was unavailable. Exact provider invoices are not reconciled. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Final artifact `87ff1ba7302e03d831d78f9fd0332bced0ce3c5c689a95775419ac1d1d6bd044`. Pre-repair assignments preserved the legacy-prompt, wrong-protocol-event, holder-only-correlation, and duplicate-audit evidence. The final retained Angmar -> Burzum assignment used one v2 `followup_task`, transitioned Drafting to Executing with delivered state, received exact `ORCHESTRATE_NO_DUP_OK` through Core, completed one Manager audit, and persisted Done with `fires: 0`. The 33-test affected set passed before fix/format/build. No agent or remote resource was created, replaced, closed, or shut down. | 0.7743 plus unreconciled mixed-provider, OpenAI, and ephemeral usage |
| 2026-08-02T19:28:02Z | OpenAI Luna API and local pane control | PFTQA-061 managed Orc active-turn `/panes` switching and transcript preservation | Exact retained Snaga used one bounded 30-second shell-tool turn plus a shorter setup probe. OpenAI invoice cost is not yet reconciled. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `87ff1ba7302e03d831d78f9fd0332bced0ce3c5c689a95775419ac1d1d6bd044`. Exact Snaga `019fc20e-c6c8-7cd1-ab9d-371651216c8b` showed running in `/panes`; control switched to Main without interruption; after completion the picker showed exact `CREW_PANE_SWITCH_OK`; returning preserved the command, output, and exit 0. No agent or remote resource was created, replaced, closed, or shut down. | 0.7743 plus unreconciled mixed-provider, OpenAI, and ephemeral usage |
| 2026-08-02T19:45:31Z | OpenAI Luna API, Claude Plan, and local Core routing | PFTQA-062 direct managed-pane result ownership and delegated parent delivery | Exact retained Snaga and Burzum performed one short response each; Angmar performed dispatch and audit turns. Provider invoices are not yet reconciled. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `27efd8d403e18c568dab9f515e28da75eb1d9bf978dab4b06209e3af7d6d0d59`. Pre-repair direct Snaga work cascaded through Burzum and replayed stale output into Angmar. The generalized admission-provenance repair passed focused classification, task wiring, direct-child, delegated-child, and public mock-API integration tests. Live exact Snaga returned `DIRECT_CHILD_NO_CASCADE_27EFD8`; after eight seconds only its rollout contained the marker and both parent rollout stats were byte-for-byte unchanged. Exact Angmar then issued one `followup_task` to exact Burzum and received one terminal envelope plus one `DELEGATED_AUDIT_OK`. No agent or remote resource was created, replaced, closed, or shut down. | 0.7743 plus unreconciled mixed-provider, OpenAI, and ephemeral usage |
| 2026-08-02T19:56:59Z | OpenAI Sol API and local pane control | PFTQA-063 active Troll switching and per-pane model projection | Exact retained Burzum used one bounded 20-second shell-tool turn. OpenAI invoice cost is not yet reconciled. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `5c731c3fd6f770ffd31db326e987abed8aae01dda959a2cad4f464c3bd509db5`. Burzum showed running, continued while control switched to Main, completed exact `TROLL_PANE_SWITCH_27EFD8`, and retained the full exit-0 transcript; only Burzum's rollout contains the marker. Pre-repair, Main inherited Burzum's Sol model label; the first incomplete repair showed unavailable. The final row reads Main's durable primary session and live from Burzum shows `deepseek-v4-flash`. The focused rendered snapshot passed. The broader picker filter remains 2/3 on an independent stale Nazgul fixture and is not called green. No agent or remote resource was created, replaced, closed, or shut down. | 0.7743 plus unreconciled mixed-provider, OpenAI, and ephemeral usage |
| 2026-08-02T20:06:25Z | Claude Plan subscription and local pane control | PFTQA-064 active Nazgul switching and managed-root state projection | Exact retained Angmar used one bounded 25-second shell-tool turn on the final artifact; an earlier bounded turn exposed the missing state. Claude Plan invoice-equivalent cost is not available. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `a9bc78485af7924956daf8af8fc3235f95d5642fa181f919405d4381b76fc740`. Pre-repair the Nazgul row omitted task state. Two focused regressions passed. Live exact Angmar showed `running`, continued while control switched to exact DeepSeek Main, then showed `completed` with exact `NAZGUL_ACTIVE_A9BC78`; returning preserved the full exit-0 transcript and only Angmar's rollout contains the marker. Exact durable Main, Angmar, Burzum, and Orc identities were retained. No agent or remote resource was created, replaced, closed, or shut down. | 0.7743 plus unreconciled mixed-provider, OpenAI, Claude Plan, and ephemeral usage |
| 2026-08-02T20:08:31Z | Claude Plan subscription and local pane control | PFTQA-065 active native Claude Plan user-pane switching | Exact retained native PFTerminal user pane used one bounded 25-second shell-tool turn. Claude Plan invoice-equivalent cost is not available. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `a9bc78485af7924956daf8af8fc3235f95d5642fa181f919405d4381b76fc740`. Exact native `PFTerminal 1` / session `019fc397-e536-7ee3-8c75-bf01df5b87c1` showed Claude Opus 5 Plan and connected Claude Plan auth, projected `running` while control switched to exact DeepSeek Main, returned idle, and preserved exact `CLAUDE_PANE_ACTIVE_A9BC78`, the no-trailing-newline note, and exit 0. Its layout class is `codex_user_pane_ids`, not `claude_pane_ids`. No pane, agent, credential, file, or remote resource was created, replaced, removed, or shut down. | 0.7743 plus unreconciled mixed-provider, OpenAI, Claude Plan, and ephemeral usage |
| 2026-08-02T20:18:00Z | Claude Plan subscription and local process control | PFTQA-066 native background-terminal discovery and explicit stop | The interrupted Claude Plan turn used subscription quota before yielding one local background shell; exact invoice-equivalent cost is unavailable. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `0150070f2e8fec0f671db6926f6fdddf8e635bb71a6ff966977947885a127abd`. Exact native pane `019fc397-e536-7ee3-8c75-bf01df5b87c1` started a 120-second Bash/`sleep`; Esc retained it under the documented unified-exec background contract; `/ps` displayed its exact command; `/stop` removed both host PIDs; `/ps` returned empty; a second `/stop` was idempotent; and the same pane immediately returned the recovery marker. The command's completion marker did not execute. Focused headless-Claude cancellation tests, including a detached process group, pass as supporting evidence only. | 0.7743 plus unreconciled mixed-provider, OpenAI, Claude Plan, and ephemeral usage |
| 2026-08-02T20:28:02Z | No inference; local Claude Plan pane lifecycle | PFTQA-067 native user-pane durable name creation and cold restoration | Naming, search, status, restart, and deletion used no model turn. The new pane retained zero input and zero output tokens. | 0.00 | 0.00 incremental; numeric total remains 0.7743 | Artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`. The ordinary creation path now calls durable `thread/setName`. Exact legacy thread `019fc397-e536-7ee3-8c75-bf01df5b87c1` cold-restored as `PFTerminal 1`. A newly created zero-turn pane `PFTQA Name 2c1c4f` / `019fc428-4e75-73e0-b3d1-15851ef618b5` was backend-confirmed, found by exact name after another cold restart, and restored exact Claude Plan/provider/Full Access identity in `/status`. The disposable new pane was explicitly deleted and its rollout is absent; the original pane and managed crew were retained. | 0.7743 plus unreconciled mixed-provider, OpenAI, Claude Plan, and ephemeral usage |
| 2026-08-02T20:31:01Z | Claude Plan subscription and local process control | PFTQA-068 multi-job `/clean` alias and pane recovery | Two interrupted Claude Plan turns used subscription quota before yielding two local background shells; the recovery response used one short additional turn. Exact invoice-equivalent cost is unavailable. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`. Exact native pane `019fc397-e536-7ee3-8c75-bf01df5b87c1` held two separate Bash process groups and two child `sleep` PIDs. `/ps` and the footer reported two exact commands. `/clean` removed all four processes, `/ps` returned empty, a second `/clean` was idempotent, and the pane returned `CLEAN_MULTI_RECOVERY_2C1C4F`. Neither command completion marker ran. | 0.7743 plus unreconciled mixed-provider, OpenAI, Claude Plan, and ephemeral usage |
| 2026-08-02T20:33:17Z | No inference; local process and session lifecycle | PFTQA-069 idle `/quit` and `/exit` durable-state preservation | Both slash commands and both cold-resume inspections were local and used no model turn. | 0.00 | 0.00 incremental; numeric total remains 0.7743 | Artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`. `/quit` and then `/exit` each terminated the TUI cleanly from exact native Claude Plan pane `019fc397-e536-7ee3-8c75-bf01df5b87c1`. Successive cold resumes restored `PFTerminal 1` and exact Main/Angmar/Burzum/Snaga/Ghash/Krimp IDs and hierarchy. No residual PFTerminal/background process, deletion, replacement, close, or agent shutdown occurred. | 0.7743 plus unreconciled mixed-provider, OpenAI, Claude Plan, and ephemeral usage |
| 2026-08-02T21:52:16Z | No inference; Task Node production auth and local HTTP client | PFTQA-070 real GitHub link, authenticated PFTerminal connectivity, and slow-backend resilience | GitHub authorization and Task Node API traffic used no model inference. No API token or credential was captured in QA evidence. | 0.00 | 0.00 incremental; numeric total remains 0.7743 | The first real link failed because Task Node returned no bytes before the old 15-second deadline. Fly showed critical health, exit 134/restart, proxy resets, route latency up to 23.4 seconds, and query read timeouts; public `/health` itself took 18.46 seconds. After backend recovery, the operator completed the real GitHub link and confirmed authenticated PFTerminal operation on artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`. Non-streaming Task Node requests now have a bounded 45-second, non-retrying timeout; the focused set passed 2/2 before fix/format/build, producing artifact `e85c474f8880ff93bcc01cd0c04566ecd4e4dbfe52baf4471fb3c29593ae5e61`. Full authenticated feature and backend-capacity matrices remain open. | 0.7743 plus unreconciled mixed-provider, OpenAI, Claude Plan, and ephemeral usage |
| 2026-08-02T22:41:58Z | Claude Plan subscription, OpenAI API, and OpenRouter API | PFTQA-071 large operator-run mixed-provider `/spawn` crew work | Five retained role rollouts contain extensive provider, tool, patch, and inter-agent activity. Exact provider invoices were not reconciled during evidence capture. | unreconciled | Numeric total intentionally remains 0.7743 pending provider reconciliation | Artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`. The operator reported the substantial `/spawn` run worked. Durable inspection confirmed exact Claude Plan Angmar, OpenAI Sol Burzum, OpenRouter DeepSeek Snaga, OpenAI Terra Ghash, and OpenAI Luna Krimp identities and parent edges; 18 completed task/turn events; 42 inter-agent communication events; real tool and patch activity; zero error/interruption events; and final crew state `ready`. No role was deleted, replaced, closed, or shut down. This is `/spawn` evidence, not `/orchestrate` assignment evidence. | 0.7743 plus unreconciled mixed-provider, OpenAI, Claude Plan, OpenRouter, and ephemeral usage |

Qualification isolation note: the `/docs` live runs and active-surface turns invoked the stable
`pfterminal` binary with `PFTERMINAL_DEBUG_HOME`. Stable correctly selects `PFTERMINAL_HOME`, so
those QA sessions added session/layout/history/state records under `/home/pfrpc/.pfterminal`
instead of the intended temporary home. No provider credential was added, replaced, or removed.
The exact paid thread is `019fc0dd-2395-7940-b076-fe0b1f95d756`; the slash-only docs runs also
created recent QA layout records. They are preserved rather than destructively deleting user
state. Subsequent stable-binary qualification must set `PFTERMINAL_HOME` to the isolated home.

The first wallet launch likewise supplied `CODEX_HOME` directly to `pfterminal-debug`; the wrapper
correctly replaced it with its binary-specific default because `PFTERMINAL_DEBUG_HOME` was absent.
That zero-balance disposable wallet was removed through the product UI, the empty residual wallet
directory was removed after validation, and `/home/pfrpc/.pfterminal-debug` now contains no wallet.
All subsequent wallet runs supplied `PFTERMINAL_DEBUG_HOME` and stayed in disposable `/tmp` homes.
During the final disposable creation, the QA harness captured one secure-view frame into
operator-visible tool output. Product-side scans found no copy in command history, rollouts, logs,
or the isolated home outside encrypted wallet state, but the capture itself violated evidence
handling. The zero-balance wallet was explicitly removed, all three wallet QA homes were deleted,
and the recovery material must not be copied into this ledger or reused.

## Defect ledger

| ID | Severity | Feature | Status | Evidence | Repair/retest |
| --- | --- | --- | --- | --- | --- |
| PFTQA-001 | P0 | `/spawn` permissions | REPAIR VERIFIED FOR ALL THREE ROLES IN YOLO, WORKSPACE-WRITE/NEVER, WORKSPACE-WRITE/ON-REQUEST, AND READ-ONLY/ON-REQUEST; EXACT FRESH `--yolo` USER-PANE CELL CLOSED; REMAINING MATRIX PENDING | Before repair, a fresh real Nazgul under `pfterminal-debug --yolo` submitted `OnRequest` plus `:workspace`, entered broken bubblewrap, and opened approval. After repair, the YOLO hierarchy reported Full Access and each role ran a real compound shell command without approval. PFTQA-031 created Main/Angmar/Burzum/Snaga under workspace-write/never; every role and a managed Nazgul -> Troll -> Orc chain wrote inside the workspace and received exit 1 outside it. PFTQA-032 applied workspace-write/on-request to the same retained hierarchy; all roles ran normal workspace work and raised real approval dialogs for outside writes. PFTQA-033 applied read-only/on-request; all roles completed compound/file reads and raised approval for rejected workspace writes. PFTQA-035 launched the literal `--yolo` CLI, created a new persistent user pane, and ran an outside-write + read + Git compound command once with exit 0 and no approval/bubblewrap. | `native_spawn_agent_config` derives from the active thread runtime config, and role application preserves runtime-only permissions unless the role explicitly overrides them. Resume uses a typed permission decision for Main and every restored native member. Non-OpenAI request adapters send only the current dynamic permission section, so stale historical contracts cannot override current settings. PFTQA-031 through PFTQA-033 prove flagless cold restoration on exact Main and Snaga without widening permissions or replacing members. PFTQA-035 removed its marker and used an explicit safe restart to verify Workspace/Ask on Main, the same new pane, and exact Snaga. Extra-root/profile, Telegram, command-variety, and remaining provider/pane cells remain required before feature-level PASS. |
| PFTQA-002 | P0 | `/spawn` hierarchy creation | CLOSED | Before repair, repeated Nazgul creation spawned a thread before failing persistence. After repair, repeating the full `/spawn` Nazgul flow returned the existing crew/root conflict with `No pane was created`; `/panes` still contained exactly Main plus the one Angmar root. | Nazgul creation now preflights the durable root conflict before any provider/thread spawn. A real event-path regression proves thread channels, navigation entries, spawn edges, and root identity do not change. |
| PFTQA-003 | P0 | `/spawn` direct Orc task | CLOSED | The UI offered `Send task to Snaga [orc]`, but the unmarked task conflicted with the Orc role's standing Troll-only reporting protocol; Snaga ignored the new command and replayed stale work. | Direct Orc submissions now carry authenticated `/spawn` hierarchy context identifying the binding Sauron/Nazgul assignment. Unit and real provider-boundary regressions pass. Live repaired DeepSeek task ran only `printf PFQA_TASK_UI_ORC_FIXED_OK`, returned the literal marker and exit code 0, and propagated the result into `/spawn` status. |
| PFTQA-004 | P0 | `/spawn` cold restart/task admission | CORE/APP-SERVER BOUNDARY CLOSED; FULL TUI MATRIX PENDING | The historical direct `turn/start` fallback evidence is superseded. A fresh DeepSeek direct root `019fc03c-201d-7db3-a605-ea296cf06f9f` spawned Orc `019fc03c-29e5-7262-8cf3-64629a9dc045`; after a new process resumed the root, `/root/qa_worker` was initially unloaded, accepted `COLD_RESUME_OK`, appended to the same child rollout, and retained the same thread, canonical path, nickname Uzg, and Orc role. | TUI native task admission now uses `thread/sendAgentMessage`; target-not-found materializes the saved Core member and retries the same stable mailbox message. The focused app-server native-mailbox test passed, including stable-ID deduplication and reuse after completion. The full interactive role/provider/restart matrix in section 13.6 remains required. |
| PFTQA-005 | P1 | `/spawn` manager auto-processing cost/loop control | ACTIVE AND COLD LOOP BOUNDARIES CLOSED; REMAINING ROLE/PROVIDER MATRIX PENDING | During failure diagnosis, Burzum repeatedly re-verified stale work and spawned an ephemeral replacement Orc. The first two paid loop qualifications still dispatched round five. Rollout inspection proved terminal-result turns admitted from the pending `InputQueue` were being classified before that queue was merged, so each automatic turn was incorrectly reset as fresh work. | Lifecycle accounting now occurs at Core task admission after explicit and pending input are combined. In paid `LOOP3`, one operator turn sent round one, three terminal-result-only turns sent rounds two through four, and the round-four result was recorded with `trigger_turn:false`. No round-five turn started. Fresh input resumed the same Troll and Orc, delivered round five once, and the Troll acknowledged without another dispatch. A separate full-process qualification delivered `COLDQ_ORC_RESULT` to a restored cold Troll with `trigger_turn:false`; its rollout remained unchanged until fresh operator input, then the same Troll emitted `COLDQ_TROLL_SEEN` without dispatch. Core owns the policy; the external-pane adapter only shares its ceiling. |
| PFTQA-006 | P0 | Native role selection | CLOSED FOR PROVIDER-NEUTRAL TOOL AND PF TYPED-SPAWN BOUNDARIES | The built-in `orc`, `troll`, and `nazgul` roles were omitted from the provider-neutral native `spawn_agent` schema unless custom roles also existed. A real DeepSeek root therefore spawned generic workers that inherited the root orchestration prompt and recursively created 20 workers. | V1 and provider-neutral V2 native spawn schemas expose `agent_type` plus the built-in roles; the final paid DeepSeek run requested one `agent_type: "orc"`, created exactly one child, and retained the complete Orc base prompt. PFTQA-025 subsequently proved first-party OpenAI reserves a smaller spawn schema that cannot carry PF role/runtime extensions. For that route, `/spawn` applies Nazgul/Troll/Orc role configuration through the typed Core spawn request outside the model-visible reserved schema. The OpenAI integration regression proves custom or built-in PF role configuration cannot mutate that reserved schema; it does not remove the PF role or prompt from product-created crew members. |
| PFTQA-007 | P0 | Provider-neutral native assignment delivery | CLOSED AT CORE PROVIDER BOUNDARY | Plaintext collaboration arguments were marked as encrypted based only on tool-call source, and DeepSeek's Responses-compatible endpoint did not expose OpenAI's native `agent_message` item to the model. The child received a taskless synthetic continuation and attempted to rediscover work by listing agents. | Durable history remains typed `AgentMessage`. Genuine OpenAI encrypted payloads remain native; explicit plaintext and non-OpenAI calls remain plaintext. At the non-OpenAI Responses wire boundary, plaintext collaboration messages adapt to ordinary user/external input. In the final paid run the Orc's first reasoning directly read `FIRST_WIRE_OK`, then the same child directly read `SECOND_WIRE_OK`, and after cold resume directly read `COLD_RESUME_OK`; it made no task-discovery tool calls. |
| PFTQA-008 | P0 | Native descendant graph restoration | CLOSED FOR NATIVE NO-TASK HIERARCHY | The diagnostic hierarchy had been created under an unmaterialized lazy Main. Child rollouts and PF layout survived, but the missing parent rollout made the graph impossible to resume; resuming Angmar alone could not address its former sibling Burzum or Burzum's child Snaga. A second live check also proved manual Troll creation made Burzum a visual child of Angmar while Core recorded it as Main's sibling. | Core now materializes and flushes a durable parent and child before publishing the native edge, and the TUI no longer writes graph edges. Manual Troll creation uses the bound native Nazgul as its Core parent. Fresh no-task qualification created resumable Main `019fc084-96de-7f42-b35d-a9e7b7f0cce4` -> Angmar `019fc084-b0bd-7423-8567-690b06a26252` -> Burzum `019fc084-ef07-7e41-90d0-67c964696084` -> Snaga `019fc085-1255-7902-8dfb-39f848acf4e1`. All four rollouts and all three Core edges existed before inference. Full restart preserved searchable panes and native addressing. The exact Core durability test, real depth-chain regression, and TUI parent-selection regression pass; no test fixture or product path is allowed to manufacture an orphan durable parent. |
| PFTQA-009 | P0 | Fresh onboarding provider persistence and event handoff | CLOSED FOR DEEPSEEK DIRECT SUB-WORKFLOW; PROVIDER MATRIX PENDING | Fresh-home DeepSeek selection reached Main only after exposing duplicate provider-save/bootstrap activity and a stale onboarding event receiver. | Provider persistence is single-flight and blocking at the vault boundary, and the live event receiver is handed to Main instead of being abandoned. The repaired fresh-home flow selected DeepSeek direct, saved the key, entered Main, and created a usable pane. Other provider/authentication routes remain required. |
| PFTQA-010 | P0 | Operator-created PF user-pane cold restoration | CLOSED FOR NO-TASK MAIN/CHILD IDENTITY; FULL PANE MATRIX PENDING | PF user panes were not durable layout members, and partial liveness projections could erase their nickname. The initial in-process picker displayed the child by raw ID instead of its saved name. Later explicit resumes of unmaterialized Main `019fc0be-9de8-7271-9332-9a010bd1e2f3` produced different Main/layout identities (`019fc0e2-3dc0-7231-b738-65ab01384a8b`, then `019fc0e5-8b83-77d3-b42a-d8956155a191`); a pane saved only under the first new layout was consequently absent from the second. | Layout state records operator-owned user-pane IDs separately from managed crew and task-only workers. The app-server now materializes every persistent TUI `thread/start` before returning it, so a no-task Main or user pane is a real resumable Core thread rather than a layout reference to a lazy ID. In a fresh isolated home, Main `019fc0f3-45e5-7b43-a1cd-5af1c61f0414` and child `019fc0f4-26ff-7ff3-b1eb-300582ee4e2e` each had a rollout immediately with zero turns. After a full exit, resuming the exact Main restored that same session and `/panes` restored `No Task Durable`; switching to it showed the exact original child session with zero tokens. Earlier `Cold Restore QA` evidence still proves transcript, direct-input, and real-tool continuity. Focused durable-Main/crew and user-pane tests pass on rebuilt binary `d27a927644defa57a16786506e11cc0628948d176db5fea910f66601fa9c887d`. |
| PFTQA-011 | P0 | `/docs` navigation, errors, and offline package | CLOSED FOR DEV/PACKAGED MAIN SUB-WORKFLOW; REMAINING PANE/EXIT MATRIX PENDING | The viewer rendered links but could not follow them, filtered only filenames, had no history, shipped no offline docs, and async load errors could remain invisible until another input event. A live anchor test also exposed the repository's `<a id="telegram">` form. | The viewer now parses Markdown links structurally, resolves bounded MkDocs paths plus generated/explicit/HTML anchors, supports back/forward, searches bounded known-page content, and redraws errors immediately. Live tmux tests opened nested pages, followed `index.md` to Ambient, moved back/forward, found `config.md` by content, followed `example-config.md` to `config.md#telegram`, handled missing/malformed requests, and survived narrow/wide resize. Staged npm package `/tmp/pfterminal-docs-package-qa.XTsYpa` loaded its packaged 50-page tree from `/tmp`. During a real active DeepSeek shell turn, `/docs` opened and the original turn later returned `DOCS_ACTIVE_OK`. Thirteen focused TUI tests and the npm metadata/staging test pass. |
| PFTQA-012 | P1 | `/panes` current-state projection | CLOSED | After creating and switching to native PF user pane `Matrix Twin`, `/panes` labelled both Main and the child `(current)`. The external-pane registry intentionally represents every native Core thread as `codex-main`, so using that registry value alone could not distinguish Main from an active native child. | The picker now uses the active Core `ThreadId` as the native current-row authority while retaining the external registry for Claude panes. The focused regression passed. Rebuilt binary `bb9aeb3f84c6010e5d3707237f370ec7b8e7260dbefd776381f096b8c946a41a` then showed exactly one current row for Main, `Cold Restore QA`, `Matrix Twin`, and `Matrix Twin (2)` across switches; the running row also stayed distinct from the current row. |
| PFTQA-013 | P0 | `/spawn` startup layout preservation and crew projection | CLOSED; FULL `/spawn` MATRIX PENDING | Candidate `d27a927644defa57a16786506e11cc0628948d176db5fea910f66601fa9c887d` rejected a valid prior-schema layout after applying a new serde default before checksum verification, then startup persistence replaced both rejected generations with an empty layout. After raw-layout checksum repair, the complete layout survived, but `/spawn` and `/panes` still omitted Troll and Orc because projection incorrectly treated transient liveness role metadata as authoritative over durable `CrewSpec`. | The reader verifies raw saved JSON before migration/defaulting; persistence refuses to replace the last unverifiable generations; restored role projection prioritizes `CrewSpec`. On rebuilt debug binary `920e4a117714f8d30e7920cd74f6fc2f411c38477c3a461836f560e770a2252a`, a cloned-home cold resume retained exact Main `019fc084-96de-7f42-b35d-a9e7b7f0cce4`, Angmar `019fc084-b0bd-7423-8567-690b06a26252` at `/root/nazgul_angmar`, Burzum `019fc084-ef07-7e41-90d0-67c964696084` at `/root/nazgul_angmar/troll_burzum`, and Snaga `019fc085-1255-7902-8dfb-39f848acf4e1` at `/root/nazgul_angmar/troll_burzum/orc_snaga`. `/spawn`, `/agent`, `/subagents`, and `/panes` agreed; Troll and Orc remained human-addressable. The 14-test pane-layout set and 81-test focused spawn set pass. |
| PFTQA-014 | P1 | `/panes` restored-Nazgul search | CLOSED | After PFTQA-013 restored the full crew, `Nazgul: Angmar [nazgul]` was visible in `/panes`, but typing `Angmar` produced no matches because only the generated root row omitted `search_value`. | The root row now indexes role, display title, and durable target ID. The generalized restored-CrewSpec regression asserts both name and thread-ID indexing. On debug artifact `1f57870a08d3e34e6ccaa9d33b032c21a867a3806e588c56b7fa18d550fd6080`, live search returned exactly the Angmar row; Enter opened the existing DeepSeek direct/high/YOLO native pane with a normal composer. A follow-up live matrix found Main, Burzum, and Snaga by name and both Angmar and Burzum by exact thread ID. No inference was submitted, and both sessions exited cleanly through `/quit`. |
| PFTQA-015 | P0 | `/panes` unavailable-pane isolation | CLOSED FOR OPERATOR PANE | A persisted operator pane with a missing rollout failed once at startup, but opening `/panes` called automatic restoration again, repeated the same `thread/resume` failure, and kept the picker unavailable. | Failed operator restores are marked unavailable and omitted from subsequent automatic candidates without deleting membership. `/panes` retains a disabled searchable row with the generalized unloaded-session reason. Live retest on `c81adcbff7f0b7998e5cdd35733dacc28b219fe92f95979005719a963f20b2a9` reported the failure once, opened the picker, selected healthy `Cold Restore QA`, reopened without a retry, and kept the failed `Matrix Twin` row disabled with its reason. The same boundary is present in the current artifact. |
| PFTQA-016 | P0 | Idle operator-pane archive cleanup | CLOSED FOR IDLE ARCHIVE; BROADER CLOSE MATRIX PENDING | In a fresh internally consistent home, `/archive` successfully moved zero-token `Archive Idle QA` to `archived_sessions`, but Main's persisted `codex_user_pane_ids` still contained it. Restart attempted to resume the intentionally archived thread and showed a ghost disabled row. | Successful archive/delete now removes only operator-pane membership, falls selection back to Main, and persists before exit. On `588130c2281439de5f320bf6a4e8f862106440d71f8efcd0c28d01ad5debbc56`, fresh zero-token `Archive Cleanup QA` (`019fc124-28d9-7412-8a59-0ed3e1a1195f`) moved to `archived_sessions`; Main `019fc11e-f165-77b0-ba33-51dcd26939d0`'s layout omitted it; restart and name search proved it did not return. The final `42e4fdda7f10e48f7345809f08b014bdcf3eedf5e82173b27f3f1fb85cb36c6a` popup also reads `exit PFTerminal`. Active archive is closed separately by PFTQA-017 and operator-pane delete by PFTQA-018; other pane-type lifecycle cases remain pending. |
| PFTQA-017 | P0 | Active operator-pane archive admission and interruption | CLOSED FOR ACTIVE ARCHIVE; BROADER CLOSE MATRIX PENDING | After `/archive` was marked available during tasks, a live active turn still queued it as ordinary follow-up text whenever input beat slash-popup rendering. The prior unit test called dispatch directly and missed the composer admission boundary. | Recognized active-task slash commands now dispatch as control-plane actions independently of popup timing; startup queueing, unavailable-command rejection, and deferred unknown-command validation remain intact. On artifact `12d4b1736a3d24c35815b08f886fd316d4b768a1aba6817720fc350b45d97238`, `/archive` opened during a real 240-second tool, confirmation aborted it after 17.1s, emitted `turn_aborted`, archived thread `019fc129-7ad2-7092-ae3d-729962d89f4b`, removed owner-layout membership, fell back to Main, and exited. Cold restart plus exact-ID search returned no match. The archived rollout has no completed marker or assistant completion. Two archive flow tests and one generalized popup-independent composer test pass. |
| PFTQA-018 | P0 | Idle and active operator-pane permanent delete | CLOSED FOR OPERATOR PANES; EXTERNAL/CREW FLOWS CLOSED SEPARATELY BY PFTQA-020/PFTQA-021 | Active `/delete` was still rejected by command availability even though the app-server delete boundary already shuts down active threads before removing data. | Delete now declares active-task availability and uses the generalized popup-independent control-plane admission. On artifact `8a4b5ec474c90120b3fc0c981a64cc46950fa45f647d4581d6c6f915f0971422`, zero-turn `Delete Idle QA` and active `Delete Active QA` were each explicitly confirmed and permanently removed. The active 240-second tool was shut down with no orphan `sleep 240`; both IDs left Main's layout; both processes exited; rollout/state records were absent; cold restart and exact active-ID search returned no match. Two delete confirmation/event tests pass, including the real composer path while task-running. |
| PFTQA-019 | P0 | Cross-owner archive/delete safety | CLOSED FOR SAFETY; CLAUDE DELETE SUPERSEDED BY PFTQA-020; CREW REMOVAL SUPERSEDED BY PFTQA-021 | Selecting a Claude pane detached the active native receiver, so confirmed native `/archive` or `/delete` could fall back to Main. Directly deleting a managed crew member could remove a native subtree while leaving durable crew/layout ownership behind. | Native lifecycle commands now allow only Main or operator-created native panes and reject external Claude panes, managed crew, and parent-controlled task workers before server mutation. On artifact `017a95b6223efa3089b1f99cc3918e2f47f60a12fcdbe3b476cac2733ff92a1f`, a real zero-turn Opus 5 Claude Plan pane confirmed both commands, received explicit Claude-pane ownership errors, stayed usable, and left Main `019fc11e-f165-77b0-ba33-51dcd26939d0` in normal sessions. No inference ran. PFTQA-020 added dedicated operator-created Claude-pane delete; PFTQA-021 added the explicit ownership-aware whole-crew boundary. |
| PFTQA-020 | P0 | Idle/active operator-created Claude-pane delete and external input routing | CLOSED FOR OPERATOR CLAUDE DELETE; WHOLE-CREW REMOVAL CLOSED SEPARATELY BY PFTQA-021 | Dedicated Claude delete was absent. The first active live retest also found that a selected restored Claude pane fell through to `No active thread is available` after a native sibling failed restoration, proving external input was incorrectly gated on native-thread availability. | `/delete` resolves a selected external pane first, cancels a running turn, removes only its verified pane-artifact directory, removes registry/layout membership, falls back to Main, persists, and exits. Managed Claude crew members fail closed behind the whole-crew boundary. Five focused tests pass. Live idle artifact `4c704d4146fe3010c44c76345bc46c4ce476247ac0aeabe4742e5cb049ba5e36` removed `claude-4a99ad7a-bf73-4cce-ba2a-53fa794416b5`. Artifact `d43e96dbdf33e41a4dffce60bbb4ef4be60a7bdfe042c6b7f421127f8c966647` routed a real Opus prompt despite the failed native sibling, displayed `Claude running`, opened confirmation before completion, terminated and removed `claude-c8b9ad2d-c970-4ecb-92c0-2bbcf2da1485`, persisted `codex-main` with empty `claude_pane_ids`, exited, and returned no name match after cold restart. Main was untouched. Claude `/archive` remains unsupported by contract. |
| PFTQA-021 | P0 | Explicit whole-managed-crew removal and empty-state projection | CLOSED FOR WHOLE-CREW LIFECYCLE | Individual managed-member deletion correctly failed closed, but no owned whole-crew lifecycle existed. The first successful live removal also exposed a UI projection defect: after durable crew state and members were gone, `/panes` synthesized `Nazgul: PFTerminal - Main` from its default root fallback. | `/spawn` now offers `Remove managed crew` only for a verified `CrewSpec`, defaults confirmation to keep, and on confirmation removes native members in reverse specification order, cancels/removes managed Claude members, clears graph/layout/runtime/status/dispatch/navigation ownership, preserves a user-bound root, persists, and returns to Main. Ordinary completed agents are not auto-shut down. Live crew `crew-019fc084-b08e-7791-bb0e-15eb9c402c27` first survived the cancel path. Its first confirmed attempt failed before mutation because the disposable copy retained source-home absolute rollout paths; all four copied paths were rebased in that clone and the retained crew retried. Angmar `019fc084-b0bd-7423-8567-690b06a26252`, Burzum `019fc084-ef07-7e41-90d0-67c964696084`, and Snaga `019fc085-1255-7902-8dfb-39f848acf4e1` were then removed. The database had zero member rows and zero member edges, no member rollout remained, and Main `019fc084-96de-7f42-b35d-a9e7b7f0cce4` remained as one usable row. Layout state had `spawn_crew: null`, no root binding, empty spawn maps, and `codex-main` active. On current artifact `2cbab6519397eb344f6435574dbc2608139ccd14892183b75914e07b8a66827e`, cold `/panes` and `/spawn status` both rendered `No managed crew`; the lifecycle item was disabled with `No managed crew exists`; name and exact-ID search returned no member match; `/status` proved Main usable. A mixed native/running-Claude integration test separately proves cancellation, artifact removal, and bound-Main preservation. Four PFTQA-021-focused regressions passed within the final 14-test lifecycle run. No inference ran. |
| PFTQA-022 | P0 | Destructive/billable confirmation admission | CLOSED; WALLET FEATURE REMAINS PARTIAL | In the wallet-removal confirmation, non-searchable list numbering treated a digit inside unrelated typed input as an immediate select-and-accept action. A passphrase containing `2` therefore removed a zero-balance disposable wallet without Enter. The same shared picker behavior could affect other destructive, billable, or security-sensitive confirmations. | The shared selection boundary now has an explicit `allow_number_shortcuts` policy. Wallet removal/disconnect/payment, whole-crew removal, archive/delete, Telegram disconnect, GPU billable/termination controls, full-access escalation, marketplace removal, goal replacement, and reset-credit consumption suppress numbered accelerators, default to Cancel/Back where the dialog has multiple actions, and require deliberate navigation plus Enter for the sensitive action. The generalized regression proves digit `2` neither moves nor accepts while Down+Enter still works; three wallet-removal tests and the updated full-access confirmation snapshot pass. On exact artifact `f850c7a30c501422cadf3070c5270479575c7ba8560559130acbad7eef896ff9`, the confirmation rendered `Cancel`/`Remove wallet` without numbers, typing a digit-bearing string left the dialog and `wallet.json` intact, Esc preserved it, and only explicit Down+Enter removed it. No inference, funds, or transaction were involved. |
| PFTQA-023 | P0 | Vault secure actions, async redraw, and truthful mutation completion | CLOSED FOR REPAIRED BOUNDARIES; VAULT FEATURE REMAINS PARTIAL | Advertised reveal/export reached a placeholder; the action menu lacked reveal/replace/delete; async history could wait for another keypress before redrawing; add/replace performed encrypted storage work on the TUI thread; and a ten-second timeout reported delete failure even though its un-cancellable blocking task later deleted the credential. | Reveal now uses a host-owned transient zeroizing view and never transcript/composer history. Add/replace cross redacted zeroizing events and run off the TUI thread. Delete uses a safe Cancel-default confirmation, ignores digit shortcuts, and awaits its actual terminal result. Read/copy/reveal/list operations likewise avoid false timeout results, and background history insertion requests a frame. Artifact `efe4f7567291f52d3064fc1d96ca519eda8422e111ca3424c50cc54994ea48ff` covered status/list/metadata, the five-action menu, protected reveal, masked replace, plaintext scans, restart persistence, cancel/digit preservation, and exposed the false-delete-timeout defect. Final artifact `b162fff7bb9f108109e4d2a16da7ce86b53a9cdd1ba635320127032359677867` returned one real add result and one real confirmed delete result with zero credentials and no timeout. The harness then polluted only its disposable home by entering an unnecessary second secret after add had closed; the invalid provider key returned 401 before inference. That home and tmux session were removed. Real-provider authentication, clipboard clearing, backend failure/concurrency, and complete inline/menu coverage remain open. |
| PFTQA-024 | P0 | Provider output metadata and benchmark truthfulness | CLOSED FOR REPAIRED BOUNDARIES; PROVIDER MATRIX REMAINS PARTIAL | The Vercel GLM 5.2 Fast route was selectable but locally terminated because its catalogue record had no maximum output limit. The TUI benchmark counted any persisted `task_complete`, so two local metadata failures produced a false success. | Vercel's provider metadata reports 128K maximum output for GLM 5.2, and identifies Fast as the same weights/API surface; both bundled GLM routes now carry `max_output_tokens: 128000`, with exact catalogue assertions. The benchmark resolves PF stable/debug homes using the product entrypoint contract, counts only turn IDs having both `model_response_completed` and `task_complete`, and can require at least one persisted function call. It evaluates the prior Vercel failure as zero and the real DeepSeek run as two. Artifact `c9af50fed3747051eead43c390c784bcc08a83a7260f83ca71a28c34641a5a91` passed strict DeepSeek direct and pinned OpenRouter response/tool turns with exactly one function call each, passed direct Anthropic and Claude Plan Opus response/tool turns, and reached Vercel's real endpoint before the resident key returned 401. Vercel auth and the rest of the provider matrix remain open. |
| PFTQA-025 | P0 | OpenAI reserved collaboration protocol | CLOSED FOR RESERVED SCHEMA AND SAME-CHILD FOLLOW-UP; FULL `/spawn` MATRIX PENDING | The first authenticated Luna request failed before inference because PFTerminal had removed OpenAI's encrypted marker from `followup_task`. After restoring that marker, OpenAI rejected the separately extended `spawn_agent` contract. The first live lifecycle then created a child but stored encrypted follow-up ciphertext as ordinary plaintext, causing the child to repeat its first result. | A provider-boundary profile now emits OpenAI's reserved spawn/message/follow-up/wait/interrupt/list schemas while keeping PF route, role, billing, and presentation metadata in typed Core/PF state. Non-OpenAI schemas remain plaintext-capable. OpenAI model-issued messages retain native encryption; non-OpenAI delivery remains plaintext. Three focused schema-plan/profile tests and five provider-wire tests pass. On artifact `3e6c5c3727477403b25102ddcdc3107e80a113db852484fb9da9c3b1aa0960f8`, Luna completed a normal two-turn probe, spawned `/root/openai_followup_fixed`, received `FIRST_OK`, then used native follow-up on the same child and received distinct `SECOND_OK`; one child rollout contains both completed turns. Running-turn messaging, interruption, cold resume, roles/panes, and full provider coverage remain open. |
| PFTQA-026 | P0 | OpenAI native control and cold-runtime continuity | CLOSED FOR NATIVE CONTROL/COLD CONTINUITY; FULL `/spawn` MATRIX PENDING | A real running child accepted steering and returned `FIRST_STEERED`, but OpenAI's exact target-only interrupt failed because the PF handler required a provider-neutral reason. After that repair, the first cold-process follow-up exposed an incomplete discovery row: the child restored as unloaded but its indexed model was null although its durable rollout contained the exact model/provider/effort. The first resume harness also falsely counted historical turns as new work. | Target-only interrupt now receives a deterministic runtime-owned audit reason; provider-neutral callers retain explicit reason/superseding metadata. Cold load recovers runtime from durable `thread_settings_applied`/`turn_context` model context when the SQLite index is incomplete. Resume benchmarking subtracts persisted turn/call baselines. On artifact `4a8a2c341bd6a2a5544ddcf0cbe6a1fa3968ef31a8f88d9f5116150a0d10198f`, exact interrupt succeeded and the same child returned `RECOVERED_FIXED`. On artifact `45cb31e3822bce7b224ef91c4f807591922ab80daaf9a1933b83bf0b223e7c6d`, a new process listed that same child unloaded, resumed it twice, and received `COLD_FIXED_ONE` then `COLD_FIXED_TWO` on the original rollout/thread/path; the parent has one spawn total. Focused target-only, provider-neutral metadata, and missing-indexed-model cold-resume regressions pass. Role/pane/provider matrices remain open. |
| PFTQA-027 | P0 | Rapid slash-command admission and current crew control | CLOSED FOR FIRST-ENTER ADMISSION; FULL PANE/`/spawn` MATRICES PENDING | Rapid short slash commands could remain wholly in the paste-burst buffer. The composer saw no slash context and appended the first Enter as pasted input, so `/panes` required a second Enter. Current-code crew QA otherwise proved direct human input and native follow-up across the retained Angmar/Burzum/Snaga hierarchy. | The input boundary now materializes a pending burst that begins with the slash protocol marker before applying Enter semantics; it does not match command names. A rapid-key-stream regression covers adjacent `/panes` and `/docs`, and the focused `burst_enter` set passes. On final formatted artifact `b8d47fe89c99859ef5809c1f5cc5a08db1cdc8a74ad383a4beb59eb8ca818af5`, `/panes` and `/docs` each dispatched on one Enter and configured `/docs` opened all 50 pages. The role run reused exact paths `/root/nazgul_angmar`, `/root/nazgul_angmar/troll_burzum`, and `/root/nazgul_angmar/troll_burzum/orc_snaga`; no member was replaced or shut down. Real planning/review, newly created-role, provider, permission, and failure matrices remain open. |
| PFTQA-028 | P0 | Fresh manual `/spawn` hierarchy and cold reuse | CLOSED FOR DEEPSEEK DIRECT/YOLO SLICE; FULL `/spawn` MATRIX PENDING | Exact artifact `b8d47fe89c99859ef5809c1f5cc5a08db1cdc8a74ad383a4beb59eb8ca818af5` created a no-task Angmar/Burzum/Snaga hierarchy from a fresh isolated home. Because manual spawn stays on the initiating pane, three immediate `NEW_*` markers actually exercised Main; QA rejected them as role evidence. | Exact `/panes` selection proved direct human input in each role with `ACTUAL_NAZGUL_DIRECT_OK`, `ACTUAL_TROLL_DIRECT_OK`, and `ACTUAL_ORC_DIRECT_OK`. The same Angmar reused the same Burzum, which reused the same Snaga, returning the full `FRESH_*` chain. A new process restored the original graph and Snaga transcript, accepted `FRESH_COLD_ORC_OK`, and retained exact IDs, paths, DeepSeek route, high effort, and Full Access. `/spawn`, `/panes`, `/agent`, and `/subagents` agreed; completed members remained reusable. PFTQA-029 closes the standard three-Orc mixed-provider retained-follow-up slice; real work, remaining permissions/failures, active-turn, and close matrices remain open. |
| PFTQA-029 | P0 | Standard-crew mixed-provider native delegation | CLOSED FOR STANDARD CREW, CROSS-PROVIDER FOLLOW-UP, AND RETAINED REUSE; FULL `/spawn` MATRIX PENDING | The fresh standard crew accepted direct input in Claude Plan/Fable Angmar, OpenAI/Sol Burzum, OpenAI/Luna Snaga, OpenAI/Terra Ghash, and OpenRouter/Grok Krimp. Initial delegation exposed missing Anthropic collaboration bindings. After flattening those tools, the next run proved OpenAI's encrypted reserved `followup_task` reached Luna/Terra but was opaque to Grok; the native call failed before admission. A first adapter build then omitted the adapter from Code Mode Only, and Burzum correctly reported it unavailable. | Anthropic capability selection now flattens namespace tools. OpenAI reserved schemas remain exact. Core resolves the target provider before admission; incompatible encrypted calls direct the model to an ordinary plaintext adapter backed by the same handler/mailbox, exposed in normal and Code Mode Only turns and redacted from logs. On artifact `9f7e758a55c526a877f8ef5ba2c8b4c02f9c7779282a14df431404a0d1ac6614`, Burzum used native encrypted calls for Luna/Terra, then `followup_task_plaintext` once for Krimp. The original Orcs returned `ADAPTER2_LUNA_OK`, `ADAPTER2_TERRA_OK`, and `ADAPTER2_GROK_OK`; the original Troll and Nazgul returned both consolidation markers. `/spawn status` retained all roles completed/reusable; no spawn, replacement, close, interrupt, or shutdown occurred. Targeted MultiAgentV2 tests passed 87/87 with focused handler, plan, request, and log-redaction coverage. |
| PFTQA-030 | P0 | Pane-local `/status` provider projection | CLOSED FOR RETAINED CLAUDE PLAN AND OPENROUTER PANES; FULL `/status` MATRIX PENDING | Selected Angmar's header and rollout identified `claude-plan` / `claude-fable-5-plan`, but `/status` rendered DeepSeek because session binding omitted the provider object and thread-settings synchronization changed only its ID. Actual inference stayed on Claude Plan, so this was false UI state rather than provider fallback. | Provider projection is now atomic across retained-session binding, thread-settings updates, and interactive selection: ID, metadata, wire API, auth behavior, and displayed URL move together. The focused regression passed. A new process running artifact `8f8e27d698aad0d15a3caa1ecb1fb507ce3b56584849e5ff38bf93d97dad9b4f` resumed the original crew; Angmar reported Claude Plan/Anthropic and Krimp reported OpenRouter with their correct URLs, original thread IDs, transcripts, roles, and reusable idle state. No inference occurred. Remaining providers, account/permission modes, and `/status` rows remain open. |
| PFTQA-031 | P0 | Permission-contract resume and cold `/spawn` reuse | CLOSED FOR WORKSPACE-WRITE/NEVER; FULL PERMISSION MATRIX PENDING | The first flagless restart widened a restricted hierarchy to Full Access because TUI sent base-config permissions as resume overrides and app-server restored model/reviewer metadata but not the durable approval/sandbox contract. | Resume now carries independent typed decisions for model and permission overrides. Only explicit CLI/session/profile permission choices override a saved thread; ordinary resume restores the latest durable approval policy and concrete permission profile. Focused TUI and app-server tests passed. On artifact `db00c6bdd3d94a699b162f4d889498043f1af64a8b9de029f5979d2b318430ef`, explicit workspace-write/never worked, then a new process with no permission flags restored that contract on exact Main `019fc246-2d5f-7d50-81bf-1f6287bd207d` and exact Snaga `019fc248-0e17-7281-a37c-741db6f6b1d3`. Both repeated allowed-inside/denied-outside checks; every outside artifact is absent. `/spawn status` retained Burzum `019fc247-6d9b-79d1-9c17-1c1e1188ff41` and Snaga as reusable, with no replacement, interrupt, close, or shutdown. |
| PFTQA-032 | P0 | On-request permission propagation and non-OpenAI current-context authority | CLOSED FOR WORKSPACE-WRITE/ON-REQUEST; FULL PERMISSION MATRIX PENDING | Explicit workspace-write/on-request initially updated Main but retained members stayed on their saved `never` contract because native member attachment hard-coded restore-only permissions. After that repair, Angmar's `/status` and durable settings said on-request, but DeepSeek still followed an older replayed developer fragment saying approval was never and submitted the outside write without escalation. | Restored member attachment now receives the same typed explicit-or-restore permission decision as Main. Non-OpenAI Responses, Chat, and Anthropic request builders retain only the newest recognized dynamic developer section at serialization, leaving durable history, persistent PF role doctrine, and the OpenAI append-only path intact. The focused retained-member, contextual-adapter, and actual non-OpenAI Responses request tests pass; the existing OpenAI permission-history integration still passes. On artifact `d136f81ef970752c8d2d66ffb5b3020381ecacd170d15e41984f45e4898ed0a7`, Main, Angmar, Burzum, and Snaga all showed Workspace/Ask, performed normal workspace work, opened real approval dialogs for exact outside writes, survived rejection, and left every target absent. Angmar reused existing Burzum, which reused existing Snaga and returned `ONREQ_CHAIN_OK`; no agent was spawned, replaced, interrupted, closed, or shut down. A flagless new process restored Workspace/Ask on exact Main `019fc246-2d5f-7d50-81bf-1f6287bd207d` and Snaga `019fc248-0e17-7281-a37c-741db6f6b1d3`, repeated rejected approval, and retained original Burzum `019fc247-6d9b-79d1-9c17-1c1e1188ff41` and hierarchy. |
| PFTQA-033 | P0 | Read-only permission inheritance, managed native reads, and cold restoration | CLOSED FOR READ-ONLY/ON-REQUEST; FULL PERMISSION MATRIX PENDING | No new product defect appeared in this cell. The open question was whether explicit read-only/on-request would reach every retained role, allow useful read work, gate even workspace-local writes through approval, preserve native manager messaging, and survive flagless restart. | On exact artifact `d136f81ef970752c8d2d66ffb5b3020381ecacd170d15e41984f45e4898ed0a7`, Main, Angmar, Burzum, and Snaga each reported Read Only/Ask, ran `pwd` plus a compound Git-history read, and read `Cargo.toml` with exit 0. Each attempted a distinct workspace marker write, received a real approval dialog, survived rejection, and left no target. Angmar reused existing Burzum, which reused existing Snaga through native follow-up; all three independently returned exit 0 and exact output `[workspace]` for the same manifest read. No spawn, replacement, interrupt, close, or shutdown occurred. A flagless new process restored Read Only/Ask on exact Main `019fc246-2d5f-7d50-81bf-1f6287bd207d` and exact Snaga `019fc248-0e17-7281-a37c-741db6f6b1d3`; both raised and survived rejected workspace-write approvals, both cold targets remained absent, and `/spawn status` retained original Burzum `019fc247-6d9b-79d1-9c17-1c1e1188ff41` and hierarchy. |
| PFTQA-034 | P0 | `/permissions` automatic reviewer provider routing and Full Access workflow | CLOSED FOR DEEPSEEK AUTO-REVIEW AND FULL-ACCESS USER FLOW; BROADER PROVIDER/PERMISSION MATRIX PENDING | On direct DeepSeek, Approve for me selected the globally configured OpenAI helper name `codex-auto-review` and sent it to the DeepSeek endpoint. DeepSeek rejected the unsupported model before reviewing the command. Unknown custom non-OpenAI providers could inherit the same invalid hidden helper. | Built-in configured providers now select a backend-valid helper model for approval review, memory extraction, and consolidation; an unknown custom provider falls back to its current active model rather than leaking an OpenAI helper. The table regression covers 16 built-in configured routes across all three helper functions, and focused Core regressions cover direct DeepSeek plus an unknown custom provider. Artifact `5e11eaf561f3c7233a42146673b37e2879a40f2c66d3d9ca198c4eeaa350ed65` proved a DeepSeek Guardian on exact Snaga returned `allow` for an outside command with no human dialog or route error. The same run proved Full Access warning cancel, explicit confirmation, approval-free outside execution, flagless cold restoration, cleanup, and safe reset to Workspace/Ask. The retained hierarchy and identities were unchanged. |
| PFTQA-035 | P0 | Exact fresh `--yolo` user-pane compound command | CLOSED | This canonical checklist cell required the literal debug `--yolo` launch, creation of a new pane, and one non-allowlisted compound command without bubblewrap or approval. Prior YOLO role evidence did not explicitly prove that exact fresh user-pane sequence. | Artifact `5e11eaf561f3c7233a42146673b37e2879a40f2c66d3d9ca198c4eeaa350ed65` resumed Main with literal `--yolo`, displayed Full Access, created persistent pane `019fc28f-d89f-7b41-92d1-b6939536ba02`, and displayed YOLO mode there. One `exec_command` wrote/read an outside marker and queried the release branch in the same compound command; exit 0, no approval event, and no bubblewrap path. The marker is absent. A safe explicit restart restored Workspace/Ask on Main, this pane, and exact Snaga while retaining all identities. |
| PFTQA-036 | P1 | Background-helper provider identity | CLOSED | Review found that custom/OSS providers could inherit a built-in helper model by reusing its credential-variable name, and a custom provider named `OpenAI` could bypass the custom-provider fallback. | Built-ins now match the full transport contract, and one shared resolver supplies approval review plus both memory phases. Two collision regressions and the focused custom Guardian test pass; the focused memory runtime fixture compiled but stack-overflowed and is not claimed green. Final artifact is `a4aa83739b953b87aa011f3eb6c8bbe2d7189c55d92d764321fcdf78432ab026`. |
| PFTQA-037 | P0 | `/vault` provider-key cache coherence | CLOSED FOR ADD/REPLACE/DELETE AUTH COHERENCE; ASYNC COMPLETION UX OPEN | Pre-repair, deleting `provider/deepseek_api_key` through generic `/vault` removed encrypted storage but did not advance provider-auth storage revision; a real request in the same process still returned `SHOULD_NOT_AUTH_AFTER_VAULT_DELETE`. | Provider-labelled generic vault mutations now use canonical provider login/delete boundaries. On artifact `a4aa83739b953b87aa011f3eb6c8bbe2d7189c55d92d764321fcdf78432ab026`, confirmed replace authenticated a real request, confirmed delete caused the same running pane to fail with missing `DEEPSEEK_API_KEY`, and confirmed generic add authenticated another real request. The modal currently dismisses before its async completion toast, so premature requests can observe the old value; acceptance requires waiting for the terminal toast until that UX is repaired. |
| PFTQA-038 | P0 | GPU catalogue and rental-state truthfulness | CURRENT CATALOGUE/LOCAL STATE CLOSED; PAID RENTAL AND PROVIDER-SIDE RECONCILIATION OPEN | The current candidate had not been exercised after the DeepSeek 0731 recipe update, while old GPU panes showed superseded recipes and stale ready screens. | Current artifact `/gpu status` shows official DeepSeek 0731 TP2 r3 as qualified and the two third-party GGUF routes as experimental. The official recipe source pins HF commit `7872f01b1d1fe23eabc4c98b48bffcef5a386062`, SGLang `0.5.15.post1`, and immutable image digest. Pre-charge cancellation created no rental. Read-only state found zero locally active/billable rentals and zero runtime providers; old standalone provider keys are unauthorized, so independent remote absence and a new bounded rental remain required. |
| PFTQA-039 | P0 | Telegram, Task Node, and operational-state truthfulness | READ-ONLY BOUNDARIES CLOSED; LIVE REMOTE MATRICES OPEN | Candidate health had not been compared with the currently running Telegram connector or actual Task Node login state, and operational state could have been mistaken for feature acceptance. | Candidate `telegram --health` identified the configured bot and exact one-chat/one-user authorization set without mutation. The active production poller is an old seven-day process whose executable has been deleted, so it was preserved and flagged rather than silently replaced. Task Node read commands fail closed with `terminal_login_required` and no secret. `doctor --json` reports 17 OK and one rollout-index parity warning with bounded counts and zero malformed/duplicate/archive errors. Live Telegram send/restart and authenticated Task Node workflows remain open. |
| PFTQA-040 | P1 | Goals, memories, MCP, hooks, sessions, and local command surfaces | QUALIFIED SLICE; COMPLETE MATRICES OPEN | These surfaces were present but had not been exercised together on the final candidate. A vague goal also demonstrated that lifecycle correctness alone does not guarantee bounded useful behavior. | The disposable-home run proved real goal continuation/pause/edit/restart/clear, completed memory consolidation artifacts, stdio MCP discovery plus approved nested tool execution, and reviewed/trusted/enabled/disabled hook execution on first turn. It also proved isolated side/btw turns, spaced-file mention/read, rename/new/resume/fork divergence, raw/copy/process controls, theme/title/statusline persistence, experimental rollback, and keymap conflict/remap/use/debug/restore. Goal budgeting, full memories, hook failure paths, MCP verbose/failures, apps, plugins, and the remaining command inventory stay open. |
| PFTQA-041 | P1 | Runtime `/import` input routing and PFTerminal identity | CLOSED FOR PROMPT ROUTING/CANCEL; IMPORT EXECUTION MATRIX OPEN | The runtime command created a second TUI event stream while the permanent input drainer still polled the broker. The modal rendered but ignored every key. It also retained Codex-branded product copy. | Both nested import screens now consume the app's existing drained-event receiver. The 23-test focused suite covers both runtime-receiver boundaries. Artifact `7b6574b7fff8ea780f61ce1b0f956d0c91237c803844fdbcd103603019522af0` accepted navigation, explicit Cancel, and Escape in live tmux, returned to the composer, and rendered PFTerminal product copy. No literal command/input special case was added. Import apply, conflict, partial-failure, fidelity, and rollback remain open. |
| PFTQA-042 | P1 | Local plugin lifecycle | CLOSED FOR LOCAL CURATED INSTALL/DISABLE/ENABLE/DISCOVERY/UNINSTALL; REMOTE/APPS MATRIX OPEN | Plugin discovery had been menu-observed but no installation lifecycle had run on the current artifact. | Build Web Apps installed from its disclosed local curated source, changed the catalogue count, appeared in `/skills` as a plugin result, disabled and re-enabled, then uninstalled and restored the count to zero. No authentication or remote marketplace mutation occurred. Restart-while-installed, remote marketplace, update, plugin hook, authenticated app, and injected-failure rollback cells remain open. |
| PFTQA-043 | P0 | `/spawn` Troll task UI, busy delivery, and cold reuse | CLOSED FOR RETAINED DEEPSEEK CREW; BROADER MATRIX OPEN | The prior Troll UI attempt gave a manager a contradictory no-delegation instruction, so it was not valid role evidence. Busy delivery and post-restart reuse also needed direct observation. | Artifact `7b6574b7fff8ea780f61ce1b0f956d0c91237c803844fdbcd103603019522af0` delivered a role-correct task to original Burzum, which delegated to original Snaga, independently verified the terminal result, and returned `TROLL_TASK_UI_OK`. A second assignment reached the same active Burzum through native messaging while `/spawn status` remained responsive. After cold restart, exact Burzum `019fc247-6d9b-79d1-9c17-1c1e1188ff41` and Snaga `019fc248-0e17-7281-a37c-741db6f6b1d3` both accepted new work; Snaga's terminal result automatically returned to Burzum. No member was replaced, closed, interrupted, or shut down. |
| PFTQA-044 | P0 | Packaged docs, fallback vault, PF pane, and adjacent feature boundaries | QUALIFIED SLICE; PAID/REMOTE MATRICES OPEN | Packaged targeted docs failed outside a MkDocs cwd; copied encrypted vault state could not decrypt because fallback key filenames are home-derived; current feature menus needed observable negative and local-state use. | Artifact `01f5ab3f9d9ca7af88f1cb31f06f315545cccc87fb8d62d13c574b35da0f4bd9` loaded bare and targeted packaged docs from an unrelated workspace with search/navigation and actionable failures. The debug launcher now supplies the managed package root, exports authoritative `PFTERMINAL_DEBUG_HOME`, and maps a copied fallback key to the debug-home-derived filename. A fresh fallback vault accurately reported its storage source, stored/listed/restarted one masked disposable credential, and kept the key 0600. A DeepSeek direct/high PF pane completed `FINAL_PANE_TOOL_OK`; rename/navigation, side, btw, and fork divergence worked. Wallet/GPU/Telegram/provider negative-state checks were truthful and created no funds, bot, rental, or remote resource. |
| PFTQA-045 | P0 | Task Node UI responsiveness | CLOSED FOR ASYNC LINK/LOGOUT BOUNDARY; AUTHENTICATED MATRIX OPEN | `/tasknode link` performed blocking HTTP and then encrypted-vault persistence on the UI event loop, freezing all terminal input. Logout had the same encrypted-storage risk. | Link and logout now run their full network/storage transaction on workers and report completion through app events; focused tests pass 2/2. On artifact `01f5ab3f9d9ca7af88f1cb31f06f315545cccc87fb8d62d13c574b35da0f4bd9`, `/status` rendered immediately while a deterministic local link endpoint was deliberately stalled, followed by the bounded timeout. `/status` also remained interactive during encrypted logout, which eventually reported session removal. No real account was linked; task/context/chat/reward mutations remain blocked on operator authentication. |
| PFTQA-046 | P0 | Exact model/provider/effort resume | CLOSED FOR ORDINARY FLAGLESS RESUME; EXPLICIT-OVERRIDE PERMUTATIONS OPEN | A named DeepSeek/high thread resumed as `default` because SQLite runtime metadata could lag or be overwritten even though canonical `thread_settings_applied` and `turn_context` records retained `high`. | App-server resume now gives the newest durable settings/turn context authority when no invocation-level model override exists; stale-projection and explicit-default tests pass. On artifact `ae0e967c3d81969b8769781b2b0ebb4d943c86902327219a2f2e6ae5e863523e`, normal `pfterminal-debug --yolo` resumed exact thread `019fc31a-ec18-7423-b915-b5181ad48848` as DeepSeek direct/high. A real follow-up returned `RESUME_EFFORT_OK`, and its newly appended turn context again records `deepseek-v4-flash` / `high`. Explicit `-m` invocation behavior remains an intentional separate override policy cell. |
| PFTQA-047 | P1 | Packaged docs in all advertised pane classes | CLOSED FOR PANE CLASS AND ACTIVE-TURN COVERAGE | Packaged fallback had been proven from Main, but the checklist still lacked actual use from PF user, managed role, and external Claude panes. | The retained exact crew cold-started on final artifact `ae0e967c3d81969b8769781b2b0ebb4d943c86902327219a2f2e6ae5e863523e`. Bare or targeted docs opened from Main, a PF user pane, Angmar, Burzum, Snaga, and a disposable Opus 5 Claude Plan pane. Earlier `DOCS_ACTIVE_OK` evidence covers opening during a real Main turn. The disposable Claude pane was deleted; managed members were not closed or replaced. |
| PFTQA-048 | P1 | Release-facing documentation accuracy | CENTRAL OPERATOR PAGES CORRECTED; HISTORICAL PAGE AUDIT OPEN | Provider/model and feature inventories predated DeepSeek/Kimi/Meta/current Claude/OpenAI routes and made preserved PF surfaces difficult to discover. | Existing central pages were corrected in place and four edited targets rendered through the actual terminal viewer. No new spec file was created. Older implementation/evidence pages still contain historical names, so complete content acceptance remains open. |
| PFTQA-049 | P0 | Slash command control-plane admission | CLOSED FOR UNKNOWN AND UNSUPPORTED-ARGUMENT BOUNDARIES | A recognized no-argument command with trailing text bypassed command dispatch and became a paid model prompt. | The parser/dispatcher now handles this class uniformly. Four regressions passed. Live final-artifact `/panes Burzum` warned and opened the bare picker; `/pane` failed locally and showed the canonical `/panes` completion. The rollout turn count stayed unchanged at ten. |
| PFTQA-050 | P0 | Vault concurrent access, wrong key, and corrupt ciphertext | CLOSED FOR STORAGE-SAFETY BOUNDARIES; LIVE MULTI-PROCESS/PARTIAL-WRITE MATRIX OPEN | Synchronized concurrent writers reproduced a real race twice: passphrase initialization and index updates were separate unlocked transactions, leaving an encrypted file that could no longer be decrypted. | Every vault operation now holds a filesystem lock across the complete storage transaction. Concurrent unique writers preserve both credentials. A wrong-key open fails without changing ciphertext and the original key still works; a newly opened vault rejects corrupted ciphertext for list and reveal. The complete serial crate suite passed 17/17. OS-keyring restart, injected partial-write/permission failures, rotation/recovery, and separate live-process concurrency remain open. |
| PFTQA-051 | P1 | Local command breadth and locally generated approval resolution | CLOSED FOR CANCEL/NEGATIVE-STATE SLICE; POSITIVE/REMOTE MATRICES OPEN | Feedback, IDE, pets, Vim, personality, rollout, process, and debug-approval surfaces had not been used together on the rebuilt candidate. Live `/test-approval` rendered but cancellation was forwarded as an unowned patch response and ended with `Not available in TUI yet`. | Feedback disclosed proposed attachments and cancelled without upload, then accurately rendered configuration-disabled state. IDE/pets/personality negative states were actionable and local; Vim toggled through real modes; rollout/ps reported current state. Approval requests now carry an explicit local-vs-thread response destination. Three regressions pass and artifact `7d25e1d8e2fb64c31a379671e8d797bb4a8a2ea8426a59aa4cdc9c3b2dba94b2` resolves live cancellation visibly. The retained standard crew kept the same Angmar/Burzum/Snaga/Ghash identities after rebuild. |
| PFTQA-052 | P1 | CLI completion and profile-help PFTerminal identity | CLOSED FOR GENERATION; SHELL-SOURCING MATRIX OPEN | All five supported completion generators registered `codex`, and Bash contained no PFTerminal token. Root profile help also advertised `$CODEX_HOME`. | The shared generator, nested plugin/marketplace metadata, examples, and profile help now use PFTerminal. One regression passed in all three CLI binary targets. Rebuilt artifact `13708c53ba2f50db1268c12cfabf3cab2afb9c1d87a7d304a2c715f69b9d4440` generated Bash/Zsh/Fish/PowerShell/Elvish scripts containing PFTerminal and no stale Codex registration. Actual shell sourcing remains open. |
| PFTQA-053 | P0 | Wallet inline no-state parity | CLOSED FOR UNCREATED STATE; POSITIVE/FUNDED MATRIX OPEN | With no wallet, inline unlock unnecessarily solicited a passcode before admitting no wallet existed, and inline lock claimed a global state transition anyway. | Unlock now preflights daemon state before any secret prompt; lock reports an idempotent no-wallet result. The focused set passed 7/7. Artifact `3fcf2685e3c11c9ec103729c0754936bfd98d70799bb8a7ab608a56491846834` live-tested status plus all five inline admissions: create/restore masked cancellation, unlock with no secret request, and truthful lock. No wallet, secret, funds, plan, or remote mutation was created. |
| PFTQA-054 | P0 | `/spawn` provider failure and same-agent recovery | CLOSED FOR DIRECT-DEEPSEEK AUTH FAILURE/RECOVERY; OTHER PROVIDERS OPEN | A provider failure had not been proven bounded across persistent managed-agent state, creating a risk that a failed turn could strand or replace a crew member. | An invalid credential produced a visible 401 on exact retained Snaga `019fc248-0e17-7281-a37c-741db6f6b1d3`; status preserved the member and continued to offer task delivery. After graceful cold restart with valid direct auth, the same Snaga completed `SPAWN_RECOVERY_OK` and crew state returned to `ready`. No agent was removed, replaced, interrupted, or shut down. |
| PFTQA-055 | P1 | CLI doctor identity and completion execution | CLOSED FOR DOCTOR/BASH; FOUR SHELLS AND ONE UNRELATED TEST OPEN | Bash generation had not been sourced, and live doctor diagnosed Codex version/home/PATH identities instead of PFTerminal. | The shared doctor boundary now emits `pfterminalVersion` and `PFTERMINAL_HOME` and inspects `pfterminal` PATH entries. Artifact `1233ac0e1f4447a91f9c080bc4df673f7669f4e3643e9fc28db3f3669472e65c` returned `overallStatus: ok` with no `codexVersion`; clean Bash sourcing completed `re` to `review` and `resume`. Zsh/Fish/PowerShell/Elvish are absent on this machine. CLI tests were 848/849 with only the unrelated cloud-sandbox integration retry failure. |
| PFTQA-056 | P0 | Provider search and local settings/extension surfaces | OPENROUTER SEARCH CLOSED; LOCAL NEGATIVE/STATE SLICE CLOSED; FULL TUI/REMOTE WORK OPEN | `/providers` advertised search but every query filtered every row because provider items had no search index. Exercised settings also retained stale Codex product values. | Every account/plan/API-key row now indexes its visible name. Final artifact `bcf0a0878d7ec79c9a8e972c751ad35c44543fa8524b365503bc9366c0a3094c` returns the OpenRouter row from real search and keeps Direct DeepSeek visible. Keymap/title/hooks/experimental product text is corrected, including the live `pfterminal` title value. Local status/config/diff/keymap/raw/theme/title/statusline/experimental/memories/skills/hooks/MCP/apps/plugins behavior was exercised. Features passed 33/33; full TUI remains red at 3,778/3,803, and no remote/plugin/connector mutation is claimed. |
| PFTQA-059 | P1 | CLI lifecycle error propagation | CLOSED FOR LOCAL ARCHIVE/UNARCHIVE/DELETE ERROR CHAIN; REMOTE LIFECYCLE OPEN | Forced deletion of a missing UUID exited nonzero but printed only `failed to delete session`, discarding the app-server cause and target identity. | The shared CLI report formatter now preserves nested causes for every session-lifecycle RPC rather than special-casing delete or a UUID. Its regression passed in the Codex-compatible, PFTerminal, and PFTerminal-debug binaries. Live forced-delete output includes `thread/delete failed`, the exact missing thread ID, and the app-server error code while keeping stdout empty and exit status nonzero. |
| PFTQA-060 | P0 | Native `/orchestrate` dispatch and result lifecycle | CLOSED FOR RETAINED DIRECT-PARENT V2 FLOW; BROADER MATRIX OPEN | Live managers were told to use a legacy text adapter despite native collaboration doctrine; actual v2 `followup_task` success was not recognized as assignment dispatch; holder-only lookup could update the wrong assignment; and Core plus PFTerminal could both schedule the same direct-child audit. | Prompts now select the protocol at the native/external boundary. Completed v1 SendInput and v2 Interacted events both project successful native delivery through exact Manager/Worker identity. Multiple assignments on one Manager no longer collapse onto arbitrary map order. Direct native parent results use Core delivery without a second mandate. Final artifact `87ff1ba7302e03d831d78f9fd0332bced0ce3c5c689a95775419ac1d1d6bd044` completed assignment-5 with one dispatch, one exact result, one audit, one `WHIP_DONE`, `fires: 0`, and stable retained IDs. |
| PFTQA-062 | P0 | Direct managed-pane result ownership | CLOSED FOR DIRECT-OPERATOR AND DIRECT-PARENT DELEGATED FLOWS; BROADER MATRIX OPEN | Core forwarded every spawned-session terminal turn to its structural parent without checking who initiated that turn. Direct operator work in Snaga therefore triggered Burzum and Angmar with stale prior orchestration output. | Turn admission now records triggering collaboration provenance across explicit and queued input. Operator input owns its result locally; real delegated work retains Core completion. Four focused regressions plus the public V2 child integration passed before fix/format/build. Artifact `27efd8d403e18c568dab9f515e28da75eb1d9bf978dab4b06209e3af7d6d0d59` kept `DIRECT_CHILD_NO_CASCADE_27EFD8` exclusively in exact Snaga with unchanged parent rollout stats, then delivered one exact Angmar -> Burzum `followup_task` result and one audit. |
| PFTQA-063 | P0 | `/panes` per-thread model projection | CLOSED FOR MAIN-VERSUS-NATIVE-CHILD MODEL IDENTITY; BROADER MATRIX OPEN | Main's picker row read the currently active chat widget, so opening `/panes` from Sol-bound Burzum falsely labelled DeepSeek Main as Sol. Reading only navigation metadata then exposed `model unavailable` because primary navigation model caching is optional. | Main now reads its authoritative durable primary `ThreadSessionState`, with navigation metadata as fallback. A rendered snapshot proves inactive DeepSeek Main beside active Sol child. Artifact `5c731c3fd6f770ffd31db326e987abed8aae01dda959a2cad4f464c3bd509db5` live shows exact Main as `deepseek-v4-flash` from exact Sol Burzum. The independent stale Nazgul fixture remains red and recorded. |
| PFTQA-064 | P0 | `/panes` managed Nazgul task-state projection | CLOSED FOR NATIVE BOUND ROOT RUNNING/COMPLETED; BROADER MATRIX OPEN | Unlike Troll and Orc rows, the bound Nazgul row rendered only `Root role binding` and hid whether its active turn was running or completed. | The row now reuses the same native/external managed-pane state projection while preserving the user-pane binding explanation. Two focused regressions pass. Artifact `a9bc78485af7924956daf8af8fc3235f95d5642fa181f919405d4381b76fc740` live projected exact Angmar from `running` to `completed` across a switch to exact Main, retained its transcript, and kept the result local. |
| PFTQA-065 | P0 | `/panes` active native Claude Plan switching | CLOSED FOR RETAINED NATIVE CLAUDE PLAN SLICE; BROADER MATRIX OPEN | Active-turn switching had been proven for managed roles but not a persistent native provider pane. | Artifact `a9bc78485af7924956daf8af8fc3235f95d5642fa181f919405d4381b76fc740` projected exact native pane `019fc397-e536-7ee3-8c75-bf01df5b87c1` running, switched to exact Main without interruption, and restored its complete exit-0 transcript. Its durable class is `codex_user_pane_ids`, not an external Claude pane. |
| PFTQA-066 | P0 | Native `/ps` and `/stop` background lifecycle | CLOSED FOR ONE NATIVE CLAUDE PLAN JOB; BROADER MATRIX OPEN | An interrupted yielded shell looked orphaned until the explicit background-terminal contract was checked end to end. | Artifact `0150070f2e8fec0f671db6926f6fdddf8e635bb71a6ff966977947885a127abd` retained the real Bash/`sleep` after Esc, exposed its exact command in `/ps`, removed both PIDs with `/stop`, returned empty `/ps`, tolerated a second `/stop`, and kept the pane usable. `/clean`, cross-pane, multi-job, restart, and platform cells remain open. |
| PFTQA-067 | P0 | Native user-pane friendly-name durability | CLOSED FOR NORMAL CREATION/COLD RESTORE; COLLISION/FAILURE MATRIX OPEN | The normal native-pane creation path persisted a nickname only in PFTerminal metadata, so a later restart could degrade the label to a short thread ID. | Registration now persists every accepted unique name through app-server `thread/setName`, updates returned session state, and visibly reports persistence failure. The focused regression passed. Artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff` cold-restored the original `PFTerminal 1` and separately created, backend-confirmed, cold-restored, searched, and selected zero-turn `PFTQA Name 2c1c4f` on exact thread `019fc428-4e75-73e0-b3d1-15851ef618b5`; the disposable pane was then deleted. |
| PFTQA-068 | P0 | Multi-job `/clean` alias lifecycle | CLOSED FOR TWO NATIVE CLAUDE PLAN JOBS; BROADER MATRIX OPEN | The single-job `/stop` slice did not prove the advertised `/clean` alias or all-job cleanup. | Artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff` exposed two exact yielded commands in `/ps`, removed two Bash groups and both child sleeps through `/clean`, returned empty `/ps`, tolerated a second `/clean`, and kept exact pane `019fc397-e536-7ee3-8c75-bf01df5b87c1` usable. Cross-pane, unrelated-process, restart, partial-failure, high-count, and platform cells remain open. |
| PFTQA-069 | P1 | Idle `/quit` and `/exit` state preservation | CLOSED FOR RETAINED TMUX/IDLE SLICE; ACTIVE AND PLATFORM MATRIX OPEN | The advertised exit commands had not been used from the retained reconstructed native-pane graph. | Artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff` cleanly exited via `/quit`, cold-restored exact `PFTerminal 1`, exited via `/exit`, then cold-restored exact Main, user pane, Nazgul, Troll, and three Orc IDs and hierarchy with no residual process or lifecycle mutation. Active turns, confirmation, background policy, non-tmux, signals, and platform cells remain open. |
| PFTQA-070 | P0 | Task Node real GitHub authentication and slow-backend tolerance | CLOSED FOR REAL LINK/AUTHENTICATED CONNECTIVITY; FEATURE AND CAPACITY MATRICES OPEN | The first real link timed out before authentication because the Task Node web machine was unhealthy and slower than PFTerminal's 15-second deadline. | Fly evidence confirmed a production backend incident rather than a credential or UI-routing defect. After recovery, the operator completed real GitHub linking and authenticated PFTerminal use on artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff`. Artifact `e85c474f8880ff93bcc01cd0c04566ecd4e4dbfe52baf4471fb3c29593ae5e61` extends the bounded non-retrying Task Node request deadline to 45 seconds and passed the focused 2-test set plus fix/format/build. Full authenticated workflows and production capacity remediation remain open. |
| PFTQA-071 | P0 | Large real mixed-provider `/spawn` crew work | CLOSED FOR SUCCESSFUL FIVE-ROLE END-TO-END SLICE; FAILURE/SOAK MATRIX OPEN | Earlier `/spawn` qualification used bounded role probes rather than one substantial operator-driven crew run. | Artifact `2c1c4f7dc4c95c8d759804bee1c6d9867ef6c6a9415698084373d99dd10a1cff` retained exact Claude Plan Angmar, OpenAI Sol Burzum, OpenRouter DeepSeek Snaga, OpenAI Terra Ghash, and OpenAI Luna Krimp roles. The operator reported full success; durable rollouts corroborate 18 completed task/turn events, 42 inter-agent communications, extensive tool/patch activity, zero error/interruption events, intact parent edges, and final `ready` state. No role lifecycle was destroyed. This does not qualify `/orchestrate`. |

## Acceptance results

Automated and menu-only evidence is supporting engineering evidence only. Overall feature PASS is
withheld until every required matrix row in the canonical specification passes.

| Feature/workflow | Status | Hands-on result |
| --- | --- | --- |
| `/spawn` manual hierarchy creation, no-task semantics, unique roles/names | SUB-WORKFLOW PASS AFTER PFTQA-028/PFTQA-029/PFTQA-071; COMPLETE MATRIX OPEN | PFTQA-028 created a fresh exact DeepSeek direct/high/YOLO Angmar -> Burzum -> Snaga hierarchy through the complete manual UI flow. Creation started no role task. After exact pane selection, every role accepted direct input; a full native follow-up chain and cold Snaga reuse retained the original identities. PFTQA-029 separately proves the standard three-Orc mixed-provider crew. PFTQA-071 adds a substantial operator-driven five-role Claude Plan/OpenAI/OpenRouter `/spawn` run with 18 completed task/turn events, 42 inter-agent communications, extensive tool/patch activity, no errors or interruptions, intact edges, and final `ready` state. Failure injection, restart-mid-run, concurrent crews, soak, and remaining provider/topology cells remain open. |
| `/orchestrate` native Manager/Worker lifecycle | SUB-WORKFLOW PASS AFTER PFTQA-060/PFTQA-062; COMPLETE MATRIX OPEN | The retained mixed-provider hierarchy created multiple simultaneous assignments on exact Angmar. PFTQA-060 projected native V2 dispatch through exact Manager/Worker identity and removed PFTerminal's duplicate audit mandate. PFTQA-062 proves Core distinguishes operator-owned child work from actual delegated work. PFTQA-071 is explicitly `/spawn` evidence and is not counted here. Other topologies, external panes, pause/resume/extend, retries, timeout, restart-mid-execution, deliberate provider outage, concurrent crews, and soak remain open. |
| `/panes` active switching across managed crew and native provider panes | SUB-WORKFLOW PASS AFTER PFTQA-061/PFTQA-062/PFTQA-063/PFTQA-064/PFTQA-065/PFTQA-067; COMPLETE MATRIX OPEN | Exact retained Snaga, Burzum, Angmar, and native Claude Plan user pane independently ran real tool turns while `/panes` projected each running. Control switched to Main without interruption; returning preserved all full exit-0 transcripts. PFTQA-062 keeps direct managed results in the addressed pane. PFTQA-063 keeps Main's DeepSeek model exact from Sol-bound Burzum. PFTQA-064 repairs the formerly state-blind Nazgul row. PFTQA-065 proves the retained native Claude Plan row/current/provider boundary. PFTQA-067 proves ordinary native-pane friendly names persist through app-server state and exact-name cold search, including a newly created zero-turn pane. Other Orcs/providers, headless Claude panes, approvals, failures, restart-mid-turn, collisions, concurrent panes, and multi-layout cases remain open. |
| Native background terminals through `/ps`, `/stop`, and `/clean` | SUB-WORKFLOW PASS AFTER PFTQA-066/PFTQA-068; COMPLETE MATRIX OPEN | A real native Claude Plan tool yielded Bash plus child `sleep`; Esc intentionally retained the background terminal, `/ps` displayed the exact command, `/stop` removed both PIDs, repeated stop was idempotent, and the owning pane remained usable. PFTQA-068 then yielded two independent Bash groups, proved `/ps` and the footer report both, removed all four processes through `/clean`, repeated clean idempotently, and recovered the same pane. Cross-pane ownership, unrelated-process safety injection, restart, partial failures, high-count, and platform variants remain open. |
| Idle `/quit` and `/exit` process/session lifecycle | SUB-WORKFLOW PASS AFTER PFTQA-069; COMPLETE MATRIX OPEN | Each command cleanly terminated a TUI from exact retained native Claude Plan pane `PFTerminal 1`; no PFTerminal/background process remained. Successive cold resumes retained that pane and exact Main, Nazgul, Troll, and all three Orc IDs and hierarchy. Active turns, confirmation behavior, background-service policy, non-tmux restoration, signals, and platform variants remain open. |
| `/spawn` base permission inheritance and cold restoration | SUB-WORKFLOW PASS AFTER PFTQA-031/PFTQA-032/PFTQA-034 REPAIRS AND PFTQA-033 QUALIFICATION | Main, Angmar, Burzum, Snaga, and the retained managed chain passed workspace/never, workspace/on-request, and read-only/on-request slices. Both approval modes opened real dialogs in every role; rejection left every target absent. PFTQA-034 additionally proved provider-valid Approve for me on exact Snaga, the Full Access cancel/confirm boundary, approval-free outside execution, and flagless Full Access restoration. QA reset the retained hierarchy to Workspace/Ask afterward. Original IDs and native managed follow-up remained reusable. Extra roots/profiles, command-variety gaps, and remaining pane/provider cells are pending. |
| `/permissions` automatic review and Full Access | SUB-WORKFLOW PASS AFTER PFTQA-034 REPAIR | Direct DeepSeek Approve for me launched a DeepSeek-bound Guardian, accepted its low-risk allow decision, and ran the reviewed outside command without a human dialog. Full Access defaulted its warning to Cancel, required explicit confirmation, ran an outside command without approval, and restored on exact Snaga after a flagless restart. Both artifacts were removed and the live profile was reset to Workspace/Ask. Other providers, roles, profiles, denials, escalation, `/approve`, and additional-root cases remain open. |
| Permission-contract resume regressions | SUPPORTING PASS | Three focused TUI tests cover explicit request emission, restore omission, raw harness flags, session/profile layers, and ordinary user defaults. Two app-server unit tests and one protocol integration prove durable workspace-write restoration over broader current defaults while preserving explicit resume overrides. The retained-member regression additionally proves explicit workspace/on-request reaches restored roles. |
| Non-OpenAI current dynamic-context serialization | SUPPORTING PASS AFTER PFTQA-032 REPAIR | The contextual adapter and actual non-OpenAI Responses request regressions retain current permissions, persistent role doctrine, and user work while excluding superseded dynamic sections. The existing OpenAI permission-history test proves first-party append-only behavior is unchanged. Live DeepSeek approval dialogs supply the user-visible boundary evidence. |
| `/panes` navigation and human addressability | SUB-WORKFLOW PASS | Switched among all three managed roles, inspected persisted transcripts, ran real commands in each role, and returned to the root without losing state. |
| Rapid `/panes` and `/docs` first-Enter admission | SUB-WORKFLOW PASS AFTER PFTQA-027 REPAIR | The pre-repair current-code run required a second Enter for `/panes`. On final formatted artifact `b8d47fe89c99859ef5809c1f5cc5a08db1cdc8a74ad383a4beb59eb8ca818af5`, a rapid literal `/panes` burst plus one Enter opened the picker; bare `/docs` plus one Enter dispatched its actionable cwd error; configured `/docs` plus one Enter opened the 50-page viewer. |
| `/panes` restored native-crew search | SUB-WORKFLOW PASS AFTER REPAIR | From cold-resumed Main, name searches returned exact Main, Angmar, Burzum, and Snaga rows; exact durable-ID searches returned the corresponding Angmar and Burzum rows. Selecting Angmar opened the same existing native DeepSeek direct/high/YOLO pane with normal human input. PF user-pane, Claude-pane, duplicate-name, active-turn, and remaining durable-ID search cases remain pending. |
| `/panes` unavailable operator-pane isolation | SUB-WORKFLOW PASS AFTER REPAIR | A missing-rollout pane produced one startup error, then remained searchable as a disabled row with an inline reason. `/panes` selected an unrelated healthy sibling and reopened without retrying the failed pane. |
| Idle PF user-pane archive and restart | SUB-WORKFLOW PASS AFTER REPAIR | A fresh DeepSeek direct/high/YOLO pane with zero tokens archived after confirmation. Its rollout remained recoverable under `archived_sessions`; its ID was removed from the owner layout; restart returned to Main and the archived pane was absent from `/panes` and name search. Active operator archive, operator/Claude delete, and whole-crew removal are recorded in their separate rows. |
| Active PF user-pane archive and restart | SUB-WORKFLOW PASS AFTER REPAIR | While a real 240-second DeepSeek-backed shell tool was active, `/archive` dispatched immediately, showed the PFTerminal confirmation, aborted the tool after 17.1s, retained an interrupted transcript, removed the operator-pane ID from Main's layout, and exited. Cold restart returned to Main and exact-ID `/panes` search returned no match. Operator/Claude delete and whole-crew removal are recorded in their separate rows. |
| Idle and active PF user-pane permanent delete | SUB-WORKFLOW PASS AFTER REPAIR | Explicit `/delete` confirmation removed a zero-turn pane and a separate pane running a real 240-second DeepSeek-backed tool. The active child process was gone, rollout/state records were deleted by contract, owner membership fell back to Main, both TUIs exited, and cold-restart exact-ID search returned no match. Operator-created Claude deletion and whole-crew removal are recorded in their separate rows. |
| Claude pane lifecycle ownership and permanent delete | SUB-WORKFLOW PASS AFTER REPAIR | A selected zero-turn Opus 5 Claude Plan pane first proved native `/archive` and `/delete` cannot touch Main. Dedicated external `/delete` then permanently removed separate idle and actively running operator-created Claude panes, canceled the active child, removed artifacts/layout membership, fell back to Main, exited, and stayed absent after cold restart. Claude `/archive` is unsupported by contract; managed Claude members remain protected from individual deletion and are removed only through the whole-crew boundary. |
| Whole managed-crew cancel/remove/restart lifecycle | SUB-WORKFLOW PASS AFTER REPAIR | The default cancel path retained the complete native crew. Confirmed removal deleted all three managed native members and Core edges/rollouts, cleared `CrewSpec` and layout mappings, preserved and returned to Main, and stayed empty after cold restart. `/panes` and `/spawn status` show `No managed crew` rather than reclassifying Main as Nazgul, and the removal item is disabled. The mixed integration test supplies the running-Claude cancellation/artifact and bound-user-root cases. |
| Wallet account, balance, recovery, and plan workflow | PARTIAL AFTER SAFETY REPAIR | Disposable isolated homes covered no-wallet status, secure creation, protected backup, wrong/correct unlock, explicit lock, one-minute autolock, process restart, exact-address restore into a second home, zero SOL/USDC reconciliation against an independent RPC, live plan listing and insufficient-funds admission, and the existing-plan recovery path with explicit `No USDC was sent` failure copy. PFTQA-022 repaired and live-qualified destructive confirmation admission. A funded minimal plan purchase, payment failure/idempotency matrix, 0.1.26 wallet migration, and every menu/inline permutation remain open, so wallet is not feature-PASS. |
| Vault credential lifecycle | PARTIAL AFTER PFTQA-023/PFTQA-037/PFTQA-044/PFTQA-050 REPAIRS | Isolated encrypted fallback storage covered bare menu, masked add, list/status/metadata, protected actions, replace, process-restart persistence, safe confirmation, and explicit delete. PFTQA-037 proves provider-labelled add/replace/delete changes same-process DeepSeek authentication. PFTQA-044 proves truthful fallback-source reporting, a 0600 home-derived key, and restart recovery. PFTQA-050 adds full-transaction locking plus wrong-key and corrupt-ciphertext fail-closed coverage. The operation modal still dismisses before its asynchronous completion toast; clipboard clearing, OS backend restart, injected partial-write/permission failures, live multi-process concurrency, and every inline/menu permutation remain open. Earlier harness-created history copies mean zero-unintended-copy is deliberately not accepted. |
| GPU catalogue, rental, endpoint, and cleanup | PARTIAL AFTER PFTQA-038 | The current candidate renders the qualified official DeepSeek 0731 TP2 r3 route, qualified GLM, and two clearly experimental GGUF routes. Its official recipe pins the required HF commit, runtime, image digest, TP2 hardware, and context limits. A real selection reached the first price-limit step and cancelled without charge. All 33 historical local rental records are terminal and no runtime provider remains, but stale standalone Vast/RunPod keys prevented independent provider inventory. A new operator-approved bounded rental, endpoint qualification, restart, stop/terminate, and provider-side billing proof remain open. |
| Telegram remote control | PARTIAL AFTER PFTQA-039 | Candidate read-only health correctly identified the configured bot, one allowed chat/user, and workspace. The production connector is still an old seven-day process running a deleted executable with no pending durable delivery; QA preserved it instead of silently replacing a live service. Real candidate inbound/outbound text, attachment, permission, restart/dedup, cancellation, stop, and disconnect remain open. |
| Task Node | PARTIAL AFTER PFTQA-045/PFTQA-070; AUTHENTICATED FEATURE MATRIX OPEN | Candidate reads fail closed with bounded `terminal_login_required` and no token disclosure. PFTQA-045 repaired the blocking link/logout boundary. PFTQA-070 adds a real operator-assisted GitHub link and confirmed authenticated PFTerminal connectivity after a documented production outage recovered. Non-streaming requests now tolerate up to 45 seconds without automatic retry or duplicate mutation. Task listing/detail/actions, context read/edit, chat, wallet projection, rewards, verification, logout/re-login, expiry/revocation, restart persistence, and backend capacity remediation remain open. |
| Goals, memories, MCP, and hooks | PARTIAL AFTER PFTQA-040 | Goal create/status/pause/edit/restart/clear worked with real continuation state; a vague objective consumed about 40K tokens in one minute, leaving a budget/prompt-risk row. Memories persisted enablement and completed real consolidation into valid local artifacts. A real stdio MCP server exposed tools and an approved nested call returned `MCP_NESTED_OK`. Hooks required review/trust and executed the enabled SessionStart command on the first real turn. Their complete failure, timeout, compaction, extraction, resource, elicitation, reconnect, and bypass matrices remain open. |
| Session and local command surfaces | QUALIFIED SLICE AFTER PFTQA-040/PFTQA-041/PFTQA-046/PFTQA-058 | Real `/side` and `/btw` turns stayed out of the parent transcript; `/rename`, `/new`, `/fork`, and named `/clear` retained correct identity boundaries. PFTQA-046 additionally proves an ordinary flagless `/resume` restores exact DeepSeek model/provider/high effort and that a subsequent real turn persists the same runtime. PFTQA-058 adds real CLI response/tool/JSON/stdin execution, a real dirty-tree review, compaction followed by a successful continuation, and the complete Plan propose/revise/stay/explicit-approve transition with read-only execution. Mention/read, `/init`, real `/diff`, provider/model catalogues, orchestration, raw/copy/process controls, UI persistence, experimental rollback, and keymap restoration worked. Explicit invocation override permutations, imported-data execution, invalid/active-turn cases, non-tmux behavior, packaging, and platforms remain open. |
| Plugin catalogue and local lifecycle | PARTIAL AFTER PFTQA-042/PFTQA-059 | The 180-entry catalogue, metadata/details view, local curated installation, immediate skill discovery, disable, re-enable, and uninstall all produced observable state changes. PFTQA-059 additionally installed `build-web-apps`, cold-restarted the exact Main, discovered the persisted plugin and its contributed `frontend-app-builder` skill, invoked that skill in a real provider turn, removed the plugin, restarted again, and proved both plugin and skills absent. Remote marketplace add/update, plugin hooks/apps, authentication, network failure, conflict, and rollback remain open. |
| CLI sandbox and lifecycle failure boundaries | QUALIFIED LOCAL SLICE AFTER PFTQA-059 | Real read-only, workspace, and Full Access commands proved read/write/network enforcement and exit propagation. Missing archive/unarchive/delete targets fail safely with empty stdout and actionable stderr; delete required a shared error-chain repair. Managed profiles, supplied sandbox-state JSON, remote app-server lifecycle, signals, and platform variants remain open. |
| DeepSeek direct provider route | SUB-WORKFLOW PASS | A real two-turn `deepseek` / `deepseek-v4-flash` TUI session returned the exact normal response, invoked the requested shell tool once, retained exact route events, and reported token usage. A separate invalid key produced one immediate 401, no model output, and no orphan. Credential persistence/replacement and the remaining capability/failure matrix stay open. |
| OpenRouter pinned DeepSeek route | SUB-WORKFLOW PASS | A real two-turn `openrouter` / `deepseek/deepseek-v4-flash-0731` TUI session returned the exact normal response, invoked the requested shell tool once, and retained exact provider/model events with no fallback configured. Picker persistence, invalid-auth recovery, and other OpenRouter models stay open. |
| Anthropic direct Opus 5 route | SUB-WORKFLOW PASS AFTER OUTPUT-LIMIT REPAIR | The rebuilt artifact authenticated with the direct API key, used catalogue max output 128K, completed a normal turn and a shell-tool turn, and recorded exact `anthropic` / `claude-opus-5` identity plus cache/token usage. Remaining reasoning, vision, JSON, failure, switch, restart, and invoice reconciliation cases stay open. |
| Claude Opus 5 Plan route | SUB-WORKFLOW PASS AFTER OUTPUT-LIMIT REPAIR | The released OAuth helper used the existing Claude login, and the rebuilt artifact completed normal and shell-tool turns as exact `claude-plan` / `claude-opus-5-plan`, with plan usage distinct from API-key billing. Fable, auth expiry/refresh, effort, restart, and quota-exhaustion cases stay open. |
| OpenAI Luna API route and native collaboration | SUB-WORKFLOW PASS AFTER PFTQA-025/PFTQA-026 REPAIRS | The exact artifacts completed normal Luna Responses turns without reserved-schema rejection, running-child `send_message`, native spawn/follow-up/list/wait, target-only interrupt, same-child recovery, and two new-process cold-resume follow-ups on one stable child thread/path. Sol, Terra, service tiers, tool/vision/JSON capability coverage, invalid-auth recovery, PF role/pane projection, and invoice reconciliation remain open. |
| Vercel GLM 5.2 Fast route | PARTIAL AFTER METADATA REPAIR | The old artifact failed locally for absent output metadata and the old benchmark falsely called it success. The rebuilt artifact used provider-published 128K metadata and reached Vercel's real Messages endpoint, but the only resident credential returned 401. No inference or spend occurred; the route is not accepted. |
| Fresh onboarding to DeepSeek-backed Main | SUB-WORKFLOW PASS AFTER REPAIR | A fresh isolated PF home selected DeepSeek direct, persisted the credential once, handed the live event stream into Main, and created a usable operator pane. This does not qualify the other provider or authentication routes. |
| Operator-created PF user pane cold restart | SUB-WORKFLOW PASS AFTER REPAIR | `Cold Restore QA` survived a full process exit as the same child `ThreadId`, name, route, effort, permissions, transcript, and direct-input surface, then completed a second real `pwd` tool turn. `/agent`, `/subagents`, and `/panes` agreed on the child. Exact Main restoration and the broader `/panes` matrix remain pending. |
| `/docs` source navigation and packaged offline fallback | SUB-WORKFLOW PASS AFTER PFTQA-047 | Live tmux use covered bare and targeted pages, nested paths, readable headings/lists/code/tables, bounded content search, internal links, HTML anchors, back/forward, missing and malformed requests, resize, close/redraw, and a real staged npm offline tree from `/tmp`. It opened during a real active Main shell-tool turn without losing `DOCS_ACTIVE_OK`. On the final artifact it also opened from Main, PF user, Nazgul, Troll, Orc, and disposable Opus 5 Claude Plan panes. Remaining work is content-by-content feature accuracy and final package/platform artifacts, not pane-class reachability. |
| `/panes` from active Main | SUB-WORKFLOW PASS | During a real 45-second shell-tool turn, `/panes` opened and labeled Main `running`; closing the picker preserved the turn, which later returned `PANES_ACTIVE_OK`. Active switching across PF user, crew, and Claude panes remains pending. |
| `/panes` active PF user-pane switching | SUB-WORKFLOW PASS AFTER REPAIR | A live duplicate-name flow created `Matrix Twin` and `Matrix Twin (2)`, F2 rename preserved collision-safe identity, and the picker showed exactly one current row after the current-state projection repair. While `Matrix Twin (2)` ran a real 45-second shell tool, the picker labelled it `running`; control switched to idle `Matrix Twin`, the background row remained `running`, and returning to the original thread preserved its transcript and exact `PF_USER_ACTIVE_OK` result. Crew and Claude active-switch cases remain pending. |
| `/panes` exact Main/layout cold restoration | SUB-WORKFLOW PASS AFTER REPAIR | A fresh no-task Main and child were both physically durable before exit. Exact resume retained Main `019fc0f3-45e5-7b43-a1cd-5af1c61f0414`, its owner layout, child name, route, and zero-turn child `019fc0f4-26ff-7ff3-b1eb-300582ee4e2e`; `/status` and `/panes` showed those exact identities after restart. Other providers, multiple layouts, active-close, and failure recovery remain pending. |
| `/spawn` task UI to Troll | SUB-WORKFLOW PASS AFTER PFTQA-043 | A role-correct task reached original Burzum, which delegated the exact probe to original Snaga, independently verified it, and returned `TROLL_TASK_UI_OK`. A second assignment entered the same active Burzum through native delivery while `/spawn status` remained responsive; cold restart restored both exact IDs and both accepted new work without replacement or shutdown. Remaining provider/permission/failure permutations stay open. |
| `/spawn` task UI to Orc | SUB-WORKFLOW PASS AFTER REPAIR | Pre-repair task was ignored because its authority was ambiguous. Post-repair, the same restored Snaga pane executed only the requested marker command and returned literal output plus exit 0. |
| Current-code retained Nazgul/Troll/Orc native follow-up | SUB-WORKFLOW PASS | Direct input worked in Angmar, Burzum, and Snaga. Burzum reused the same Snaga through native follow-up; Angmar reused the same Burzum, which reused the same Snaga, and all expected markers returned to the initiating pane. No spawn, replacement, close, or automatic shutdown occurred. This was a protocol round trip, so the real planning/review and newly created-role rows remain open. |
| Standard mixed-provider crew and cross-provider native follow-up | SUB-WORKFLOW PASS AFTER PFTQA-029 REPAIR | The standard Fable/Sol/Luna/Terra/Grok crew accepted direct input and retained exact IDs. Angmar delegated once to Burzum; Burzum used native encrypted follow-up for Luna/Terra and the typed plaintext adapter for OpenRouter/Grok after the native call failed before admission. All three Orc results and both consolidation results returned. `/spawn status` kept every role completed and reusable. Real planning/review, active-turn cross-provider `send_message`, remaining permissions/failures, and full lifecycle matrices remain open. |
| `/spawn` and `/panes` cold restart | SUB-WORKFLOW PASS AFTER PFTQA-013 REPAIR | The current candidate cold-resumed the exact four-thread Angmar/Burzum/Snaga crew from an isolated clone. `/spawn`, `/agent`, `/subagents`, and `/panes` agreed on IDs, canonical paths, hierarchy, roles, and status; Burzum and Snaga opened as normal DeepSeek direct/high/YOLO human-addressable panes. Both layout generations retained the complete crew. Provider, permission, injected-failure, active-turn, and multi-restart matrices remain pending; whole-crew removal and its subsequent cold-empty state are closed by PFTQA-021. |
| TUI automated regression suite after `/spawn` repairs | SUPPORTING PASS | `just test -p codex-tui` completed on the repaired tree: 3,742 tests run, 3,742 passed, 9 intentionally skipped, including the real provider-boundary and cold-restored direct-turn regressions. This supports but does not replace the hands-on feature matrix. |
| OpenAI Codex native agent graph and mailbox | SUB-WORKFLOW PASS | A paid DeepSeek direct root used native `spawn_agent` to create exactly one Orc at `/root/qa_worker`. The Orc received its literal task without discovery, accepted a second task through `followup_task` on the same thread/path, and accepted a third task after cold-process resume. Completion did not close or replace it. |
| Native manager loop breaker | SUB-WORKFLOW PASS, ACTIVE AND COLD | Paid `LOOP3` stopped automatically after the fourth child result, retained that result in the Troll mailbox, resumed the same Troll/Orc after fresh input, and ended on a no-dispatch acknowledgement. After a separate full restart, Snaga's completion was durably submitted to cold Burzum with `trigger_turn:false`; Burzum did not run until fresh operator input, then consumed the queued result and acknowledged without dispatch. |
| Core native-agent automated regression suite | SUPPORTING PASS | `NEXTEST_TEST_THREADS=4 just test -p codex-core agent`: 458 tests run, 458 passed, 2,867 skipped. This includes role-schema, provider-neutral message, same-member follow-up, cold resume, manager chain limiting, operator-input recovery, and a real root/parent depth-boundary regression. |
| Core durable native graph regression | SUPPORTING PASS | `NEXTEST_TEST_THREADS=1 just test -p codex-core durable_thread_spawn_materializes_lazy_parent_child_and_core_graph_edge`: 1 test run, 1 passed. It proves Core materializes and flushes both durable endpoints before publishing the edge. |
| TUI focused `/spawn` automated regression suite | SUPPORTING PASS | `NEXTEST_TEST_THREADS=4 just test -p codex-tui spawn`: 81 tests run, 81 passed, 3,696 skipped. The native event path sends stable mailbox assignments, does not use direct `turn/start` fallback admission, records a manually selected Troll beneath its bound native Nazgul in Core, and projects searchable restored roles from durable `CrewSpec` metadata. |
| TUI pane-focused automated regression suite | SUPPORTING PASS | `NEXTEST_TEST_THREADS=4 just test -p codex-tui pane_`: 50 tests run, 50 passed, 3,727 skipped. It includes failed-pane isolation, terminal operator-pane membership cleanup, picker identity/search projection, schema-compatible layout persistence, direct-input preservation, and pane-local failure/status behavior. |
| Active-task slash control-plane regressions | SUPPORTING PASS | Popup-independent composer admission: 1 passed, 3,778 skipped. `/archive` confirmation/event group: 2 passed, 3,777 skipped. `/delete` confirmation/event group: 2 passed, 3,777 skipped. The adjacent `/status` assertion proves the input repair applies to the declared active-task command class rather than one command literal. |
| Rapid slash-command first-Enter regressions | SUPPORTING PASS | The real rapid key-stream regression covers adjacent `/panes` and `/docs`, and the focused `burst_enter` set passed before the required TUI fix/build step. Pending slash bursts are handled by protocol shape, not command-name regex. |
| Native lifecycle ownership regression | SUPPORTING PASS | `terminal_lifecycle_scope_blocks_external_managed_and_parent_owned_panes`: 1 passed, 3,779 skipped. It allows Main/operator panes and rejects Claude, durable crew, and task-only ownership classes before mutation. |
| Claude operator-pane delete and external-routing regressions | SUPPORTING PASS | Five focused tests passed: idle artifact/restoration cleanup, running-turn cancellation, app-event delete cleanup, managed-Claude-member refusal, and selected-external routing before native-thread resolution. |
| Whole-crew lifecycle and empty-projection regressions | SUPPORTING PASS | Four PFTQA-021-focused regressions passed within a 14-test lifecycle run: managed-Claude crew-boundary removal/cancellation, rejection of operator-pane removal through that boundary, mixed native/Claude whole-crew removal with bound-Main preservation, and empty-state projection without a synthetic Main-as-Nazgul row. |
| Safety-sensitive selection confirmation regressions | SUPPORTING PASS | The generalized no-number-shortcut regression and three wallet-removal focused tests pass. They prove digits do not accept a safety confirmation, numbering is not rendered, explicit navigation plus Enter remains functional, and wallet confirmation lifecycle behavior is preserved. |
| Vault TUI focused regressions | SUPPORTING PASS | 35 tests pass: masked entry, redacted async add/replace, secure reveal zeroization/no-history/no-debug leak, safe delete confirmation, menus, inline-secret recall rejection, metadata-only rendering, and command routing. |
| Encrypted vault regressions | SUPPORTING PASS | All 17 `codex-vault` tests pass serially, covering add/update/reveal/delete, encrypted-at-rest persistence, duplicate/invalid/missing labels, distinct-label secret isolation, synchronized writers, wrong-key ciphertext preservation, and reopened corrupt-store rejection. |
| Local approval response routing | SUPPORTING PASS | Three focused regressions pass: local response cancellation, `/test-approval` command wiring, and unchanged thread-owned patch response submission. |
| CLI completion identity | SUPPORTING PASS | The all-shell generator regression passes in the Codex-compatible, PFTerminal, and PFTerminal-debug binary test targets; each rejects stale Codex registration. |
| Wallet unlock/no-state routing | SUPPORTING PASS | Seven focused unlock tests pass, including the no-wallet preflight that cannot open secret entry and the existing fixed/custom-duration retry and continuation contracts. |
| Async history redraw regression | SUPPORTING PASS | Background history insertion requests a frame without user input; live PFTQA-023 status/list output appeared without a wake-up key. |
| Pane-layout compatibility and preservation regressions | SUPPORTING PASS | `NEXTEST_TEST_THREADS=1 just test -p codex-tui pane_layout_`: 14 tests run, 14 passed, 3,761 skipped. Prior-schema raw checksums remain valid after new defaulted fields are introduced, verified recovery generations are usable, and neither one nor two unverifiable generations are destructively replaced. |
| External-pane loop-adapter regressions | SUPPORTING PASS | `just test -p codex-tui external_pane_auto`: 2 tests run, 2 passed, 3,753 skipped. They prove chained dispatch pauses at the shared ceiling, fresh input resumes it, and acknowledgement without dispatch ends the chain. |
| App-server native mailbox regression | SUPPORTING PASS | `just test -p codex-app-server thread_agent_message`: 1 test run, 1 passed, 1,072 skipped. It verifies stable-ID deduplication, cold-manager terminal results are effectively queue-only, a second task to the same completed worker, and child-to-parent delivery through `thread/sendAgentMessage`. |
| App-server non-TUI lazy-start compatibility | SUPPORTING PASS | `NEXTEST_TEST_THREADS=1 just test -p codex-app-server thread_start_creates_thread_and_emits_started`: 1 test run, 1 passed, 1,072 skipped. The ordinary app-server client still receives a precomputed but unmaterialized path, proving the new durability boundary is limited to persistent TUI and native-agent starts. |
| App-server role-instruction and resume regressions | SUPPORTING PASS | `NEXTEST_TEST_THREADS=1 just test -p codex-app-server multi_agent_v2_developer_instructions`: 13 tests run, 13 passed, 1,060 skipped. It verifies configured role/developer-instruction precedence across no/full/bounded history, compaction, completion, and cold resume while accepting both native OpenAI and provider-neutral assignment wire forms. |
