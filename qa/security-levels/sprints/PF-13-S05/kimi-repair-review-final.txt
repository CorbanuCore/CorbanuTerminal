Sandbox is read-only even for /tmp scratch. I'll compare inline instead.
The 12 shared files are byte-identical to the prior snapshot. Only 4 files changed: `tui/src/tui.rs`, `scripts/security_credential_canary.py`, `scripts/test_security_credential_canary.py`, `tui/src/credential_panic_tests.rs`. Reading the new patch and changed files.
All verification complete. The 12 unchanged files are byte-identical to the prior snapshot; only 4 files changed, and I've inspected each change in this new snapshot directly.

# PF-13 Final Repair-Cycle Review — base `f0a160eee5b820bc16fcd116013274d44e7d9be4`

**Verification:** All 16 frozen files verified (16/16 OK). Delta confirmed scoped to the four changed files: `tui/src/tui.rs`, `tui/src/credential_panic_tests.rs`, `scripts/security_credential_canary.py`, `scripts/test_security_credential_canary.py`.

## P3-A resolution — terminal-restore hook now checks the scoped flag
`tui/src/tui.rs:537` (`set_panic_hook`, now `pub(super)`) inserts the `codex_vault::scoped_credential_callback_active()` early-return **before** `restore_after_exit()` and before chaining to the previous hook. This is exactly the hardening I recommended: the hook no longer relies on the inner vault-aware hook for suppression, and it also correctly avoids tearing down the terminal for a panic that Vault's `catch_unwind` will recover. The comment accurately states the rationale. The production test now installs the **real** chain — `color_eyre::install()` → `install_panic_hook()` → `tui::set_panic_hook()` — in both `vault-first` and `tui-first` orders, and asserts the secret canary is absent from stdout/stderr while ordinary panics still report. **Resolved correctly.**

One accuracy note (not a defect): the test chain covers the real ordering, but a truly minimal hook that does `format!`/`tracing` of `panic_info` *before* checking the flag would still leak — the invariant is "check before formatting," which both hooks now honor. No further action needed.

## P3-B resolution — overflow error now carries byte counts
`security_credential_canary.py` now reports `command {surface} exceeds the capture limit: {n} bytes > {limit} bytes`, and the Python test asserts the exact measured/limit message (`MAX_CAPTURE_BYTES + 1` vs limit). This distinguishes "transcript too large" from a secret finding, addressing my clarity concern while keeping the fail-closed behavior. **Resolved correctly.**

## TUI fixture schema correction
The fixture's `authorization` object now includes `"schema_version":1` (tui/src/credential_panic_tests.rs:22). This matches `AUTHORIZATION_SCHEMA_VERSION = 1` (security-policy/src/authorization.rs:12) and the `deny_unknown_fields` deserialization of `AuthorizationRequest`. The grant fixture remains at `schema_version: 2`, matching `GRANT_SCHEMA_VERSION = 2`. The fixture is internally consistent and now parses the real contract — the omission the first real run caught is fixed, and the fixture no longer deserializes a malformed request that would have failed before reaching the hook.

## Re-confirmed unchanged items (hash-identical, prior analysis stands)
C1 scan-before-limit, C2 vault thread-local guard + `Once`-installed global hook, F3 home/keyring tests, F4 zeroizing + sensitive scoped header, F6 label comment — all byte-for-byte identical to the prior review and remain correctly implemented.

## Findings
No P1, P2, or P3 defects. Both prior P3 suggestions are resolved and the fixture schema error is corrected. I found no new concrete bug in this delta.

```json
{
  "reviewed_base": "f0a160eee5b820bc16fcd116013274d44e7d9be4",
  "findings": [],
  "coverage": [
    "REPAIR_FILES.sha256: 16/16 entries verified OK; 12 files hash-identical to prior snapshot, 4 changed (tui/src/tui.rs, tui/src/credential_panic_tests.rs, scripts/security_credential_canary.py, scripts/test_security_credential_canary.py)",
    "REPAIR_DIFF.patch read in full",
    "P3-A: tui/src/tui.rs:537 set_panic_hook now checks scoped_credential_callback_active() before restore_after_exit() and chaining; test installs the real color_eyre -> install_panic_hook -> set_panic_hook chain in both orders",
    "P3-B: scripts/security_credential_canary.py overflow error includes measured/limit bytes; test asserts exact message",
    "TUI fixture: authorization.schema_version=1 added (matches AUTHORIZATION_SCHEMA_VERSION=1); grant schema_version=2 (matches GRANT_SCHEMA_VERSION=2); deny_unknown_fields satisfied",
    "Re-verified unchanged: C1 run_command scan-before-limit + timeout denial, C2 vault/src/credential_panic.rs guard + Once hook, F3 home/keyring tests, F4 scoped_openai_header_value zeroizing+sensitive, F6 label comment",
    "tui/src/lib.rs module wiring (mod tui; #[cfg(test)] mod credential_panic_tests; pub(super) visibility), codex-vault dependency present in tui/Cargo.toml"
  ],
  "verdict": "The complete repair delta is correct and self-consistent. Both prior P3 suggestions (terminal-restore hook flag check with real-chain test; overflow byte-count error) are properly resolved, and the TUI fixture schema omission caught by the first real run is fixed and now satisfies the security-policy deserialization contract. No new defects introduced. The five accepted fixes (C1/C2/F3/F4/F6) are implemented as scoped, with PF-23 native wiring, legacy/Permissive headers, and dummy shaping correctly excluded. This does not certify full Core/platform readiness, which is running separately."
}
```
