autoreview clean: no accepted/actionable findings reported
overall: patch is correct (0.82)
Reviewed the staged diff against the surrounding crate. The recipe parser rejects non-canonical, zero, u32::MAX, colliding and role-mismatched arguments; `SyntheticLaunchRecipe::new` enforces absolute paths and a distinct nonzero identity table; `command()` builds only the fixed argv with cleared env, `/` cwd and null stdio. The probe's `--prepare-synthetic-child` path requires exact-5-field UTF-8 argv, all-zero UID/GID triplets, a single thread and a closed FD allowlist before any setter, then applies no_new_privs/keepcaps(false) → setgroups → setresgid → setresuid → re-verification, followed by the existing hardening pass (which now legitimately runs as the dropped nonroot identity; self-access to /proc/self/fd remains allowed after nondumpable) and a final identity/FD recheck. Failures yield only the fixed diagnostic and exit 78. Feature/target gating in lib.rs and the tests is consistent (the synthetic-only tests are fully cfg'd out on non-Linux so the `nix`/`tempfile` uses do not break default builds), and clippy config allows expect/unwrap in tests. Test fakes cover ordering/stop-on-failure, and the real-process test proves nonroot/malformed denial without kernel mutation in the runner. No dependency or lock changes. I found no concrete defect, security exposure, or blocking test gap introduced by this change.
