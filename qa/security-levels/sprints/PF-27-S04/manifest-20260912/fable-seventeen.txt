autoreview clean: no accepted/actionable findings reported
overall: patch is correct (0.86)
Reviewed files.rs, schema.rs, mod.rs, the two test modules, Cargo wiring and evidence artifacts. The inspection path is fail-closed: openat2 with RESOLVE_BENEATH|NO_SYMLINKS|NO_MAGICLINKS plus O_NOFOLLOW|O_NONBLOCK|O_CLOEXEC (O_DIRECTORY for directory kinds), per-component uid/gid, type, exact-mode (or 0o7022-clean ancestor) and nlink==1 checks on the opened fd, and the read baseline stamp is taken from the same fd metadata that was permission-checked, then re-compared after the bounded read. `mode & 0o7022 == 0` parses correctly in Rust ((mode & mask) == 0). The manifest read is bounded by take(MANIFEST_LIMIT+1) with an explicit oversize rejection; serde derive with deny_unknown_fields rejects unknown, missing and duplicate fields at every nesting level, and source_commit/probe_sha256 are lowercase-hex length-checked so the derived path component cannot traverse. Role-table validation reuses SyntheticLaunchRecipe::new (sentinel IDs, collisions, anchor overlap). The image digest is streamed with a hard byte bound, ELF64/LE/x86-64/EXEC|DYN identity check, and no error string echoes injected detail. The only public factory fixes path and principal (all six res UIDs/GIDs must be 0), the result exposes no getters, Clone or command factory, and the module is gated behind linux+synthetic-fixture consistent with lib.rs. Cargo feature/optional-dep changes match the existing target-table pattern; Cargo.lock delta is the three expected edges. Evidence documents disclose the initial fixture failures and the umask/SUN_LEN repairs honestly. Hardcoded RTX home paths appear only in evidence scripts, not policy. No landable-blocking defects found.
