autoreview clean: no accepted/actionable findings reported
overall: patch is correct (0.85)
The 20-line spawn.rs correction is behavior-preserving and resolves the deny-level `expect_used` violations without introducing new lint exposure (`clippy::panic` is not denied in the workspace). The missing-permit path now returns the owned image with an `io::Error` while `self` (permit already `None`) drops without releasing anything; the worker path's `let-else` `panic!` stays inside `catch_unwind`, so the impossible lost-image case lands in `Status::Quarantined` with the permit retained, matching the frozen no-force-reset contract; the failed-start path still returns the retained image, and the documented `Builder::spawn` ownership contract makes its `panic!` branch unreachable. `mut self` remains required for the post-start `self.permit.take()`. Reservation/permit ordering, sticky cancellation, mutex scoping, and late-child handling are untouched. Evidence artifacts show strict feature-enabled Clippy exit 0 and identical test results on the corrected source. The remaining diff is QA evidence and ledger text only.
