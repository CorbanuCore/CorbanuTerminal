autoreview findings: 1
[P3] README links to evidence logs that are gitignored and absent from the change bundle
qa/security-levels/sprints/PF-27-S04/root-composition-20260912/README.md:39
The new README states the original transitive Clippy failure is "[preserved](initial/clippy-default.log)" and that commands/captures are in `rtx/`, but `initial/clippy-default.log` and every `rtx/*.log` (fix, fmt, clippy-*-scoped, default, fixture, affected, build, bazel-lock) match the repo-wide `*.log` ignore rule and are not in the untracked set of this bundle (only `binaries.sha256`, `tmux-*.txt/json` appear). `CURRENT.md` in the same directory is likewise not in the bundle. Prior stages (`child-admission-20260912/rtx-final/*.log`) force-added these files so the record was self-contained. As staged, the committed record would have dangling links and the 356/2-skipped, 3/3, 17/17, --no-deps Clippy pass and preserved-failure claims would be unverifiable from the repository, which conflicts with the policy that missing evidence must not be represented as present. Smallest fix: `git add -f` the `initial/` and `rtx/*.log` files (and `CURRENT.md` if intended) before landing, or change the README to state the logs live only on RTX.

overall: patch is correct (0.85)
Inspected `TrustedChildRun`, `ControllerRoot::{open_journal_system,open_policy_system,serve_child}` and `RootError` against the new adapter. Ownership on partial construction is sound: `Composition::open` takes `TrustedChildRun` by value, so either root-open failure drops it and `Drop -> shutdown()` fences via pidfd SIGKILL and hands children to the preallocated reaper; an already-opened journal root is released by local drop. Role selection derives only from the kernel-verified slot role returned through `TrustedChildRun::admit`, mapped to the matching stored root; no channel bytes influence it. Root lifetime is held by `Arc<Roots>` cloned into each handler closure, so roots outlive stalled handlers until fence/shutdown unblocks them, and `shutdown()==true` implies handler threads (and their Arc clones) are finished. Typed errors, including `Ambiguous`, are stored before the closure returns (hence before the fence), so `poll_health().is_err()` implies `root_error` is populated for error returns; panic/Ok paths correctly yield `None` as documented. `failures` mutex is never held across `serve`, avoiding poisoning/deadlock. Non-root denial comes from `system_authority()` and fences children through the same drop path. `RootError` is `Copy`, so `[None; 2]` and `root_error` are correct. Linux-only dependency and lock edge match the workspace; protected-state is already Linux-gated. Tests exercise reverse-order admission, both open-failure orders with drop counts, native non-root denial, and Fail/Panic/Return terminal paths; claims in the README are consistent with the on-disk RTX logs. The only actionable item is evidence traceability: linked logs are gitignored and not in the bundle.
