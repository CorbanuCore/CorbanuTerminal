autoreview clean: no accepted/actionable findings reported
overall: patch is correct (0.88)
The new private `SystemPreflight` acquires the global `Reservation` first, obtains two `SyntheticManifestInspection`s via the existing root-only factory, and compares executable path, full image stamp (dev/ino/mode/uid/gid/nlink/len/mtime/ctime — no atime, so the second read cannot self-invalidate), digest, all three identity pairs and anchor gid before consuming either; failures on any `?` drop the owned intermediates and then the reservation. Struct field order (`_images`, `_roots`, `_reservation`) guarantees images/roots drop before the permit is released. `System` maps `RootError` to a denial and uses only `open_*_system` (no enrollment path). Private-field access on `SyntheticLaunchRecipe` is legal from the descendant module, `manifest::tests` helpers are correctly widened to `pub(super)`, and `elf::tests::fixture` is already `pub(crate)`. The eight injection stages match the actual call sequence (inspect×2, seal/profile×2, root×2); descriptor and `Arc::strong_count` baselines are taken after `Roots::fresh()` so leak checks are sound. Test use of the global permit and `waitid` ECHILD mirrors existing sibling tests under nextest's per-process model. `launch` remains gated by `synthetic-fixture`, so the unconditional `SyntheticRootFixture`/`tempfile` use in tests is consistent with existing files. `qualify-rtx.sh` correctly chains the predecessor runner and inherits `run`/`$out`. Evidence and process docs are consistent with the code. No actionable defect found.
