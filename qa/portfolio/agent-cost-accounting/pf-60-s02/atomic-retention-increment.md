# PF-60-S02 — C2 atomic retention candidate

Implemented and validated, uncommitted; integrator size disposition, independent review and receiving-tree integration remain pending. This is the complete private C2 fixture candidate, not completion of S02, permission to start S03, or human/live/release readiness.

Product initiative: **Product measurement / Measurement targets**, “No commercial performance numbers have been supplied.” Used `/Users/Neo/.codex/skills/corbanu-terminal-development/SKILL.md` to route through root/Rust AGENTS, the full active accounting plan/current sprint, plan/sprint processes and approved retention-design-handoff. No additional nested AGENTS applies to these paths. The manager's top correction in retention-coupled-next.md authorizes exactly six paths; source control, shared governance, independent review and integration remain parent-owned.

Worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`, branch `workstream/accounting-pf60-s01-20260911`. Clean launch and unchanged final HEAD `adb36c528153247e252cb90bb23050298f8ebbe0`; verified allocation ancestor `43e1e7864f5aec056425942e8a16e37fb91ea92a`. The earlier b0e7d95d launch returned with zero edits and consumed no review allowance. Current allocation packet SHA-256 `2e9cfb619c0b8e8618da1e14a310eeaa9b25e419f156d60bf79e9d6a225535c9`; approved design SHA-256 `19c5afb1b4c8400e9d2a43f977e16689a0cd43f4d97c05b9ed27066557026beb`.

| Exact path relative to worktree | Added/deleted | Complete-file SHA-256 |
| --- | --- | --- |
| `codex-rs/state/src/runtime/accounting_retention_atomic.rs` | +124/-1 | `145b10a21edf1781713e144ad42a68cf41cd640c0eb68ade43ad5b3974298cf0` |
| `codex-rs/state/src/runtime/accounting_lifecycle.rs` | +3/-4 | `573b0e6a4c9d199e177c9cd49847ecdb0648be2846242e197dc737dba1fa905e` |
| `codex-rs/state/src/runtime/accounting_retention_atomic_test_support.rs` | +49/-0 | `2460aea45e768b7e7288b7f747b921f886c7b02529c99489ccd1d0b0887a8c98` |
| `codex-rs/state/src/runtime/accounting_retention_atomic_tests.rs` | +1273/-0 | `9cfbaa9bd3fa85463e81a06c0cc6405e71de79547a6540189e600bc719295302` |
| `codex-rs/state/src/runtime/accounting_retention_consumers_tests.rs` | +0/-7 | `800cd9433d0e9e4a3c7bf60c9fc609f0f695e39e82b16021434e90d8ff02cc95` |
| `qa/portfolio/agent-cost-accounting/pf-60-s02/atomic-retention-increment.md` | +58/-0 | Final handoff supplies hash; no circular self-hash |

Rust total: +1449/-12 = 1461 changed lines; implementation 132, synthetic proof/support 1329 (including the seven removed C1 lines). Receipt adds58 non-test lines: final +1507/-12 = 1519 total,190 non-test. Total exceeds target800 by719; non-test remains below500 by310. This is a measured overage requiring the integrator's disposition, not inherited C1 exception authority. All mandatory proof is retained and normally formatted; no compressed tests, extra prerequisite/C3, partial transfer or extra source paths. The first formatted 1265-line Rust overage was disclosed while working; subsequent additions close explicit replay, older-estimate and day365-floor proof requirements. No acceptance/landing claim is made.

Maintenance acquires BEGIN IMMEDIATE, decodes fixture state and prepares through unchanged A/B on that same connection. It upserts exact compact values and unioned snapshot references, inserts unexpired anonymous tombstones, removes all five raw representations in FK-safe order, expires compact references/days, GCs only snapshots with neither raw nor compact owners, expires tombstones and atomically writes checkpoint+activation. Explicit rollback handles statement errors; failed commit propagates and SQLx rollback cleanup is awaited by connection reacquisition in the proof. No externally supplied plan or independently callable partial apply API exists.

Installed-schema deletion keeps the existing transaction-owning lifecycle wrapper and executes maintenance, target raw/compact removal and final shared-snapshot GC within that transaction. Legacy no-retention behavior is unchanged. Normal runtime registration remains cfg(test), and initialization still creates zero draft accounting objects. No schema, production migration, ABI, dependencies, manifest, lockfile, collector, scheduler, native ownership or runtime enablement changes.

The authorized seven-line C1 removal is only the obsolete installed-schema deletion-rejection call/assertion. The surrounding old-read error, complete no-write dump and all subsequent reader checks remain byte-for-byte. Its replacement maps to C2 `latest_original_price_null_binding_intent_and_known_zero` (raw+compact deletion after two reopens), `exact_boundaries_replay_shared_prices_and_two_reopens` (compact-only deletion after two reopens), `target_deletion_faults_follow_maintenance_and_preserve_all_owners`, and `target_failure_rolls_back_day365_expiry_and_replay_floor`. All other C1/A/B expectations and source files are frozen; git diff verifies A/B, journal, shared A/B support and runtime registration unchanged.

| C2 executable proof | Observations |
| --- | --- |
| `fourteen_reachable_sql_faults_rollback_reopen_retry_both_routes` | All14 SQL events, each through maintenance and deletion (28 cases), exact `retention_fault_<site>` marker, ten-table rollback, two reopen comparisons, trigger-only removal, literal expected successful state, equal-time repeat and two successful reopens. Separate existing compact UPDATE, expired ref/day/tombstone and unused-snapshot GC seeds make every event reachable. First activation stays NULL/inactive on failure, retained read remains NeedsActivation, staged append succeeds only within a rolled-back setup transaction. Active advancement failures retain the prior checkpoint. |
| `target_deletion_faults_follow_maintenance_and_preserve_all_owners` | Nine target-specific sites: tombstone INSERT, five raw DELETEs, compact ref/day DELETEs and final snapshot GC. Predicates require target ID/key and the new checkpoint, proving failure after maintenance. Staging activation and active advancement roll back; all ten tables survive two reopens. Retry removes raw+compact target, preserves other owner's shared reference, repeats idempotently, reopens twice and finally GCs the last owner's snapshot. |
| `target_failure_rolls_back_day365_expiry_and_replay_floor` | A target tombstone fault after day365 maintenance rolls back day0 expiry, UUID expiry, checkpoint and aggregate floor0→1 along with deletion. Two reopens retain old coverage; trigger removal and retry commit complete expected rows. Expired original replay remains rejected after its tombstone disappears. |
| `deferred_foreign_key_commit_failure_rolls_back_and_retries` | Both wrappers use the actual dedicated one-connection FK-on pool with deferred checking. AFTER-checkpoint trigger inserts an orphan into the existing compact-reference table only after preparation/application. Commit returns SQLite787/FOREIGN KEY constraint failed; reacquisition awaits rollback, all ten tables match, two reopens preserve baseline, trigger-only removal/pragma reset permits exact retry/repeat and two successful reopens. |
| `four_real_writer_orders_and_reader_snapshot_transfer` | Real held append→maintenance, maintenance→append, deletion→maintenance and maintenance→deletion. Channels signal only after BEGIN IMMEDIATE and the real connection operation; peer busy-timeout0 wrapper must return SQLite5/6 while holder is uncommitted. Release occurs only afterward. Retry and complete tables prove exact 1e-24 versus revised2e-24 serialization outcomes. A SELECT-held old WAL read snapshot remains complete before/after another connection's transfer, returns NeedsMaintenance for the newer time, while a fresh reader sees complete mixed totals with the same2e-24 amount. |
| `exact_boundaries_replay_shared_prices_and_two_reopens` | Literal full tables/totals at90d−1,90d,90d+1ms,91d,365d,365d+1ms, including dispatch0/1ms, repeated sweeps, day1 and shared snapshots. Day0 expires conservatively; UUID0 and UUID1 tombstones expire exactly1ms apart. Compact-only deletion follows two reopens; final owner removal GCs price. |
| `latest_original_price_null_binding_intent_and_known_zero` | Stale50→60/read10 keeps original3/0.3 rates and0.000183 USD. Mixed raw patch explicitly sets input1/read0/write0/output0: known[1,61,10,0,0,0,1], unknown[1,0,0,1,1,2,1], unknown estimates2/attempts2. Null binding and unbound intent stay unknown; fully known zero remains distinct. Complete rows and reads survive two reopens; target deletion preserves another owner's raw snapshot binding. |
| `transferred_replay_suppression_survives_two_reopens_without_setup` | Two tiny members retain2e-24 after transfer and each actual close/reopen, with full table/read equality. Original revisions, new aged UUID, changed-dispatch tombstoned UUID and erased retry predecessor remain rejected without changes; repeated maintenance adds nothing. |
| `expired_first_sweep_and_invalid_times_never_resurrect_detail` | First activation at365d and strictly later365d+1ms removes expired raw-only days without creating aggregates. Negative/backward/future/checked-overflow maintenance and deletion fail unchanged; checked-overflow read also proves no logical or total_changes mutation. |
| `corrupt_inputs_fail_maintenance_read_and_deletion_then_restore_and_retry` | Older estimate corruption with a valid newer quote, foreign contribution attribution, malformed compact payload/ref/snapshot and active NULL fail through all three real paths with full dumps unchanged. Only the deliberately changed row is restored; exact maintenance/deletion retry succeeds. |
| `checked_compact_overflow_propagates_through_all_coupled_paths` | Known-count, unknown-count and exact decimal alignment overflow reach read/maintenance/deletion. Deliberately corrupted payload/checkpoint are restored exactly, then complete expected transfer/read/deletion succeeds. |

Every read_checked success/error checks all ten ordered tables plus total_changes(); mutation rollback checks logical rows, never misuses total_changes() as physical-zero proof. Expected compact amounts/populations/owners/expiry keys are literal case inputs; retained_rows only preserves explicitly untouched baseline raw rows and serializes those expected values, never calls the mutator or A/B to generate expectations.

Validation completed September12,2026 at12:16UTC on the final formatted Rust bytes. All Rust build/test commands ran from the assigned `codex-rs` using `env RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true UV_OFFLINE=true CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/target`. No installs, network fetches, target replacement, environment repair or direct cargo test.

| Command | Exit and actual result |
| --- | --- |
| `sandbox-exec -p PROFILE just fix -p codex-state` | 101; recorded TCP lock-listener bind restriction, Operation not permitted. PROFILE is `(version 1)(allow default)(deny network*)(deny file-write* (subpath W))(allow file-write* (literal EACH_OF_THE_FIVE_RUST_PATHS_ABOVE) (subpath W/codex-rs/target))`, with all paths expanded as quoted absolute strings. No successful fix/Clippy claim. |
| `env RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 rustfmt --edition 2024 --config skip_children=true` followed by the five absolute Rust paths in table order | 0 before final tests; stable imports_granularity warning. Manager-recorded literal-file formatting scope used; no broad format. |
| `just test -p codex-state retention_plan::reduction::atomic::tests` | Final0:11passed/255filtered/0leaky,3.054s, run23c3062c-41d5-4b78-b35b-2615f6f454b6. |
| `just test -p codex-state runtime::accounting` | Final0:76passed/190filtered/1leaky,6.183s, run0fb3a2a3-8a8e-4617-bf9f-8381d206262a. Includes all3A,5B,5C1 plus journal/pricing/storage/lifecycle/codec. LEAK: latest_tests::current_stale_reordered_and_caller_rollback_survive_two_reopens. |
| `just test -p codex-state` | 0:266passed/0skipped/2leaky,13.386s, run7f520b4e-d68f-491d-b09c-e0b7f1646642. LEAK: log_db::tests::event_queue_drops_new_entries_when_full and extract::tests::turn_context_sets_permission_profile_metadata. |
| `cargo check --offline --locked -p codex-state --lib` | 0; normal dev profile,0.74s. |
| `git diff --check`; frozen-path `git diff --exit-code HEAD -- ...`; `git diff --cached --exit-code`; allocation `git merge-base --is-ancestor ... HEAD` | All0. No staged changes or writes outside six paths. Parent owns shared governance checker runs. |

Initial C2 run26caee04-a936-4ced-93ca-db4efd2034f7 exited100:8passed/1failed/255filtered,60.400s including configured retry. The failure was test-only pool starvation: the generic rejected helper held the sole peer connection while invoking its wrapper. Corrected the new test to take/release the baseline connection before wrapper retry; no journal/support behavior was relaxed. Intermediate strengthened10-test selectors passed; intermediate75-test accounting selectors passed with1leaky. Final11-test/all-accounting/full-state evidence above supersedes earlier bytes. No source edits followed final formatting/tests. Test build reports11 dead-code warnings (3duplicates) from reused fixture-support inclusions; no leak-clean or warning-clean claim. Final JUnit at assigned target/nextest/local/junit.xml records266tests,0failures,0errors,0skipped and the matching full-suite UUID.

Literal uncommitted Rust diff SHA-256: `af6d74af943271c965aecfee2cb6eca0e5dc6a66b1b65bae378bcc83fbea9b89`. Reproduce from W by concatenating `git diff --binary --full-index --no-ext-diff --no-color HEAD --` the four tracked Rust paths (Git's lexical output order), then `git diff --binary --full-index --no-ext-diff --no-color --no-index /dev/null codex-rs/state/src/runtime/accounting_retention_atomic_tests.rs`. To obtain the complete six-path literal diff, append the same no-index command for this receipt. No-index exits1 when differences exist; that is expected. Final handoff supplies receipt and full-diff hashes without writing an unallocated patch file.

Integrator next: disposition the measured size exception, perform the already-allocated independent C2 review/correction as needed, and run receiving combined-tree/governance checks before integration. No worker reviews, children, delegation, commits, pushes, shared ledgers or unrelated live actions; prior A/B/C1 review history and remaining allowances are untouched. Code-blind/TUI/live-repository proof is N/A for this private fixture only. Native ownership/admission wiring,90..365-day compact-only late imports, human/runtime acceptance, crash/power-loss/uncertain-commit durability beyond this FK case, physical database/WAL erasure, complete S02 goldens, benchmarks, shipped docs and release qualification remain unqualified. Production stays OFF; S02 remains in_progress and S03 remains draft.
