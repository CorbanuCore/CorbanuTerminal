# PF-60-S02 — pure retention reduction B

Status: implemented and tested, uncommitted; awaiting manager review/acceptance and receiving-tree validation. S02 remains in_progress; S03 remains draft. This receipt does not complete a sprint or accept runtime behavior.
Product initiative: **Product measurement**, “No commercial performance numbers have been supplied.” Used corbanu-terminal-development to verify root/Rust AGENTS, active accounting plan, current S02, process indexes and the complete B packet before coding; also inspected the approved retention handoff and native seams. The user's exact allocation keeps shared plan/sprint records read-only and replaces broad formatting with scoped formatting.
Checkout: `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`; branch: `workstream/accounting-pf60-s01-20260911`. Verified clean launch and unchanged final HEAD `466474fe1704d9c36a1c3f50d3b7e9f8025caa6a`; declared base `fd46c5897c3c3a1c146a97f544e5bacf4e996f9d` is its ancestor. B packet SHA-256: `727f20ab64453b972b6ee4b01d89ed9e4ac1d0d78b7e9822a8a452459127d98c`.
Accepted reader A remains frozen except three registration lines. Its prior clean review and combined 325 state/TaskNode result are inherited provenance, not a new combined-tree run. No duplicate review or child agent was used. Manager retains review/integration ownership and the existing allowance ledger.

| Changed path (repository relative) | Added/deleted | Complete-file SHA-256 |
| --- | --- | --- |
| `codex-rs/state/src/runtime/accounting_retention_plan.rs` | +3/-0 | `beb71387c3aeca6916ae6214c6db57d320fffdb9b8dc0fcd4d6c7040c78f527a` |
| `codex-rs/state/src/runtime/accounting_retention_reduction.rs` | +91/-0 | `c5922e36f6db565fb10ac214c5cc847d4d852046a90944d1381c0f691c6d0d7b` |
| `codex-rs/state/src/runtime/accounting_retention_reduction_tests.rs` | +376/-0 | `5e51bab16b44864429e1ffd2e8ec5beb37dfc48bc479b2433c04a49f4bf02ca0` |
| `qa/portfolio/agent-cost-accounting/pf-60-s02/retention-reduction-increment.md` | Count/hash in final handoff to avoid self-reference | This receipt |

Rust diff: +470/-0 (94 source/registration, 376 tests). Initial count was +416 before the additional raw-day cases; formatted count stayed below the target with receipt allowance. The fixture support file is reused unchanged; only four of the five permitted paths changed.
Rust-only diff SHA-256: `fa534194cd72c36359de639342fc75faf49e0c3d68f1e9646d689f92ea9b3a15`. Reproduce from checkout root by concatenating `git diff --binary --full-index --no-ext-diff --no-color HEAD -- <registration-path>`, then `git diff --binary --full-index --no-ext-diff --no-color --no-index /dev/null <new-path>` for reduction and tests in table order, and hashing the bytes with SHA-256. Final handoff includes the full diff with this receipt appended using the same new-file command.

Private `prepare_on_connection` calls `read_validated_input_on_connection` within the caller-owned transaction, then a synchronous checked `reduce`. Returned ephemeral values contain previous/computed as-of, surviving compact values and snapshot-reference unions, expired existing compact keys, raw UUID/replay-expiry removals, and untransferred raw day totals/freshness. Neither function starts/commits/rolls back a transaction, mutates storage or applies/serializes a plan. Existing cfg(test) ancestry remains intact.
Equality expires at dispatch+90 days for detail and UTC-day-start+365 days for aggregates; returned replay expiry remains dispatch+365 days. Expired raw-only days are never recreated, and expired_days names only existing compact keys. Latest quotes preserve original immutable binding and absent-price unknowns without catalog repricing. Exact seven-metric populations and 24-place USD use the accepted checked arithmetic. Any missing/stale unexpired member yields NeedsRefresh for its whole day; raw sums are checked before freshness masks them. Validated tombstones do not authorize pruning or replay-floor advancement.
Preserved draft source: `/Volumes/CorbanuDrive/Corbanu/.codex-work/manager-accounting-retention-reslice-20260912.32IaBj/`; read PRESERVATION.md and verified its implementation/test/support SHA-256 values (`5916d202…`, `7929b0f4…`, `9a93bda3…`). Reused all three original B scenario tests and all three deferred compact merge-overflow cases; the original A corruption/time-key proof remains in unchanged A tests and passed in the full suite. No original proof was dropped or counted as candidate evidence before execution.
Five B tests cover mixed days/two owners/shared snapshot unions and repeated preparation; exact90/exact365, dispatch0 and one-millisecond conservative day loss; null/unbound/intent/bound-without-estimate/stale/current cases with an eligible alternative catalog; known/unknown/decimal merge overflow; and additional multi-member raw totals, missing/stale first/last members and masked raw overflow. Seven fixture variants each pass two on-disk close/reopens without reinstalling fixture tables. Every successful/failing B call compares complete literal plans or expected errors, all ten ordered table dumps and same-connection total_changes(). This is no-write/reopen proof, not retention application or process-kill proof.

Validation completed September 12, 2026, 09:48 UTC. Commands ran from this checkout's `codex-rs`, with cached rustc `1.95.0 (59807616e 2026-04-14)`, `RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true UV_OFFLINE=true CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/target`. sandbox-exec denied network and checkout writes except the four allocated Rust paths/assigned target. No dependency, tool or environment repair was attempted.

| Command/check | Exit and actual result |
| --- | --- |
| `just fix -p codex-state` | 101, one attempt: Cargo fix TCP lock-listener bind denied, Operation not permitted. Known limitation preserved; no successful Clippy/fix claim. |
| `rustfmt --edition 2024 --config skip_children=true state/src/runtime/accounting_retention_plan.rs state/src/runtime/accounting_retention_reduction.rs state/src/runtime/accounting_retention_reduction_tests.rs` | 0 before final tests; expected stable-channel imports_granularity warning. No broad format. |
| `just test -p codex-state retention_plan::reduction::tests` | 0; 5 passed, 245 skipped, 0.465s; run `84e0f36e-444a-47e5-92f8-af5de73f32f1`. |
| `just test -p codex-state` | 0; 250 passed, 0 skipped, 9.034s; run `451e00ac-4f3f-4fd7-8fa2-910d05629d9d`; includes unchanged A tests. |
| `cargo check --offline --locked -p codex-state --lib` | 0; normal dev profile, 0.79s. |
| Root `python3 docs/plans/check.py` / `python3 docs/sprints/check.py` | Both 0 before implementation and after Rust validation; active 3/3, current 115, archived 126. |
| Root `git diff --check`; new-file `git diff --no-index --check /dev/null <path>` | Tracked check 0; new-file checks 1 for added-file differences with no whitespace diagnostics. |

Test compilation reports two dead-code warnings for unchanged A-only `unchanged`/`reopens` helpers when the same fixture support file is included by B; B uses its own plan-typed assertion/reopen helpers. No Rust test or library-check failure occurred. Tool outputs/run IDs are the execution record; no separate logs were written.
Next manager action: review and accept this bounded B candidate, inspect literal scope/digests, then rerun affected combined-tree checks and allocate the coupled mutation/read/delete/admission increment. Atomic transfer, compact-only deletion, shared-snapshot GC, admission fencing/replay-floor advancement, late imports, rollback/contention and actual crash/physical erasure proof remain later work. No application, runtime/API registration, production migration, manifest/lock change, collection, credentials, network operation, commit, push or external action was introduced.
Code-blind/TUI/live-repository proof is N/A for this private reduction only. Native human acceptance, runtime qualification, shipped-feature docs, benchmarks and release evidence remain pending; there is no sprint-completion or release claim.
