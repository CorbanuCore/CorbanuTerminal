# PF-60-S02 exact quotation — frozen uncommitted increment

Product initiative PF-60, sprint PF-60-S02 still in_progress. Product citation: **Product measurement** — “No commercial performance numbers have been supplied.” The corbanu-terminal-development skill routed this work through root/Rust policy, the active accounting plan, current S02 and the completely read exact-pricing allocation. Approved defaults remain final; shared ledgers and independent review/integration belong to the parent.
Worktree: `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`; branch: `workstream/accounting-pf60-s01-20260911`. Clean launch and unchanged HEAD: `9ae04495c20570308bad4bcc0bf5db9c2c46f989`; allocation ancestor `c33d47f6ccd00a64fcb05a057472d8ca9f0139d4` verified. Only four allocated files changed, all unstaged/uncommitted. No commits, pushes, agents, reviewers, other-model calls, credentials, live pricing or billing.
Rust size: 283 helper lines + 2 registration lines = 285 non-test-file lines (conservative, includes test registration), 479 test lines, 764 Rust lines. Receipt is within the remaining 36-line allowance; total <=800. accounting.rs changes exactly two child-registration lines.

| Frozen Rust file (absolute path) | SHA-256 |
| --- | --- |
| `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/state/src/runtime/accounting.rs` | `5c8c24f1e8c6947038f2f046ddb6d89d9b8e4437d594e39e0a9f3d84ba24e0ac` |
| `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/state/src/runtime/accounting_pricing.rs` | `209d7854fb61d5eeff8482aae94ed22a5310bda65fd176688064b91ad3b87755` |
| `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/state/src/runtime/accounting_pricing_tests.rs` | `62a8178874633aaa4510d73ae9caab30bb9516c014122c82c598811854d06329` |

Fourth path is this receipt: `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/qa/portfolio/agent-cost-accounting/pf-60-s02/exact-pricing-increment.md`. Its SHA-256 is supplied in the delivery to avoid self-reference. Reproduce file digests with `shasum -a 256`.
Validation: 2026-09-12 UTC, final Rust/governance results captured by 00:51:46Z (September 11 Phoenix). Cached rustc 1.95.0 (59807616e), aarch64-apple-darwin, nextest 0.9.143. Rust commands from codex-rs used `RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true`; formatting also used `UV_OFFLINE=true`. macOS sandbox denied network and worktree writes except the three allocated Rust files during formatting, or codex-rs/target during build/tests. No toolchain/dependency/environment repairs or lock/process cleanup. Synthetic on-disk test home uses existing unique_temp_dir plus scopeguard cleanup; no real account home is opened.

| Command / attempt | Actual result |
| --- | --- |
| Guarded `just fmt`, initial and after correction | Both exit 1: Rust, Just and Bazel succeeded; Python SDK/scripts failed with existing uv `exclude-newer = "7 days"` warnings and scope-denied .venv creation. No out-of-scope rewrites; final formatting preceded all passing Rust checks. |
| First focused launch wrapper | Exit 65, malformed sandbox profile parentheses; no tests started. Corrected only the command. |
| First focused build | Exit 101, E0433: tempfile is not a codex-state dependency; 0 tests ran. Replaced test setup with existing helpers without dependency changes. |
| `just test -p codex-state runtime::accounting::pricing::tests` | Exit 0; 12 passed, 0 failed, 202 skipped; 0.071s; run dae01b04-ef5d-4d52-bac2-e28958221b33. |
| `just test -p codex-state runtime::accounting::tests` | Exit 0; 12 passed, 0 failed, 202 skipped; 0.139s; run e24afba4-2cfb-4f6f-a21e-ae1edf283d19. |
| `just test -p codex-state` | Exit 0; 214 passed, 0 failed, 0 skipped; 6.218s; run 0b2f6c3b-1620-4dbb-ab8c-90c0949e7cf6. Existing extract::tests::turn_context_sets_model_and_reasoning_effort marked LEAK (one); no accounting leak. |
| `cargo check --offline --locked -p codex-state --lib` | Exit 0; normal library compiled, 0.66s. No direct cargo test command invoked; just/nextest owns its build subprocess. |
| Root `python3 docs/plans/check.py`; `python3 docs/sprints/check.py`; `git diff --check` | Exit 0 each; 3/3 active plans, 115 current / 122 archived sprints; no whitespace errors. |

Contract: pure Attempt validation plus accepted replay; <=64 snapshots; exact provider/model/scope selection at dispatch with half-open prospective intervals, duplicate IDs/overlap rejection, typed USD/per-million/source-kind/UUID fields, unknown-field rejection and 128-byte nonblank/control-free metadata. Results copy the Attempt, complete ordered observations (including revisions/source positions), replayed Usage and selected typed snapshot. Missing usage remains unknown even at a zero rate; measured zero needs no rate. Four disjoint buckets yield a known subtotal and optional all-buckets-priced estimate, never a completion/billed-spend claim.
Decimal limits: ASCII nonnegative strings, <=38 total coefficient digits (leading zeros count), <=18 fractional places, <=39 bytes including decimal point; no signs, exponent, empty fraction, numeric/bool JSON. Canonical trailing-zero removal is exact. Checked u128 products/alignment/sums; quoted scale <=24, coefficient <=u128::MAX. Out-of-range arithmetic errors explicitly, even if a wider representation could fit the rational result. Six-place half-even display preserves exact amount; rounded means discarded nonzero remainder, nonzero_sub_micro means exact amount strictly between zero and one micro-USD. Remainder comparison cannot overflow.
Literal evidence: Anthropic input50/read10 at 3/0.3 yields 0.000153 with inclusive/full quote unknown; inclusive cumulative replacements and reasoning subsets; absent/null/zero and missing rates; exact identities, boundaries, prospective ordering, malformed catalog/decimal rejection and all three arithmetic overflow paths. Half-even ties 0.0000005 -> 0.000000 and 0.0000015 -> 0.000002; two 0.0000004 components sum exactly to 0.0000008 before display 0.000001. Real journal close/reopen quotes twice against complete expected objects while preserving observations/positions. Later catalog data leaves old-dispatch selection and already returned copies unchanged.
OFF proof: runtime.rs retains its existing `#[cfg(test)] mod accounting;`; this increment adds only a private child. Normal-library check passes, existing normal-init/no-draft-tables regression passes, and no production API, constructor, caller, migration, journal writer/type, dependency, toolchain, collection, filesystem/clock input in the helper, or live fetch changes.
Parent owns exact-diff/hash verification, independent review and combined-tree integration. This helper does not enforce durable snapshot immutability or persist estimates. Next allocation remains immutable estimate/snapshot storage and atomic per-thread/day contributions/deletion, before separate production promotion and dispatch/presence wiring. Full S02 golden totals, process-crash recovery, retention/tombstones, lineage and S03 remain open. No interactive surface changed; TUI/live-repository evidence, named runtime human acceptance, benchmarks and release qualification are not claimed. Technical review needs only these hashes, cached tools and synthetic disposable homes; the parent's earlier combined test counts are inherited context, not rerun claims.
