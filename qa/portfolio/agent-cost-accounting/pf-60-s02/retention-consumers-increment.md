# PF-60-S02 — C1 retention consumers candidate

## Manager disposition after return

The original candidate record below is preserved as history. Integrator explicitly
authorized reviewing its1121 lines (345implementation,725proof,51receipt) as a
one-candidate exception to the manager's800-line allocation, not compliance.
Splitting admission/read again would fragment their shared connection/reopen proof;
C1 is already the consumer-only stage before C2. No tests or scope were removed.
Aristotle's one allocated Astra High structured helper exited0/findings[]; parent
read the literal implementation, tests and reviewJSON. Review-only size disposition
and original outputs remain in private manager-continuation.9Id1V1.
This append-only receipt initially added16 lines:1137total/412non-test including handoff.
Code is accepted for receiving-tree integration; combined-tree validation and
C2 allocation are still next, not implied by that code-review result. No new review
of unchanged C1 or approval from Travis is needed. C2 remains excluded here.

Combined proof: after merging receiving a37eb0b6a into worker8cdb6dcf, pinned
offline `just test -p codex-state -p codex-tasknode-session` passed335/0skipped
in9.285s, run df4aea21-b6f1-4213-9852-62f0869467bc. Nextest reports2 leaky
tests; this is not leak-clean. Four fixture-helper warnings (one duplicate) remain.
Parent source publication stays frozen separately; receiving integration follows
the active Luna postflight. This six-line evidence addition plus count correction
is documentation only; the tested Rust tree is unchanged.


Status: implemented, formatted and tested; uncommitted and **not size-gate compliant**. Parent acceptance/integration is blocked by the measured 800-line overage below. No C2 transfer, activation writer, apply/maintain stub or production registration exists. No automatic third prerequisite is proposed.

Product initiative: **Product measurement / Measurement targets**, “No commercial performance numbers have been supplied.” Used corbanu-terminal-development to read root/Rust AGENTS, plan/sprint indexes, full active accounting plan/current S02, accepted C1 packet and approved retention-design-handoff before edits. The user's ten-path allocation supersedes shared-record updates and broad formatting; parent owns governance, independent review and integration.

Worktree: `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`; branch `workstream/accounting-pf60-s01-20260911`. Clean launch and unchanged final HEAD: `4642785db64f0b2424b4b88dbfe9980e779f9ada`. Verified allocation base `fcd6aad8b9d5f5a571f22a3238f4eb47833fa968` is an ancestor. Accepted packet SHA-256: `32bfd2c9e207a96278dde2ee53d0ac8f96734484ae74bbd167665e319fe9e653`.

| Exact changed path relative to worktree | Added/deleted | Complete-file SHA-256 |
| --- | --- | --- |
| `codex-rs/state/src/runtime/accounting.rs` | +166/-46 | `2324bd267a9f25267ad6c9e4f1f1265964411a037af3d7cf56c6f9b6fde45a68` |
| `codex-rs/state/src/runtime/accounting_lifecycle.rs` | +13/-0 | `3c71aa30a1621f4bfc8fd7a1f8f09da7d59a75a9a6b62b767f2cd99a0035eddb` |
| `codex-rs/state/src/runtime/accounting_retention_plan.rs` | +1/-1 | `3feb653494915231d32f69f8a5825093b3a24c99cb701db4c6738dce1703c929` |
| `codex-rs/state/src/runtime/accounting_retention_reduction.rs` | +3/-0 | `9398f06b824ae9312adf18d169fc0d9cf9cf8fb963c51edd78baa03f7e6cc073` |
| `codex-rs/state/src/runtime/accounting_retention_atomic.rs` | +115/-0 | `98e7185e671bf49b80a520639aeca5996295bc4442f85e60e0c938b30aa3ec0e` |
| `codex-rs/state/src/runtime/accounting_retention_test_support.rs` | +5/-3 | `25d535703582dc62964e22ccdca1dcca4ba417646273a556441d39a0c9d0672f` |
| `codex-rs/state/src/runtime/accounting_retention_plan_tests.rs` | +1/-1 | `dc6158739d1d695cef77324c4a888367e83ad07fdc62f109fe1255e5be5f5b85` |
| `codex-rs/state/src/runtime/accounting_retention_atomic_test_support.rs` | +192/-0 | `c831fe4713c54c19aaf424e1327ae12d4632a497d9fb400b76f34591408cccd8` |
| `codex-rs/state/src/runtime/accounting_retention_consumers_tests.rs` | +523/-0 | `59e5f3383f17aa2a64ef82bafe6d3505b9fc0430aa7aacf941a7478b9f17fb38` |
| `qa/portfolio/agent-cost-accounting/pf-60-s02/retention-consumers-increment.md` | +51/-0 | Final handoff hash; no circular self-hash |

Rust: +1019/-51 = 1070 changed lines, including moved journal body lines counted twice. Runtime implementation/registration is 345 non-test lines despite its cfg(test) ancestor; synthetic tests/support are 725. Receipt adds 51 non-test lines: final +1070/-51 = 1121 total, 396 non-test. **Total exceeds hard800 by 321; non-test remains below500 by 104.** No existing A/B proof was deleted, relabeled or compressed to claim compliance. Early count was 279 before new proof; first formatted overage was1039 before receipt and was disclosed while working. Corrections retained the specified proof; this candidate must not be represented as an accepted bounded increment.

Rust diff SHA-256: `0630aa6b4dbf8a6dec16f9238681ef6d2e4f01ebc554ce81616b825ed042c338`. Reproduce by concatenating per-path diffs in table order: tracked paths use `git diff --binary --full-index --no-ext-diff --no-color HEAD -- PATH`; new paths use `git diff --binary --full-index --no-ext-diff --no-color --no-index /dev/null PATH`. SHA-256 the concatenation. Final handoff includes full diff with this receipt appended.

Strict connection metadata checks exact table names, column types/nullability/key positions, singleton population/key, integer/nonnegative checkpoint and activation bit. No-table legacy remains available; every partial subset and malformed schema fails closed. Staging NULL and staging Some(0) permit synthetic seeding but retained reads require activation; active NULL fails after two reopens. No installation/repair occurs on attach.

Journal's wrapper still owns BEGIN IMMEDIATE and rollback; extracted connection body repeats attempt/batch validation and preserves identity, revision, source-position, replay and retry checks. Active admission uses the durable checkpoint, checked replay365 first, explicit unsupported90..365 imports, future-dispatch rejection and existing UUID tombstones. Failed wrapper calls compare all ten logical tables. Boundary proof covers dispatch0/checkpoint0, 90d−1ms, equal90/100/364, equal365/older without tombstones, future dispatch, changed identity/revision, erased retry ancestry, tombstoned UUID with changed dispatch and overflow.

The read wrapper owns one snapshot; connection reading validates through accepted A/B and combines exact compact/raw values. Tests assert seven populations and1e−24 USD, unknown versus fully-known-zero, empty-day full-USD-None, missing/stale NeedsRefresh, owner-specific oldest day, inclusive90 cutoff, conservative365 floor, expired day, later time and pending raw/day/tombstone cleanup, negative/backward/future/overflow errors and representative authority/ref/payload/old-estimate corruption. Successful/error connection reads compare complete ordered ten-table dumps and total_changes(). Old raw-only read and interim deletion explicitly reject retention schemas. Synthetic active setup is not evidence of a sweep.

Five exercised consumer tests include attach/reopen, literal active metadata, reusable SQL-ABORT fixture, controlled peer connection and no-write assertion helpers. Contention obtains the actual main DB filename from pragma_database_list, uses a dedicated single-connection FK-on/busy-timeout0 pool, and waits for the holder's real append under BEGIN IMMEDIATE before attempting the real wrapper. SQLite BUSY/LOCKED must occur before releasing the holder; retry succeeds. Peer closes separately. Observation-INSERT fault requires its exact marker, full-table rollback, two reopens, trigger-only removal, successful retry and idempotent repeat. This proves append/append and the observation fault only; all coupled mutation/deletion/snapshot/commit-failure sites remain C2.

A's three tests retain every assertion, input and two-reopen loop; its sole edit is VALUES(2,0)→VALUES(2,0,0). B's five tests are byte-identical, SHA-256 `5e51bab16b44864429e1ffd2e8ec5beb37dfc48bc479b2433c04a49f4bf02ca0`. Shared setup adds only explicit staging bit/seed/comment and includes it in the existing ten-table dump. None/Some(0) meanings remain unchanged. Parent's accepted330 combined pass is inherited evidence, not repeated B review or a new combined-tree claim.

Validation on September12,2026, completed10:18UTC: cached Rust1.95.0, `RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true UV_OFFLINE=true CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/target`. Commands ran from assigned codex-rs. sandbox-exec denied network and checkout writes except nine literal Rust paths/assigned target. No installs, environment repair, external fetches or extra logs were written.

| Command / proof | Actual result |
| --- | --- |
| `just fix -p codex-state` | Exit101; known Cargo fix TCP lock-listener bind denied, Operation not permitted. No successful fix/Clippy claim. |
| `rustfmt --edition 2024 --config skip_children=true` plus nine literal Rust paths | Exit0 before final tests. Stable imports_granularity warning; no broad format. |
| `just test -p codex-state retention_plan::reduction::atomic::consumer_tests` | Final exit0; 5passed/250skipped,0.164s; run `b7e6152d-32cf-4432-aeb8-8dd7ad4fc46f`. Earlier compile failed on macro ambiguity and SQLx dynamic SQL typing, then corrected; first successful run cb45e5a8 preceded schema-proof corrections. |
| `just test -p codex-state` | Exit0; 255passed/0skipped,9.428s; run `53cfb210-09af-438d-8be3-95780c922078`. Includes all3A/all5B and journal/lifecycle/pricing/codec regressions. |
| `cargo check --offline --locked -p codex-state --lib` | Exit0; normal dev profile,1.45s. |
| Diff/scope inspection and normal init | No staged changes; only ten allocated paths. New consumer test sees zero draft accounting objects before explicit install; runtime.rs cfg(test) registration unchanged. |

Four test-build dead-code warnings (one duplicate) arise from A-only unchanged/reopens helpers re-included by sibling tests. Inspected JUnit report has255tests,0failures,0errors,0skips and no LEAK marker; terminal output was truncated in the middle, so no stronger output-completeness claim. Full report is assigned target/nextest/local/junit.xml. Whitespace/new-file checks and final hashes are recorded in the final handoff. Shared governance checkers belong to the parent and were not run here.

Parent owns the size disposition, one added C1 code pass, acceptance and combined-tree integration. Prior A/B review history and unused correction allowance remain untouched; no review, child, staging, commit, push, shared plan/state/CI/manifest/lock or other-worktree edits occurred. C2 remains the fixed immediate successor after accepted C1; its766-line estimate is unvalidated and no mutation capacity/headroom is claimed from this overage. S02 stays in_progress; S03 stays draft.

Code-blind/TUI/live-repository proof is N/A for these private fixture consumers. Native ownership, human/runtime acceptance, complete90..365-day late imports, actual coupled retention/deletion, crash/power-loss durability, physical erasure, benchmarks, shipped docs and release qualification remain unqualified. Nothing enables collection or accesses credentials/live records.
