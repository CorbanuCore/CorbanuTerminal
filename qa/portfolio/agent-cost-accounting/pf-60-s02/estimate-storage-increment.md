# PF-60-S02 — immutable estimate storage, uncommitted receipt

Product initiative PF-60; PF-60-S02 remains in_progress. Citation: **Product measurement**, “No commercial performance numbers have been supplied.” The corbanu-terminal-development skill routed policy/allocation verification; approved defaults remain final. Parent owns shared ledgers, exactly one independent review and integration.
Checkout `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`, branch `workstream/accounting-pf60-s01-20260911`; clean launch and unchanged HEAD `27d98d227b8970643c0d8422ed8e30c8e1d0cec2`. Accepted ancestor `486d2fb9481c01f7d73a6f8d9992c6e1b96359dd` verified with `git merge-base --is-ancestor`. All five allocated paths are unstaged/uncommitted; no unrelated starting changes.
Rust changed-line count (additions plus deletions): 758 = 236 implementation + 500 tests + 22 shared seams. Conservative non-test Rust count 258. This 34-line receipt gives 792 total changed lines and 292 including receipt as non-test; below hard 500/800. Test target exceeded the allocation's soft 380-line target to cover all required cases; hard budget intact.

| Exact file under checkout above | File lines; added/deleted | SHA-256 |
| --- | --- | --- |
| `codex-rs/state/src/runtime/accounting_pricing.rs` | 287; +12/-8 | `d9d8a2510e9dd506cd00bb6b1e24ce7d09f6f8dc9ea55a3a39f07e36c6afc06b` |
| `codex-rs/state/src/runtime/accounting_types.rs` | 199; +1/-1 | `3184ab29aa5f1d653e42275746df14bccc306dbec7a0c387508a603a2d966307` |
| `codex-rs/state/src/runtime/accounting_estimates.rs` | 236; +236/-0 | `0bc34c2a074f936c5015c560361bfae26871acd44e55aa2c96f5ce7d71e01790` |
| `codex-rs/state/src/runtime/accounting_estimates_tests.rs` | 500; +500/-0 | `bee1f270a2eb57fcfc3e77f039c3c249a7bc45cf24fb90bd283e5baef3c099f5` |

Fifth path: `qa/portfolio/agent-cost-accounting/pf-60-s02/estimate-storage-increment.md`; its hash is delivered separately to avoid self-reference. Reproduce with `shasum -a 256`, `wc -l` and `git diff --numstat` plus untracked-file counts.
Validation on September 12 UTC / September 11 Phoenix, completed Rust checks by 01:27:39Z. Cached rustc 1.95.0 (59807616e), aarch64-apple-darwin; nextest 0.9.143. Every Rust command used `RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true`; tests/check/fix explicitly used this checkout's `codex-rs/target` through CARGO_TARGET_DIR, formatting also UV_OFFLINE=true. Commands below run from codex-rs unless marked root.
Safety wrapper: macOS `sandbox-exec -p` profile `(version 1)(allow default)(deny network*)`, plus deny file-write* to this checkout subpath and allow file-write* only to the four literal Rust paths above; builds additionally allow this checkout's codex-rs/target subpath. First fmt omitted target allowance; second fmt used the build wrapper. Disposable synthetic homes use native unique_temp_dir and scopeguard cleanup. No real account home, credentials, network service, toolchain/dependency repair or lock/process cleanup.

| Command / attempt | Actual result |
| --- | --- |
| Guarded `just fmt`, twice, last before final affected tests | Both exit 1: Rust/Just/Bazel succeed; Python SDK/scripts fail. uv cannot parse `exclude-newer = "7 days"` in their pyproject.toml, then scope-denied `.venv` creation. No out-of-scope rewrite. |
| Guarded `just fix -p codex-state` | Exit 101 before compilation: cargo fix cannot bind TCP listener to manage locking (Operation not permitted). No fix applied; Clippy/fix not qualified. |
| First `just test -p codex-state runtime::accounting::pricing::storage::tests` | Exit 101, zero tests; two E0716 temporary-array borrow errors in tokio::join!. Corrected with named arrays, then formatted. |
| Final `just test -p codex-state runtime::accounting::pricing::storage::tests` | Exit 0; 6 passed, 214 skipped, 0.446s; run `1e519fd7-c29d-4f52-953c-5e2364546755`. |
| `just test -p codex-state runtime::accounting::pricing::tests` | Exit 0; 12 passed, 208 skipped, 0.063s; run `915aeddd-c40e-4277-a0b3-79bffea4262f`. |
| `just test -p codex-state runtime::accounting::tests` | Exit 0; 12 passed, 208 skipped, 0.129s; run `ce5f1163-1b24-4996-afae-924775d08129`. |
| `just test -p codex-state` | Exit 0; 220 passed, 0 skipped, 6.845s; run `b2d81fef-f266-44df-9311-e741c1ace981`. No leak warning reported in this run. Parent's combined269/two warnings were not rerun or attributed; their provenance remains unresolved. |
| `cargo check --offline --locked -p codex-state --lib` | Exit 0, 0.70s. No direct cargo test invoked; just/nextest owns its build subprocess. |
| Root `python3 docs/plans/check.py`; `python3 docs/sprints/check.py`; `git diff --check` | Each exit 0 before/following implementation; active 3/3, current115/archived122. Final receipt/tree checks rerun at handoff. |

Contract: private EstimateStore over native Journal; BEGIN IMMEDIATE serializes selected snapshot, immutable nullable binding and exact-evidence estimate insertion. Candidates all validate and conflict-check even when unselected; only selected snapshots persist. Later observations reuse the original binding, including unavailable. No UPDATE/repair/price fill. Atomic SQL/arithmetic/validation/conflict failures preserve journal evidence and roll back storage writes. Reads use one transaction, typed replay, physical journal key checks, retained-observation equality and whole canonical quote recomputation. Quote amounts never pass through the rate parser.
Literal proof: Anthropic partial 0.000153, unavailable/null and measured zero/no rate, half-even 0.0000005 and 0.0000015, sum-before-rounding 0.0000008, and 24-place 0.000000000000000000000001. Seven cases each close/reopen the real disk database twice and compare whole quotes, snapshots and source rows; no process-kill claim. Concurrent duplicate writes yield one version; conflicting snapshot writers yield one winner. Invalid/missing attempts, candidate bounds/conflicts, injected SQL abort, corrupt quote fields/flags/source positions/snapshots, changed or missing retained evidence reject. New and late-lower revisions retain separately readable historical versions; they are never summed as spend.
OFF proof: unchanged runtime.rs `#[cfg(test)] mod accounting;`, unchanged journal/quotation behavior and old tests; only private pricing child registration plus exact Serialize derives/import. Normal-library check and normal-init/no-draft-tables test pass. No production migration, public API, live collector, dispatch/provider/wallet/auth/billing/aggregation/deletion wiring, dependency or BUILD changes. No agents/reviewers/models, commits/merge/push, other worktrees, source cutover, shared plans or PF13 edits.
Remaining gates: parent exact-diff review and combined-tree checks; Clippy/fix and unrelated Python formatting limitations above; full S02 goldens, crash recovery, production promotion, native presence/dispatch, lineage, inspection/export/deletion, true-TUI/live-repository proof, runtime human acceptance, benchmarks and release qualification. This test-only increment is not a finished product or completed S02; S03 stays dependency-gated.
Smallest next concrete work: parent reviews this frozen patch, then allocates the same-S02 lifecycle slice joining one authoritative per-attempt evidence version to exact per-thread/day contributions, atomic deletion/tombstones, 90-day detail/365-day aggregates, cutoff/pruning, snapshot cleanup and replay-versus-deletion concurrency; native delete_threads_strict integration and production enablement remain later gates.
