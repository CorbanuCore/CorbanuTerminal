# PF-60-S02 — validated retention input, slice A receipt

Slice A is implemented, uncommitted and ready for parent review, not integrated or independently accepted. PF-60-S02 remains in_progress; B and S03 are not activated. Product initiative: **Product measurement**, “No commercial performance numbers have been supplied.” corbanu-terminal-development routed the policies/active plan/sprint already fully read; the integrator amendment was read completely before this turn's code changes.
Checkout `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`, branch `workstream/accounting-pf60-s01-20260911`, unchanged launch/final HEAD `b8da69d744aa65ea68e5b71c199d401922f84602`; original clean launch was verified before the first draft. This continuation began with that draft plus the parent's allocation amendment, not a falsely claimed clean checkout. Base `87e31f521672e627e6230d48fc16a4cfaa7ff44c` remains ancestry.
Parent-owned amendment `docs/research/agent-cost-accounting/retention-plan-next.md` is untouched: **+29/-0**, SHA-256 `625a2552708a25d8879d8c4d54b2a8d91c6c879f50ba375475123b3690a4d6ad`. It is excluded from candidate size/hashes below.
Before edits, all five complete draft files (including original receipt) were copied byte-for-byte to private mode0700 directory `/Volumes/CorbanuDrive/Corbanu/.codex-work/manager-accounting-retention-reslice-20260912.32IaBj/`. Its `PRESERVATION.md` maps original paths, basenames and all five SHA-256 hashes; manifest SHA-256 `209d4b67a622013a6481abeb651093af1922407dfcad1435a260bed2ba35131c`. Hashes were checked immediately after preservation and again after final tests. Original receipt SHA-256 `5f5160e836fa4145d46ee42f6f998378049e7971f836b6892d63de6edcb7bd1c`; original planner/test/support hashes remain in the manifest.

| Candidate path (repository relative) | Added/deleted vs launch HEAD | Complete-file SHA-256 |
| --- | --- | --- |
| `codex-rs/state/src/runtime/accounting_lifecycle.rs` | +3/-0 | `cda1bd84f63a5bd4056bcec73d4a158a8ce038616c60a950ae0bb73084008bd0` |
| `codex-rs/state/src/runtime/accounting_retention_plan.rs` | +219/-0 | `3c15533c5b3a4298fe4f632cd6c9af7b0bf5c20fe8fc1e91cf313c7f7c961128` |
| `codex-rs/state/src/runtime/accounting_retention_plan_tests.rs` | +369/-0 | `3b00d530543c0da2c3e1bbb9c684c675616c2c98b58530c349c593005bceddb5` |
| `codex-rs/state/src/runtime/accounting_retention_test_support.rs` | +154/-0 | `f7f889fe360ec04d5e45ba57856c236a3200a6ff29fba69d88033b93050ec1da` |
| `qa/portfolio/agent-cost-accounting/pf-60-s02/retention-plan-increment.md` | +30/-0 | Final handoff supplies digest to avoid self-reference |

Candidate total **+775/-0**, including receipt: **252 conservative non-test**, **523 exclusively test/helper** lines. Early formatted count was745 Rust lines; final count includes all new files. Above the712 target by63, within hard800/500. Complete literal-input, no-write/reopen and33-corruption proof is retained; no required A test was dropped for size.
Private `read_validated_input_on_connection(&mut SqliteConnection, i64)` returns only ephemeral validated input: previous/as-of checkpoint, every compact row/reference set and its checked expiry, every raw latest quote/day/checked detail-replay-day expiry with Missing/Stale/Current contribution freshness, and every tombstone. It neither filters expired data nor computes totals/removals, merges compact values, transfers authority, serializes a capability, owns transactions or writes. One sibling registration remains under the existing cfg(test) ancestor; no production API/caller/migration exists.
Validation covers singleton NULL versus zero, malformed/negative/backward checkpoints, strict SQLx integer decoding, canonical keys, all fixture foreign keys, every snapshot/compact payload including expired/unreferenced rows, orphan/foreign contributions, raw/tombstone collisions, negative/future timestamps and checked day/detail/replay expiry arithmetic. Every raw attempt uses the frozen latest-quote reader and validates all historical estimates with original immutable bindings, including SQL NULL and absent bindings. No fresh catalog candidates or transaction-owning quote persister are called; approved90/365 durations remain unchanged.
Three tests execute46 reads: four successful whole-input assertions and42 error assertions (33 corruption injections plus9 time/overflow cases). Every call compares the complete typed result or expected error, every column of all ten ordered fixture tables and same-connection total_changes(). Complete-input proof retains expired detail/day/tombstone rows, an unexpired day, shared snapshot references, stale bound quote3e-24, unbound intent and a current explicit-NULL quote alongside another eligible price; all seven usage fields, quote provenance and exact display metadata are compared. Caller-staged checkpoint0 is visible, caller rollback restores NULL, and complete input/storage survive two disk reopens without reinstalling fixtures.
Test support atomically installs only the three new fixture tables/checkpoint after the existing seven-table setup. Compact payloads contain accepted exact aggregates only. Corruption includes malformed/overflow compact amounts/populations, checkpoint shape/types/times, foreign/orphan keys/references, corrupt old estimates despite latest authority, binding/snapshot corruption, invalid replay expiry and future snapshot evidence. Additional cases cover negative/future raw dispatch and day-start/day-expiry/detail-expiry/replay-expiry overflow. Fixture-only FK disabling is restored before pool return.
Commands used cached rustc1.95.0 and `RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true UV_OFFLINE=true CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/target`, from assigned codex-rs. sandbox-exec denied network and checkout writes except four allocated Rust files/target. Scoped fix then formatting preceded final tests; no repairs, tool installs or restriction weakening occurred. Outputs/run IDs in this task are the execution record; no separate logs were written.

| Final validation, September12 UTC | Exit / exact result |
| --- | --- |
| `just fix -p codex-state` | 101 once for A: Cargo fix cannot bind its TCP lock-manager listener under denied network. Known environment limitation, no successful Clippy/fix claim |
| `rustfmt --edition 2024 --config skip_children=true state/src/runtime/accounting_retention_plan.rs state/src/runtime/accounting_retention_plan_tests.rs state/src/runtime/accounting_retention_test_support.rs` | 0; stable imports_granularity warning only. Registration inspected; no unrelated/full-repository formatter |
| `just test -p codex-state retention_plan::tests` / `just test -p codex-state` | Both0; **3 passed/242 skipped**,0.108s, run`c2f3ef7e-a581-40c6-994c-59c623f2a35f`; **245 passed/0 skipped**,8.494s, run`a2183b12-1f79-422b-88e7-08650e3db972` |
| `cargo check --offline --locked -p codex-state --lib`; root plan/sprint checkers; whitespace | Library0,0.77s; governance0:3 active/115 current/126 archived; tracked diff check0, all new-file no-index checks have no whitespace diagnostics (exit1 means added-file difference) |
A had no failed test/compiler/library runs; only the disclosed fix failure. Historical draft failures remain in the preserved receipt. A read-only source grep initially used the preservation directory with a checkout-relative path; it was rerun against the correct checkout. No direct cargo test invocation, commit, push, external operation, credentials/live collection, other agent or review occurred.
B preservation/remaining: the original planner and all480 original test lines remain hashed in the backup. This includes mixed-day/shared-snapshot reduction, exact90/exact365/dispatch0/one-millisecond loss, null/unbound/stale/original-price mode/reopen cases and merge-overflow cases. They are **deferred B evidence, not passed A reduction tests**. Parent must review/integrate A, then allocate B separately within PF-60-S02. Atomic transfer/read/delete/admission/checkpoint/replay-floor coupling, contention/crash/physical-erasure proof and runtime/native wiring remain later work. No interactive entry changes: code-blind/TUI/live-repository proof is N/A only for A; human acceptance, shipped docs, benchmarks and release qualification remain pending.
