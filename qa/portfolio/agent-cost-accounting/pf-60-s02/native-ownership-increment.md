# PF-60-S02 — Native ownership/deletion bridge candidate

Implemented and verified, uncommitted. Parent owns the one new code review plus necessary correction and receiving combined-state/TaskNode validation. This is the dispatched native bridge, not full S02/S03, normal-build accounting, human/live/release readiness or a new prerequisite chain.

Product initiative: **Product measurement / Measurement targets**, “No commercial performance numbers have been supplied.” Used corbanu-terminal-development skill, full current root/Rust AGENTS, plan/sprint processes, active accounting plan/current in_progress PF-60-S02 and the complete native-ownership-allocation.md. No nested AGENTS applies. User implementation dispatch supersedes historical documents-only prose; shared ledgers remain parent-owned.

W = `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`; branch `workstream/accounting-pf60-s01-20260911`. Clean launch and unchanged final HEAD `165da0ef33d1f323c03d3c1135ff5bafa685f691`; allocation ancestor `d5608c58d91a75396fea78b447477c44091d4625` verified. Parent accepted allocation f13e9740a/integratedce8a81980, first Astra High native-ownership-allocation-review.json exit0 clean, then checked dispatch acceptance. Sole accounting threads.rs ownership was manager-verified. No reviews/model calls/children/delegation, commits/pushes or live actions performed here.

Accepted history stays frozen: A/B/C1 receipts and original failures/review usage; C2 six-file b7a3466de integratedd560, first Astra High c2-atomic-review.json exit0 clean; parent combined346/346,0skipped13.556s, run06b633c7-51ae-4dd8-bf96-81a2665c2871, no final-summary LEAK,11 fixture warnings. Its1519total/190non-test disposition and C1's1121-line exception are historical, not inherited authority. C2's obsolete seven-line interim assertion removal/replacement mapping remains unchanged.

| Exact path relative to W | Added/deleted | Complete-file SHA-256 |
| --- | --- | --- |
| `codex-rs/state/src/runtime/accounting.rs` | +3/-0 | `91d43ee23b659f5bf3b739baf9e1491a9478d06461ee77b3d511b6e5dcd3caca` |
| `codex-rs/state/src/runtime/accounting_native.rs` | +91/-0 | `d31631f4f93c08f4b81b9957d4318fc5a2144d83b7993a22b91f7fe59f82c6a2` |
| `codex-rs/state/src/runtime/accounting_retention_atomic.rs` | +21/-0 | `5cae362ec9685512e846d27b99c9ac050d7bf9f55423c22fbfd51d0720ce3714` |
| `codex-rs/state/src/runtime/threads.rs` | +50/-5 | `c4139656d078bda7b7876fbf114423198638f8b17013f07dd6ea3d5a00e8072a` |
| `codex-rs/state/src/runtime/accounting_native_tests.rs` | +723/-0 | `0c750539f7fb90ac75f6c8e39107e006f66243ed8db95703413013547e463c06` |
| `qa/portfolio/agent-cost-accounting/pf-60-s02/native-ownership-increment.md` | +57/-0 | Final handoff supplies hash; no circular self-hash |

Rust +888/-5 =893 total; conservative non-test170, tests723. All test-gated implementation and registration count as non-test, moved lines counted twice. Receipt57 adds to total/non-test: complete +945/-5 =950total/227non-test, below manager targets1200/500 by250/273. No overage, proof compression, size exception inheritance or extra paths.

Native admission takes caller-selected expected ThreadId and existing Attempt/observations/as-of. One BEGIN IMMEDIATE holds Active validation, equality/existence check, complete maintenance and Journal append; rejection rolls back clock and all accounting changes. It neither creates native rows nor authenticates a new principal. Existing raw Journal callers/A/B/C1/C2 semantics are unchanged. Narrow inherent forwarding reaches only complete C2 operations, not independent partial transfer or prepared-plan application.

Native public delete signatures, counts, empty/missing-row cleanup and ordering remain intact. The unchanged public wrapper captures one test-accounting time and shares its body with explicit-time tests. Installed accounting gets BEGIN IMMEDIATE and complete owner deletions before native tools/edges/rows on the same connection; native or commit failure rolls all main-state work back. Accounting hooks/schema/time/activation checks are cfg(test); normal-build accounting registration/installation remains absent and the no-schema route retains its deferred transaction. Fixture installation is explicit, not concurrent schema migration. No schema/manifest/lock/BUILD/Core/API/TUI change.

| New executable proof (native_tests selector) | Actual covered behavior |
| --- | --- |
| `native_admission_owner_replay_and_archived_rows` | Real parent/child native rows/edge, independent request IDs, known input observation/replay equality, expected-owner mismatch and missing owner no-writes, archive remains admissible with unchanged native rows, deleted owner rejects old and fresh UUIDs, two disk reopens preserve all rows. |
| `native_raw_compact_missing_counts_and_shared_price_survive_reopens` | Three fixtures: raw+compact owner, existing compact-only owner, missing native row with compact ownership. Two real reopens before deletion; literal complete accounting/native expected state, exact repeat/missing counts, other owner's shared original-price provenance and 1e-24/unknown populations remain usable; two successful reopens. |
| `native_post_accounting_faults_and_second_owner_rollback_retry` | Four exact-marker triggers: second owner's accounting attempt deletion and native tools/edges/thread deletion. Predicate requires first owner's raw removal and advanced checkpoint, proving failure after accounting work. Full13-table rollback and unchanged discoverable retry graph, two reopens, remove only trigger, exact multi-owner success/count2, repeat0 and two successful reopens. |
| `native_commit_foreign_key_failure_is_jointly_atomic` | AFTER-native-thread-delete trigger inserts orphan compact reference on dedicated FK-on/one-connection pool with deferred constraints. SQLite787/FOREIGN KEY failure, reacquisition awaits rollback, full13-table equality, two reopens, trigger-only removal, native/accounting successful retry and two reopens. |
| `native_two_real_writer_orders_and_joint_read_snapshots` | Actual append-held→native-delete and native-delete-held→append. Distinct connections, held/release channels and busy_timeout0 peer observe SQLite5/6 before release; append revision commits before delete retry, absent owner rejects replay and fresh UUID. Held SELECT snapshot sees complete old13-table state; fresh snapshot sees joint deletion. Exact expected rows and two reopens, no sleeps as proof. |
| `native_invalid_time_activation_and_late_import_roll_back` | Negative/backward time on both entrypoints; compact-only late import, future dispatch and conflicting observation after potential maintenance all roll back checkpoint/native/accounting rows. Staging, Active-NULL and partial schema reject both paths; complete malformed-state dump survives two reopens. No late-import guard relaxed. |
| `public_delete_wrapper_and_absent_schema_keep_native_contract` | Actual public wrappers with recent synthetic accounting and no-schema fixtures, zero normal-init accounting objects, empty input0, present1/repeat0, exact tombstone, captured clock bounds; absent-schema missing deletion ignores accounting-only negative time, no tables installed; two reopens. |
| `separate_store_failures_preserve_retry_graph_without_global_rollback_claim` | Exact logs/memory/goals/native fault markers preserve13-table state and native discovery. Separate-store remaining row counts respectively(1,1,1)/(0,1,1)/(0,0,1)/(0,0,0), explicitly proving earlier cleanup can commit before later failure. Remove only fault, two reopens, successful native/accounting retry and two successful reopens. |

Every joint dump selects every column of all ten accounting tables plus threads/thread_spawn_edges/thread_dynamic_tools in one read snapshot; missing schema is explicit, never silently omitted. Expected deletion rows use literal case IDs/expiries and preserve only explicitly untouched baseline rows; they are not generated by running C2/reduction. Reused support is read-only. Original A/B/C1/C2 tests and schemas are unchanged, not weakened or duplicated.

All Rust commands below ran from W/codex-rs with exact prefix `env RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true UV_OFFLINE=true CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/target`. Reused pinned/cache/target; no installation, fetch, environment repair or direct cargo test. Final full-suite timestamp2026-09-12T13:08:48.607Z.

| Command after prefix | Exit / actual result |
| --- | --- |
| `sandbox-exec -p PROFILE just fix -p codex-state` | Two attempts101: TCP lock-listener bind, Operation not permitted. PROFILE = `(version 1)(allow default)(deny network*)(deny file-write* (subpath W))(allow file-write* (literal EACH_OF_THE_FIVE_RUST_PATHS) (subpath W/codex-rs/target))`, expanding quoted absolute paths in table order. No successful fix/Clippy claim; no environment repair. |
| `rustfmt --edition 2024 --config skip_children=true` plus five absolute Rust paths in table order | Both0, before affected tests; stable imports_granularity warning. Recorded literal-file scope replaces broad formatting. |
| `just test -p codex-state retention_plan::reduction::atomic::native_tests` | Final0:8passed/266filtered,0.665s, run39fa1477-b2ba-483e-8d8d-ceefcb06d675. |
| `just test -p codex-state runtime::accounting` | 0:84passed/190filtered/1leaky,7.692s, runc84483d3-4adc-4814-b978-ebf317042400. LEAK: existing compact_values::tests::every_count_rejects_wrong_types_negative_and_overflow. |
| `just test -p codex-state delete_thread_cleans_associated_state` | 0:1passed/273filtered,0.056s, run2c1c1419-fd43-4953-9aa9-39088bc2e997. |
| `just test -p codex-state delete_thread_keeps_retry_graph_on_cleanup_failure` | 0:1passed/273filtered,0.053s, runb83e543e-3b23-4add-a7aa-39a3d48cd724. |
| `just test -p codex-state` | 0:274passed/0skipped,14.816s, run1a6c961b-7810-4d1a-9717-1f2a90fb38b1. No LEAK marker in final summary. Assigned target/nextest/local/junit.xml confirms274tests,0failures/errors/skipped and UUID. |
| `cargo check --offline --locked -p codex-state --lib` | 0; normal dev profile,2.41s. |
| From W: `python3 docs/plans/check.py`; `python3 docs/sprints/check.py`; `git diff --check` | All0: active3/3; current115/archived126; no whitespace errors. No shared-ledger edits. |

Original runf085b552-2354-44c5-9271-80e6b58f997a exited100:7passed/1failed/266filtered,0.736s including configured retry. Native archive metadata stores whole seconds; the new test incorrectly compared a fractional timestamp against its persisted form. Corrected only the synthetic timestamp to exact1700000001seconds, retained the equality assertion and strengthened whole-native-row equality. Added existing compact-only ownership alongside missing-row compact cleanup; then repeated scoped fix/format before final affected tests. No Rust edits after final formatting/tests. Test builds report21 fixture-support dead-code warnings(9duplicates), versus11 in the parent C2 baseline; no warning-clean or universally leak-clean claim.

Literal Rust diff SHA-256 `13126fa3b16b9c78962e1075421a7c56d2316c53caf4ba1bb0aa9744c961466f`. Reproduce from W: concatenate `git diff --binary --full-index --no-ext-diff --no-color HEAD --` the three tracked Rust paths in the table, then the same flags with `--no-index /dev/null` for accounting_native.rs and accounting_native_tests.rs in that order. Append the no-index receipt diff for the complete six-path artifact. No-index exits1 for differences; final handoff supplies receipt/full-diff hashes without an unallocated patch file.

Limits: logs/memories/goals are separate commits, not covered by the main-state rollback guarantee; no physical SQLite/WAL erasure or process-kill/power-loss/uncertain-commit proof. Native row recreation remains possible through legitimate upsert; no permanent deleted-UUID fence/auth principal. Production installation, durable actual-send/retry/presence/original-price wiring,90..365-day compact-only late imports and complete S02 native goldens remain unqualified, not waived. Feature OFF, S03 draft. True-TUI/code-blind/live-repository/benchmark/release proof is not applicable to this private accounting bridge verification and is not claimed; normal deletion compatibility is covered by actual native regression tests. Parent must independently review this exact candidate and run combined receiving tests before integration; human/runtime acceptance and release gates remain open.
