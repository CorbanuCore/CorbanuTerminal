# PF-60-S02 — retained contributions and recorded-attempt deletion receipt
Product initiative; **Product measurement**, “No commercial performance numbers have been supplied.” PF-60-S02 remains in_progress. Astra High worker used corbanu-terminal-development policy routing; canonical 155-line allocation and approved rolling-expiry defaults govern. Parent owns shared ledgers, one independent review, scoped corrections and integration.
Checkout `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`; branch `workstream/accounting-pf60-s01-20260911`; clean launch and unchanged HEAD `e9aeef368a9a3e68cb2fd9a889c1b5e3daf1cd9a`. Accepted ancestor `56295f66825e87dc924705d86fcbcebf258ba7f9` verified with `git merge-base --is-ancestor`. No commits, merges, pushes or other-worktree writes.
Final changes: 775 Rust lines = 269 lifecycle + 477 tests + 29 seam additions/deletions. This 25-line receipt gives exactly 800 total (+795/-5), including 323 conservative non-test lines. Only the five allocated files changed, all uncommitted. No cases removed; repeated fixture attachment/table-count plumbing shares helpers. Receipt SHA-256 returned separately to avoid self-reference; preserve prior receipts.

| Exact Rust file under `codex-rs/state/src/runtime/` | File lines; added/deleted | SHA-256 |
| --- | --- | --- |
| `accounting.rs` | 143; +7/-0 | `160c5a72660fc12685bb19d8502279313f390189cafcc249a3f5dbacdd4cc857` |
| `accounting_estimates.rs` | 248; +17/-5 | `a9e6d1cf5445254dde70d9baab490b92f1833e008fa296977848f52b0d65a824` |
| `accounting_lifecycle.rs` | 269; +269/-0 | `686afb3da830d95e7c07464e1295b3c3407a6d9b6024ef46b37f6aab37986755` |
| `accounting_lifecycle_tests.rs` | 477; +477/-0 | `80a7e52a2058dc96416342ed0ff5eec588743b47e1932236b8ea2bb41338383f` |

Commands ran from codex-rs on September 12 UTC / September 11 Phoenix; final Rust checks complete by 02:09:25Z. Prefix: `RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true UV_OFFLINE=true CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/target`. Each Rust command used `sandbox-exec -p` with `(version 1)(allow default)(deny network*)`, deny file-write* to this checkout subpath, allow file-write* only to the four literal Rust paths above and this checkout's target subpath. Guarded `just fmt` ran four times, last before every final command below: Rust/Just/Bazel succeeded, overall exit1 from unrelated Python SDK/scripts `exclude-newer = "7 days"` parsing and scope-denied `.venv` creation. Guarded `just fix -p codex-state` exit101: TCP-listener locking prohibited, no fix applied or bypass. First lifecycle build exit101/zero tests: two SQLx E0277 dynamic-SQL errors; corrected to literal SQL. Subsequent focused runs passed. Earlier full run `7e8767c3-d0b7-4629-a74a-ac8207c1912a` passed226 with two unattributed leak markers; then the race test was strengthened to write a new revision/estimate/reference and all final commands rerun. No environment repair or process/lock cleanup.

| Final command | Actual result; nextest run ID |
| --- | --- |
| `just test -p codex-state runtime::accounting::pricing::storage::lifecycle::tests` | exit0; 6 passed/220 skipped, 0.180s; `8c6e983e-4c06-4dc8-a46d-cacaafb64fc1` |
| `just test -p codex-state runtime::accounting::pricing::storage::tests` | exit0; 6 passed/220 skipped, 0.441s; `e99ecae8-91f8-4880-886a-c961010c27f2` |
| `just test -p codex-state runtime::accounting::pricing::tests` | exit0; 12 passed/214 skipped, 0.064s; `ad9b61ae-a6ac-4121-8f63-b698885576d2` |
| `just test -p codex-state runtime::accounting::tests` | exit0; 12 passed/214 skipped, 0.150s; `244802ea-9ace-4bab-8cad-cebabd327d82` |
| `just test -p codex-state` | exit0; 226 passed/0 skipped, 7.394s; `ae16f0e6-2a73-4fda-ab33-2daf8bb584a1`; LEAK markers: `log_db::tests::event_queue_drops_new_entries_when_full`, `extract::tests::turn_context_sets_model_and_reasoning_effort`. Cause uninvestigated; not a leak-clean claim. |
| `cargo check --offline --locked -p codex-state --lib`; root `python3 docs/plans/check.py`, `python3 docs/sprints/check.py`, `git diff --check` | each exit0; library 0.72s, plans3/3 and current115/archive122. No direct cargo test; just/nextest owns its compilation subprocess. |
Proof: whole seven-metric known/unknown objects distinguish Anthropic50/read10 at3/0.3 =0.000153, unknown price and explicit zero, two0.0000004 amounts summed before display, and exact24-place amounts. Duplicate refresh changes no rows; higher and late-lower revisions replace one reference while immutable quotes remain readable. Missing/stale evidence returns NeedsEstimate/NeedsRefresh; corrupt attribution/source and checked count/amount overflow reject. SQL abort and invalid/future/overflow deletion times preserve all seven tables' complete row dumps. Deletion removes all versions and unquoted intentions; shared snapshots survive until last owner. Opaque two-column tombstones expire at exact dispatch+365*86400000, remain blocking while present and never extend on repeated deletion. Barrier-ordered replay/estimate/refresh-before-delete and delete-before-replay cases prove both serialization orders; a held read transaction sees its complete prior snapshot. Two actual disk reopens prove retained reads and deletion/replay rejection; no process-kill or simultaneous lock-contention proof claimed.
OFF proof/limits: unchanged runtime.rs cfg(test) accounting registration; private storage child and two narrow seams only. Normal library and normal-init/no-draft-tables test pass. No native thread deletion, new/re-keyed-attempt admission rejection, pruning/expiry removal, production migration/promotion, public API, provider/dispatch/auth/billing/allowance/balance/UI wiring, dependencies/BUILD/locks, services/credentials/Keychain/seeds/wallets, source cutover, PF13, shared plans, agents/reviewers/model calls or budget resets. No production/TUI/live-repository/user acceptance/benchmark/release claim; these remain pending, as do full S02 goldens/crash/lineage and parent review/combined-tree qualification.
Private successor handoff: after parent acceptance, allocate the smallest coherent same-S02 Stage B covering independently deletable compact thread/day totals, retention-aware reads, shared snapshot ownership, rolling90-day detail/365-day aggregate cutoffs and durable monotonic import admission together. Include exact amount decoding, partial-day no-double-counting and prune/replay/delete boundary races; remove tombstones only atomically with the durable cutoff. Native delete_threads_strict ownership/admission integration and production promotion follow later. S03 stays gated. This receipt is the fifth exact allocated path: `qa/portfolio/agent-cost-accounting/pf-60-s02/contribution-deletion-increment.md`.
