# RETURN — acct-bounds-18
Action acct-bounds-18; claim 643f01ea-af27-4e64-a1a8-abb18542d62c; runtime gpt-6-astra / high.
Allocation digest e1a2878db2ffd7d4219c4f1ea99f0266810625d3ce45fb87bcf63a0c1d95903d.
Brief SHA-256 verified: 51cc702367effd54b4adb2979239e93776fef8b59b9b9acd3ae6365fe97a5b78. Clean starting HEAD: 5c0634ed3f1c6a6d6c7897f4bff0dbfc4f1a6f81.
Bounded correction under active portfolio-agent-cost-accounting / PF-60-S03 (in_progress), bootstrap/acct-inspect-20260915 at the allocated worktree.
Product heading **Measurement targets**: “The following metrics must be instrumented, with targets set through the decision rights defined above.” The plan's older “Product measurement” heading is stale.

Each day/range call permits at most 4,000,000 budgeted whole-table scan-row units (plus one cardinality sentinel) and 20,000 candidate/ancestry/attempt/history/sibling/retry visits; the 12,001-row, 600-UNKNOWN-owner fixture must return TooLarge through both public APIs within 30 seconds.
Cardinality is sampled once with LIMIT 4,000,001; each owner inspection reserves five scans of attempts+compact rows, candidate discovery reserves one, and range aggregate discovery reserves one. This conservatively covers the repeated full-table JSON scans and contribution probes. Visits also debit row units, even for unrelated roots. There is no refund or per-bucket reset. These are scan/traversal bounds, not a fixed wall-time guarantee or a byte/I/O/SQLite-instruction budget; per-attempt indexed evidence validation retains its existing payload/observation bounds.
The public 72-hour range regression fits all result limits and proves shared-budget refusal while each constituent day remains Ready. Exhaustion on the final UNKNOWN owner propagates TooLarge rather than an unavailable count.
Provisional ancestry is a separate map: every insertion immediately checks ancestry.len()+discovered.len()>10,000 and packet+provisional graph bytes>4 MiB. Unrelated resolution discards provisional output, never work charges. Tests cover a >10,000-hop unrelated chain and a one-hop oversized unrelated source.
The global SQL ownership guard rejects every noncanonical UUID spelling before equality filtering; authority parsing then reasserts typed owner and dispatch-window identity. The own-ID test proves simple, URN and braced spellings parse to the requested ThreadId yet inspection errors instead of omitting its amount.
Deleted the tautological contribution-total ensure. Totals now accumulate once, before stale-result handling, retaining overflow errors. Ledger reconciliation is per attempt only; no independent day-level ledger reconciliation is claimed.
Resolved the orphan helper by calling inspect_quote_on_connection only for zero/one-version histories. Larger histories keep incremental validation and bound_quote; the helper's eager version collection cannot defeat that path.

Coverage decision: preserve and explicitly disclose the narrowing's divergence rather than reinstate the whole-store retention planner. It is reachable when the checkpoint advances beyond overdue raw/compact data on another day, including externally restored/inconsistent stores; this reader does not globally require maintenance. The regression advances the checkpoint to day 400 with raw day 0 retained: read_day says NeedsMaintenance, inspecting day 400 is Ready with oldest_recorded_day=0 below aggregate_day_floor=36, and inspecting day 0 says CheckpointLag. The oldest field describes stored history, not a promise that detail can be inspected. This is disclosed divergence, not unified coverage-contract qualification.

Verification:
- State gate: just test -p codex-state accounting, exit 0; 165 passed, 0 final failures, 187 skipped, 81.064s; 23 slow, 1 flaky (late-import identity/retry/prices), 2 LEAK markers (every_checked_addition_rejects_overflow_without_mutating_operands; schema_requires_exact_fields_version_object_and_array_arity). All six new tests passed, including public UNKNOWN refusal (18.608s total fixture) and shared range budget (7.108s). Raw attempts remain in acct-bounds-18-state-2.log.
- TUI gate: just test -p codex-tui usage, exit 0; 91 passed, 0 failed, 4,071 skipped, 0.430s; no flaky/leaky summary markers. Log: acct-bounds-18-tui.log.
- Broad Clippy: just clippy -p codex-state -- -D warnings, exit 101; 1 lint error in unchanged protocol/src/security.rs:28 (large_enum_variant), before state linting. State-only just clippy -p codex-state --no-deps -- -D warnings also exits 101: 12 unchanged-code errors (10 dead-code diagnostics across accounting.rs, accounting_estimates.rs, accounting_lifecycle.rs, accounting_retention_atomic.rs and accounting_native.rs; 2 large_enum_variant diagnostics on InspectionDay / OriginalPriceEvidence). The inspection quote helper is no longer orphaned. Broad CI and scoped -D warnings remain blocked; fixing the complete diagnostic set needs paths/contract changes beyond this allocation. Logs: acct-bounds-18-clippy.log and acct-bounds-18-clippy-state.log.
- Governance/scope: sprint checker passes (115 current, 127 archived); scoped rustfmt only; diff whitespace check passes; only allocated paths changed.
- Intermediate state-1: 164 run, 162 passed (1 LEAK), 2 failed, 187 skipped; exit 100. Both failures retried once by nextest. Failures exposed swallowed work exhaustion and loss of early overflow validation; both corrected. LEAK: schema_requires_exact_fields_version_object_and_array_arity. Raw log preserved.
- Authored counts against dispatch HEAD: 248 changed test lines; 166 production Rust + 28 receipt = 194 outside tests, 442 total (target 200 / hard 320 outside tests). Cumulative net against 1b1ea6912: 606 tests; 423 production + 54 prior receipt + 28 current receipt = 505 outside tests, 1,111 total. Generated ignored logs excluded from authored counts and retained separately: 2,628 lines across five logs (941 / 448 / 695 / 374 / 170). Four-file source diff SHA-256: 965922b111b3bdc33db9ba88982dfacf629fba01ab722b74e7cb3aeb6ea03f6c.

Brief corrections: resolved unrelated roots already skipped Journal inspection at the base, so the claimed four whole-table scans for every unrelated candidate and ~30M JSON extractions in the 600-root fixture do not follow from that code. UNKNOWN candidates and repeated range windows do incur the repeated scans. The work/ancestry defects remain real. The 10,000-row fixture is only non-regression evidence; 12,001 rows prove crossing the removed cap.
All existing tests remain. No schema/index change, live profile, credentials, workspace formatting, push or release. Read test-isolation guidance before testing; Rust execution uses guarded just test.
This is a worker implementation return to Fable, not human-test readiness. Independent review, isolated code-blind execution/evidence review, true-TUI proof, live-repository qualification and receiving approval remain unclaimed manager-owned sprint gates.
