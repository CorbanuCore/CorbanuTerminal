# PF-60-S03 acceptance gap — sprint citation

Round-75 follow-through: [owner acceptance work order](../acct-acceptance-75/acceptance.md)
adds effort estimates and separates existing manager authority from human decisions.
The historical gap inventory below remains evidence of the round-72 handoff.

Status at allocation acct-controls-72: **not accepted; no unqualified human-test
handoff**. This allocation repairs internal evidence validation only. Its
mutation results and guarded Rust gates do not qualify an interactive workflow.
The active plan is `docs/plans/active/portfolio-agent-cost-accounting.md`;
PF-60-S03 remains `in_progress`. Product context is **Product measurement**,
excerpt “No commercial performance numbers have been supplied.”
The frozen assignment authorizes this QA worktree; it does not amend the
sprint's older implementation coordinates or authorize runtime changes.

| Required item | Can this assigned lane produce it now? | Authorization/allocation required | Smallest honest first slice |
| --- | --- | --- | --- |
| Independent code-blind functional design | No. This worker knows the code, old cases and results. It can prepare evidence inputs, but cannot be the independent designer. | Fable allocates a fresh-context designer and records design/review spend in the existing ledger; the named integrator may authorize an extension under the September 12 delegation. Supply only intent, essential constraints and neutral screenshots outside the source checkout. No new product approval is needed to design already-authorized developer-only behavior. | Freeze the designer's original prioritized cases for one recorded-request overview → request → attempt journey, with unknown-cost and cancel/recovery expectations. Preserve every proposal and question; this is design evidence only, not full feature coverage or execution. |
| Independent code-blind execution and evidence review | No. Replaying saved captures or implementer-run PTYs does not supply independent execution. This allocation provides neither a sealed executor environment nor an exact packaged candidate with qualification receipts. | Fable allocates a separate fresh executor and an evidence reviewer independent of executor/implementer. Provision enforced filesystem, tool, process/IPC and network boundaries, denied repository/history and real credentials, read-only package/helpers, synthetic fresh/existing state, and mediated inference. Authorize any infrastructure/spend or external endpoint separately; do not infer permission to use live profiles. | First qualify the enclosure: actual executor and child negative probes plus positive package/PTY controls. Then execute one frozen overview-to-attempt case with real keys on the exact package, prompt and Enter separately, preserving raw evidence and schema-2 receipts. Have the independent reviewer check it. All other frozen cases stay open until executed or explicitly dispositioned by product authority. |
| Live-repository proof | No. Existing synthetic capture arithmetic does not prove a workflow in either default repository. Their acceptance paths/base commits remain unallocated in the plan. | Fable allocates disposable worktrees from recorded TensorCash/Isometric origins and base commits, approved inputs, bounded inference and an evidence owner. Product authority must accept any feature-level applicability exclusion; a release suite still owes both repositories. | Resolve one applicable repository and base; perform one bounded real TUI request in its disposable worktree and inspect its recorded request/attempt estimate, including the estimate-versus-billed distinction. This is one repository case, not completion of both-repository qualification. |
| Platform and profile proof | No. These Rust gates ran on this macOS host; they are not packaged macOS/Linux/Windows interactive proof. | Fable records the applicable platform/profile matrix and assigns isolated hosts/VMs, exact packages/helpers and resource limits. Product authority accepts any scope exclusion. Native credential qualification requires a separate test account/VM, never the operator's Keychain. | One platform's sealed package, fresh synthetic profile and representative existing synthetic profile, running one frozen success and recovery/resume journey with actual keys. Other platforms and affected paths remain blocked/unexecuted, not implicitly passed. |
| Named-human acceptance and receiving decision | No. This worker cannot supply Travis Good's acceptance or Fable's receiving decision. | After required evidence passes, Fable presents the bounded candidate and limitations to Travis Good, the recorded product acceptance authority, and records his explicit decision plus receiving disposition. No release/push authority follows from this QA assignment. | Present one exact candidate-bound workflow with its verified evidence and named remaining gaps. Record a dated, named limited-testing decision if Travis expressly agrees; that does not convert unresolved cases into full S03 acceptance. |

For S03 closure, Fable must also reconcile every still-open sprint Remaining,
Verification and Exit-evidence checkbox against the final receiving tree:
affected state/TUI/core integration gates; true-TUI success, cancel/failure,
recovery and resume evidence; preserved review findings and accepted
dispositions; final output provenance; and ledger/archive/plan updates.
The three Rust filters in this assignment are not the whole receiving suite.
Inherited S02 coverage limits, current scope/coverage wording findings,
whole-store inspection limits, retention/range gaps and other disclosed
limitations require explicit receiving/product disposition where applicable;
this worker neither silently discharges them nor invents an N/A.

Developer-only collection activation remains the authorized boundary.
Shipping `/usage requests` to users remains held; a distinct product decision is
required to change that boundary. The independent designer must receive that
constraint without implementation rationale or prior results.

The root [policy](../../../../../AGENTS.md) and
[code-blind workflow](../../../../code-blind-functional/README.md) own the
requirements. This statement identifies the remaining work; it grants no
approval, no acceptance, no platform exclusion and no release authority.
