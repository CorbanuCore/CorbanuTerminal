# Engineering completed for the round-76 decision

This is a QA-only preparation record at `1aadfc123592530f657794489249e954f5e3885a`,
not an independent execution receipt. It replaces the round-75 request that
owners first perform the worker's evidence reconciliation and input preparation.

## Completed reconciliation

| Obligation / prior claim | Current disposition and concrete evidence |
| --- | --- |
| Activation unavailable everywhere | Superseded by authorized developer-only activation. Sprint Remaining records Travis's September 16 decision; earlier `no-activation-path-20260916.md` is historical, not the current blocker. Shipping remains held. |
| Scope-zero | Open P2: [actual text, four priced attempts, root controls](../acct-scope-62/reader-findings.md). Zero is valid for the fresh root only. Put this directly in the fitness sentence. |
| Inherited gateway, prewarm, auxiliary and legacy acquisition | Still unqualified: [inherited audit](../acct-inherited-64/RETURN.md) and [collector next slice](../acct-receipt-66/next-step.md). Passing auxiliary test names are not proof of complete application collection. No authoritative prewarm economics or settlement source exists in these receipts. |
| Complete application coverage, 365-day fencing, ordering P3 | Still open in sprint Remaining. No evidence here discharges them. Anonymous fencing is not permanent; ordering test does not pin the state lock. |
| Six inherited leaky markers | Historical suite findings remain. Zero leaky markers in the current three filters does not resolve findings outside those filters. |
| Whole-store/candidate bounds | Open measured limitation: [round-50 limits](../acct-qualify-50/RETURN.md) and sprint Remaining. A small root may be refused. Narrowing the persistent index needs product-authorized schema work outside this lane. |
| Ranges, retention, dates and UI boundaries | Supporting [round-58 actual-key boundary evidence](../acct-boundaries-58/RETURN.md) exists; it does not complete independent final-package execution. Compact attribution/full-range totals remain unavailable. |
| Two-provider UI / positive billed comparison | Blocked prerequisites: [exact residual analysis](../acct-scope-62/residuals.md). Changing identity or treating tariff calculations as settled dollars would not qualify them. |
| Monetary claims | 9 priced, 4 unknown-cost, 2 zero-recorded views; 147 named checks and 159 diagnostic mutations. Digest failure precedes most monetary assertions in qualifying mode; zero-view value assertions are vacuous. [Round-75 correction](../acct-acceptance-75/RETURN.md). |
| Historical timeout | Round-66 feature test timed out twice; round-69 alone/in-lane passes suggest load sensitivity, without a demonstrated causal fix. Current fresh replay is separately recorded in `test-results.json`. |
| TUI regression/receiving scope | Current requested filters are core accounting default/feature and TUI usage, not the complete receiving suite or all 553 tests cited in sprint Verification. No receiving-tree result is claimed. |
| Final provenance / archive / acceptance | Output hashes and gate base are supplied by this revision. Canonical sprint is still in_progress; no independent acceptance or named-human sign-off is invented. The receiving manager owns canonical record updates. |

## Prepared inputs and matrix

[designer-intent.md](designer-intent.md) is a neutral intent-only input prepared
now, not a frozen independent design. Do not give the designer this engineering
record, acceptance document, source, old tests, or results. The scope contains
text/PTY captures but no screenshot files suitable for the requested designer
packet. Do not fabricate images from text or call a text transcript a screenshot.
The host operator must obtain representative actual screens from the exact
package in the permitted environment. The final designer packet must be outside
the source checkout; this worker may write only this checkout's QA subtree.

[prepare_package.py](prepare_package.py) builds the developer-accounting CLI and
matching code-mode/test helpers with locked offline dependencies, then stages
read-only copies under ignored `package/`; [package-manifest.json](package-manifest.json)
records the command, platform, base, hashes and sizes. It does not launch the
package. This is a local debug artifact, not a signed distribution or a verified
executor environment; staging it outside source and denying source/debugger
access still belong to enclosure qualification.

The following matrix is now explicit. Every cell is **unexecuted for independent
final-candidate qualification**; historical supporting captures stay linked above.
No platform, profile, or repository has been excluded by product authority.

| Platform / environment | Profile variants | Required workflow coverage | Present evidence / missing prerequisite |
| --- | --- | --- | --- |
| macOS developer build | fresh empty synthetic; existing synthetic with recorded requests; retained/expired history | overview → request → attempt; scope and UTC grouping; success, cancel/failure, recovery/resume | Current guarded test host and local read-only developer package exist. Prior code-aware PTYs support behavior. Independent package launch and probed enclosure still required. |
| Linux developer build | same three variants | same affected workflows | No allocated Linux host/package receipt in this assignment. Fable provisions under existing QA authority. |
| Windows developer build | same three variants | same affected workflows | No allocated Windows host/package receipt in this assignment. Fable provisions under existing QA authority. |
| Native credential paths where applicable | dedicated test account/VM and synthetic credentials | actual launcher/native permission behavior | Ordinary debug keyring denial cannot qualify these. No personal Keychain access permitted. |
| TensorCash | disposable repository plus fresh/existing synthetic profile | real bounded TUI repository task, then inspect recorded request/attempt estimate | Canonical origin `https://github.com/agtico/tensorcash.git`; active plan still says path/base UNALLOCATED. No path/base is silently borrowed from an old release. |
| Isometric Game | disposable repository plus fresh/existing synthetic profile | real bounded TUI repository task, then inspect estimate; relevant visual interaction | Canonical origin `https://github.com/goodalexander/isometricgame.git`; same unallocated path/base state. A release owes both repositories. |

## Execution boundary, not a request for permission

Engineering completed here: reconcile every class of open sprint item above,
prepare neutral design intent and explicit platform/profile/repository matrix,
reproduce the requested gates and alone/in-lane comparison, build and hash a local
read-only developer package, and repair inventory provenance. No engineering preparation above is left as an owner question.

The independent design, enclosure provisioning and execution themselves are
**not completed**. This worker's host tools are unrestricted, no sealed executor
is supplied, and the writable scope excludes staging an external packet or
editing canonical sprint/plan coordinates. A blind agent spawned with these same
tools would not meet the isolation contract. Only a permitted independently
probed environment can supply execution evidence. Fable can allocate that work
under existing authority without asking Travis to approve ordinary QA again.

The remaining engineering sequence is fixed by the existing contract: stage
intent and actual screenshots outside source; freeze the independent designer's
cases; build/stage the exact candidate and matching assets; have a separate
executor demonstrate all boundary denials and positive package/PTY controls;
execute frozen cases and preserve raw failures/replays; obtain independent
evidence review and run the schema-2 handoff checker; rerun receiving-tree gates
and reconcile canonical ledgers. No step here is represented as passed.

Runtime fixes, a new settlement/data boundary and live-provider spend are outside
this allocation. Only their actual authorizations belong with product authority.
This report does not request those changes or ask for a waiver to save engineering.
