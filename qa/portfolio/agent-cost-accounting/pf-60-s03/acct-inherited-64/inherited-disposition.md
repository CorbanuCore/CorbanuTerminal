# Inherited collection — none qualified

**No inherited item can be QUALIFIED from the current collection.** This uses
the brief's explicit refusal branch, not a request to waive the four items.
Gateway economics lacks an admitted gateway accounting population and a bound
economic source; startup prewarm is intentionally outside sampling observations;
auxiliary calls are exercised but deliberately add no accounting evidence; and
the accepted compact importer has no qualified legacy-source acquisition
adapter. Thus none supplies an end-to-end population, original economic inputs
and reader reconciliation for the inherited claim. Passing exclusion tests,
reopening a recently collected session, or displaying an estimate cannot fill
these gaps. All four remain open with Fable; no new product scope is requested
or implemented in this evidence-only allocation.

## Evidence and specific missing inputs

Every source excerpt below is frozen with full-file SHA-256 and line bounds in
[source-evidence.json](source-evidence.json). The named Core tests passed in
**both** current default and developer-accounting lanes, not merely an old
receipt; [test-results.json](test-results.json) and lossless gate logs record the
actual executions.

| Inherited item | What the current evidence really proves | Specific missing input needed to qualify it |
| --- | --- | --- |
| Corbanu plan gateway economics | `developer_accounting_mode` supports built-in direct Anthropic/OpenAI only. Chat eligibility explicitly rejects `is_pfterminal_plan()`. `suite::accounting_chat::accounting_chat_native_gateway_exclusion_and_header_parity` exercises success and released retry, then requires the accounting store absent. Request headers are transport evidence, not a price/settlement ledger. | A supported gateway accounting route, authoritative per-request economic evidence bound to gateway identity/account, physical attempt, model, dispatch time and currency, with the price/usage or settlement/balance semantics distinguished. Need positive collection, retry/release accounting, independently calculated values, and exact reader output; no gateway attempt or dollar binding exists in this collection. A direct OpenAI tariff cannot be substituted. |
| Startup prewarm | In `client.rs`, warmup forces deferred sampling to None; `prewarm_websocket` drains completion without recording usage. `suite::accounting_responses_ws::accounting_responses_ws_native_prewarm_preconnect_and_cached_reuse` waits with zero observations, then requires one sampling observation with input 100. Its fixture sends warmup input/output 999/999, so this is deliberate noncollection, not missing wire traffic. | An authoritative startup-prewarm attempt/owner/time/usage record and applicable economic classification or original price binding, plus a supported collector and reader. The 999/999 wire fixture is neither stored usage nor a tariff or evidence of free operation. The cached-reuse test is not startup lifecycle accounting qualification. |
| Auxiliary collection | HTTP `suite::accounting_responses::accounting_responses_native_sampling_and_auxiliary_scope` and Chat `suite::accounting_chat::accounting_chat_native_sampling_auxiliary_scope` retain two sampling records after an actual compact POST, unchanged. WS `suite::accounting_responses_ws_recovery::accounting_responses_ws_native_auxiliary_scope_and_event_parity` retains one after compaction. Metadata/event parity does not establish auxiliary cost. | A defined auxiliary operation population (including applicable compaction/memory work), physically bound request/attempt/owner identity, provider usage presence and original pricing or explicit unknowns, collected through its native path and inspected. Existing compact fixtures/rows contain no priced auxiliary contribution to sum. |
| Legacy evidence acquisition | `token_usage_replay.rs` emits restored `ThreadTokenUsageUpdated` display notifications using the latest TokenCount and turn attribution. It is not an accounting importer. The accepted compact-import receipt explicitly excludes legacy-source acquisition. Round 61's “historical” sample was generated through `one(...2026-08-08...)` in the current collector and subsequently reopened; it is not an acquired legacy record. | A supported legacy acquisition adapter and an authoritative original source corpus with request/attempt/retry/owner identity, dispatch time, raw field-presence and original-price provenance (or explicit unknowns), plus finality for sealed compact imports. Need deduplication/reopen/retention evidence from that real acquisition path, then exact reader reconciliation. Cumulative TokenCount cannot recover missing attempt identities or lost presence. |

The compact import substrate **has** been accepted. The older pre-import
“90..365-day late import remains unqualified” note is historical, so it is not
used to claim the substrate is missing. The absent piece here is acquisition
and provenance. Likewise, these limitations do not imply all providers or all
future embeddings are inherently incapable of accounting.

## Existing collection cross-check and exact reader text

The independently re-executed [scope audit](scope-recheck.json) binds four
same-day direct OpenAI/Sol attempts, their stored contribution evidence and
original prices to three roots. For each, observed input 100 contains cache-read
20, output is 10, and the frozen USD-per-million rates are 5, 0.5 and 30:

`(100 − 20) × 5 / 1,000,000 + 20 × 0.5 / 1,000,000 + 10 × 30 / 1,000,000 = 0.00071 USD`.

Four attempts sum to **0.00284 USD**. Cache-write zero adds zero; reasoning
is an output subset, not an extra charge. This is independent estimate arithmetic
for direct sampling, **not** arithmetic for one of the inherited items.
No invented amount is assigned to any of those four items.

The preserved [fresh-root selection](../acct-scope-62/scope-run-01/fresh-scope-zero-selected.json)
contains exactly:

> No recorded attempts in this day; collection coverage unknown.
> Known subtotal exact USD: 0

The same inspector also states:

> Billed cost: unavailable — no settlement evidence

Those are existing real-key observations, rechecked rather than newly executed.
The zero excludes the other roots and is the retained **P2 scope/coverage
finding**. It proves neither a day-wide zero nor zero gateway/prewarm/auxiliary/
legacy cost. Seven direct-sampling priced pages reconcile; **zero positive
inherited-item qualifications and zero measured-dollar comparisons** result.

## What remains unproven

There is no arithmetic or exact rendered positive amount available for any
inherited item, because the bound input population/economic evidence is missing.
The current 342 passing lane executions prove their actual tests, including
exclusion and parity; they do not prove acquisition, complete application
coverage, billed dollars, or successful UI routing for those items. The new
[38 rejecting page controls](page-controls-01/results.json) and
[28 rejecting amount-removal controls](removal-controls-01/results.json)
qualify the auditor's checks, not the unimplemented product workflows.
No new TUI run, independent code-blind acceptance, live-repository/platform
qualification, human acceptance or release is claimed. All prior residuals,
including two-collected-provider and settlement comparison, remain open.
