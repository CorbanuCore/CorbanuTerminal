Proposed tests only; none have been executed. The screenshots establish historical UI context, including masked token entry, provider status labels, a separate packaged-tool authentication error, and multiple indistinguishable model rows. They do not establish current defects.

“Blocker” means failure prevents a core workflow, obscures the active credential/billing route, loses saved setup, or exposes credentials. “Advisory” means a usability or resilience issue whose severity depends on the remaining usable path.

Run applicable blocker cases on macOS and Linux, including a fresh profile and an existing profile. Use designated test accounts and disposable credentials.

| ID | Priority | Starting conditions | Concrete actions | Observable expected results |
|---|---|---|---|---|
| PF58-F01 | Blocker | Fresh profile without credentials. | Launch normally; open `/providers`; inspect each route; open `/model`; cancel to chat. | App remains usable. Unconfigured routes are recognizable and offer setup. Selection status does not imply authentication health. No provider is falsely presented as ready for chat. |
| PF58-F02 | Blocker | Fresh profile; eligible OpenAI account. | Open `/providers`; choose OpenAI account login; complete the offered login; choose an available model and effort through `/model`; send a short prompt. | Login completes through the named account route; provider becomes usable; chosen provider/model/effort are clear; a response arrives without restarting the terminal. |
| PF58-F03 | Blocker | Fresh profile; eligible Claude subscription account. | Choose Claude subscription login, complete it, select a subscription model and effort, and send a prompt. | Subscription authentication succeeds; the UI identifies the subscription route; chat works without requiring an Anthropic API key or silently switching billing routes. |
| PF58-F04 | Blocker | Fresh profile; valid Claude managed subscription token. | Choose managed-token entry; paste the token; confirm; select the subscription model; send a prompt. | Entry remains masked. Token is accepted for the named subscription route, status becomes usable, and chat succeeds without restart or token disclosure. |
| PF58-F05 | Blocker | Fresh profile; valid Anthropic API key. | Configure the Anthropic API route; select a supported model and effort; send a prompt. | UI clearly identifies the API route. Chat uses that route without requiring Claude subscription login or silently substituting subscription authentication. |
| PF58-F06 | Blocker | Designated valid credentials for each supported other API-key provider; configured custom-provider fixture with known endpoint/model. | Configure each supported provider separately. Open `/model`, select its model, and send a prompt. Repeat with the custom provider. | Each supported route is reachable and usable. Custom providers/models remain identifiable. Selection and responses correspond to the chosen provider. Exact provider coverage depends on the supported-provider list. |
| PF58-F07 | Blocker | Claude subscription and Anthropic API routes both available; distinguishable test credentials or provider-side request records. | Configure both; select and chat through each; recover one credential; chat through both again. Separately attempt to enter an API key in subscription-token setup and vice versa. | Routes remain distinct. Recovery changes only the intended credential. Wrong-route input is not silently reinterpreted or stored under another route. Request evidence identifies the selected billing/authentication route. |
| PF58-F08 | Blocker | Credential-entry screen for a new provider; a separate healthy provider already configured. | Submit empty input, whitespace-only input, and an invalid credential in separate attempts. | No unusable input is reported as successful setup. Failure identifies the affected route and provides a useful correction/retry path. The other provider remains usable. |
| PF58-F09 | Blocker | Masked entry screen for each secret-bearing route. | Type and paste a distinctive test secret, edit it, submit a rejected value, and repeat with cancellation. Inspect the resulting UI and chat scrollback. | Secret contents never appear in entry rendering, confirmation, error messages, or chat. Cancelling or retrying does not replay the secret into the chat composer. Long-token paste behaves as one credential entry. |
| PF58-F10 | Blocker | Healthy configured provider with working credential and an unfinished chat message. | Start replacement/recovery; enter a different credential; press Escape before confirmation. Also cancel account login before completion. Return to chat and send a prompt. | Cancellation returns to a usable screen; the existing working credential and selection remain available; no partial replacement is reported as complete. The unfinished message is not sent accidentally. |
| PF58-F11 | Blocker | Profile containing healthy, disabled, unconfigured, checking, and expired-credential examples; expired provider is selected. | Open `/providers`; move through entries; open the selected provider’s actions; compare with `/model` and chat status. | Enabled/disabled status, current selection, and authentication health are distinguishable. The selected expired provider visibly needs attention. Its recovery action identifies the affected route. “Current” or “active” alone does not imply healthy authentication. |
| PF58-F12 | Blocker | An expired/revoked credential for the selected provider; another healthy provider; existing conversation. | Send a prompt to encounter the failure; open `/providers`; complete recovery; retry the prompt in the same process. | Error names the failing service/route and offers a usable next step. Recovery restores chat without terminal restart, conversation loss, or changes to the other provider. A retry does not send the same user message twice unintentionally. |
| PF58-F13 | Blocker | Two healthy providers; one remains selected. | Recover or replace the credential of the nonselected provider; return to chat; use the selected provider; then select and use the recovered provider. | Recovery targets the chosen provider. It does not unexpectedly replace the current selection or damage the other provider. Both remain usable through their respective routes. |
| PF58-F14 | Blocker | Multiple configured providers, including the selected provider. | Disable a nonselected provider; inspect `/model`; re-enable it. Repeat by disabling the currently selected provider and attempting chat. | Disabled providers cannot be used unknowingly. Re-enabling preserves usable setup. Disabling the current provider produces a clear, actionable state or explicitly communicated selection change; it never silently sends through an unexpected route. Exact fallback policy requires clarification. |
| PF58-F15 | Blocker | Multiple configured providers/models, including identical model display names where supported. | Navigate `/model` across providers and rows; inspect the current markers; choose a different provider/model/effort; reopen the menu. | Every selectable choice has enough information to distinguish its provider and effective selection. The resulting current provider/model/effort is unambiguous. Duplicate labels do not conceal meaningful differences or falsely mark conflicting choices as current. |
| PF58-F16 | Blocker | Two models with different supported effort choices, including a model with no configurable effort if supported. | Change effort; switch models and providers; confirm selections; reopen `/model`; send a prompt after each change. | Only valid combinations are accepted. Any adjustment to an unsupported carried-over effort is visible. Menu, chat status, and actual request behavior agree about provider/model/effort. |
| PF58-F17 | Blocker | Active conversation and saved selection. | Browse other providers/models/efforts without committing; Escape at each level; resume chat. Then explicitly confirm a new selection and continue the conversation. | Browsing/cancellation leaves the committed selection intact. Confirmation changes it clearly. Conversation remains usable and the next request uses the committed choice. |
| PF58-F18 | Blocker | Successful setup and selected model/effort; at least one disabled configured provider. | Exit cleanly and relaunch twice. On macOS use the Applications shortcut each time; on Linux use the normal launch path. Open both menus and send a prompt. | Credentials, selection, and disabled status persist. Ordinary relaunch does not repeat completed setup. Chat remains usable. Any native consent is recognizable as such and does not masquerade as missing provider configuration. |
| PF58-F19 | Blocker | Existing supported profile created before the candidate, with multiple credentials, selected model/effort, and any configured custom provider. | Launch the candidate using that profile; inspect both menus; chat; exit and relaunch. | Supported saved setup remains usable without credential-route mixing, unexpected duplicate selections, or unnecessary reauthentication. Unsupported/missing selections receive a clear recovery path without damaging the profile. Supported migration range requires clarification. |
| PF58-F20 | Blocker | macOS installation launched through Applications shortcut; controllable Keychain test credential/access state. | Complete setup and native consent; relaunch and use chat. In separate runs deny or cancel Keychain access, then allow it and retry. | Successful access permits ordinary continued use without repeated completed setup. Denial/cancellation yields an actionable credential-access explanation, preserves saved setup, and supports recovery. Credentials stay masked throughout native-access failures. |
| PF58-F21 | Blocker | Linux terminal over SSH in tmux; designated working account/key and model. | Complete supported setup using keyboard; use `/providers` and `/model`; send a prompt; detach and reattach tmux; continue chat; launch a later session. | The flow is usable in the stated environment. Any account-login handoff offers a workable path or clearly explains the required handoff. Input, selection, setup persistence, and chat survive the ordinary tmux workflow. |
| PF58-F22 | Blocker | Healthy chat provider and a packaged tool with a separately expired credential; then the inverse arrangement. | Launch; attempt chat and a harmless packaged-tool operation; recover the failing credential through the offered path; retry the operation without restarting. | Errors identify the actual failing service. Tool authentication failure does not falsely mark unrelated model authentication as failed, and vice versa. After recovery, the affected chat/tool operation works in the same terminal. Independent healthy routes stay usable. The intended tool-recovery surface needs specification. |
| PF58-F23 | Blocker | Provider checking or recovery in progress; test-controlled network failure, timeout, rate-limit response, and server error available. | Trigger each failure separately; inspect status and next steps; restore service and retry. | App remains responsive; checking resolves or can be cancelled/retried. Errors identify the affected service and distinguish authentication recovery from network, quota, or server trouble when known. Working credentials are not erased and unrelated providers stay usable. |
| PF58-F24 | Advisory | `/providers`, token entry, or `/model` open; a long provider/model name and enough entries to require navigation. | Resize from the screenshot’s 116×43 context to a narrower/shorter terminal; navigate to the last item, submit/cancel, and enlarge again. | Selected item, critical identity/status information, and available controls remain discoverable. Layout changes do not corrupt input or select the wrong row. If size is unsupported, the app communicates that limitation and recovers when enlarged. |
| PF58-F25 | Advisory | Keyboard-driven menus and credential entry open. | Use documented movement keys, Enter, Escape, and provider switching; repeat open/cancel cycles; navigate at list boundaries. | Focus follows the visible selection; keys perform the displayed actions; no unintended confirmation, chat submission, or stuck modal occurs. Keyboard-only users can finish and exit each flow. |
| PF58-F26 | Advisory | Slow health check for provider A; provider B also configured. | Start checking A; switch to B before A completes; return to A after completion. Repeat with cancelling/reopening the menu. | Delayed results update the correct provider. They do not replace the selected row, overwrite another route’s state, or leave contradictory health indicators. |

Unresolved product questions:

- Which provider routes and custom-provider configurations are officially supported, and which are required for release qualification?
- Are Claude browser/account login and managed-token entry both supported on both platforms? What is the expected SSH login handoff?
- Does “active” mean enabled, selected, or authenticated? How are these independent facts intended to appear?
- What should happen when the selected provider is disabled, loses authentication, or its saved model disappears? Is any automatic fallback permitted, and how must the user be informed?
- Which effort options belong to each model, and how should incompatible saved effort settings be handled?
- Where should packaged-tool credentials be recovered, and which tools must recover without restarting?
- What existing-profile versions/storage formats must remain compatible? What behavior is expected if credential saving fails?
- What minimum terminal size is supported, and which keyboard bindings are contractual?
- When should health checks run, what constitutes “healthy,” and when does a check time out?
- What Keychain prompts count as normal first-use/security consent, versus an unwanted repeat of completed setup?
- What independent request/account evidence is available to verify the selected credential and billing route without exposing credentials?
- Are the three visually identical historical model rows intended to represent different settings? The screenshot does not reveal a usable distinction.

Exact inputs and tools accessed:

- `/Volumes/CorbanuDrive/Corbanu/.codex-work/pf58-blind-20260910/packet/intent.md` — read using `tools.exec_command` with `sed -n '1,240p'`; returned the complete brief.
- `/Volumes/CorbanuDrive/Corbanu/.codex-work/pf58-blind-20260910/packet/token-entry.png` — inspected using `tools.view_image`.
- `/Volumes/CorbanuDrive/Corbanu/.codex-work/pf58-blind-20260910/packet/provider-manager.png` — inspected using `tools.view_image`.
- `/Volumes/CorbanuDrive/Corbanu/.codex-work/pf58-blind-20260910/packet/model-picker.png` — inspected using `tools.view_image`.
- One `functions.exec` call orchestrated the above accesses and displayed the images.

No other files, repository instructions, skills, source code, existing tests, implementation explanations, prior results, network sources, or accounts were accessed.
