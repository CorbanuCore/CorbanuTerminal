# 20. Continue your ordinary session

In the normal Permissive test session, inspect `/permissions` and cancel without changing it. Use `/model` to switch between already-configured providers, send a harmless message, then restart and repeat `/security`.

Pass: ordinary provider, permission and session behavior is unchanged. Viewing profiles creates no policy change that survives restart. Do not edit your real config to manufacture a protected-mode test.

