# 15. Check API-key and browser guidance

Inspect an API-key provider's entry screen, then cancel. If an OpenAI account setup action is available, open it and inspect the login link and device-code guidance, then cancel.

Pass: API-key guidance identifies its environment variable and credential storage, and says it is never added to chat. The OpenAI link remains clickable when wrapped, and explains signing in from another device. No repeated keychain prompts appear during this pass.

