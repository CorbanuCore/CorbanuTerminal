# 26. Recover according to the credential source

Press `r` on an environment-backed provider, an external command/AWS provider, and a local provider where configured. Repeat in a narrow terminal window.

Pass: clear, readable instructions identify the owning shell/tool or local model server. Corbanu does not offer a fake browser login or a vault override for an environment credential. “Configured” does not claim a successful remote validation.

