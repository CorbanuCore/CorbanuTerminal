# Frozen 26-case dispositions

The original design is unchanged. “Targeted pass” below is not a whole-case pass
where a broader prerequisite remains. No exclusions have been silently approved.
Final package identities and per-platform outcomes are in the adjacent receipts.

| Case | Evidence and remaining boundary |
|---|---|
| F01 | Targeted inventory, unconfigured custom entry, repeated browse/cancel and no unintended requests pass. Fully empty-profile/all-route inventory remains. First-run onboarding exposes separate OpenAI account/API actions with aggregate provider status; this is not proof that each method has a valid credential. |
| F02 | User-completed fresh OpenAI device login, model/effort selection and same-process live reply passed on the predecessor Mac package. Final-package retained-profile evidence is separate; no fresh Linux live-account login is claimed. |
| F03 | Synthetic Claude login/cancel/recovery and compatibility paths pass. Real managed-token setup works; a fresh Claude CLI/browser-login variant remains unverified. |
| F04 | Fresh masked managed-token entry and same-process Claude reply passed on the predecessor Mac package. Final-package retained-profile evidence is separate. |
| F05 | Independent Anthropic API form/catalog is exercised with a synthetic key. A designated live Anthropic API key is missing; no subscription token was substituted. |
| F06 | Custom-route setup and requests pass, including same-model distinct providers. Final Mac Ambient and DeepSeek each pass two live launches/replies. Other unconfigured API vendors still lack designated live credentials. |
| F07 | Scoped credential replacement and unaffected-route requests pass. Complete crossed subscription/API input and live billing-route matrix remains. |
| F08 | Empty/whitespace rejection, masking/cancel, stored-but-unverified wording, real 401 failure and repair are covered. Credential storage is deliberately not remote validation; the stronger “no invalid value reported as successful setup” expectation still needs contract clarification. |
| F09 | Synthetic secret canaries remain absent from captured entry/history/error evidence; designated real token was masked. Every secret-bearing route's long-paste/edit/rejected-value variant is not claimed. |
| F10 | The expanded journey cancels replacement, then completes a separate repair and sends using the repaired credential. It does not send between cancellation and repair, so this receipt alone does not prove original-credential preservation. Full unfinished-composer/account/key combinations remain. |
| F11 | Named rejected-provider health, configured-versus-verified copy and unaffected-provider state are tested. Complete transient checking/state matrix remains. |
| F12 | Selected managed-key 401 → cancel replacement → repair → same-process successful request, without duplicate sending, passes. Captured warning names the actual provider. Environment-owned credentials still require their owning environment to change; no silent import/restart waiver. |
| F13 | Expanded journey explicitly repairs nonselected B, requests on unchanged A, then selects B and requests with B's replacement credential. Candidate-bound captures/request records prove both routes. |
| F14 | Expanded journey covers disabled nonselected persistence, reactivation, current-provider disable cancellation, explicit replacement and requests, then reactivation without credential loss. |
| F15 | Custom rows now include provider IDs; selecting the no-effort route preserves that ID. Expanded journey checks one current marker, persisted provider and the actual request route; existing catalog tests cover native provider tabs. |
| F16 | Requests prove Astra High, Sol Low and the no-effort custom model; picker and request state agree in those tested combinations. This does not exhaust all advertised models/efforts. |
| F17 | Picker and effort cancellation preserve configuration and subsequent routing; explicit confirmation changes the next request. Full browse-at-every-level matrix remains. |
| F18 | Two restarts preserve selection, disabled provider and working requests. Applications-specific launches and controlled native-consent variants remain separate human prerequisites. |
| F19 | Existing-profile native/live and synthetic upgrade/restart evidence cover representative profiles, not an unspecified universal migration range. |
| F20 | User reported no noticed prompts during the earlier four native launches; eight final-package direct launches also replied without agent-entered passwords. This is not controlled consent/deny/cancel evidence. The exact replacement's Applications-path check remains a limited human check; no ACL changes were made. |
| F21 | Linux SSH TMUX setup, menus, requests, real PTY attach/detach/reattach and subsequent session use are exercised with synthetic working credentials. A fresh live-account Linux handoff is not inferred. |
| F22 | Canonical-origin Apps expiry → unrelated healthy chat → account recovery → actual MCP tool result in the same process passes. The old off-origin safety test also remains. The inverse arrangement and every separately authenticated tool are not claimed. |
| F23 | Visible 403/429/500 responses do not poison credential health; explicit retry after restoration works. 401 is separately scoped and repaired. Transport disconnection/timeout and every checking race remain. |
| F24 | Advisory: provider manager resize at 80×24, 40×18 and 140×44 reaches the last custom row and returns to working chat. Long-name/all-popup layouts remain. |
| F25 | Advisory: repeated keyboard open/cancel, provider navigation, recovery, selection, resizing and reconnects pass. Every advertised key/boundary combination remains. |
| F26 | Advisory: a real delayed response arriving with another provider row selected preserves focus. Stale credential attempts are unit-tested. All visible async setup/health races are not claimed. |

Other human-guide prerequisites: package Code Mode/shell/MCP, concurrent sessions,
restart and Escape interruption are checked by the separate exact-package harness.
These are real prerequisites, not substitutes for credential/billing/native cases.
