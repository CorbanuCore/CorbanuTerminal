# Flash recovery review follow-up

User-authorized bounded repair following `corbanu-flash-retry-fix-code-review-2026-09-10.md`. Product specification **Corbanu API — TO BUILD**: “Reservations, settlement, idempotency, and insufficient-balance enforcement remain atomic and server-authoritative.” Worktree `/mnt/HC_Volume_101713660/pfrpc/scratch/corbanu-wallet-launch-hotfix`, base `c6b48e6b36`; API base `6b39fa5` at `/home/pfrpc/repos/CorbanuAPI`.

- Finding 1: preparation failures release and signal the exact identity. Errors after inference dispatch cannot fall back to releasing a reservation when settlement fails. The signal also requires explicit upstream rejection, rather than assuming an absent response means inference never ran.
- Finding 2 corrected: legacy duplicate reservations previously returned `authorized`, allowing repeated upstream work without a new settlement. Both stores now distinguish duplicates; inference rejects them before dispatch. An explicit rejection and pre-dispatch credential failure carry the shared release signal. Deep research preserves its existing atomic claim/replay for pending reservations, while rejecting terminal reservations without a matching job.
- Finding 3: the existing retryability rule is retained. The client test covers 80 combinations of header, status, reservation state and identity match, including non-retryable 400/401/403 and retryable 429/503.
- Finding 4: the catalog test pins Flash's parallel capability to false, and a request serialization test verifies that function tools and High reasoning remain present while the unsupported parallel control is absent.
- Finding 5: both committed incident records are valid parsed JSON.
- Finding 6: prices are unchanged in this follow-up. API documentation records that picker prices are bundled, while the gateway owns the effective price version; dynamic picker-price synchronization is not claimed.
- Finding 7: shared request-header constants and mutex-poison recovery replace repeated names and lock panics.

Automated validation: 256 focused Rust tests; 91 gateway tests plus 16 PostgreSQL tests passed. PostgreSQL ran against a fresh, uniquely named `_test` database in the existing local PostgreSQL container; that disposable database was dropped afterward. Type checking and API build passed. Tests cover concurrent duplicate exclusion, released and settled duplicates, preparation recovery, post-dispatch settlement failure, uncertain transport, and existing deep-research behavior. The original production incident row remains unchanged at zero charge.

Final build and true PTY passed using the existing `corbanu-debug` launcher binary. Gateway release 35 is complete with both machines healthy (API commit `4ff7d52`). The final run deliberately threw during preparation before upstream dispatch, then completed two real upstream calls: statuses `[503, 200, 200]`. Actual tools remained present and `parallel_tool_calls` was absent on every terminal request. The TUI ran `printf CORBANU_TOOL_OK`, returned the tool output to the model, rendered the final marker and retained Flash after restart. Synthetic charge: 2,392 microdollars, exactly matching upstream receipts; no production customer ledger writes. The temporary gateway process and forwarding connection were stopped.

Evidence: [flash-review-followup-2026-09-10](flash-review-followup-2026-09-10/), including final executable SHA-256, PTY script/captures/result, billing and test summaries. This updates the existing local `corbanu-debug` candidate and does not publish a Terminal release.
