# Flash request compatibility and retry recovery

Bounded reliability fix for the user-reported failure of the newly authorized Flash route. Product specification **Corbanu API — TO BUILD**: “Reservations, settlement, idempotency, and insufficient-balance enforcement remain atomic and server-authoritative.” **Shipping MVP — LIVE / Multi-provider inference** authorizes the existing provider workflow. Worktree `/mnt/HC_Volume_101713660/pfrpc/scratch/corbanu-wallet-launch-hotfix`, base `91995c339c`.

The user's request `d01af978-5540-4856-9790-6a47b521bdad` received upstream HTTP 503 at 19:33:21 UTC, then terminal HTTP retry with the same identity received 409 / released at 19:33:22. A representative request captured from the actual TUI reproduces 503 through the pinned upstream route when `parallel_tool_calls` is present; removing only that control produces a successful stream and tool call. Removing strict flags instead still fails. The previous live canary omitted the parallel control and the previous PTY used a synthetic response. Those separate tests did not establish compatibility of the full terminal request with the real gateway.

The API now declares the route's parallel-control capability and omits the unsupported field at the upstream boundary. The Terminal catalog disables parallel tool calls for this model. For explicit upstream rejection, the gateway returns its completed released-reservation state in a response header. The HTTP retry path rotates identity only when that header confirms release of the exact sent ID; settled, reserved, unconfirmed, mismatched and transport-uncertain requests retain their identity. The existing retry limit, delay, and duplicate ledger protection remain in effect. No ledger migration or reactivation of released reservations is involved.

The live recheck also found that the endpoint catalog and receipt now price this route at $0.22/M input, $0.007/M cache reads and $0.66/M output, plus the existing $0.0001 request fee. Version `at-cost-2026-09-10.2` and the terminal description use those rates. Prior evidence records retain their original observed prices; historical reservations are not rewritten.

Final validation passed: 261 focused Rust tests (including the API client suite), 39 gateway tests, API typecheck/build and final Terminal build. The reviewed picker snapshot reflects current pricing. True PTY with the final binary passed dropdown/cancel, High reasoning, forced rejection and identity rotation, real `exec_command` execution of `printf CORBANU_TOOL_OK`, a second live model request containing the tool result, final rendered answer and restart persistence. Gateway request statuses were `[503, 200, 200]`. Actual synthetic balance charge was 1728 microdollars, exactly equal to rounded upstream receipts. Additional exploratory runs recovered actual upstream 503s both before and after tool execution; the first harness assertions incorrectly assumed those extra transient failures could not occur and were corrected before the final run.

The gateway is deployed as release 34, API commit `6b39fa5`. The final rebuilt binary is already launched by `corbanu-debug`. Your failed production request was verified read-only as released with zero charge. Evidence: [deepseek-flash-recovery-2026-09-10](deepseek-flash-recovery-2026-09-10/). The isolated live QA process uses the deployed gateway code and actual Vercel inference, an in-memory synthetic balance, and a forced first upstream rejection to exercise recovery before a real terminal tool round trip. No production customer ledger writes are made by that process. This remains an unpublished `corbanu-debug` candidate.
