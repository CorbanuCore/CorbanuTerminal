# Team Context available in Corbanu debug

Implementation: Terminal `721d6f974d`, branch `fix/tasknode-team-context`; Task Node `39e051c`, deployed web release 737. User explicitly authorized adding the existing Team Context report to Corbanu. Product-initiative extension PF-76 under the active Task Node integration plan; product heading **Shipping MVP — LIVE**, **Task Node and identity**: “Tasks, evidence, verification, rewards, balances, chat, context, linked identity”.

`/tasknode team` and the Task Node menu's Team Context entry open a report menu with Read full report and Refresh. The read-only pager shows structured contributor summaries, shared rewarded-task counts, report time, previous-report status and the personal-context preference. The CLI is `corbanu tasknode team context --json`. Embedded Task Node agent guidance now documents the same command. Session scope and asynchronous response guards are reused; private context documents and new sharing grants are outside this change.

The existing `/home/pfrpc/.local/bin/corbanu-debug` launcher points at this worktree's final `codex-rs/target/debug/corbanu`; it still resolves the user's existing home. Restart `corbanu-debug --yolo` to use the new binary. Local QA used the exact same executable with synthetic credentials in isolated homes. Its source is a local 0.1.42 debug candidate; the public release workflow was already pinned to the earlier release source and does not include this subsequent feature.

Final automated checks: 10 CLI tests, 9 TUI tests including the reviewed report/menu/error snapshots, and 5 embedded-skill tests passed. Backend checks in a fresh disposable PostgreSQL database passed route auth, read-only methods, web parity, foreign-account arguments, feature gating, stale report grant revocation and session revocation. That database was dropped. The live deployed modules passed a synthetic-session probe with a temporary runtime store and database disabled; no customer data or inference was written.

The final binary passed real keyboard workflows in the existing isolated release QA worktrees for TensorCash (`dd6e92024254090de0f596b090bd5c74c4d97b90`) and Isometric Game (`a5427fc7a44c59b9985462e19603210ca649475f`). Keys covered `/tasknode`, selecting Team Context, Enter on Read full report, scrolling, Esc, refresh, previous report, 503 failure/recovery, canceling a delayed response, `/tasknode team`, exit and restart. CLI JSON equaled the fixture report and the updated embedded guidance was installed on startup. HTTP identity/report fixtures were used; these are real TUI checks, not live reads of the user's collaborator data. No repository changes or model calls occurred in the workflows.

An initial PTY attempt ran before the binary link completed; another correctly returned to the parent Task Node menu after cancellation, and the harness needed to close that menu before typing the next slash command. Both were corrected before the final complete runs recorded here. The broader benchmark cycle remains incomplete and named-human acceptance is pending the user's local test. The seven inherited sprint-documentation errors are unchanged.
