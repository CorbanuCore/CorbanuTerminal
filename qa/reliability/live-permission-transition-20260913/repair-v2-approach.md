# PF83 review remediation cycle 1: bounded approach

Sole source owner: fresh Astra High implementation context. PF-83-S01 remains
in_progress in the allocated worktree at base005cc644f59b1e762e5497b329e106c67925d4ed.
Product initiative; Permission selection confirmation — TO BUILD: “A submitted
selection is not a confirmed change.” Only the two confirmed Fable01 findings
are repaired. Parent owns plan/sprint bookkeeping and review/execution gates.

Completion requires the correlated Applied reply plus an observed selected
profile (an already matching session snapshot covers no-op). Both arrival orders
must work. Carry the latest effective permissions into App defaults/runtime
overrides; never install a saved requested snapshot over a newer observation.
Missing observation has a bounded uncertain recovery. Stale IDs/thread results
cannot mutate the current session. Held initial input resumes only for a valid
matching success with normal gates; otherwise restore it without submission,
preserving the composer, image placeholders, mentions, pastes and queues.

Fixture coverage: native embedded app-server both directions/no-op/errors;
deterministic TUI event ordering for built-in and named profiles, newer effective
observations, duplicate/stale/error outcomes, and initial-input success versus
non-submitting recovery with complete payload and queued drafts. These prove
local behavior only: native Windows, actual old binaries, independent real-key
execution and evidence review remain parent-owned prerequisites.

Preserve all v1 artifacts and failed attempts. Format before final affected
exact-tree `just test` using pinned1.95, locked/offline and the existing private
target. Measure <=750 non-test/<=1650 total changed hand-authored Rust lines;
generated schemas separately. Freeze v2 patch, manifest, commands/results and
gaps separately. No Core/protocol repair, source jobs left running, commit,
stage, push, merge, install, live app/profile/launcher/credential changes or
portfolio resumption. Fresh Fable review of v2 remains required.
