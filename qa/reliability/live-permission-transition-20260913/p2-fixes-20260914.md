# PF-83 P2 review follow-up — September 14, 2026

Action: `pf83-p2-fix-01`; claim: `7ce8e91e-88b4-4796-99d5-d8562fb18751`.
Allocation digest: `a7a442f64d2739555db851f9f61a59c66e91ec6ba79b71051552589b1ec43d58`.
Worker: gpt-6-astra, high; receiving manager: Fable.

## Scope and authority

This is regression verification of the already-authorized PF-83 repair, with no
new runtime behavior. It supports product initiative
`docs/plans/active/p0-security-levels.md` (active), PF-83-S01 (in_progress),
specifically its remaining review-correction task.

Product citation: **Permission selection confirmation — TO BUILD**,
`docs/corbanu-product-spec.md`: “A submitted selection is not a confirmed change.”
The same heading requires: “Error, disconnect and restart recovery must not claim
success or retry an uncertain change blindly.”

The explicit frozen worker assignment supplies these checkout coordinates:
- Worktree: `/Volumes/CorbanuDrive/Corbanu/worktrees/bootstrap-pf83-p2-20260914`
- Branch: `bootstrap/pf83-p2-20260914`
- Base: `1fdb3fc1d85fdfaa041577146da0f0de0c1c3704`

The plan/sprint still record the earlier repair checkout; manager reconciliation
remains outside this six-path writable allocation. The sprint checker passed
(116 current, 126 archived); this does not validate the new checkout allocation.
The initial working tree was clean.

## Findings and disposition

Review input: `/private/tmp/pf83-review.RsSlTe/review.json`, read before source
inspection. Both reviewed regressions were already repaired in the assigned
base, by ancestor `03503976785551a5f21df399fa71e7298b460616`
(`fix: confirm permission changes and preserve next-turn state`).

1. **Confirmed selection lost by /new or fresh thread start — fixed in base;
   regression coverage expanded here.**
   `finish_permission_confirmation` adopts the observed effective permissions
   and reviewer into App config and records both runtime policy overrides only
   after a correlated Applied result and matching-thread observation.
   New test `permission_confirmation_dispatch_preserves_policy_for_fresh_threads`
   routes completion through `handle_event`, exercises Full Access → workspace
   → Full Access, asserts App config and runtime overrides, reloads config using
   the /new path, and verifies the complete permission profile, active profile,
   and approval policy returned by embedded-server fresh thread starts.
2. **Held initial prompt orphaned after selection — fixed in base;
   dispatcher regression coverage expanded here.**
   The completion helper already calls
   `submit_initial_user_message_after_permission_confirmation`, which preserves
   setup/suppression and active-turn/queue gates. Failure, unsupported, uncertain,
   and superseded outcomes restore held input without automatic submission.
   The existing `permission_confirmation_held_input_resumes_only_after_matching_success`
   test now routes terminal completion through the real App event dispatcher.
   It retains assertions for exactly one successful submission, restored input
   on unsuccessful outcomes, stale-reply rejection, notification ordering,
   and no duplicate submission.

The only production-file change is a dispatcher comment documenting those
existing completion responsibilities. No redundant input submission or
optimistic policy mutation was added.

## Verification

Read `docs/development/test-isolation.md` before the campaign. Only guarded
`just test` is used, with disposable profiles, debug native-keyring denial, and
locked/offline dependencies. No live profile or credential files are read.

Formatting was limited to the edited test file with pinned Rust 1.95 rustfmt:
`rustfmt --edition 2024 --config skip_children=true,imports_granularity=Item tui/src/app/permission_confirmation_tests.rs`.
It passed, with the stable-toolchain warning that imports_granularity is nightly
only. A repository-wide `just fmt` could write outside the explicit six-path
scope and was not run. `git diff --check` passed before testing.

From this checkout's `codex-rs/`:
- `just test -p codex-tui -E "test(permission)" --locked --offline`
  — attempt 1: build failed (exit 101), zero tests executed. Three redundant
  `.into()` calls in the new test caused E0283 inference errors. Removed those
  calls and formatted before retrying the identical guarded command.
  — attempt 2: exit 100; 91 tests run, 87 passed (2 reported leaky), 4 failed,
  4019 skipped. Run ID `11558f70-9d2c-493f-a02e-9d1c81243ac0`.
  The new fresh-thread test expected symbolic workspace paths while the server
  returns resolved paths. Its expectation now uses the effective profile, as
  the existing native confirmation test does; requested App profile and explicit
  active-profile/approval-policy assertions remain. Formatted before replay.
  The held-input dispatcher regression passed.
- `just test -p codex-tui -E "test(permission_confirmation)" --locked --offline`
  — final focused replay: exit 0; 9 tests run, 9 passed, 4101 skipped;
  9.129 seconds. Run ID `853204f9-b500-4e52-a1a9-f30c9a3c8676`.
  Both dispatcher regressions and the existing native confirmation, ordering,
  stale/failure, unsupported-server, deferral, and outcome-snapshot cases passed.
  No native credential prompt was observed in this campaign.

The broader selector also failed three unchanged tests outside the writable scope:
- `chatwidget::tests::guardian::guardian_approved_request_permissions_renders_request_summary`:
  snapshot differs by the added `via ambient` provider label.
- `status::tests::status_snapshot_shows_auto_review_permissions`:
  snapshot expects v0.1.38, binary reports v0.1.42.
- `suite::provider_convergence::tmux_permission_reload_preserves_active_runtime_over_saved_default`:
  failed; detailed diagnosis remains with the receiving manager.

Nextest's existing local profile automatically retries failures once; both
attempts remained failures in that broader run. Its two passing-but-leaky cases
were `converts_request_permissions_into_granted_permissions` and
`native_spawn_config_inherits_active_thread_permissions_not_stale_app_defaults`.
The two generated `.snap.new` files are test outputs, not accepted snapshot
updates; their mismatches are preserved above. These broader failures are not
represented as a passing permission suite.

## Handoff limits

This is an implementation/regression-test return to Fable, not a human-test or
release qualification. Exact-package independent code-blind execution, evidence
review, real-key TUI workflows, TensorCash/Isometric Game qualification, and
named-human acceptance remain with the existing PF-83 integration gate.
No new behavior or UI wording was introduced; existing outcome snapshot coverage
remains applicable. No release, installation, push, or benchmark claim is made.
