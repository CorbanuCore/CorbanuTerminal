Independent designer: Franklin the 2nd, 01a0a0fe-2b65-7df0-b9f1-33057792f779.
Fresh context; intent-only, no screenshots. Original response frozen before candidate execution.

Design only, based solely on the stated intent. No repository, files, tools, code, prior findings, or implementation context inspected. No execution performed. No screenshots supplied; visual layout and clipping cannot be assessed.

Freeze this synthetic fixture before testing: current directory **P**; another directory **Q**. Saved sessions **A** (OpenAI/P), **B** (Claude/P), **C** (Fable/P), **D** (OpenAI/Q), **E** (Claude/Q). Each has a unique searchable title and distinct historical context marker. Active chat **X** uses OpenAI and has a recognizable transcript and unsent draft. Fixture UI checks use no credentials or inference. Reset to the specified starting state for each case.

**P0** = core continuity, privacy, or unintended-action risk. **P1** = essential picker usability.

| ID | Priority | Frozen setup | Actions | Expected observable result | Limitations |
|---|---|---|---|---|---|
| FT-01 | P0 | Active X in P; picker closed; current-directory scope. | Open `/resume`; inspect candidates. | A, B, C are discoverable regardless of X’s OpenAI provider. D and E are excluded by directory scope. Opening the picker leaves X recoverable. | UI evidence establishes visibility, not backend provider support. |
| FT-02 | P1 | Picker open in P; empty search; A/B/C available. | Type a title fragment unique to B; edit it using backspace; enter a guaranteed no-match query; clear it. | Search receives the keystrokes, filters consistently, shows an understandable empty state, and restores eligible candidates when cleared. No search text enters or submits the chat. | Does not prescribe fuzzy matching, case sensitivity, or search over transcript contents. |
| FT-03 | P1 | Picker open; visible A/B/C; no search filter. | Use the presented keyboard navigation; move to both list boundaries; filter to one result, then zero; attempt selection in the empty state. | Focus and selection remain coherent. Selection identifies the visibly selected session. Empty results cannot activate a stale or hidden candidate. | Wrap versus stop at boundaries is unspecified; either is acceptable if consistent. |
| FT-04 | P0 | Active X with transcript and draft; picker opened; B highlighted after searching. | Cancel using the advertised cancel control; resume editing X’s draft. | Returns to X with transcript, provider, and draft preserved. Picker search/navigation does not contaminate the draft, select B, or send a message. | Draft preservation applies to cancellation; selection-related draft policy is unspecified. |
| FT-05 | P0 | Reset active X separately for each target A, B, C. | Open picker; select target; inspect restored history and provider; type a new unsent message. | Correct session and context marker appear; saved provider remains associated with it. Composer accepts input. B/C are neither omitted nor silently converted to OpenAI. | Provider display and restored transcript cannot prove the actual request destination or model-visible context. |
| FT-06 | P1 | B has been resumed; picker closed. | Reopen picker; search for C; cancel; reopen; choose A; reopen once more. | Each opening is usable; cancel returns to B; choosing A restores A. Subsequent picker state is internally consistent, with no stale selection, dead input, or lost focus. | Search/scope reset versus persistence is unspecified; visible state must match behavior. |
| FT-07 | P0 | No active chat; startup environment in P defaults to OpenAI. Reset separately for B and C. | Invoke the startup resume command; search/navigate to target; choose it; type a draft. | Startup picker supports selection and enters the chosen saved chat with its history and saved provider. No preliminary inference or credentials are required for these fixture UI operations. | Exact command spelling is unspecified. Actual continuation requires FT-12. |
| FT-08 | P0 | No active chat; startup resume picker open. Run once with eligible saves and once with an empty fixture. | Type/search if available; cancel; then start a fresh chat or invoke startup resume again. | Cancellation reaches a clear, usable state or exits cleanly. Recovery works through the appropriate subsequent action. No session is chosen and no message is sent accidentally. | The intent does not determine whether startup cancellation exits or opens a fresh-chat screen. |
| FT-09 | P0 | Picker in P with current-directory scope. Repeat from active chat and startup. | Switch to all directories; search for E; switch back to current directory; clear search. | All scope exposes A–E, including cross-provider E. Current scope exposes only A/B/C; E cannot remain an actionable stale selection. Scope is visible and filtering respects both scope and query. | Does not prescribe whether scope persists across separate invocations. |
| FT-10 | P0 | X active; B is the intended selection. Freeze two input streams: opening `/resume` immediately followed by search text; selecting B immediately followed by another confirmation key and text. | Deliver each stream rapidly, without waiting for the next screen. | Opening-transition input is handled coherently. Selection happens once. Queued confirmation does not send a chat message; trailing input cannot silently change the chosen session or provider. Chat remains editable afterward. | Requires controlled input timing; this design does not prove actual event-queue behavior. |
| FT-11 | P0 | X with its draft; picker open with B highlighted. Repeat with synthetic sessions subject to an existing history privacy restriction. | Rapidly cancel, then issue queued navigation/confirmation keys and text. Reopen picker, search, change scope, and cancel again. | Cancellation cannot select B or submit X’s draft. Recovery remains usable. Picker interactions preserve existing history restrictions; restricted history is not newly exposed through search, previews, or scope changes. | Freeze the actual privacy policy before execution. No particular retention, deletion, or visibility policy is supplied here. |
| FT-12 | P0 | Separate authorized backend test; saved OpenAI, Claude, and Fable chats each contain a unique earlier fact absent from the new prompt. | Resume each from a different current/default provider where applicable; ask a follow-up requiring that earlier fact. | Verifiable request evidence shows the saved provider receives the appropriate prior conversation and new message; the response continues the selected session. No silent provider substitution or unexplained context omission occurs. | **Requires real backend proof**, credentials, and inference—or trusted backend request evidence. Fixture UI, labels, and a plausible answer alone cannot establish this. |
