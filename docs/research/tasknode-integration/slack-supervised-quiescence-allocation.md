# PF-80-S01 — real owned listener supervision

Manager allocation, September 12. Existing product initiative, specification
**Internal delivery control — TO BUILD**: the matching Slack thread must log the
answer and route it through a verified identity/revision-aware manager handoff.
This is the concrete runtime successor, not another proposal-only assignment.

## Acceptance and exact ownership

Recovery review07 completed0 with no findings. Exact five-file worker0faa96e16
receiveda89a48548f644a64cbb8cd9da090b5b75578c922; receiving251 tests passed
144.128s. Original failures and reviews01–07 stay preserved. Recovery526total/
242non-test fits its600/260 ceiling. No review08 was spent on unchanged code.
The accepted recovery API still refuses without an actual quiescence hook.

James, existing native Astra High worker, sequentially moves to
`/Volumes/CorbanuDrive/Corbanu/worktrees/slack-supervisor-20260912`, branch
`workstream/slack-supervisor-20260912`, base
`a89a48548f644a64cbb8cd9da090b5b75578c922`. Prior workers remain frozen history.
Manager owns canonical plans, shared registration, publication and integration.

Exact five writes:

- scripts/initiative_control/decision_manager.py
- scripts/initiative_control/slack_transport.py
- scripts/initiative_control/test_decision_manager.py
- scripts/initiative_control/test_slack_transport.py
- qa/initiative-control/pf-80-s01/decision-projection/slack-supervised-quiescence-receipt.md

Target700total/340conservative non-test; STOP850/400, measured additions plus
deletions from the base, including the receipt and fixture migrations. Manager
accepts the narrow above800 ceiling because one process-lifetime protocol needs
inseparable real-child crash/race/EOF/SDK-close proof; it is not inherited from
prior exceptions. Report before exceeding scope or ceiling; never compress proof.
One new independent Astra High review08 plus one necessary correction review09
is authorized; preserve prior usage and classify substantive recurrence.

## Accepted runtime contract

Implement the private proposal's ManagedListener retained Popen owner and framed
foreground supervise-listener command. Fixed pinned Python exec/module, private
inherited pipes, one child only, no arbitrary command or PID. Start is explicitly
live-gated; stop/inspect/repair/status do not load credentials or SDK. Existing
finite listen must use the same real adapter. Keep ongoing lifetime separate
from per-command bounded framing; EOF is an owned shutdown request.

The child owns a0600 process-lifetime `.listener.runtime.lock` until process
death. Optional schema2 runtime_guard pins version1/device/inode, created and
fsynced only at first transport initialization before any SDK constructor.
Every entry must require a genuine internal child capability; no unguarded
Transport.listen fallback. Parent closes its spawn duplicate without LOCK_UN.
No child cleanup/finalizer may unlock while SDK callbacks could survive.

Stop requests graceful shutdown5s, then terminate/wait2s, kill/wait2s using only
the retained owned handle. Actual death/reap, not a child stopped frame, is proof.
Child watchdog observes controller EOF and has a monotonic forced-exit deadline
if SDK close hangs. No descendants or guard inheritance beyond this child.
Quiesced context stops/reaps before store locks, acquires the exact guard
nonblocking, and holds it across snapshot revalidation and complete recovery.
Its root/binding/descriptor-bound None-returning hook expires on context exit.

Manager-operation serialization → runtime guard → store → transport → nonblocking
lifecycle owner is the lock order. Never wait for guard/stop under store locks.
After controller loss, acquire only the same verified birth-time guard; unknown
busy holders stay held, never signal a guessed PID. Missing/replaced guard or
legacy initialized store without birth pin stays UNKNOWN, including epoch0:
SDK threads can predate Session persistence. No retrofit/adoption/reset flag,
answer copying or fabricated clean root. A future legacy cutover needs positive
external process-boundary proof. No live listener has yet been qualified.

Preserve all canonical decisions, answers, native submissions/ACKs, permits,
loss incidents, historical alert refusals and current explicit gap gates.
Guard acquisition proves quiescence only, never authorization or reconciled
history. No service/scheduler/install, native Rust/dependency, credential-store,
shared control.py/status/feed or Task Node/public-beta changes.

## Required actual evidence and handoff

- Real pinned SDK queue/executor callback with old ingress descriptor, inactive
  lifecycle lease, concurrent stop/repair: no mutation before actual child death.
- Hung close, fake stopped frame, controller death before/after ready/renewal/
  stop/reap; EOF watchdog, SIGSTOP honest hold, real forced wait and exclusion.
- Two controllers and direct listen vs repair; rejected starts construct no SDK.
  Wrong binding/root/fds, inheritance leaks, symlink/replaced/missing guard,
  legacy epoch0 pre-Session threads and partial initialization fail closed.
- Spawn/pipe/fsync/read/write/wait/kill failures retain exclusion or prove death;
  seven accepted recovery crash boundaries run through the REAL scoped hook.
  Re-entry keeps incident identity and old journals; no new authority from repair.
- Preserve every251 baseline assertion, full pinned Python suite with source
  frozen during exports, Facilities Node tests, governance and exact diff/hashes.
  Run formatting before final proof. Receipt records failures, actual processes,
  commands, counts, exits, size and unproven live/phone/native-ACK limitations.

The manager accepts internal-only N/A for this default-OFF implementation
increment's integration, not an operator-ready handoff or plan-wide exemption.
Before supervisor/recovery is advertised usable, independent permission-isolated
code-blind execution under policy1.7 is mandatory: frozen human cases, exact
opaque package/service boundary, real executor-and-child negative probes,
schema2 receipts and separate evidence review. A readable Python module packet
does not qualify as binary-only. Existing unit/loopback tests are supporting
evidence only. Manager owns sandbox provisioning and actual Slack/phone/restart/
CAS/native-agent ACK qualification; no new user approval is needed for allocation.
