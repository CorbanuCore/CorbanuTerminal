# PF-80-S01 — decision feed transport and dashboard wiring

Allocated September12 after corrected decision candidate4ff73485c was reviewed
clean and integrated ataa3f9e2c15b4cd84de28f0900f4055a6abcee80d;
combined100Python tests pass2.809s. Product **Internal delivery control — TO BUILD**:
“Show blockers, rendered sprints, human test plans, machines, run logs and freshness.”
Same TaskNode worktree/branch in active plan and S01. Base is receiving above;
manager records clean launch descendant. No fourth initiative or successor.

## Literal worker writes

scripts/initiative_control/decision_feed.py (new transport helper),
test_decision_feed.py (new), export.py, activate.py, control.py, status.js,
test_control.py (only fixture compatibility if necessary), and
qa/initiative-control/pf-80-s01/decision-projection/feed-receipt.md.
Accepted decisions.py, attention.py and their tests remain frozen. No other
files/state/wrappers/services/native code or shared plan/sprint edits.

## Exact behavior

Connect existing validated records to export/activation/collect/overview/health.
Input is manager-owned private state/decisions.fixture.json through accepted
validation, not agent histories or raw logs. Do not create/edit real input.
Only validated publication-safe data may be exported; missing or invalid input
is explicit unknown, never assessed empty. Do not infer decisions from prose.
Retain full stable history and original assessment/revision; export/render/
server timer must never refresh them. Reuse20minute decision freshness.

Pin canonical feed payload identity/digest alongside source provenance; verify
local input unchanged in export and existing --verify path, and transferred
bytes before activation and collect. Include explicit missing/invalid states;
old manifests lacking feed metadata mean unknown for compatibility.
Prefer one immutable snapshot in each source export, referenced by manifest,
so activation cannot mix a new source with an old mutable decision file.
No arbitrary paths; exact fixed artifact only, bounded bytes, safe read errors.
Do not add files to source-hash verification that cannot resolve in local repo;
use a separately verified feed record when input is state-owned.

Prominent Needs your decision section above workstreams uses accepted renderer,
including exact current/historical sprint links, full accordion context and
resolved history. Corpus remains only existing approved sanitized published
documents; missing links explain absence, no recursive evidence inclusion.
Health/manifest show feed identity/assessment/unknown or stale state independently
of collection and publication times. A page left open must visibly age decision
freshness without suggesting a refreshed assessment; unknown remains unknown.
Slack not connected, answer in manager task, no forms or implied delivery.
Preserve Facilities, operations notices, activity labels, guarded serving,
last-good publication, source/config drift checks and disabled posting.

## Evidence and boundaries

Target<=700/hard800changed,500non-test including receipt; count before expansion.
Test actual fixture export -> verified activation (service calls stubbed) ->
collect -> publish, fresh/stale/empty/missing/invalid, tamper/drift/rollback,
repeat renders preserving assessment, exact links, secret canaries and failures
retaining last-good content. All existing tests remain. Pinned dashboard venv.
Map original VoltaDEC001..026 unchanged; desktop1440x900/phone390x844/keyboard
and independent evidence inspection remain required before human readiness.
No live server sync/deploy, external network, credentials, Slack sends/replies,
posting flags, real state, commits/pushes or child agents/reviews.
Integrator adds one scoped feed code review; prior design/code/correction usage
retained, existing future evidence pass remains. Parent reviews/integrates and
owns real state/publication and next Slack alert/reply allocation.
