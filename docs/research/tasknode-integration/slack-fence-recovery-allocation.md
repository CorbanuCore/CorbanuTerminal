# PF-80-S01 — explicit local loss recovery

Manager allocation, September 12. Product initiative, existing PF-80 scope:
**Internal delivery control — TO BUILD**, “Show blockers, rendered sprints,
human test plans, machines, run logs and freshness.” This implements supported
recovery without granting authority from an unverified Slack reply.

## Accepted baseline and ownership

Gated transport and shared registration are accepted at receiving
b9e671e60416b952cc20d7b7260d3678080aebd0. Combined review06 is clean; staging244
passed, receiving243 passed plus one exact source-guard rerun passed. Preserve
the original source-drift error and every review01–06 result; no repeated review
of unchanged accepted code. Live connection, phone and actual-agent ACK unproven.

James, native Astra High, uses new worktree
`/Volumes/CorbanuDrive/Corbanu/worktrees/tasknode-slack-recovery-20260912`, branch
`workstream/tasknode-slack-recovery-20260912`, baselineb9e671e60416b952cc20d7b7260d3678080aebd0.
Old tasknode-pf80-s01 checkout remains untouched and frozen, not a second worker.
Manager owns all plans, registration outside the five paths, state and deployment.

Exact worker files:

- scripts/initiative_control/slack_transport.py
- scripts/initiative_control/decision_manager.py
- scripts/initiative_control/test_slack_transport.py
- scripts/initiative_control/test_decision_manager.py
- qa/initiative-control/pf-80-s01/decision-projection/slack-fence-recovery-receipt.md

Target450 changed lines/205 non-test; stop before600/260. Counts are incremental
from the accepted baseline, not a reset of historical2675/1024 worker or3131/1314
combined transport work. One new independent Astra High review07 plus one scoped
correction review08 authorized for this materially new recovery unit. Preserve
old review usage; classify any contract expansion or two-cycle nonconvergence.

## Manager disposition of the private proposal

Accept local incident bookkeeping and irreversible refusal of alert revisions
present at the loss cut. This does NOT cancel canonical decisions, erase answers,
invalidate earlier executed work, or blacklist all future revisions of an ID.
Keep requests, receipts, uncertain attempts, replies, intents, actual submissions,
ACKs and consumed permits unchanged. Record original cancellation values first.
Only a genuinely new explicitly asked canonical question/revision and new Slack
thread with normal authenticated reply/CAS/native-ACK gates may support new work.
No answer-copying, automatic reissue, recovered-history assertion or gap waiver.

Implement the proposed inspect_fence_loss/recover_missing_fence private seam:
snapshot-bound preview, append-only optional schema2 fence_losses incidents,
prepared/restored phases, durable pending hold before cancellation/publication,
and bounded nonzero synthetic loss barrier. It is a loss marker, not recovered
events. Never advance ingress as a side effect of repair. No schema1 migration,
counter reset, overwrite of unrelated files, pruning or force-ready operation.
Preserve the original private proposal and correct its outdated assertion that
canonical receiving lacks slack_transport.py: accepted baseline already has it.

Quiescence is a hard precondition, not a trusted boolean or guessed PID. The
injected integration hook must own and actually stop/join the runtime and its
callbacks outside locks. No currently qualified production hook is established.
Missing hook/unknown runtime must refuse without mutation. The callable and
explicit local CLI preview/repair registration may be implemented with default
refusal; do not advertise operator-ready recovery until manager wires/proves the
real supervisor. No process registry, service installation or broad supervisor
architecture is allocated here. Return any required additional seam before edits.

Use store→transport→nonblocking owner locking, exact snapshot revalidation,
no-replace same-directory publication and fsync, resumable incident identity,
and cancellation validation at qualification. Prepared incidents cannot be
cleared by initialize, Session startup, qualify or counter equality. Restored
incidents remain held for the existing exact manager-reviewed session/epoch gap.
Ordinary publish/status/OFF must never import SDK, connect or perform recovery.

## Required evidence

Test actual nonempty fence loss, old open-descriptor callback shutdown, real
process/file locks, crash/re-entry at every durable boundary, failed storage,
snapshot/binding/inode drift, wrong/symlink/unrelated artifacts, bounded capacity,
no historical dispatch even with late ACK or consumed permit, and fresh-question
normal flow after explicit restored-incident/gap qualification. Compare all old
ledgers/canonical feed byte-for-byte except the explicitly allowed cancellations
and incident/hold fields. Unknown runtime or missing hook is a tested refusal.

Run the full pinned Python suite, Facilities Node checks and both governance
checkers; record exact commands/counts/failures/hashes. No source edits during
source-export tests. No live credentials, SDK requests, Slack sends, Task Node
posting, public beta, main/push/release or whole-sprint acceptance. Manager still
owes actual supervisor wiring, connection/phone/restart/CAS/native-ACK evidence.
