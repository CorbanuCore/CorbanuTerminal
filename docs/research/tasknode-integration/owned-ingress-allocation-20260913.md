# PF-80-S01 — bounded synthetic owned-ingress implementation

September13 manager allocation under **Internal delivery control — TO BUILD**,
“Use sequential sprints per initiative”, and the mandatory independent isolated
execution policy. This is internal qualification infrastructure in the existing
responses-api-proxy, not a new service, auth store, provider or live model route.
James owns the ten crate-local paths below in
`/Volumes/CorbanuDrive/Corbanu/worktrees/tasknode-owned-ingress-20260913`, branch
`workstream/tasknode-owned-ingress-20260913`, source base
`6dde60fface29b0678b792926cbcff7bc8cc542e`. Import allocation bookkeeping first.

Manager read the full pinned Hyper/Tokio design and tiny_http feasibility.
The existing tiny_http public path cannot enforce pre-parser/drop deadlines;
do not wrap it or patch cached dependencies. Default behavior stays unchanged.
Accepted scope target1250 authored / STOP1500total850non-test, tests/docs included;
later generated locks separately counted. One complete transport with real
socket cancellation/streaming proof justifies this explicit allowance, not an
inherited exception or permission to compress tests. Runtime modules under500.

## Exact worker paths

All paths under `codex-rs/responses-api-proxy/`:

1. `src/lib.rs`: opt-in argument and dispatch before credential reader.
2. `src/synthetic.rs`: runtime/admission/deadlines/owned connection scopes.
3. `src/synthetic_io.rs`: monotone byte-capped async I/O, no HTTP parser.
4. `src/synthetic_exchange.rs`: direct owned Hyper client and limited bodies.
5. `src/synthetic_policy.rs`: fixed literal synthetic request corpus.
6. `src/synthetic_tests.rs`: actual socket cancellation/resource cases.
7. `src/synthetic_policy_tests.rs`: corpus shape and negative mutations.
8. `tests/synthetic_cli.rs`: exact binary startup/default compatibility proof.
9. `README.md`: invocation and exact internal limitations.
10. `Cargo.toml`: declared existing pinned dependencies only, after manager lease.

Parent reserves next integration of root Cargo.toml and both generated locks;
those three files are NOT worker-owned. Existing PF27 reservations will be
explicitly serialized before any dependency operation. Current phase authorizes
source/test authoring only; no Cargo/dependency/build/format command yet, because
accounting's review correction owns the Mac target and PF13 owns RTX. Manager
must issue the exact Mac target/lease and resolved aliases before first build.
This is an executable coding assignment, not a human blocker. Do not install,
download, regenerate locks, touch caches or create a private competing target.

## Frozen behavior

- Explicit `--synthetic-loopback-upstream 127.0.0.1:PORT`, nonzero upstream and
  explicit distinct nonzero downstream port. Reject dump/server-info/shutdown and
  nondefault production URL combinations. Dispatch before stdin credential read,
  reqwest initialization or file writes. No auth/cookie/env proxy/DNS/TLS/redirect.
- Bind only loopback; tests own upstream fixture listeners. Forward only fixed
  origin-form `/v1/responses` and constructed Host, not caller-selected targets.
- Use already-pinned Hyper1.8.1, hyper-util0.1.20, http-body-util0.1.3 and Tokio
  1.52.3 public APIs. Parent later adds exact aliases; no versions selected anew.
  Keep both HTTP/1 connection futures directly owned in one cancellation scope;
  no legacy client pool, detached driver, blocking I/O or per-request threads.
- Exactly four small fixed synthetic byte packets grounded in existing Responses
  serializers: messages/inline image, functions/namespaces and call outputs,
  custom calls, structured text/image outputs. No external prompts or real IDs.
  Exact-byte corpus admission before upstream; changed/extra/duplicate keys,
  hosted tools, references, arbitrary models and remote images reject. No general
  schema engine, normalization, tool execution, model inference or live route.
- Fixed bounds:2 owned connection tasks,8 accepted attempts,60s whole run;
  malformed attempts count. No application queue/refund/retry/automatic restart.
  Requested backlog2 is OS policy, not a proven global kernel connection cap.
- Ingress head16KiB raw/64fields/2s absolute from accept; upload1MiB DATA,
  total raw1MiB+16KiB and5s from accept. Require Content-Length; reject transfer
  encoding/Expect/compression/upgrades/trailers without draining rejected bodies.
- Connect and upstream final head each2s, clipped by accept+10s/run end; head
  raw16KiB/64fields, informational replies never reset caps. Response<=4MiB DATA
  and4MiB+32KiB total raw, no trailers; SSE incremental and JSON supported.
  Downstream blocked writes/flush remain inside accept+10s, never renewed.
- Monotone counters/phases and explicit time checks at transitions/completion;
  parser timer alone starts too late and DATA limits do not bound framing bytes.
  Preserve valid buffered final response frames after clean driver completion.
  Cancellation drops both streams/bodies/drivers; abort outstanding root JoinSet
  entries and drain actual joins, retaining errors/panics. Zero tasks is measured,
  not inferred from process death or graceful shutdown. No real-time OS guarantee.

## Required proof and acceptance

Actual sockets: slow/incomplete/oversized heads and65fields, tiny stalled bodies,
trickle/length/framing/chunked/Expect refusals, pipeline no second forwarding,
concurrency/attempt caps, each upstream phase stall and informational/chunk floods,
blocked upstream/downstream writes, disconnects/truncation, buffered completion,
run expiry and panic/cancel/join/no survivors. Observe actual downstream
backpressure, not a response that merely fits buffers. Positive SSE first frame
arrives before upstream completion, with exact bytes; JSON and all corpus cases.
Actual CLI held-open stdin/no input proves no credential read; preserve default
tests. One case uses production deadlines, shorter limits private test-only.
Controller kill/finally cleanup is failed enforcement, never a passing timeout.

After the manager grants the build lease, fix/fmt precede locked final tests,
exact binary/source receipts and unchanged-input checks. Manager derives/checks
Cargo/Bazel parity, resolves actual BUILD targets; current macro already includes
sibling modules/integration tests/binary runfiles, no BUILD edit presumed.
Allocate one Astra High material code review plus necessary correction, preserving
prior Slack/isolation history. Worker does not launch reviews or child agents.
Internal-only policy1.7 N/A applies to this fixture-only transport. A real confined
executor/package, approved mediated service identity, actual negative access
probes, separate execution/evidence review and live Slack phone/native-ACK proof
remain mandatory. No Slack credential access, live TaskNode send or gate waiver.
