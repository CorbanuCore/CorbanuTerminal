# PF-60-S02 next allocation: original-contract native persistence golden

Manager accepts this exact two-path test-only allocation September13 UTC.
Review02 exit0/findings[] confidence0.93; compact import received and all combined
gates pass at81d0f90e77c1e9217a16e70fef1019ff9aa13753. S02 stays active,
S03 dependent; no new product decision. Worker: existing Astra High Mendel,
01a0956e-927c-7e70-b1d7-29e1de70e6b7; checkout
/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-contract-goldens-20260913,
branch workstream/accounting-contract-goldens-20260913, source base81d0f90e77c1e9217a16e70fef1019ff9aa13753.
Manager supplies the clean allocation-doc launch SHA and exclusive target lease
at dispatch. Parent owns integration, shared ledgers and review assignments.
[Receiving proof](../../../qa/portfolio/agent-cost-accounting/pf-60-s02/compact-late-import-receiving.md).

## Inputs and exact base boundary

- Proposal inspection (historical) canonical HEAD: 655c02cd502922d919c0eea66bb963c5c1c7666d in
  /Volumes/CorbanuDrive/Corbanu/worktrees/management-workstreams-20260911.
- Inspected compact worker launch37d6ffcc97bd3d3e1149ced136ccdae690c75658,
  source baseeb58559590dd2b9332f2445971ee086988c67080, plus frozen candidate diff
  360abc83584fbbe5e0f929335ca0170aa36250a329632e7b5bd787c687a11907.
  Current2099/515 and original1970/463 are different preserved candidates.
- The next implementation base must be the parent's actual receiving commit
  containing that reviewed diff, plus allocation-only documentation. Its exact source SHA and sequential worker are recorded above;
  manager records the clean documentation launch SHA at dispatch and verifies
  disjoint ownership. This is normal receiving work, not a new prerequisite stage.
- Read full current active plan/S02, accepted S01 contract, retention handoff and
  compact allocation; inspected actual normal facade, late tests/support, native
  delete/descendant interfaces, Core sampling/prices and native test symbols.
  Root1.7/Rust policy and isolated-execution contract apply via Corbanu skill.
- Product initiative PF-60; exact product heading Product measurement,
  excerpt “No commercial performance numbers have been supplied.”
  This is S02's existing “Replay each fixture twice and after process restart;
  persisted and reconstructed totals equal the approved fixture exactly” criterion.

## Accepted unit and two literal write paths

Execute the original S01 mixed-provider numeric fixture through the normal public
AccountingStore and actual native owners/edges, with exact raw/reopen/compact
parity. Existing family goldens use different inputs; this closes that concrete
traceability gap without creating another library, adapter contract or authority.

| Allocated literal repository path | Estimate total / non-test | Work |
| --- | ---: | --- |
| codex-rs/state/tests/accounting_contract_golden.rs | 590 /0 | New external normal-library target: about220 fixture/expected-value/helper lines and370 test lines; three complete tests below. |
| qa/portfolio/agent-cost-accounting/pf-60-s02/original-contract-native-golden.md | 110 /110 | New exact fixture mapping, final command/hash/failure receipt and acceptance-limit matrix. |

Estimate700total/110non-test; approved target800/150, STOP900/200, including
receipt. Above800 contingency covers one complete six-attempt raw/reopen/compact
parity unit with a readable receipt, not compressed tests or inherited2200/750. Stop before exceeding either ceiling or needing another path.
No compressed assertions or edits to old goldens to fit. One accounting owner,
no worker children/reviews; manager allocates one new material Astra High review plus necessary
correction under standing discretion, preserving prior review usage.

This supersedes the layout estimate in the preserved earlier157-line private
accounting-after-late-import-next.md: add one isolated external target instead of
growing the accepted411-line integration file or changing shared test support.
Reuse its existing #[path = "../src/runtime/accounting_late_import_test_support.rs"]
module read-only for home/open/native/connection/dump. Public crate linkage only;
do not include the implementation module. Keep all eight accepted paths untouched.
Existing state Cargo target auto-discovery and root defs.bzl tests/*.rs glob
provide registration; no Cargo/lock/BUILD change. No include_str! of external QA
files and no new fixture resource/dependency. The test embeds bounded typed
numeric transcription, not a generic parser or collector.

## Literal fixture and provenance mapping

Source: qa/portfolio/agent-cost-accounting/pf-60-s01/fixtures.json, SHA256
96ba9416a8d0e69436fdd07c4ca6ddbb20cc30ece757b983eb1de2d708ca95dd.
Preserve its six attempts, three price descriptors, nine observations,
duplicate_reordered_indices and checkpoint_indices; do not change the Python
oracle or original fixture. Receipt lists each string-ID to deterministic UUID
mapping and each wire numeric field to Patch field. Fixed UUIDs/sequence IDs are
synthetic fixture identities, not discovered historical request/source authority.

Use Inclusive for the explicitly defined Responses/Chat fixture arithmetic and
NativeAnthropic for its native Messages arithmetic. Preserve omitted fields,
explicit output/cache-write/reasoning zero and cumulative revisions. There is no
need to repeat accepted broad null/overflow/fault matrices. Source positions map
one-to-one to original observation indices; revision retains its original value.
Provider/account/endpoint tuple maps to one explicit opaque synthetic scope.
No credentials, fingerprints, source scanning, or new principal registry.

Snapshot mappings preserve rates, captured/effective intervals and original
old/new binding, with documented synthetic approval-time/source-reference values
required by the existing Snapshot shape. Existing SourceKind is a fixture
simulation only: it must not relabel invented prices as real provider evidence.
Use OriginalPriceEvidence::Bound for the originally selected descriptor and
Unpriced for child-b. No present-day catalog or remote-price refresh is involved.

The existing Attempt/Observation DTO does not retain every S01 descriptive field:
provider_response_id, free-text observation status/provenance, billed line,
allowance and balance must be explicitly mapped as nonpersisted/out-of-store.
Do not claim complete metadata/invoice persistence, invent a field or suppress
this limitation. Raw immutable IDs/source positions are checked before compaction;
anonymous tombstones cannot later prove erased payload equality or request count.

## Three executable tests and literal expectations

1. accounting_contract_golden_raw_replay_and_two_reopens:
   create root, child-a, child-b and independent history using public
   upsert_thread/upsert_thread_spawn_edge. Admit in original dispatch chronology
   with eligible original snapshots (historical old stays old). Open at the
   earliest fixture time; maintain monotonic operation as-of while preserving
   original dispatch timestamps. Deliver checkpoint_indices, close/reopen, then
   duplicate_reordered_indices twice through observe, using a common valid
   young as-of no earlier than the latest dispatch. Compare full per-owner/day
   RetainedDay/DayTotals and literal original snapshot/binding/source sets after
   two closes/reopens. Finish any fallible fixture helper with Result/context/?.
   Use existing public SqliteConfig pool acquisition and explicit connection/
   pool/runtime close; no direct bare SQLite connection or production test hook.

2. accounting_contract_golden_original_price_and_native_family:
   use public list_thread_spawn_descendants and compare the exact two children;
   include root once, exclude history. Raw SQL inspection proves four family
   attempts/three request IDs and child-a-2 retry_of child-a-1 before expiry.
   Compare every owner/day against literal metrics rather than only summing two
   implementations of the same reducer. The independently stated family values:
   input420/unknown0; noncached210/unknown1; read90/unknown0; write60/unknown1;
   output60/unknown1; reasoning8/unknown2; total400/unknown1;
   known USD0.001084, unknown estimates2, attempts4.
   Noncached is the explicit derived seventh metric absent from S01 aggregate.
   Individual estimates are root0.000220, child-a-1 known0.000228/unknown,
   child-a-2 complete0.000636, child-b known0/unknown,
   historical-priced0.000110 (NOT0.000220), historical-unknown known0.000040/unknown.
   Separate billed0.000300/allowance/balance remain outside this estimate proof.

3. accounting_contract_golden_compact_original_bundle_parity:
   database A uses raw admission/observation above then maintain; independent B
   imports complete original owner bundles using import_retained, including the
   complete child-a retry chain. Use an explicit as-of latest original dispatch
   plus100 days so all six are90..365 days old, preserving original day keys.
   Compare each path to literal totals/coverage first, then full ten accounting
   tables against each other; exact selected snapshots/references/tombstone UUID
   plus dispatch+365-day expiry sets, no raw detail. Repeat originals and perform
   two real closes/reopens; all-suppressed repeat cannot add/extend/reprice or
   silently advance coverage. No catalog filling of previously unknown input.
   This is original contract parity, not another SQL fault/process test matrix.

Avoid a time trap: the original fixture plus100days lies ahead of the current
production clock. Do not call public Utc::now-based deletion on that advanced
database, clamp the clock, rewrite fixture timestamps or add a clock API to make
the test pass. Reuse the already-qualified now-relative public deletion and
explicit-time expiry tests below as separate acceptance evidence.

## Reconcile remaining provenance and deletion claims, do not redo them

| Ledger wording | Actual evidence / manager disposition |
| --- | --- |
| Actual per-send/presence/original-price provenance | Qualified only for explicit direct Anthropic opt-in: core Sampling/transport, accounting_prices::original (Anthropic eligible bundled rows, prospective acceptance time), accounting_anthropic_native_presence_prices_and_two_reopens, actual_presence_revisions_and_remote_only_price_unknown, retry/role/redirect tests. This unit adds synthetic contract parity, not real provider-price authority. |
| Native goldens | Existing returned native_family_literal_golden_and_young_reconstruction and actual Anthropic spawned-child/reopen tests stay accepted. Add the original six-attempt S01 mapping only; do not call all native proof missing. |
| Deleted owner / delayed native usage | Existing accounting_anthropic_deleted_native_owner_cannot_be_resurrected_by_delayed_usage and public_delete_off_recreated_owner_and_snapshot_gc prove the bounded behavior. No new owner registry required by this allocation. |
| Compacted recorded IDs through replay lifetime | Existing public delete/OFF/recreated-owner test and replay_price_and_two_reopens suppress recorded IDs, retain anonymous fences and GC only final shared snapshot references. Corrected serialized_outcomes plus real contends_without_winner_assumption retain both order and actual contention proof. |
| Expired fence / old replay | Existing boundaries_and_no_raw_staging proves365-day rejection including after GC and conservative day-start+365 with+1ms residual fence. The dispatch-age admission floor, not a permanent UUID registry, rejects expired original input. Reuse those gates; do not claim tombstone expiry is missing. |
| Permanent deletion-fence qualification | Historical Remaining wording is broader than the accepted compact allocation: native upsert may recreate owner; unseen new identity is not permanently revoked; anonymous erased data cannot certify changed historical input. Record this as a limitation, not a pass or implicit waiver. Manager can reconcile wording to approved90/365 scope; if stronger permanent revocation is actually required, classify that product retention change separately. No new human blocker is necessary for the proposed tests. |
| Legacy acquisition/reconciliation | import_retained accepts caller-supplied complete original evidence; it is not a qualified rollout importer. App-server token_usage_replay is connection-notification snapshot attribution, not a durable per-attempt source. Cannot infer absence/revision/retry/original rates by adjacency or subtraction. This remains unqualified, not a reason to build an invented source now. |
| Other actual dispatch paths | Responses HTTP/WebSocket, Chat/Corbanu and other excluded/auxiliary routes are not qualified by direct-first Anthropic or this synthetic test. Keep concrete open S02 rows; choose the next bounded actual caller separately, not an unwired generic layer or automatic route enablement. |

Do not archive S02 just because these tests pass. This allocation closes the
original numeric persistence/reconstruction mapping only. Core/protocol history,
provider correlation/legacy coverage and required user-facing qualification
must retain honest dispositions; no full-runtime/native-price coverage is inferred.

## Required validation commands (after manager dispatch)

All Rust commands run from the manager-dispatched worker's codex-rs directory,
prefixed with this environment after the target lease is returned:
env RUSTUP_TOOLCHAIN=1.95.0 RUSTUP_AUTO_INSTALL=0 CARGO_NET_OFFLINE=true UV_OFFLINE=true CARGO_TARGET_DIR=/Volumes/CorbanuDrive/Corbanu/worktrees/management-workstreams-20260911/codex-rs/target

- Scoped just fix -p codex-state --locked under the recorded file-write/network
  restrictions; preserve lock-listener denial if it recurs, no environment repair.
- rustfmt --edition 2024 --config skip_children=true state/tests/accounting_contract_golden.rs
- just test -p codex-state --test accounting_contract_golden --test-threads 1 --locked
  Must execute all three new tests; no empty selector counts as proof.
- just test -p codex-state -E 'test(accounting_late_import)' --test-threads 1 --locked
  Reuses accepted deletion/expiry/fault tests, no re-authoring.
- just test -p codex-state -p codex-api -p codex-tasknode-session --test-threads 1 --locked
- just test -p codex-core -E 'test(accounting) | test(agent::role::tests) | test(memory_stage_one::tests) | test(exec_env::tests) | test(tools::handlers::shell::tests)' --test-threads 1 --locked
- just clippy -p codex-core -p codex-api -p codex-login -p codex-http-client -p codex-state -p codex-tasknode-session --locked --no-deps
- cargo check --offline --locked -p codex-core -p codex-api -p codex-login -p codex-http-client -p codex-state -p codex-tasknode-session --lib

From worker root: python3 docs/plans/check.py; python3 docs/sprints/check.py;
git diff --check (run as separate commands). No direct cargo test, full Core/
workspace rerun for appearance, global -D warnings, unrestricted formatting,
cache repair, dependency changes or installations. Preserve all warnings/LEAKs/
filtered versus executed skips and earlier full-Core failures.
Capture each execution's start/end/actual exit in its own file immediately;
copy JUnit before the next selector overwrites it. Validate exactly three time
lines, never parse command metadata from a concatenated digest listing.
Return whole literal patch, counts/digests/receipt for parent review/receiving.

## Applicability and authority

Approved defaults stand: OFF/no implicit install, exact USD, original price,
known/unknown distinction,90/365 conservative daily expiry. Test layout/next
provider ordering/ledger evidence reconciliation/leases are manager engineering
choices. Stronger identifying retention/permanent revocation, historical
repricing, billed authority, live credentials/spend or public activation are
separate product/operational authority, not prerequisites invented for this unit.

Integrator accepts increment-specific internal-only N/A: test-only
normal library evidence changes no user control, collection or source routing.
This does not satisfy root1.7/schema2 code-blind acceptance. Before affected
collection/replay handoff and S03 UI qualification, manager supplies exact binary,
frozen independent cases, separate fresh binary-only executor and evidence
reviewer; enforced filesystem/tool/IPC/network/child isolation with actual
negative probes and positive package/PTY controls. No duplicated harness here.
True-TUI, applicable TensorCash/Isometric, named-human, benchmark and release
gates remain open; S03 remains draft/dependent.

The private proposals, original manifests/logs/receipt and reviewed eight-file
candidate remain unchanged. Manager receiving evidence is linked above. This
allocation plus current plan/sprint are source authority only after exact clean
launch dispatch; worker returns frozen uncommitted output for parent acceptance.
