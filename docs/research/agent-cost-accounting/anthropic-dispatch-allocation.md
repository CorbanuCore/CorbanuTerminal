# PF-60-S02 next executable unit: opt-in direct Anthropic native sampling

Manager-approved same-S02 allocation, September12 16:08UTC. Original private proposal SHA402a86937f1523f3c5bfdd665117db1bac85c8c0be751d9638b2092415e5e9f1 is frozen; preparation-only statements below describe that original evidence, not the present dispatch.

## Authoritative manager acceptance and ownership adjustment

18:44 exact reallocation for review02: manager inspected private login builder
selection and the HTTP builder's terminal bare-client fallback; a Core-local
copy would duplicate or lose existing routing/CA policy. Accept the four
additional existing files: codex-rs/login/src/auth/default_client.rs,
codex-rs/login/src/auth/default_client_tests.rs,
codex-rs/http-client/src/client_builder.rs and
codex-rs/http-client/src/client_builder_tests.rs. No other reserved lane declares
these paths. Worker now owns24 files, manager retains three serialized dependency
paths. Bound combined27-path candidate at3900total/1450non-test, a150-line total
extension over prior3750STOP, preserving all earlier counts/review history.
Only additive no-redirect route construction and policy-preserving fallible CA
fallback plus focused regressions are allowed. ON construction must error rather
than silently restore redirects on terminal fallback; existing OFF constructors,
proxy/sandbox selection, default headers/UA/cookies and ordinary CA behavior
remain unchanged. No dependency/env/config/protocol change or general client
refactor. Existing review03 covers the repaired material diff, including these
four paths; preserve the two-cycle convergence rule and full-Core limitations.

18:35 correction02 disposition: role correction now passes28 accounting and42
role/stage-one tests after recoverable exact32-fingerprint rebuild and five
test-fixture repairs. Full Core3405pass/133fail/19skip/2leaky; all133 failure
names match launch baseline, not a clean gate or identical-cause proof.
Review02 found a real endpoint-binding bypass via automatic307/308 redirects.
Manager read actual transport/build/factory paths and accepted this as an
existing-contract same-owner blocker. Mendel is allocated the second narrow
correction: prohibit following redirects for accounted requests before any
redirected send, preserve OFF and all existing headers/proxy/CA/stage-one policy,
and test two real endpoints with zero redirected requests/attributed usage.
Same20 files and3750/1450STOP. A required shared API/file change must return for
exact manager reallocation first. One existing corrective review03 remains;
after two patch cycles reclassify any remaining defect rather than broadening.
Initial review02 bundle refusal matched the synthetic-child fixture string;
manager verified it synthetic and staged only that test file using the helper's
documented path. No credential or source content changed; actual review02 failed.

Store6b2dbcab5 is independently reviewed clean (helper85655), integrated at9518184ac;
combined367 state/TaskNode tests pass,1leaky,0skipped16.922s. Receiving allocation
base is c8358dd9b4329036c8f8fb525015bbd795433513, not a running old review.
Manager approves direct-Anthropic-first, explicit native in-memory opt-in/default
OFF, prospective bundled native-catalog estimates with absent rates unknown,
and the bounded fail-visible pre-/post-send policy below. No new user decision.

Accept target3500total/1350non-test for this connected native caller and its real
request/retry/presence/price/reopen evidence; original3122/1172 estimate remains.
Worker owns ONLY table rows1–20. Manager owns core/Cargo.toml, codex-rs/Cargo.lock
and MODULE.bazel.lock as a separately serialized dependency step: row21/22 plus
the required Bazel lock update omitted in the original proposal. Combined23-path
STOP3750/1450 allows at most250 additional lock/receipt lines; report before any
overage or package/version change. No compressed proof or unrelated refactor.
Security owner currently reserves the two lockfiles; explicit short lease
requested in its existing task. No manager/worker lock edits until coordinated.
Worker may implement the disjoint20 source/test/receipt paths immediately after
recorded dispatch; builds depending on SQLx wait for manager's dependency commit.
Do not run Cargo commands that mutate a lock while that step is pending.

Integrator grants one new independent code pass plus up to two scoped corrective
passes for this material caller increment, preserving all earlier reviews.
After two correction cycles, apply the root convergence/classification rule.
No review of unchanged clean store. Actual native tests are mandatory; no human
readiness, live provider invocation, public enablement or S03 activation inferred.
Worker cannot edit shared plans/sprints, manifests/locks, add files, commit,
delegate or run its own reviewer. Parent records actual clean launch below.

Dependency step completed under the existing security owner's explicit lease:
offline Rust1.95 metadata succeeded; Cargo.toml adds only SQLx workspace dev edge,
Cargo.lock adds only that codex-core edge, no package/version changes. Required
`just bazel-lock-update` first failed2 on the existing disk-cache symlink, and
disk-cache-disabled retry failed37 on the repository-cache symlink. The equivalent
`bazel mod deps --lockfile_mode=update --disk_cache= --repository_cache=/tmp/corbanu-bazel-lock.UmX786/repositories --repo_contents_cache=/tmp/corbanu-bazel-lock.UmX786/contents`
succeeded0 with isolated per-command caches; no shared cache repair or rc edits.
MODULE.bazel.lock stayed byte-identical. Existing platforms/rules_cc selected-version
warnings were retained, not “fixed.” This is dependency resolution, not native
test acceptance; future caller tests and combined review remain mandatory.

## Receiving dependency and preserved history

- Worker: `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911`, branch `workstream/accounting-pf60-s01-20260911`.
- The latest supplied status is second-correction review helper03/session85655 RUNNING. Do not infer its verdict or combined receiving results. During read-only preparation the worker advanced from `ef13b347c435fb2d8e923124c70b8134bc9a5abf` to clean `6b2dbcab564d1a002199e7eef883cdec3be97d32`, whose commit contains the exact 17-file store increment, +1906/-151. That is observed repository state, not a substitute for the parent's review/integration evidence. Parent must name the accepted receiving base before this next source dispatch.
- Preserve the store's original 1703total/728non-test, first-correction 1879/778 and second-correction 2057/829 measurements. The latter includes the 144-line receipt and has specific 2150/1000 permission, not permission inherited by this proposal.
- Keep helper81230 exit1/two P2s and helper13225 exit1/one P2, both corrections, original failed regressions, warnings and LEAKs. Latest worker store test evidence is 287passed/5leaky/0skipped,15.940s, run32b28183-9170-47bc-b67d-ed3bf1493bf0; normal library check exited0. No clean-review or receiving-combined claim is added here.
- Current plan is `docs/plans/active/portfolio-agent-cost-accounting.md`; current in_progress sprint is `docs/sprints/current/portfolio-agent-cost-accounting/pf-60-s02-idempotent-usage-persistence-and-replay.md`. Their remaining actual-dispatch/presence/trusted-price work directly motivates this unit. Product citation remains Product measurement / Measurement targets: “No commercial performance numbers have been supplied.”

## Outcome, rather than another preparatory contract

Implement one normal-library, default-OFF Core collector on the actual native direct Anthropic Messages sampling path. A native user turn, and supported native children, must durably admit each transport attempt with its original price binding before send; preserve raw numeric field presence from actual SSE; update the accepted store before acknowledging usage/completion; and retain correct owned results through cancellation, retry and native reopen.

The existing normal store is the only dependency. Do not add a second journal, migration, accounting registry, preparatory facade, universal-provider abstraction or partial-transfer API. This is independently useful for fresh direct Anthropic sampling even while other providers and historical imports remain unqualified. Store installation/retention/atomicity proofs remain accepted dependencies, not tests to re-author.

First route: provider configuration ID `anthropic`, `WireApi::Anthropic`, API-key direct Messages API. Match the resolved endpoint and exact outbound model, not a provider's display name. Claude subscription/plan, Corbanu, OpenRouter, Bedrock/Vertex, compatible custom gateways, Responses HTTP/WebSocket, Chat Completions, compaction and memory-only sampling are not rollout-qualified by this unit. Unsupported routes remain on their existing behavior, with no invented measured-zero accounting rows and no claim of complete application coverage.

## Actual seams inspected

| Existing seam | Consequence for implementation |
| --- | --- |
| `core/src/session/turn.rs::run_sampling_request` owns the loop around `try_run_sampling_request`; the latter makes new inference traces and calls `stream_with_same_turn_attempt`. | Allocate one accounting logical request outside the outer stream-retry loop. Neither trace identity nor the same-turn counter is the durable primary key. Multiple sampling calls in a turn must remain distinct. |
| `core/src/client.rs::stream_anthropic_messages_api` creates a new endpoint client inside auth/history-repair/413 retry handling. `ModelClientSession` is turn-scoped. | Carry a narrowly scoped native accounting context through the real sampling call and its endpoint clients. Do not leave it attached to later compaction or unrelated calls on the reused session. |
| `codex-api/src/endpoint/session.rs::stream_encoded_json_with` applies async auth inside each retry closure, then invokes `HttpTransport::stream`. | A transport adapter below this closure sees every actual HTTP retry after auth, including retries invisible to Core's outer loop. No endpoint-session or generic retry-policy rewrite is needed. |
| `core/src/memory_stage_one.rs::StageOneGuardedTransport` calls its guard before the inner Reqwest transport; `client.rs::build_api_transport` constructs it. | Keep ordering auth -> stage-one check -> accounting admission -> existing Reqwest send. Make the guard's inner transport generic with its current transport as the default; do not weaken the guard or create another authorization path. |
| `codex-api/src/endpoint/anthropic_messages.rs` deserializes four usage fields to `Option<i64>` and merges them into zero-defaulted `TokenUsage`. | Observe whitelisted raw field presence before that lossy conversion. Existing display/telemetry merge semantics remain unchanged. |
| `core/src/session/mod.rs::state_db` returns an optional handle; `rollout/src/state_db.rs::init` can emit a warning and return None. `LiveThread::persist` flushes pending metadata. | Enabled accounting cannot silently proceed without a usable native database/owner. Use the existing strict materialization path and let store admission validate the native owner; do not synthesize one or use the warning-only helper. |
| `core/src/tools/handlers/multi_agents_common.rs::build_agent_shared_config` clones effective parent config; `core/src/agent/role.rs::reload::build_next_config` rebuilds it. | Normal clone inheritance already works. Explicitly preserve the new internal opt-in across role reload, then recheck the child's actual provider route. Do not change spawn edges or grant children new provider authorization. |
| `models-manager/src/lib.rs::bundled_models_response` exposes embedded model data. `manager.rs::apply_remote_models` explicitly preserves bundled billing metadata; `get_model_info` permits prefix/suffix matching. | Read the embedded catalog directly for price authority, with exact slug/provider matching. Do not use fuzzy model resolution or a remotely supplied row as accounting price authority. |

## Concrete runtime contract

### Activation, native ownership and failures

Add an in-memory Core `Config` mode, Disabled by default. The enabled variant binds an opaque local endpoint/account scope UUID and an explicitly approved direct Anthropic endpoint. It carries no key, credential fingerprint or arbitrary supplied rate. The ordinary config loader always initializes Disabled: no TOML/CLI/env/Feature entry, config schema, TUI control or default installation in this unit. A native embedding can explicitly set this normal-library configuration and execute the existing native user-turn path; this is not a cfg(test)-only collector.

The production binding uses the built-in direct endpoint `https://api.anthropic.com/v1`. A test embedding may explicitly bind its isolated loopback endpoint and synthetic auth in the same typed in-memory configuration. Do not infer that trust from a user-edited provider base URL. This explicit endpoint binding is application trust configuration, not a new auth principal or permission to dispatch. Changing the provider route must fail the binding check before collection, not silently apply direct prices to a gateway. Do not persist endpoint URLs or credentials: attempts use the opaque bound scope and bounded provider ID.

The scope UUID identifies the application's selected local account/endpoint context, is reused across retries and config-inherited children, and is supplied again by the embedding on resume. It is not an invoice account identifier, security capability, credential-derived ID or newly registered identity. Authentication and provider authorization remain exclusively in the existing machinery. Public-list estimates do not claim negotiated/account-specific invoicing rates.

OFF must bypass accounting setup entirely, including when `StateDbHandle` is None. An already installed store must still receive the existing atomic native deletion cleanup while OFF. When ON, materialize the native owner using the existing strict path, obtain the optional state handle as a required dependency, and explicitly open/activate the accepted store before the supported sampling dispatch. Missing/failed database or owner, unsupported schema, failed activation and failed admission produce a bounded non-retryable accounting failure and zero provider sends; never degrade to unrecorded ON collection. Existing ordinary startup remains optional and unchanged for OFF/unsupported routes.

Use the existing `CodexErr::Fatal` classification (`protocol/src/error.rs::is_retryable` returns false), not a new protocol error variant. At the HTTP boundary admission failure maps to non-retryable `TransportError::Build`; retain an accounting-failure latch in the logical request context so API error mapping and Core stream retries cannot accidentally turn persistence errors into another send. Check the latch before Core retry decisions and before a successful sampling result. Error text must not expose SQL, home paths, credentials or response bodies.

After a send, an observation failure terminates this sampling request without a repair send, keeps already committed intent/evidence and leaves unobserved usage unknown. Cancellation retains committed evidence. Use awaited observation writes, not a detached persistence queue; cancellation may leave either the complete committed observation or the previous complete state, never a synthetic completion. Reopen reads the accepted store and resumes normal native history; it does not infer missing usage from cumulative rollout `TokenCount` or automatically replay a previous network send.

Store timestamps remain explicit and exact. Sample fresh wall time immediately before each facade operation after caller-side waits; handle a rejected stale/backward time as a visible accounting failure. Do not clamp with a checkpoint, silently coerce explicit time, reset activation, or assume that a timestamp sampled before an awaited writer is ordered. This collector does not change the store's explicit-time API or claim to repair cross-process clock skew. Native deletion's reviewed writer-owned production clock remains untouched.

### Physical attempts and original identities

The logical context is owned by one native `ThreadId`, native turn ID and one `run_sampling_request` invocation. Create its random request UUID once; each invocation of the accounting transport's `stream` allocates a fresh attempt UUID, links `retry_of` to the previous attempt in this logical request, and durably admits that exact identity before delegating to Reqwest. Capture the exact encoded request model and resolved supported route for the attempt, not a fuzzy catalog alias or stale display model.

HTTP429/5xx/transport retries, signed-history/413 rebuild retries and Core consumed-stream retries must all preserve logical identity but acquire a distinct attempt identity on each actual send. A reconnect notice is not a send. Auth, encoding or stage-one preflight failure before this boundary creates no dispatch attempt. Admission is durable intent, not proof that bytes reached the provider: a crash immediately after admission remains an unknown attempt. Retries can each consume tokens, even without provider request IDs.

Do not change Corbanu gateway request-ID rotation, reservation release handling, auth recovery, native inference trace IDs or provider headers. Missing/reused provider IDs cannot merge accounting attempts. Direct API-key auth normally has no refresh recovery (`unauthorized_recovery` returns None when the provider has an env key); test its actual401 terminal behavior rather than inventing a direct-provider auth retry.

Bind the usage observer to the correct physical successful response: each endpoint-client invocation gets its own shared success slot, populated once by its transport adapter only when the successful `StreamResponse` is returned. Failed HTTP attempts remain admitted but do not fill the slot. The SSE observer captures that immutable successful-attempt handle. A later endpoint/outer retry gets another slot; never attach old-stream usage through a mutable logical-request-wide “current attempt”. This avoids modifying the generic HTTP response type or every provider API.

### Actual SSE token presence and durable source positions

Add an optional, awaited typed observer on `AnthropicMessagesClient`; default None avoids new accounting parsing on all existing callers. Put the small wire DTO/observer trait in a dedicated API module, re-exported from the existing API lib. API has no dependency on Core or state. Nest the module using an explicit path declaration from `anthropic_messages.rs`, avoiding an otherwise unnecessary endpoint module-list edit.

Before the existing lossy deserialization, extract only `message_start.message.usage` and `message_delta.usage`: `input_tokens`, `cache_read_input_tokens`, `cache_creation_input_tokens`, `output_tokens`. Preserve each as Missing, explicit Null or exact nonnegative integer, including explicit zero. Reject negative, fractional, string, boolean and out-of-i64 numeric forms for enabled accounting. Absent/null usage containers provide no invented numeric fields. Ignore non-usage content and unrecognized provider metadata; never pass whole messages, bodies, text, reasoning, headers or auth to the store.

Map to the existing `Dialect::NativeAnthropic` and `Patch`; do not subtract caches from Anthropic noncached input. Do not invent reasoning or total fields; use accepted state derivation only when its required components are known. A null patch does not erase a previous known count, omitted fields do not become zero, and cumulative revisions replace rather than add. Preserve the old `TokenUsage` and ResponseEvent surfaces and their existing tests.

The physical stream has an immutable source UUID and a strictly increasing wire source position assigned by the sequential SSE parser, starting at one. Persist that original position as revision/sequence on the attempt; do not derive ordering from wall clock or callback completion order. An identical local persistence retry reuses the original tuple, not a new revision. A new physical stream is a new attempt/source. There is no historical source reordering or provider-reconciliation importer in this unit.

Await durable storage of each recognized usage event before exposing its derived response events/completion downstream. Stop on persistence/validation failure. Ensure the observer also sees start-only usage when the provider subsequently errors, disconnects or the caller cancels. The completion callback alone cannot recover missing start-cache fields.

### Trusted original prices, not arbitrary caller snapshots

The production adapter constructs snapshots itself from `codex_models_manager::bundled_models_response()`. Select exactly one exact model slug with `ModelOrchestrationMetadata::Eligible`, provider ID anthropic and `ModelBilling::Metered`. No fuzzy prefix lookup, remote-only row, model display text, subscription pricing, wallet balance, Corbanu price response or request-supplied price can authorize this snapshot.

Concrete existing evidence: bundled `claude-opus-5` carries input5000/output25000/cached-input500 milli-USD per million; `claude-fable-5-1` carries10000/50000/1000. These are repository catalog facts, not live market-price verification. Convert integer milli-USD to exact USD decimal strings by integer quotient/remainder, never floats or early rounding. Bind noncached input/output and optional cache-read only. The bundle has no cache-write rate: retain NULL rather than infer a multiplier, TTL or tier. Nonzero cache-write usage therefore leaves that price component unknown; explicit zero needs no rate under the existing engine.

Use `SourceKind::NativeCatalog`. Derive `source_reference` with the existing UUIDv5 capability over a documented canonical tuple of projection-version, exact provider/model, units and the selected raw billing fields. This is a reproducible content identity, not a claimed cryptographic authenticity proof. Persist all selected exact rates and scope through `AccountingStore::admit`; missing/unsupported/ambiguous catalog matches bind no price and remain unknown. Do not block measured token collection just because a trusted price is unavailable.

Catalog rows have no upstream effective date. The collector's explicit native opt-in approves that bundled projection prospectively for estimates: observed/approved/effective-from share the local acceptance timestamp, no earlier than capture and no later than that attempt's dispatch. Never claim that it is the provider's historical price start. Freeze the binding before admission and use it for every observation of that attempt; a later catalog/route/config change cannot reprice it. New attempts can bind their own prospective snapshot. No retrospective re-estimation or billing reconciliation is added.

## Exact proposed write scope and honest estimate

One native-accounting implementation owner, no delegation. Paths are relative to the named worker. Manager must reserve these literal files disjointly before dispatch, especially the existing client, turn, role-reload and stage-one transport files; the narrow transport genericization does not authorize adjacent PF13/security changes. No ownership assertion is made merely because this private proposal lists them.

| # | Literal path | Estimated added+removed lines | Classification/purpose |
| --- | --- | ---: | --- |
| 1 | `codex-rs/core/src/lib.rs` | 4 | Non-test module registration. |
| 2 | `codex-rs/core/src/config/mod.rs` | 40 | Non-test internal opt-in/binding type, default OFF initialization. |
| 3 | `codex-rs/core/src/agent/role.rs` | 8 | Non-test preserve native opt-in across role config rebuild. |
| 4 | `codex-rs/core/src/session/turn.rs` | 90 | Non-test logical owner lifecycle, required state/materialization, fatal failure/cancellation handling. |
| 5 | `codex-rs/core/src/client.rs` | 110 | Non-test scoped context and actual Anthropic transport/observer wiring, no other wire changes. |
| 6 | `codex-rs/core/src/memory_stage_one.rs` | 30 | Non-test generic inner transport, unchanged guard semantics/order. |
| 7 | `codex-rs/core/src/accounting.rs` | 260 | New non-test native context, store adapter, source mapping and failure latch. |
| 8 | `codex-rs/core/src/accounting_prices.rs` | 150 | New non-test exact bundled authority projection and binding. |
| 9 | `codex-rs/core/src/accounting_transport.rs` | 170 | New non-test per-send admission, retry chain and immutable response slot. |
| 10 | `codex-rs/core/src/accounting_tests.rs` | 350 | New focused context/transport/failure-order tests. |
| 11 | `codex-rs/core/src/accounting_prices_tests.rs` | 200 | New authority/route/exact conversion/prospective-binding tests. |
| 12 | `codex-rs/core/tests/suite/mod.rs` | 2 | Conservatively non-test registration of two new suite modules. |
| 13 | `codex-rs/core/tests/suite/accounting_anthropic.rs` | 450 | New native turn/child/HTTP/SSE goldens, imports local support by path. |
| 14 | `codex-rs/core/tests/suite/accounting_anthropic_recovery.rs` | 450 | New retry/cancel/reopen/deletion and failure-barrier native goldens. |
| 15 | `codex-rs/core/tests/suite/accounting_anthropic_support.rs` | 200 | New bounded local HTTP/SSE barriers, native fixture/readback helpers shared by the two modules. |
| 16 | `codex-rs/codex-api/src/lib.rs` | 6 | Non-test typed observer re-exports; no common ResponseEvent change. |
| 17 | `codex-rs/codex-api/src/endpoint/anthropic_messages.rs` | 80 | Non-test optional observer before lossy usage parsing/merge. |
| 18 | `codex-rs/codex-api/src/endpoint/anthropic_accounting.rs` | 120 | New non-test whitelisted wire-presence DTO, observer and sequential source positions. |
| 19 | `codex-rs/codex-api/src/endpoint/anthropic_accounting_tests.rs` | 300 | New raw SSE/presence/position/error tests, external test bodies. |
| 20 | `qa/portfolio/agent-cost-accounting/pf-60-s02/anthropic-dispatch-increment.md` | 100 | New non-test implementation receipt, exact proof/failures/scope/digests/limitations. |
| 21 | `codex-rs/core/Cargo.toml` | 1 | Conservatively non-test: add only `sqlx = { workspace = true }` under dev-dependencies for separate-connection native proof. |
| 22 | `codex-rs/Cargo.lock` | 1 | Conservatively non-test: only the existing SQLx dependency edge in the codex-core package; no new version/package. |

Estimate3122total =1950tests +1172conservative non-test, including receipt, registrations, the two manifest/lock edges and both sides of edits. Proposed bounded target3500total/1350non-test. This is new manager approval to seek, not existing permission. It is above800 because native ownership across two retry layers, pre-send durability, pre-merge presence and prospective trusted pricing require one connected caller plus substantial actual-request/failure/reopen proof; an800-line promise would hide missing functionality or compress tests. It inherits none of the store/C2 exceptions. Report measured overage or any extra path before expansion; do not remove proof to meet the estimate.

Core additions are narrowly native caller/price/store glue, not a relocated accounting engine: the accepted state implementation owns all replay, arithmetic, migrations, retention and deletion; API owns only its wire-presence observer. Existing Core runtime dependencies already include state, API, models-manager, UUID and HTTP types. Core currently has no direct SQLx dependency; the explicit dev-only edge above reuses the workspace's existing pinned SQLx0.9.0/features used by state. This enables real separate connections, controlled SQLite failures and exact stored identity assertions without a test-driven production API or external sqlite executable. Parent must reserve the shared lockfile edge and verify its literal one-package-only change; any package/version resolution expansion needs a new disposition. No new schema, model catalog, BUILD, generic HTTP-client/retry, protocol, TaskNode, shared plan/sprint or accepted test/receipt edits are proposed. New module bodies are kept separate; no opportunistic refactor of large Core files.

## New proof required, without repeating the store test project

| New executable test family | Required observable assertions |
| --- | --- |
| OFF/default/unsupported route | Ordinary native turn sends its expected request without installing any accounting object, also with missing optional state. Installed-but-OFF deletion uses the accepted public cleanup. Other wire APIs and plan/gateway paths acquire no collector and retain prior request behavior. |
| Raw SSE presence | Actual start/delta SSE covers each field omitted/null/zero/positive, start-only usage, null after known, cumulative replacement and split cache fields. Compare complete stored patches, revisions and exact native day metrics, not only displayed totals. Invalid types/ranges fail accounting without silently changing the legacy OFF parser. |
| Trusted binding | Exact bundled row and milli-USD conversion; original rates/NULL and reproducible source UUID survive later projection changes and reopen. Missing cache-write/model/rate remains unknown. Remote/fuzzy/plan/model/endpoint mismatch cannot acquire trusted direct rates. Loopback native golden uses an explicit synthetic embedding binding, never real auth or market-price claims. |
| Actual retry identity | Count actual mock POSTs for429/5xx,413 repair, disconnect after committed start usage and subsequent outer retry. Assert one logical UUID per sampling call, distinct chained attempt UUIDs and one response-local source each. Missing/reused provider IDs do not collapse attempts. Direct401 is terminal. An old delayed parser cannot attribute a patch to a new retry. |
| Auth/preflight/persistence boundary | Controlled auth/guard denial yields zero POSTs and no attempt. Hold the store writer before admission and prove no send before commit; release and prove exactly one send. Missing state/owner and injected admission failure yield no send and no retry loop. Observation failure after a POST preserves previous evidence, emits no successful completion and causes no repair POST. |
| Native owner goldens | One native root and two real native spawned children, including a role-reloaded child and concurrent child sampling; assert owned request/attempt IDs against native spawn edges. Context fork/history copy creates no child charge. Do not hand-seed accounting attempts to stand in for real requests or sum displayed inclusive parent totals. |
| Native recovery and deletion | Cancel after admitted intent and after a committed start usage event, reopen native thread/state twice, compare exact owned stored values and issue a fresh user turn without replaying old sends. Delete a real owner while a local stream is held: a late observation must not resurrect it, and unrelated owner data/deletion remains valid. Exercise existing public deletion, not private SQL as its substitute. |

Use existing `TestCodexBuilder::build_with_auto_env()` and normal Core submission/resume/spawn helpers; API parser tests alone are insufficient. Use synthetic local credentials and an isolated local mock Messages endpoint. Assert outbound method/path/model/request counts and unchanged payload/auth-header behavior; no external network/model/live provider call. New support code must be bounded and reusable, not compressed tests or a second state dump framework. Root/children in this row are future deterministic native test fixtures, not permission to spawn agent workers during implementation.

Public facade readback plus narrowly scoped read-only SQLite fixture assertions should inspect exact persisted attempt/presence/provenance identity where the facade exposes only day totals; do not add production read APIs solely for tests. Cancellation/reopen tests cover new caller gaps, not every accepted installation/commit-fault site again. Existing full store and native suites remain unchanged regression gates.

## Execution and verification gates after manager acceptance

1. Parent supplies accepted receiving base/review result, integrates the allocation, verifies all22 paths and sole ownership (including the lockfile dependency edge), and dispatches this unit. No additional abstract contract prerequisite is proposed. Existing review failures/allowances remain recorded; manager names the new allocation's review allowance separately.
2. Read current root/Rust/nearest AGENTS, full plan/sprint and approved allocation. Reuse cached Rust1.95.0, `RUSTUP_AUTO_INSTALL=0`, `CARGO_NET_OFFLINE=true`, `UV_OFFLINE=true` and the assigned target at `/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-pf60-s01-20260911/codex-rs/target`. No dependency fetching, environment repair, direct cargo test or broad formatting writes.
3. Before final tests, attempt the recorded literal-scope `just fix -p codex-core -p codex-api` under a profile allowing writes only to the allocated Rust files/assigned target. Format those literal Rust files with `rustfmt --edition 2024 --config skip_children=true`; record blocked TCP lock-listener/other environmental exits honestly. Do not claim Clippy success based on formatting success.
4. Run nonzero focused selectors: `just test -p codex-api anthropic_accounting`, `just test -p codex-core --lib accounting`, `just test -p codex-core --test all accounting_anthropic`, plus the existing Anthropic payload-limit/parser, stage-one guard and role-reload selectors discovered from actual test names. Record discovered test counts before execution; a zero-match selector is not proof.
5. Run affected full `just test -p codex-api` and `just test -p codex-core`, then existing `just test -p codex-state` as the unchanged store consumer gate; normal `cargo check --offline --locked -p codex-core -p codex-api -p codex-state --lib`. Use existing test harnesses, no provider calls. If offline Core caches cannot build these crates, report the exact blocker; do not expand environment authority or substitute store-only tests for native caller evidence. Remote execution is not newly authorized: auto-env-compatible fixture design follows Core/remote-tests guidance, with unavailable platforms explicitly unrun.
6. Run both governance checkers and whitespace/new-file diff checks without editing shared ledgers. Return uncommitted literal diff, exact file line counts and SHA-256 digests, commands/exits/run IDs/pass/filter/skip/LEAK/warning counts, original failures and scope limitations in the new receipt. Parent owns review and combined receiving tests; no worker reviewer/model calls, commits/pushes or live actions.

## Remaining work and authority boundary

Approved product defaults stand: feature/collection OFF until explicitly opted in, USD exact estimates separate from billed amounts, known/unknown preservation,90-day numeric detail,365-day retained aggregate policy, native ownership/deletion and no credential/body persistence. This proposal does not reopen those decisions or ask Travis to settle file layout, scope size, a direct-first rollout, failure-latch design or bundled-source projection. Those are manager engineering decisions.

Explicit prospective acceptance of the trusted bundled direct catalog is an implementation choice using the existing native catalog authority, not approval to infer historical prices or private billing terms. If implementation instead requires paid/live execution, new credential storage/principals, negotiated billing, retrospective re-estimation, a different retention policy or public default enablement, that would be genuinely new authority and is excluded rather than silently acquired.

The90..365-day compact-only late-import guard is NOT a prerequisite for new immediately admitted sends. This unit imports no rollout/backfill data, manufactures no original source positions and never re-dates an old attempt to get past retention. Observation replay at/after the existing detail cutoff remains rejected when unsupported; very long suspended calls must fail visibly with retained unknown coverage, not bypass the guard. Accepting90..365-day late imports, including durable tombstones/deduplication and original-source provenance through expiry, remains explicit same-S02 work, not waived or implemented here.

Other providers/compaction/memory paths, complete provider reconciliation, full application native goldens, permanent deletion-fence qualification and a human-facing enablement/control remain outside this first direct route. Existing installed-store native deletion and its cross-database limitations stand. No subtree UI, invoice/balance reconciliation, S03 time-range/filter implementation, human/TUI/live/release qualification or full S02 completion is claimed. S03 stays draft.

Corbanu development skill and Core guidance shaped the direct native integration proof, bounded ownership and receipt gates; the remote-tests guidance supplies the auto-env fixture requirement only. No remote executor was started. This private file is the only authored artifact of this preparation.
