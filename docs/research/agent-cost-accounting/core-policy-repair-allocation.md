# PF-60-S02 — Core policy correction allocation

## September12 23:13 — exact test-helper continuation

Frozen first candidate835total/249non-test,100focused/575shared/normal check
pass; original eight Core errors absent. Clippy101 exposes24 emitted diagnostics
at16 unique pre-existing unwrap sites in the two already-allocated native test
files. Manager read every site/diagnostic and existing tests/all.rs expect_used
policy. Authorize ONLY those16 test assertions to become descriptive expect
assertions (or existing Result/context where directly compatible), retaining
identical failure conditions, bodies/IDs/route/retry/zero-send assertions.
No production panic, fallback/default, new lint allowance or generic helper
rewrite. Same ten paths/checkout/base,950/300target1100/375STOP; estimate40–65
extra test/receipt lines. First material review remains unspent until corrected
candidate frozen. Preserve original835/100/575/Clippy101 manifest and all failures.
Runtime and other seven source files frozen. Reformat two tests, rerun100focused,
575shared, same Clippy/check/governance and return exact corrected manifest.
Any further outside-scope errors return for classification before repair.

September12 22:44UTC. Manager accepts the implementer's inspected ten-path
proposal within existing PF-60, **Product measurement**, “No commercial
performance numbers have been supplied.” This is a bounded restoration of the
approved opt-in collector contract; OFF remains OFF. Not a fourth initiative.

## Receiving evidence and ownership

Prior eight-file correction2939d46bf7059979d614b53be5d47916fa732e0b received at
37f23b991d6d79ba782aadc50dc5652674c91d20 after clean independent review01
(confidence0.96). All eight file bytes match. Parent actual575/575, zero skipped,
29.165s, nextest ab28db7b-2ab7-4f04-8543-40c72596c5ff; normal six-library check
exit0 in37.26s. The eight Core Clippy errors remain, not a lint-clean claim.
Keep all original external-test failures, full-Core baseline/common/additional
failures and LEAK history. Old accounting workers are frozen historical sources.

Mendel moves sequentially to
`/Volumes/CorbanuDrive/Corbanu/worktrees/accounting-core-policy-20260912`, branch
`workstream/accounting-core-policy-20260912`, base
`37f23b991d6d79ba782aadc50dc5652674c91d20`. Sole Core accounting/client/turn/env
owner; PF13 owns adapter/protected-state and James private isolation tooling.
No shared Cargo/lock/BUILD/config writes. Parent returns exclusive canonical Mac
Rust target lease on dispatch, pinned1.95/offline/locked/autoinstallOFF.

## Exact scope and engineering disposition

- codex-rs/core/src/accounting.rs
- codex-rs/core/src/client.rs
- codex-rs/core/src/session/turn.rs
- codex-rs/core/src/exec_env.rs
- codex-rs/core/src/accounting_policy_tests.rs (new)
- codex-rs/core/src/accounting_tests.rs
- codex-rs/core/src/exec_env_tests.rs
- codex-rs/core/tests/suite/accounting_anthropic.rs
- codex-rs/core/tests/suite/accounting_anthropic_support.rs
- qa/portfolio/agent-cost-accounting/pf-60-s02/core-policy-repair.md (new)

Estimate795total/255non-test. Target950/300, STOP1100/375 explicitly granted
for coherent synchronization/cancellation/poison and real native proof, not an
inherited exception or permission to compress tests. Receipt/deletions count.
One NEW independent material code review and one necessary corrective pass;
prior Anthropic01–03 and policy01 remain recorded, no reset or repeated clean review.

Replace four async MutexGuard spans with a process-wide one-permit operation
gate, acquired before every operation timestamp and retained through durable
store completion. Short lexical std locks copy/publish previous under that gate;
publish attempt UUID only after successful admission. Recheck sticky failure
after acquisition. No OwnedMutexGuard evasion, lint allow, timestamp clamp,
per-owner ordering substitute, spawned write task or lock retained across HTTP.

Cancellation waiting for capacity has no DB effect; an incomplete in-operation
admit/observe marks its Sampling failed with a private completion guard and
releases capacity by RAII. Do not assert SQL cancellation rolled back a possibly
committed row. A fresh Sampling may start normally; no repair-send or speculative
retry pointer. Start cancellation publishes no Slot. Larger persistent recovery
protocol is outside this correction and returns to manager before expansion.

Slot attach/read become fallible: poison takes/rejects stale evidence (and new
attachment), leaves poison intact and returns existing fatal accounting error,
never absent/disabled collection. Drop rejects poisoned stale data without panic;
healthy Drop clears exactly as before without invalidating healthy response
evidence. Previous poison rejects rather than guessing predecessor. Propagate
the attach error at the existing turn Result boundary, no new public API.

For concrete Option<String> profile use serde_json::Value::from(profile).to_string:
Some stays escaped JSON string, None literal null. This is concrete infallibility,
not fallback or manual quoting. Preserve exact home and case-insensitive stale
scope removal; no other env callsite edits. Two existing accounting test fixture
connections may use the accepted public SQLite pool/acquire/detach/close pattern,
preserving foreign keys, close/reopen timing and schema fault capabilities.

## Required final proof and next handoff

Controlled polling/barriers prove queued and in-operation cancellation, capacity
release, checkpoint ordering, sticky poison and linear retry chains. Verify
possibly committed interrupted rows by reopen twice, no fabricated atomicity.
Actual held HTTP must allow another owner's admission/observation; native cancel
then fresh turn/resume preserves owner/attempt data and no stale Slot/send.
Preserve root/children/retry/route/307/308 zero-target and original71 cases.
Full env maps cover None/empty/quotes/backslash/control/Unicode exact roundtrips.
No source cleanup beyond ten files, credentials, live collection or price/schema
changes. Preserve all failed attempts and frozen prior candidates.

Format/fix before final tests. Run nonzero accounting/role/stage-one/exec-env
selectors, new native cases and affected shell/profile tests, full state/API/
TaskNode serial suites, normal six-library check and same six-crate existing
policy Clippy --locked --no-deps. No global -D warnings or unrelated repair;
outside-scope diagnostics return to manager. Both governance checks/diff check.
Manager reviews literal frozen diff, receives exact candidate and reruns affected
combined proof. Do not repeat full Core merely to overwrite historical failures.

Increment-specific isolated functional N/A accepted only for internal corrective
engineering: no new user control or human-ready handoff. Actual collector/replay
and S03 range/interval handoff still require separate code-blind design, enforced
binary-only executor/children boundaries and independent evidence review under
policy1.7. S02 stays in_progress, S03 draft; bounded90..365 compact-only import
is the next real feature preparation after these corrective gates, not waived.
