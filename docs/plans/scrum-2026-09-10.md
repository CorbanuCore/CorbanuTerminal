# Alex scrum — portfolio and sprint decisions

Prepared 2026-09-10 for discussion. **16 proposals, 52 draft sprints, no new
implementation allocations.** Publication on main makes the planning available;
it does not activate the backlog or declare any proposed feature finished.

## Start here

- [Latest call amendments and research assignments](call-followup-2026-09-10.md):
  native Task Node reuse, index API/replay/creator phases, sourcing and API docs.

- [Portfolio map](portfolio-2026-09-09.md): all 17 discussion threads, including
  existing security/auth, recording anchors, dependencies and proposed priorities.
- [Proposal directory](proposed/index.md): 16 plans, each linking its ordered
  sprint records, tests, acceptance flows, exclusions and decision gates.
- [Human acceptance packet](portfolio-2026-09-09.md#acceptance-packet-the-unit-the-human-reviews):
  the evidence a human reviews; these are future requirements, not executed tests.
- [Management and tester-calibration pilot](proposed/portfolio-agent-management-pilot.md):
  prove that independent testers reject obvious failures before adding builders.

## Agenda and decisions

| Discussion | Proposed outcome |
| --- | --- |
| Protect existing security/auth commitments | Confirm receiving owners, current evidence and ledger-repair owner; do not displace ongoing work |
| Choose the next available initiative | Consider PF-75 management/QA calibration and PF-60 accounting; PF-61 backend audit is an alternative if source truth blocks accounting |
| Incorporate the latest call | Broaden PF-70 to index API/replayability, with creator economics later; confirm Alex's prototype location and review the separately owned research tickets |
| Park speculative work deliberately | Select research kill tests only with a named owner, input rights and budget; no implied delivery dates |
| Make three independent initiatives operable | Agree explicit handoffs and policy integration, with one sequential sprint per initiative and shared integration seams serialized |
| Bound human load | About one hour/day: 10 minutes triage, 30 evidence review, 20 decisions; deeper reviews are additional |

Priorities and implementation roles in the drafts are proposals. The follow-up
register separately records the call's research-topic assignments. Research
and financial concepts remain evidence/paper decisions; unknown commercial,
legal, provider or data-rights inputs block implementation rather than invite
agent guesses.

## Receiving main and existing work

Publication baseline: `3cec54d9917b776bedaffb586247d7cd7c633df6`
(`docs(release): record 0.1.41 tests, PTY qualification and publication run`).
Drafts originated on the older recovery baseline
`6f8f4ce46446e951437e1ce7e7c4a526e5b9e546`; its runtime and governance are not
merged wholesale.

| Main ledger at publication baseline | State preserved by this merge |
| --- | --- |
| [P0 security](active/p0-security-levels.md) | Active; PF-27-S04 and PF-35-S01 are in progress |
| [Unified provider authentication](active/unified-provider-auth.md) | Active; receiving-main integration/release records retained |
| Proposed PF-60–PF-75 | Draft, no owners assigned by this merge, worktrees/branches/base/write scopes unallocated |

Main already has **seven sprint-checker errors**: missing PF-43/PF-44 feature
links and backlinks, plus duplicate PF-43-S01, PF-44-S01 and PF-45-S01 IDs across
existing records. This publication adds none and does not renumber old work.
The baseline had 60 current/121 archived sprints; the initial publication added
50 drafts (110 current). The latest-call amendment adds two more PF-70 drafts:
112 current/121 archived. Global sprint-checker status remains failing.
Before dispatch, reconcile the inherited ledger under its owning plans; do not
use the planning-only no-new-error check as an execution or release waiver.

Repository evidence is in `qa/portfolio/2026-09-10-main-integration/`.
The September 9 review packet remains historical evidence on the recovery
candidate, not a claim of validation on main.
The initial publication landed at `6ca801add7eeda9feb9723e7f4935fbf6b29a73b`.
Amendment evidence is separate in `qa/portfolio/2026-09-10-call-followup/`.

## Operating model

The user's approved direction is **up to three independent product initiatives,
each with sequential sprints**. Builders, independent reviewers and testers may
support a sprint; they are not extra product initiatives or overlapping writers.
The smaller two-builder PF-75 pilot is an initial calibration experiment, not a
permanent ceiling on the eventual three-initiative model.

Main still enforces two active plans, allows opt-in within-plan concurrency,
and caps reserved in-progress/blocked sprints at three globally. Its two active
security sprints must receive explicit handoffs before a sequential-only policy
can truthfully describe their allocation. This docs-only merge preserves those
rules and allocations. The user's direction is approved; the policy/checker
transition and concrete lane assignments are **not landed by this merge**.

Requested supporting capabilities, tracked here for the scrum:

- **Feature flags:** incomplete product features default off, hidden from normal
  discovery and gated at actual entry points. Merging code is distinct from
  enabling it; flag-off behavior and rollback need evidence. No runtime flag is
  added or enabled by this planning publication.
- **Private progress map:** approximately half-hour updates of workstreams,
  blockers, machine assignments, agent progress, rendered sprint Markdown and
  human test-plan links. One publisher owns the view; workers submit separate
  reports instead of editing a shared dashboard document.
- **Task Node progress:** project accepted progress/evidence into the existing
  system, not a second dispatcher. Confirm the destination, write contract and
  scoped authorization; use deduplication, retry/reconciliation and visible
  failure. Do not equate a goal update with task completion or a reward claim.
  Reuse `/tasknode`, its JSON CLI and shared profile-scoped session/capture
  infrastructure. PF-75 owns explicit task/run/machine mapping, independent
  acceptance and capture-coverage/retry qualification; PF-72 adds outcome/ROI
  attribution. Basic progress does not wait for accounting. Details are in the
  [native boundary](call-followup-2026-09-10.md#native-task-node-boundary).

The private dashboard implementation/deployment and its operations plan remain
separate from this merge. This packet does not switch its source checkout,
publish it publicly, start workers or enable live Task Node writes. Its existing
private deployment is not proof those capabilities are shipped in Terminal.

## Before the first new sprint

1. Resolve the existing ledger errors and integrate the approved concurrency
   policy with explicit handoffs; confirm a free initiative and sprint slot.
2. Confirm product authority, accountable owner and independent human acceptance
   owner; make any required product-spec amendment.
3. Pin the receiving commit, distinct machine/worktree/branch, literal write
   scope, dependencies and combined-tree integration gate.
4. Freeze the user journey, feature-flag/rollback boundary, evidence requirements,
   spend/runtime caps and external-write permissions.
5. Keep at most two packets waiting for human acceptance and at most three new
   consequential questions/day; pause new starts when the review queue is full.
   Urgent security issues still escalate immediately.

The coordinator may summarize and recommend. It cannot accept its own work,
silently repair the tester's environment, substitute a requested model, or call
an unverified outcome complete. No test system is represented as bulletproof.
