# Cancelled plans

This directory preserves product initiatives stopped before release.

A cancelled record declares `status: cancelled` and states who made the
decision, when it was made, why the work stopped, what—if anything—shipped, and
how any partial implementation was removed, disabled, or reclassified.
