---
sprint_id: "PF-83-S01"
title: "Request-correlated permission confirmation"
status: in_progress
plan_file: "docs/plans/active/p0-security-levels.md"
plan_feature: "PF-83"
execution_order: 84
owner: "Astra High permission-confirmation worker"
parallel_lane: "permission-confirmation"
write_scope: "codex-rs/core/src/session/mod.rs, codex-rs/core/src/session/turn.rs, codex-rs/core/src/session/tests.rs, codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__interrupt_exec_marks_failed.snap, codex-rs/tui/src/chatwidget/command_lifecycle.rs, codex-rs/tui/src/history_cell/approvals.rs, codex-rs/tui/src/exec_cell/model.rs, codex-rs/tui/src/exec_cell/render.rs, codex-rs/tui/src/exec_cell/live_output_tests.rs, codex-rs/tui/src/chatwidget/tool_requests.rs, codex-rs/tui/src/chatwidget/tests/approval_requests.rs, codex-rs/tui/src/exec_cell/snapshots/, codex-rs/app-server/src/request_processors/turn_processor.rs, codex-rs/app-server/src/thread_state.rs, codex-rs/app-server/src/bespoke_event_handling.rs, codex-rs/app-server/src/connection_cleanup.rs, codex-rs/app-server/src/lib.rs, codex-rs/app-server/src/settings_confirmation.rs, codex-rs/app-server/src/settings_confirmation_tests.rs, codex-rs/app-server/tests/suite/v2/thread_settings_update.rs, codex-rs/app-server/README.md, codex-rs/app-server-protocol/src/protocol/v2/thread.rs, codex-rs/app-server-protocol/schema/, codex-rs/tui/src/app.rs, codex-rs/tui/src/app_server_session.rs, codex-rs/tui/src/app/config_persistence.rs, codex-rs/tui/src/app/thread_settings.rs, codex-rs/tui/src/app/thread_routing.rs, codex-rs/tui/src/app/permission_confirmation.rs, codex-rs/tui/src/app/permission_confirmation_tests.rs, codex-rs/tui/src/chatwidget/permission_popups.rs, codex-rs/tui/src/chatwidget/settings.rs, codex-rs/tui/src/chatwidget/input_restore.rs, codex-rs/tui/src/chatwidget/tests/permissions.rs, codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__permissions_selection_history_after_mode_switch.snap, codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__permissions_selection_history_full_access_to_default@windows.snap, codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__permissions_selection_history_full_access_to_default.snap, codex-rs/tui/src/app/test_support.rs, codex-rs/tui/src/app/snapshots/, qa/reliability/live-permission-transition-20260913/, docs/sprints/current/p0-security-levels/pf-83-s01-permission-confirmation.md"
integration_gate: "Repair coordinator receives isolated candidate, audits exact scope and reruns app-server/protocol/TUI and relevant unchanged Core regressions; fresh Fable review and enforced independent functional execution/evidence required. Canonical integration remains existing Fable-owner coordinated; no merge/push/install or live session change in this allocation."
worktree: "/Volumes/CorbanuDrive/Corbanu/worktrees/live-permission-transition-20260913"
branch: "fix/live-permission-transition-20260913"
base_commit: "005cc644f59b1e762e5497b329e106c67925d4ed"
depends_on: "none"
created: 2026-09-14
updated: 2026-09-14
---

# PF-83-S01 — Request-correlated permission confirmation

## Execution mandate

- Deliver one app-server/TUI confirmation path with honest next-turn command permission scope.
- Exclude authorization-policy changes, live-turn hot swap, global quiescence, automatic approval/replay, Core lifecycle changes, dependency changes and app installation.
- Product authority: Travis's explicit expanded-scope approval; security owner's explicit PF27 reservation handoff.

## Plan linkage

- Plan: [P0 security and permission initiative](../../../plans/active/p0-security-levels.md#current-permission-confirmation-allocation--september-14).
- Feature: PF-83. Product heading **Permission selection confirmation — TO BUILD**: "A submitted selection is not a confirmed change."
- No unfinished security dependency: existing Core settings events and native lifecycle are inputs, not newly enabled protected controls.

## Code boundaries

- Exact allowed files/prefixes are front matter; pending-outcome leaf modules are planned.
- App-server processor/listener/connection state may correlate existing Core submission IDs; no Core source writes.
- v2 settings request/response types and their generated schema fixtures may support narrow opt-in confirmation; no unrelated wire changes.
- TUI permission selection, native client/response/error routing, state and tests only. Snapshots/generated changes must be listed exactly in handoff.
- Manager-reviewed cycle2 allowance: <=750 non-test and <=1750 total hand-authored source/test changed lines; generated fixtures separately mechanical. Extra100 test lines cover Fable review02 unsupported-server routing regression only; cycle1 fixes unchanged. No new paths or product scope. Stop at measured overage for staging.

## Preconditions

- [x] Active plan amended for the explicit user-approved confirmation contract.
- [x] Existing security owner returned paused PF27 reservation; no broker resumption.
- [x] Exact worktree/base recorded; no unfinished sprint dependency.
- [x] Selected scope disjoint from accounting and Task Node recorded allocations.
- [x] Both governance checkers passed before source dispatch: three active plans,116 current/126 archived sprints; PF27 note compacted to its100-line limit without dropping evidence.

## Done

- [x] Recorded source ACK insufficiency and preserved previous rejected broad proposal.
- [x] Frozen independent original F01-F11 retained; no execution/acceptance claim.
- [x] Registered request-specific confirmation before Core submission; exact Core success/no-op/error completion, independent of snapshot deduplication.
- [x] Added end-to-end deadline and listener/connection/shutdown cleanup; single deferred reply owner, ordered bounded enqueue, no uncertain-effect retry.
- [x] Preserved literal legacy `{}` acceptance; new TUI requires explicit Applied and treats acceptance-only/unsupported responses as unconfirmed.
- [x] Implemented native asynchronous requested/outcome UI, stale-result rejection, confirmed-only reviewer persistence and Core-owned next-turn settings.
- [x] Added pending-turn saved-draft deferral without automatic queue replay; no Core authorization/current-turn/MCP source changes.
- [x] Documented API and regenerated affected schema fixtures; retained all failed attempts and formatter-restoration diagnosis in repair QA.
- [x] **Guest network isolation executed and verified, 2026-09-15.** Tailscale was found connected on the guest, a second default route that a filter on `en0` alone would not have closed; it is logged out and disconnected. A default-deny packet filter now leaves only inbound SSH from the host, its reply path, DHCP with the host and host ICMP echo. The mediated inference pinhole is deliberately absent: it was opened on port 8111 and verification found Docker already listening there on the host, so it pointed the guest at an unrelated published port. The guest is sealed until a real mediator exists on a verified-unused port. All deny checks fail as required and host SSH works, reproduced after a reboot, with the filter restored at boot by a LaunchDaemon. `pflog0` does not exist on this build, so denial is evidenced by rule counters instead, which are stronger than timeouts: the block rule matched and dropped 839 packets after the reboot. [Receipt](../../../../qa/initiative-control/management-bootstrap/pf83-isolation-executed-20260915.md).
- [x] **Per-run OS enforcement implemented, reviewed and received: 32 of 175 controls valid, up from 2.** Bridge-only `sandbox-exec` enforcement, 648 lines, positives unchanged at 2 of 13, all 14 offline checks passing, `native_dispatch` still false and no case run. Independent Opus 5.0 High review found **no P1 or P2**: it traced eight newly valid controls to genuine kernel refusals, confirmed `getaddrinfo` failures stay in the EAI domain so a resolver error cannot be laundered into `EPERM`, rehashed 269 sealed files with zero mismatches, and confirmed the frozen validator, `cases.json` and the package pin are byte-identical. The manager pushback on the three firewall-blocked witnesses was implemented: loopback responders were built for `dns` and `internet_http`, then **deliberately not credited** as `supporting_only`, because a witness inside the same perimeter proves nothing about egress. `lan_tcp` is credited on a genuine but self-interface witness and is disclosed as such so 32/175 is not over-read. Seven non-blocking P3s recorded. [Receipt](../../../../qa/initiative-control/management-bootstrap/pf83-sandbox-received-20260915.md).
- [x] **Qualification standard set by the owner, 2026-09-15, and the environmental prerequisite is met under it.** The isolation exists for one purpose: the executor must not be able to read the product source. That is verified — `github.com`, `raw.githubusercontent.com` and `codeload.github.com` are denied instantly from the guest while the model endpoints are reachable, reproduced after a reboot — and the executor can run and perform inference, proven by a live smoke test through the pinned package. Those two conditions are the gate. **The 175-control preflight count is explicitly no longer the gate**, the target-containment question is withdrawn as moot, and further control-count work stops here. The manager had been treating 30 of 175 as a scoreboard; it is a rubric that does not match the objective.
- [x] **Overlap handoff to PF-60-S03, released from write_scope 2026-09-16.** This sprint reserved codex-rs/tui/src/app/event_dispatch.rs, codex-rs/tui/src/app_event.rs, codex-rs/tui/src/app/tests.rs, and the S03 inspection unit needs all three. No PF-83 action holds them: the active PF-83 work is entirely in the harness directory outside the repository, so the reservation was stale rather than contended. The three paths are released to the `accounting-pf60-s03-inspection` lane. If PF-83 needs them again it re-reserves explicitly rather than assuming.
- [x] **Native-prompt guard false positive diagnosed and repaired.** The guard stopped every run before either owner condition could be measured. The original unfiltered query returned 0 with stdout `306` — the uid 92 `_securityagent` system daemon, up since boot, not a prompt; the uid 503 query returns 1. The guard now uses the uid 503 supervisor fence, and `native_prompt_present` and `guard_query_failed` are **distinct recorded states**, both still stopping successors, because a monitor that cannot run is not evidence about the thing it monitors. The real prompt fence is unchanged: the incident it exists to prevent, a test binary prompting against the owner's personal Keychain, actually happened.
- [x] **Functional execution in the sealed guest proven, 2026-09-16.** Three cases ran end to end against the pinned package in disposable profiles: read-and-count, sort-unique-to-file under a workspace-write sandbox, and a shell command read back, all correct, with `github.com` and `raw.githubusercontent.com` denied from inside the same runs. Every earlier attempt failed for one reason: the guest was staged with a single `corbanu` binary `3e99a6cb6901` instead of the pinned package `57b1915392`, so `codex-code-mode-host` was absent and every tool call failed closed. That is a harness staging defect, not a macOS ceiling. Not claimed: these are my cases, not the frozen F01-F11 set, and I am not a code-blind executor. [Receipt](../../../../qa/initiative-control/management-bootstrap/pf83-functional-qualified-20260916.md).
- [x] **The harness stages the whole product, and a case runs against it, 2026-09-16.** `build_native.py` built one binary and `macos_adapter.stage()` staged a hardcoded two-member list, so `codex-code-mode-host` never reached the guest and every tool call failed closed. Both now derive from the verified manifest inventory, built from the same pinned source. Independent review confirmed no source disclosure, no escape and no host path reaching the guest, and its two P2s are closed: the attestation and package paths are confined to the harness, and a 322 MB `corbanu-debug` byte copy justified by a packaging policy that does not exist is removed. Rebuilt, restaged, and a real case executed in the guest with all three source hosts denied afterwards. [Receipt](../../../../qa/initiative-control/management-bootstrap/pf83-package-stage-20260916.md).
- [x] **All three unran-command falsehoods closed, 2026-09-16.** A command that never ran is no longer rendered as one that did. The completion handler was discarding `Declined` and defaulting every absent exit code to 0, so a refusal, a guardian timeout and a failed approval request all drew a green 'Ran' with '(no output)'; absence is now modelled as unknown, a decline reads 'Did not run / Approval declined', and `mark_failed` no longer stamps exit 1 on a call that may never have been admitted. Genuinely executed commands are unaffected, including an interrupted one that captured output. The first was found code-blind by our own F01/F02 run; review of its fix found the other two. Review also found a stale snapshot I confirmed myself - the worker's verification filter excluded the module that would have caught it, which is how a rendering change hides its blast radius. Attributed against the tip: 15 identical failures both sides plus 6 timeouts that pass at low load. Focused gates 185/185.
- [x] **F04 FAILS deterministically, 2026-09-16: authority survives a restriction the UI says has applied.** Four completed trials, four executions. A probe submitted into a running turn AFTER the applied message ran without approval and wrote outside the workspace; execution proven by fixture reads before and after rather than a screen line, which matters because this build renders declined commands as 'Ran'. Controlled in every trial: the same command is gated as a baseline under restriction and gated again on a fresh turn, and passes only in the window after the UI reports the restriction applied. Review narrowed my first, weaker claim to a 45ms pending window and named the trial that would decide the severity; that trial, and three repeats I ran myself, decided it. [Finding](../../../../qa/initiative-control/management-bootstrap/pf83-f04-authority-20260916.md).
- [x] **Frozen cases F01 and F02 executed code-blind over a PTY, both PASS, 2026-09-16.** The first functional results this sprint has produced. At each idle transition the level selected, the level the UI claimed effective and the probe's real behaviour were recorded separately and agreed; a submission during the downgrade's pending window was refused and wrote nothing. Independent review called the evidence stronger than most first-run functional claims: both pending windows produced real artifacts, marker absence was demonstrated by enumeration rather than asserted, approvals are auditable from a full keystroke ledger, and the probe is a verified two-way discriminator with a positive control. Scope is narrow and stated - one session, one route, pending windows of about 74 and 54 ms - and nothing generalises to admitted-turn transitions, restart or races. The evidence package was independently reviewed and corrected before being cited: the one overstated row now says refusal happened at submission and nothing was ever admitted, and every cited screen is indexed with its timestamp and raw offset.
- [x] **Both owner conditions measured true at run time, not asserted:** all three source hosts failed DNS resolution and the inference endpoints returned HTTP 405 and 401. Owner-authorized admission succeeded, recorded under its own named basis and separately from strict admission so 30 of 175 travels with the result and can never be mistaken for it.
- [x] **pf83-unran-18 source correction, 2026-09-16; functional qualification remains open.** Bounded truthful-reporting fix under product heading **Permission selection confirmation — TO BUILD**, excerpt "This is confirmation plumbing and truthful UI". Missing exit codes now become `CommandOutcome::Unknown`, not exit 0; interruption without captured output also becomes Unknown, not exit 1 plus an elapsed duration. Both previously showed "• Ran echo pending / (no output)" (green for Failed/None, red for interruption); both now show red "✗ Execution unconfirmed echo pending / No exit status received", with no exit code or duration, and stay in history. `Exited(None)` would still assert an exit. Admission cannot be inferred: app-server emits ItemStarted before approval and suppresses the canonical duplicate; ExecCall has no admission field. Silent admitted commands therefore also remain unconfirmed, never guessed to be unrun. No chatwidget.rs or app-server change was needed for this fallback; no approval/admission/decision changes. Captured-output interruption retains "Ran", output, and its existing exit-1 convention; Declined remains "Did not run / Approval declined".
- [x] **Both next-turn directions, unchanged MCP refresh and pending-approval semantics verified with focused native fixtures, received at `b28882afe`** - and in the same increment **F04 now reproduces natively for the first time**. It had only ever been shown in the sealed guest. `thread_settings_update.rs` asserts `(approvals, wrote) == (1, false)` and observes `(0, true)`; independent review confirmed the fixture genuinely reaches the applied state before the work is admitted, that ordering is enforced by request/response sequencing rather than sleeps, that same-turn admission is proven, and that the witness is a filesystem read plus approvals counted off the wire rather than a rendered line - which matters in a build that renders declined commands as "Ran". **The review also corrected me:** the specification defines the outcome as applied-to-*next*-turn and says active-turn snapshots are not retroactively changed, so steered work running under the turn's captured authorisation is *permitted*. F04 is a truthfulness and re-binding gap, not the boundary violation I published; the decision is corrected. The reproduction is committed behind an explicit `#[ignore]` with its assertion verbatim so it runs on demand and cannot turn the suite red, and it was relaxed to accept **either** valid repair - admit-then-require-approval, or refuse to steer into a superseded turn - because a guard that permits only one fix is a guard against fixing. Both directions pass; MCP is claimed only as "status responses unaffected", not as refresh equivalence, because the cross-lane equality was satisfied by construction. Gates as written: app-server settings and TUI permission both exit 0. Zero non-test Rust lines.
## Remaining
- [ ] **F05 through F11, and a route for them.** The executor's own model now refuses this class of work as a cybersecurity request and points at Trusted Access, which is the model declining rather than the sandbox, the harness or a rate limit. Raised as `pf83-post-applied-authority-20260916` alongside the finding. F03 is recorded as a display-truthfulness finding rather than a failure: the behaviour matched an outcome the case permits and the NEXT TURN qualifier was on screen, but `/status` read 'Full Access' while the active turn was still gated.
- [ ] **Re-binding is DECIDED, 2026-09-16: Travis ruled that newly admitted work in a running turn must re-bind to the latest authorisation, and that "Applied" must say what it applied to.** That reverses what F04 *is*: under the old contract - applied-to-next-turn, active-turn snapshots not retroactively changed - steered work running on the turn's captured authorisation was permitted, which is why I corrected my own claim that it was a violation. Against the new rule it is a defect, and the native reproduction already received at `b28882afe` becomes its acceptance test. Two pieces of work, not one: the **display** half needs nothing further and is mine; the **boundary** half is a change to permission code and gets frozen scope, independent review on Opus 5.0 High, and the F04 case un-ignored only when it passes for the right reason. The fixture stays relaxed to accept either valid repair - admit-then-require-approval, or refuse to steer into a superseded turn - because the rule decides the outcome, not the mechanism. Not authorised by this: retroactively revoking an already-granted approval, or altering work already running in the turn. The write scope above now includes three `core/src/session` paths, which this sprint previously excluded; that widening is Travis's answer, recorded here rather than taken quietly.
- [ ] Freeze implementation; obtain fresh Fable code review and correct substantive in-scope findings.
- [ ] Provision independent code-blind executor with enforced filesystem/process/IPC/network boundaries and negative controls; execute original cases with explicit approved-scope amendment.

## Verification

- [x] Pinned Rust1.95, isolated cache, locked/offline dependencies; formatting before final tests.
- [x] Focused app-server settings/lifecycle/public JSON-RPC cases via `just test`: final8 passed.
- [x] App-server-protocol unit/schema parity via `just test`: final8 passed.
- [x] Focused TUI permission/client/snapshot/pending-control cases via `just test`: final8 passed.
- [x] Focused unchanged Core next-turn/MCP tests via `just test`: final8 passed, no workspace-wide test.
- [ ] Real-key exact-package PTY success/cancel/failure/recovery/continuation in both disposable live repositories.
- [ ] Independent evidence check and schema-2 isolation receipts; original F01-F11 mapped, no unaccepted waiver.

## Exit evidence
- **pf83-snapshot-20, 2026-09-16 (routine snapshot/evidence correction):** on worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-def015-20260916`, branch `bootstrap/pf83-def015-20260916`, base `aff87db80be708dd20a1293c9f84da7ec7159b33`; brief SHA-256 `11ad56bc9e2b6e9b25bc95105d19e5fc3e778c075270e207e58bcd7bf8297ce7` verified. Product heading **Permission selection confirmation — TO BUILD**, excerpt "This is confirmation plumbing and truthful UI". Tool regeneration `INSTA_UPDATE=always just test -p codex-tui -E 'test(interrupt_exec_marks_failed)' --locked --offline --retries 0` passed 1/1 (run `64fcd2a2-4778-4b66-8ceb-ef3df73b1dba`); only `chatwidget/snapshots/codex_tui__chatwidget__tests__interrupt_exec_marks_failed.snap` changed, including source metadata. Full rendered text changed from `• Ran sleep 1\n  └ (no output)\n` to `✗ Execution unconfirmed sleep 1\n  └ No exit status received\n`: truthful because the fixture establishes neither admission nor output nor an exit status. Broader `INSTA_UPDATE=no just test -p codex-tui -E 'test(exec) | test(approval) | test(command) | test(lifecycle) | test(review_mode) | test(interrupt)' --locked --offline --retries 0` exited 100: 596 run, 575 passed, 15 failed, 6 timed out, 3566 skipped (run `568d62ab-9cd0-4aea-9807-4415f7abc730`); no exclusions. The first broad invocation added `--no-fail-fast` and exited 2 before testing because the wrapper already supplies it. Corrected interruption, all exec-cell tests and Unknown/Declined/captured-output regressions passed. No further stale execution-outcome snapshots found. Thirteen unrelated snapshot failures remain untouched: two command-popup branding/catalog snapshots, ten ambient-provider footer snapshots (four exec-flow, three Guardian, review queue, compact queue, pending-token activity), one status-version snapshot. Other failures: command-auth integration missing `codex` binary, slash-dispatch integration blank-viewport timeout; six app interrupt/safety-buffering/session-lifecycle tests timed out at 60 seconds (causes unestablished). Raw complete results: local ignored `codex-rs/target/nextest/local/junit.xml`; generated slash-dispatch artifacts preserved at `codex-rs/target/pf83-snapshot-20-tmux-artifacts/`. No production/test-source edit or formatter run; guarded disposable profiles only, no native prompt observed. This pass changes no user behavior; it does not perform or waive the integrator's existing independent functional/PTY/live-repository/human-acceptance gates and makes no readiness/release claim.
- Declined completion now carries `CommandOutcome::Declined` without an exit code or duration. History changes from green `"• Ran echo refused\n  └ (no output)\n"` to red `"✗ Did not run echo refused\n  └ Approval declined\n"`; expanded history reads `"$ echo refused\n✗ Did not run (approval declined)\n"`. Three new inline snapshots in `exec_cell/render.rs` cover preview/transcript and mixed read groups before/after neighbour completion; existing snapshots unchanged. Group admission remains intact. Cancellation retains existing captured output, red failure marker and exit-code-1 convention: “Ran” is justified for the tested command with captured output, but interruption alone does not prove every pending command started. No distinct model-initiated abort established; Guardian review Aborted maps to Declined.
- Baseline `just test -p codex-tui -E 'test(declined_command_history_never_claims_execution_or_success)' --locked --offline` exited 100 (both attempts failed on “Ran”; run `dc0f347e-bafc-4433-a9a0-9589af9e5286`). Intermediate `just test -p codex-tui -E 'test(exec_cell::) | test(chatwidget::tests::approval_requests::) | test(chatwidget::tests::exec_flow::)' --locked --offline` exited 100: 77/82 passed, including that regression; run `97212b94-1912-4e5e-9318-9bee4e0f1b25`. Four footer snapshots differ by “via ambient” (excluded below); the new preview/transcript test's duplicate-inline restriction was corrected with `insta::allow_duplicates!`. Four generated `chatwidget/snapshots/*.snap.new` files were removed without accepting tracked changes.
- Final command: `INSTA_UPDATE=no just test -p codex-tui --lib -E '(test(exec_cell::) | test(chatwidget::tests::approval_requests::) | test(chatwidget::tests::exec_flow::)) & not (test(preamble_keeps_working_status_snapshot) | test(unified_exec_begin_restores_working_status_snapshot) | test(unified_exec_wait_status_renders_command_in_single_details_row_snapshot) | test(image_generation_begin_restores_working_status_after_single_line_preamble))' --locked --offline --retries 0`; exit 0, 78/78 passed (3988 skipped). Regression now PASS on the final source tree. Added `declined_command_preview_and_transcript`, `declined_exploring_call_keeps_running_neighbour_visible`, and `interrupted_command_preserves_captured_output` cover status text/styles, finalization, mixed groups and cancellation output. Guarded disposable-profile tests only; no native credential prompt observed. Exact-package PTY, independent functional evidence, live-repository proof and named-human acceptance remain open; no release/install/human-test-readiness claim.
- Formatting from `codex-rs`: `rustfmt --edition 2024 tui/src/chatwidget/command_lifecycle.rs tui/src/chatwidget/tests/approval_requests.rs tui/src/exec_cell/model.rs tui/src/exec_cell/render.rs`, twice, exit 0 (stable imports-granularity warning). `git diff --check`, `git diff --numstat` and `git status --short` exited 0. `python3 docs/sprints/check.py` initially passed; an overlong draft receipt failed its 100-line cap (exit 1), then was compacted without changing prior evidence; final checker passed (exit 0; 115 current/127 archived). Diff: command_lifecycle +5/-1; approval_requests tests +45/-0; exec model +35/-8; exec render +180/-22; this sprint +5/-0. Total 301 changed lines: 120 production Rust, 176 test Rust, 5 record lines.
- Final8 support run: 350/350 passed; exact command, frozen patch/manifest hashes, 517 non-test/1250 total changed Rust lines, retained failures and limits: `qa/reliability/live-permission-transition-20260913/implementation-checkpoint.md`.
- **pf83-unran-18 evidence:** worktree `/Volumes/CorbanuDrive/Corbanu/worktrees/pf83-def015-20260916`, branch `bootstrap/pf83-def015-20260916`, base `eaae5c3d9f35d5fc772eef076128c6cd8f9dc47e`; brief SHA-256 `a6493f4ca62dc33cca102e181ceb096a72357f08476693867302b4cfa44e2b5d` verified. Baseline `INSTA_UPDATE=no just test -p codex-tui --lib -E 'test(failed_command_without_exit_code_never_claims_execution_or_success) | test(interrupted_pending_approval_never_claims_execution_or_success) | test(interrupted_command_preserves_captured_output)' --locked --offline --retries 0`: exit 100, both new regressions FAIL on "Ran", captured-output PASS (run `2ffbd790-010e-4b5a-8df9-56571fba33b5`). After scoped `rustfmt --edition 2024 tui/src/chatwidget/command_lifecycle.rs tui/src/chatwidget/tests/approval_requests.rs tui/src/exec_cell/model.rs tui/src/exec_cell/render.rs` from codex-rs (exit 0), final `INSTA_UPDATE=no just test -p codex-tui --lib -E '(test(exec_cell::) | test(chatwidget::tests::approval_requests::) | test(chatwidget::tests::exec_flow::)) & not (test(preamble_keeps_working_status_snapshot) | test(unified_exec_begin_restores_working_status_snapshot) | test(unified_exec_wait_status_renders_command_in_single_details_row_snapshot) | test(image_generation_begin_restores_working_status_after_single_line_preamble))' --locked --offline --retries 0`: exit 0, 83/83 PASS, 3988 skipped; four previously attributed ambient footer cases excluded. Both regressions now PASS; captured-output and Declined still PASS. Four new inline snapshots in render.rs: unknown preview/transcript across three sources, mixed read group before/after interruption, and retained diagnostic output. Existing snapshots unchanged; prior "Ran"/"Explored"/success text falsely implied execution. Guarded disposable profiles only, no native credential prompt observed. Scope/diff and sprint checks pass; 281 Rust changed lines (73 production, 208 tests). Independent design/execution/evidence, exact-package PTY, live-repository proof and human acceptance remain with the integrator; no human-test readiness, release, install or push claim.
- [ ] Exact candidate/tree, changed files, commands/results and independent reviews retained in repair QA.
- [ ] Receiving scope audit/combined-tree evidence; canonical integration writer coordinated separately.
- [ ] Human acceptance and release/benchmark status honestly recorded; no installed/runtime-fixed claim beforehand.
- [ ] Done/Remaining reflect results; archive only after all mandatory acceptance evidence is complete.
