# Handoff — multi-provider orchestration, 2026-07-26

Branch `feat/multimodel-native-orchestration`. Head at handoff: `fe07f93f5`.

## Read first: a correction I got wrong

`MODEL-ECONOMICS-DATA.md` (commit `fe07f93f5`) lists plan-routed models with `—`
for cost. **That is wrong and it is the dangerous kind of wrong.** Plan is not
free. Verified 2026-07-26:

- Claude Fable 5 has no bucket of its own. On Max it draws the same weekly
  allowance as every other model, capped at 50% of it, and weighs roughly
  **2x an Opus session** against that pool.
- When the weekly window is exhausted, Claude does **not stop** — it overflows
  into prepaid usage credits billed at API rates ($10/M in, $50/M out) and
  charges the card automatically.
- Agentic subagent spawning multiplies this: one task branching into 4-6
  concurrent calls multiplies consumption. That is exactly what `/spawn` does.
  Reported cases of a 5-hour Max window gone in 8 minutes.

Consequence for this work: **`agents.provider_allowlist = ["claude-plan", ...]`
does not bound spend.** It bounds *which provider* an agent may select. A
claude-plan agent that exhausts the weekly pool silently becomes metered on the
same provider ID, and the allowlist cannot see that. Do not tell the user that
config protects them from spend. It does not.

The correct mental model: plan = prepaid finite pool with silent overflow;
within the pool there is still a strong cost gradient (Fable ~2x Opus).
Fix `MODEL-ECONOMICS-DATA.md` before anyone builds allocation logic on it.

## What is committed and verified

| commit | what | verified how |
|---|---|---|
| `2f6eb1e48` | provider auth preflight before worker creation | 6 focused tests |
| `a23ee3aa7` | human sessions never wait on the shared worker provider lease | unit + live |
| `0425f897a` | COHERENCE-AUDIT.md, one-authority-per-fact audit | n/a |
| `3996f17a9` | `agents.provider_allowlist` enforced at TUI chokepoint | tests |
| `d9934bfbc` | same allowlist enforced in **core** spawn path | tests + live |
| `fe07f93f5` | model economics data (see correction above) | web research |

Live proofs performed:
- Cross-provider spawn: Kimi K3 root -> `openai/gpt-5.6-sol` child. Rollout
  metadata confirms distinct runtimes, not model self-report.
- Claude Opus 5 Plan root -> `openai/gpt-5.6-sol` (SOL_OK) and
  `claude-plan/claude-fable-5-plan` (FABLE_OK); `anthropic/claude-fable-5`
  refused with the policy message.
- Manager reachable while a foreign owner held the `kimi-code/k3` lease, at
  92,231 input tokens / 386,175 bytes (2.9x the 32k / 128KiB thresholds).

## User environment changes (not in git)

`~/.pfterminal/config.toml` — backup at `config.toml.bak-*`:
```toml
[agents]
provider_allowlist = ["claude-plan", "openai"]
[features]
multi_agent_v2 = true
```
`multi_agent_v2` was OFF by default and absent from the user's config, which is
why every orchestration fix built over ~16h was inert in their daily
environment. Qualification sessions passed only because their copied homes had
it enabled.

Immutable binary: `/tmp/pft-fixed2` (built at `d9934bfbc`).

## Open work, in priority order

1. **Fix the economics data** per the correction above, then build the module:
   `BillingClass` (plan/metered/local) derived from `ModelProviderInfo` auth
   shape, cost tier, vision flag; surface in `spawn_agent_models_description`.
   User's stated allocation intuition: prefer plans; frontier model for
   PhD-hard work, fast cheap model for grunt work *even within plans*.
2. **Catalog gap 1** — `third_party_provider_without_spawn_model_switching`
   (`core/src/tools/spec_plan.rs:1172`) returns an **empty** model list when the
   parent is on kimi/openrouter/zai/ambient/baseten/vercel. Those managers are
   told nothing and cannot discover cross-provider options. Likely meant to
   block model-only switching, not provider naming; scope it down.
3. **Catalog gap 2** — the advertised menu never consults
   `agent_provider_allowlist`, so agents are shown runtimes they will be
   refused. Discovery and enforcement disagree.
4. **Audit Fact 1** — five independent admission gates
   (execution limiter, residency, registry cap, provider lease, cooldown).
   Three raise the same `AgentLimitReached`. Collapsing them retires the
   recurring "manager unreachable" class. See `COHERENCE-AUDIT.md`.
5. `maximum_spend_usd` still has zero readers. Either build per-turn cost
   accounting or delete the field so it stops implying a guarantee.

## Traps that cost me time — do not repeat

- `pgrep -f "cargo build ..."` **matches its own command line**. I reported
  "STILL BUILDING" for 24 minutes on a dead build. Use `pgrep -f "[c]argo"`.
- A lease test is **vacuous** unless the request exceeds 32,000 input tokens or
  128 KiB. My first "passing" proof ran at 8,358 tokens and proved nothing.
  Grow context first, then verify `last_input_tokens` in
  `provider_request_state`.
- There are **two** spawn chokepoints. `ensure_native_spawn_provider_ready` is
  TUI-only; model-driven `spawn_agent` goes through core
  (`multi_agents_common.rs`). A guard in one does not cover the other.
- Authorize **before** mutating config. My first core guard ran after
  `corrected_catalog_provider` had already reassigned the provider, so a
  refused switch left the child pointed at the unauthorized provider.
- Flipping `MultiAgentV2` `default_enabled: true` adds ~7 test failures on top
  of the 9 pre-existing. Tractable but not contained; I reverted it.

## Pre-existing failures (NOT caused by this work)

`cargo test -p codex-core --lib`: 2094 passed, **9 failed**, all failing at HEAD
before any of this. Includes `catalog_v2_allows_agents_max_threads_when_feature_disabled`
and six `multi_agents::tests::spawn_agent_*`. One
(`multi_agent_v2_wait_agent_uses_configured_default_timeout`) passes in
isolation — parallel-run flake.

`cargo check --workspace --tests` now passes; it did not before (Phase 8T added
`agent_class` and never updated rollout-trace/analytics tests; fixed in
`3996f17a9`).

## Standing instruction from the user

Stop asking them to arbitrate internal product decisions they have no context
for. Research and decide. The goal is feature parity with OpenAI's orchestration
layer, biased to plan-backed routes.
