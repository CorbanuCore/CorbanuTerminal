# What: classify every proc-macro crate in the locked workspace for cfg=exec.
# Scope: the rules_rs Cargo resolver. Keep this manifest in sync with Cargo.lock.

diff --git a/rs/extensions.bzl b/rs/extensions.bzl
--- a/rs/extensions.bzl
+++ b/rs/extensions.bzl
@@ -92,6 +92,106 @@ def _split_proc_macro_deps(deps_by_triple, proc_macro_fq_crates):
         proc_macros = proc_macros,
     )
 
+# Cargo's registry index does not record target kinds, and the rules_rs
+# extension intentionally uses `cargo metadata --no-deps`. This checked list
+# is therefore the hermetic source of truth for registry proc-macro crates in
+# this workspace. A repository policy test compares it with full Cargo
+# metadata so lockfile updates cannot silently leave target-built DLLs behind.
+_LOCKED_PROC_MACRO_CRATE_NAMES = {
+    "allocative_derive": True,
+    "aquamarine": True,
+    "asn1-rs-derive": True,
+    "asn1-rs-impl": True,
+    "async-recursion": True,
+    "async-stream-impl": True,
+    "async-trait": True,
+    "aws-smithy-runtime-api-macros": True,
+    "borsh-derive": True,
+    "bytemuck_derive": True,
+    "cached_proc_macro": True,
+    "clap_derive": True,
+    "codex-app-server-protocol-noop-macros": True,
+    "codex-experimental-api-macros": True,
+    "const_format_proc_macros": True,
+    "curve25519-dalek-derive": True,
+    "darling_macro": True,
+    "derivative": True,
+    "derive_arbitrary": True,
+    "derive_more-impl": True,
+    "diplomat": True,
+    "displaydoc": True,
+    "divan-macros": True,
+    "document-features": True,
+    "dupe_derive": True,
+    "enum-as-inner": True,
+    "enumflags2_derive": True,
+    "fax_derive": True,
+    "futures-macro": True,
+    "i18n-embed-fl": True,
+    "i18n-embed-impl": True,
+    "include_dir_macros": True,
+    "indoc": True,
+    "instability": True,
+    "jiff-static": True,
+    "jni-macros": True,
+    "jni-sys-macros": True,
+    "linktime-proc-macro": True,
+    "logos-derive": True,
+    "maybe-async": True,
+    "num-derive": True,
+    "num_enum_derive": True,
+    "openssl-macros": True,
+    "pagable_derive": True,
+    "palette_derive": True,
+    "paste": True,
+    "pastey": True,
+    "pest_derive": True,
+    "phf_macros": True,
+    "pin-project-internal": True,
+    "proc-macro-error-attr2": True,
+    "prost-derive": True,
+    "rama-macros": True,
+    "ref-cast-impl": True,
+    "rmcp-macros": True,
+    "rust-embed-impl": True,
+    "rustversion": True,
+    "schemafy": True,
+    "schemars_derive": True,
+    "serde_derive": True,
+    "serde_repr": True,
+    "serde_with_macros": True,
+    "serial_test_derive": True,
+    "solana-sdk-macro": True,
+    "sqlx-macros": True,
+    "starlark_derive": True,
+    "strong_hash_derive": True,
+    "strum_macros": True,
+    "teloxide-macros": True,
+    "test-case-macros": True,
+    "test-log-macros": True,
+    "thiserror-impl": True,
+    "thiserror-impl-no-std": True,
+    "time-macros": True,
+    "tokio-macros": True,
+    "tracing-attributes": True,
+    "tracing-test-macro": True,
+    "ts-rs-macros": True,
+    "wasm-bindgen-macro": True,
+    "wayland-scanner": True,
+    "wezterm-dynamic-derive": True,
+    "wincode-derive": True,
+    "windows-implement": True,
+    "windows-interface": True,
+    "wit-bindgen-rust-macro": True,
+    "yoke-derive": True,
+    "zbus_macros": True,
+    "zerocopy-derive": True,
+    "zerofrom-derive": True,
+    "zeroize_derive": True,
+    "zerovec-derive": True,
+    "zvariant_derive": True,
+}
+
 def _generate_hub_and_spokes(
         mctx,
         hub_name,
@@ -297,5 +397,9 @@ def _generate_hub_and_spokes(
 
     proc_macro_fq_crates = set()
+    for locked_package in packages:
+        if locked_package["name"] in _LOCKED_PROC_MACRO_CRATE_NAMES:
+            proc_macro_fq_crates.add(_fq_crate(locked_package["name"], locked_package["version"]))
+
     for metadata_package in cargo_metadata["packages"]:
         if any([
             "proc-macro" in target.get("kind", [])
