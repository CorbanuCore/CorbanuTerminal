{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "recorded": {
      "description": "False when the server is not collecting, or does not recognise the route. Never an error: reporting spend must not fail the work.",
      "type": "boolean"
    }
  },
  "required": [
    "recorded"
  ],
  "title": "ThreadRecordSentModelRequestResponse",
  "type": "object"
}