# Wallet plan overnight defects

| ID | Run/story | Severity | Failed boundary | Evidence | Fix / regression | Replay |
| --- | --- | --- | --- | --- | --- | --- |
| WUX-001 | Pre-run / US-3 | P1 | Purchase result discarded x402 settlement and described queued plans as activated | Live 94×57 receipt inspection before `1f1391d01`; targeted tests | `1f1391d01` preserves settlement, reconciles account, persists latest receipt, distinguishes queued state, wraps receipt fields | Pending Run 1 and Run 6 |
| WUX-002 | Run 1 / US-3 | P1 | Receipt `Done` opened `/wallet` behind the active receipt and consumed its one-shot action | Live `pft_wallet_receipt_qual` at 69×45; repeated Done left receipt visible | `226915f4b` adds a dedicated close-receipt event that dismisses the modal before refreshing wallet; regression added | PASS on exact `226915f4b` binary at 94×57 |
| WUX-003 | Run 1 / US-5 | P1 | `/usage` advertised PfTerminal Plan usage but routed to `/wallet`, which exposed remaining tokens only | Live `pft_wallet_receipt_qual`; selecting item 1 opened the wallet | `12deacbca` adds a dedicated server-authoritative usage view exposing prepaid USDC, used, reserved, limits, remaining, resets, period, and queued plan; route and data regressions added | PASS on exact binary `d67dd68b…` at 94×57 |
| WUX-004 | Run 1 / US-5 | P1 | Usage spend and ISO boundary lines clipped inside the popup at 94×57 | Live `pft_wallet_receipt_qual` on binary `b6472fa9…`; `charge`, weekly end, and queued end were truncated | `12deacbca` wraps every variable-length usage line to the popup's 64-column content width without splitting ISO dates; wrapping regression added | PASS on exact binary `d67dd68b…` at 94×57 |
| WUX-005 | Run 2 / US-2, US-5 | P1 | `/wallet` plan totals and period boundaries clipped at 94×57 | Live exact binary `9adcb02f…`; monthly text, weekly timestamp, and queued end were truncated | Width-aware wallet header, exact used/limit totals, current/queued prepaid price, refresh timestamp, and no ISO-date splitting | PASS on exact binary `cb9f40d0…` at 94×57 |
| WUX-006 | Run 2 / US-2 | P1 | Receive surface did not state network, canonical asset, or SOL fee guidance; catalog did not identify affordable plans | Live locked-wallet inspection at 94×57 | Wallet names Solana network plus canonical USDC/SOL and fee requirement; server catalog compares atomic balance and labels/disables unaffordable tiers | Wallet header PASS on exact binary `cb9f40d0…`; catalog replay requires unlock |
| WUX-007 | Run 2 / US-8 | P1 | Fresh passcode backup overlay mislabeled the secret as `API key — masked` | Live backup entry on exact binary `9adcb02f…` | Secure-entry component now requires an explicit semantic status; wallet recovery, passcode, GPU key, and provider key callers supply the correct type | PASS for semantic label and wrong-passcode failure on exact binary `cb9f40d0…`; successful export requires the user's passcode |
| WBILL-001 | Run 3 / US-4, US-5 | P0 | Successful streaming tool rounds without upstream usage metadata were charged at the full maximum-output reservation | Live turn at 2026-07-19T04:17Z: 7 missing-usage rounds plus 1 authoritative round raised usage from 20,996 to 219,524 | `156ef2a30` keeps reservations for concurrency only, meters completed output from authoritative usage or bounded received content, releases ambiguous work, marks usage source, and refunds legacy unmetered full-reservation charges | Automated 44/44 including PostgreSQL PASS; live account restored to authoritative 17,349-token usage; Run 3 long-turn replay in progress |
| WTEST-001 | Run 3 / qualification infrastructure | P0 | PostgreSQL tests accepted the live database URL and their destructive setup cleared customer tables | Live database row count fell to zero during qualification | `156ef2a30` refuses destructive store tests unless the database name ends in `_test`; dedicated `ambient_gateway_test` created. Paid periods were rebuilt from both finalized on-chain receipts, the existing vault credential was rebound, and only authoritative usage was retained | Guard replay PASS: live period rows stayed 2 before/after; dedicated-database suite 44/44 PASS |
| WUX-008 | Run 2 / US-1 | P1 | First wallet creation entry labeled the initial secret as a confirmation | Fresh-wallet creation in `pft_wallet_fresh_basic` at 94×57 | Initial create and restore prompts now name the wallet passcode; only the second entry says confirmation | Targeted prompt regressions PASS; fresh-TUI replay pending |
| WUX-009 | Run 2 / US-3 | P1 | Basic purchase confirmation allowance text clipped at 94×57 | Fresh 20-USDC purchase confirmation in `pft_wallet_fresh_basic` | Purchase confirmation uses the same width-aware wallet line wrapping as account and receipt surfaces | Wrapping regression PASS; fresh purchase replay pending |
| WUX-010 | Run 2 / US-3, US-7 | P0 | Purchase and recovery consumed the TUI's unlock capability, so the same TUI immediately appeared unlocked elsewhere and demanded the passcode again | Fresh Basic purchase after a 5-minute unlock | `12ef935e7` makes the daemon retain the original timed capability until its absolute TTL or explicit lock while still serializing each signing operation | Wallet and daemon regressions PASS; fresh Pro purchase live replay PASS on exact binary `7ee40739…`: wallet remained `ready to sign in this TUI` after settlement |
| WCTX-001 | Run 3 / US-4 | P0 | Ambient GLM used the shared 202,752-token catalog limit although the live route rejects prompts above 101,376; auto-compaction therefore ran too late | Fresh paid Basic review failed after 4m26s with an exact 101,376-token provider error | Provider/model route contract applies 101,376 only to Ambient and PfTerminal Plan GLM; explicit user overrides still win and OpenRouter retains catalog metadata | Route and provider-switch regressions PASS; exact binary `9cbe0971…` crossed compaction without overflow; final exact-binary replay in progress |
| WTURN-001 | Run 3 / US-4 | P0 | After successful mid-turn compaction, Ambient GLM emitted an announcement of future work and stopped without performing it or returning the requested report | Exact binary `9cbe0971…`: post-compaction response ended after “I'll build on the prior review…” | Existing structured task-completion classifier is now enabled for the demonstrated Ambient and PfTerminal Plan routes as well as Kimi; unrelated chat providers remain unchanged; continuation is bounded to three assessments | Classifier and provider-scope regressions PASS; final exact-binary replay in progress |
| WBILL-002 | Run 3 / US-4, US-5 | P0 | Ambient sometimes raised a transport error after it had already delivered the SSE completion marker; the TUI accepted the completed round while the gateway released its reservation as ambiguous and charged zero | Attempt 3 produced valid tool output while the live ledger accumulated 128 released rounds with zero charge | `69dcbe2b5` records protocol completion independently of transport closure; a post-`[DONE]` error settles measured usage and closes the client response, while a pre-completion disconnect remains uncharged | Dedicated PostgreSQL suite 46/46 PASS; Attempt 4 live ledger is settling positive upstream usage per completed round |
| WBILL-003 | Run 3 / US-4, US-5 | P0 | A gateway process restart stranded open reservations in PostgreSQL, permanently reducing weekly and monthly capacity | Two orphaned Attempt 3 rows retained 126,095 reserved tokens after the owning process was gone | `8a33a10f9` atomically releases orphaned startup reservations and restores both allowance counters before accepting traffic | Dedicated PostgreSQL suite 47/47 PASS; live restart reduced fresh Basic reserved tokens to zero before Attempt 4 |
| WUX-011 | Run 2 / US-7 | P1 | Disconnect and remove completion opened refreshed wallet surfaces behind the active confirmation modal, leaving stale overlapping state and requiring extra cancellation | Live disconnect/recover/remove replay in `pft_wallet_pro` | `12ef935e7` closes the action modal first; `879841713` replaces wallet surfaces instead of stacking; `66edf7b96` refreshes wallet/model state before provider replacement | Targeted TUI regressions PASS; final-binary live disconnect/recovery replay pending Run 7 |
| WUX-012 | Run 2 / US-3, US-7 | P0 | The wallet daemon implemented every capability as one-shot even when the user explicitly selected a timed 5/15/60-minute unlock | Live Basic purchase immediately exhausted a five-minute signing grant | `12ef935e7` separates capability lifetime from operation serialization: timed grants remain usable until absolute expiry, one-action grants remain one-shot, and global lock clears all grants | Wallet crate 6/6 and wallet-daemon 2/2 PASS; fresh Pro payment left the five-minute grant live on exact binary `7ee40739…` |
| WUX-013 | Run 2 / US-1, US-7 | P1 | Wallet menu actions were stored as one-shot events, so cancelling Create, Restore, or another action made the same row inert until the menu was rebuilt | Fresh Pro create was cancelled once; selecting Create again did nothing | `b9e1ad46b` stores action factories and creates a fresh event per selection; regression invokes the same action twice | Targeted TUI regression PASS; fresh Pro create retry live PASS on exact binary `7ee40739…` |
| WIPC-001 | Run 3 / US-2, US-3, US-9 | P0 | Wallet-daemon IPC and its HTTP/RPC clients could wait forever after a peer accepted a connection but never completed a response | Final-binary attempt 5 code audit confirmed unbounded `read_line`, payment/key HTTP, blockhash RPC, and balance RPC paths | `f0ec8d266` applies operation-specific IPC deadlines, short connect/RPC deadlines, a bounded payment/key deadline, and returns ambiguity to the existing do-not-repay recovery path | Wallet 6/6, daemon 3/3, and wallet TUI 25/25 PASS; stalled-socket regression proves bounded failure; live fault replay pending Run 4 |
| WUX-014 | Run 3 / US-3, US-10 | P1 | During a network signing operation the daemon reported `locked=false`, causing the wallet UI to advertise ready-to-sign actions that could only return `operation_in_progress` | Final-binary attempt 5 code audit plus stalled-gateway daemon test | `f0ec8d266` adds backward-compatible daemon `busy` state; TUI labels the active operation, suppresses conflicting unlock/purchase/recovery actions, and keeps global Lock available | Daemon busy-state and TUI action-set regressions PASS; live replay pending |
| WTURN-002 | Run 3 / US-4 | P1 | A free-form turn explicitly requiring at least ten minutes returned a completed report after 8m11s | Pro attempt 5 at 2026-07-19T06:39:40Z–06:47:51Z | No literal timer was added to model behavior; the run is rejected and the next free-form objective must be naturally large enough to cross ten minutes while completion remains judged semantically | Attempt 5 FAIL; final-binary replay pending |
| WUX-015 | Run 2 / US-7 | P1 | Repeated Enter input could dispatch a destructive wallet confirmation more than once before its modal disappeared; the second removal reported a spurious missing-wallet error after the first had succeeded | Live cleanup of the retired Pro qualification wallet at 94×57 | `341064f0c` makes every completed selection inert, dismisses wallet purchase/disconnect/remove confirmations immediately, and makes an already-completed removal idempotent | Wallet-daemon 3/3, repeated-accept regression, and wallet TUI 25/25 PASS; final-binary live replay pending |
| WSEC-001 | Run 2 / US-8 / qualification driver | P0 | The qualification driver captured a secure recovery view while trying to automate backup/restore, placing the fresh Pro recovery material in tool output | Pro backup attempt after the 2026-07-19T06:36:20Z purchase | This was a driver-procedure breach, not a secret-view product bypass. The wallet was immediately retired, its local plan credential disconnected, local wallet removed, server key revoked, and the seven-run count reset. Future backup checks may open and acknowledge the secure view but must never capture, scrape, copy, or parse it. | FAIL. A new wallet, new plan key, and clean evidence scan are mandatory; no readiness claim may use this wallet. |
| WPROC-001 | Rejected pre-final Run 3 / qualification driver | P1 | A read-only provider prompt was obeyed for source files but the model deleted 19 pre-existing untracked Insta snapshot candidates while attempting repository cleanup | Git status before/after rejected attempt showed the `.snap.new` files disappear; the run was rejected independently for being under ten minutes | Treat model file mutations as untrusted even under a read-only objective; qualification now watches the worktree externally and rejects any mutation. The exact 19 user-owned snapshot candidates were regenerated by their existing tests and remain untracked. | Artifacts restored; exact-binary Run 3 uses a read-only prompt plus external worktree watcher. This is a qualification-procedure finding, not a wallet-product repair. |
| WTURN-003 | Run 3 / US-4, US-5 | P0 | Completion-guard attempts reset whenever a corrected continuation used another tool, so classifier false negatives created a self-sustaining paid continuation loop after valid final answers | Exact-binary rollout `019f79c4-8dc7-75d0-be38-1c0cabe7d546`: seven identical developer corrections, repeated final reports, 100 settled rounds / 5,117,485 tokens, and another ten-minute soak announced after the visible task was already complete; external driver terminated the isolated session | Completion attempts now persist across tool calls. After three incomplete/uncertain assessments PfTerminal accepts the latest response with a visible review warning instead of continuing or failing the turn. | Four state tests PASS; mock chat-provider regression with tool work after every correction and repeated classifier false negatives terminates in nine requests with `TurnComplete`, visible bounded warning, and no error. Live exact-binary replay required. |
| WTURN-004 | Run 5 / US-4, US-5, US-10 | P1 | A malformed or uncertain completion-classifier response was treated as evidence that completed work was incomplete, triggering more paid inference | Two simultaneous two-line tool turns on `fe155ab96` generated 13 settled requests and 128,200 tokens. Logs show invalid classifier JSON (`missing field decision` and non-JSON output) converted to `Uncertain`, followed by duplicate final answers and, in one TUI, the three-attempt warning. | Only a valid structured `incomplete` decision may continue a paid turn. `uncertain` accepts the response, and classifier transport/schema/parser failure accepts with a diagnostic warning instead of injecting another model-visible correction. | Four state tests and three mock-provider integrations PASS, including malformed structured output completing in exactly three requests with no correction or user warning. Live same-binary replay required. |
| WUX-016 | Run 6 / US-6 | P1 | Upgrade guidance was pushed as one unwrapped render line, so the narrow TUI clipped the sentence after `It starts … after` | Exact `98e6a977f` binary at 69×45 on the Pro no-higher-tier screen | Upgrade guidance and preservation copy now use the shared wallet wrapping boundary, which preserves words and ISO timestamps within the 64-column content width | Copy/wrapping regressions PASS; same-binary 69×45 replay required. |
| WUX-017 | Run 7 / US-7 | P1 | Credential disconnect aborted when the encrypted provider vault was unreadable, even though recover had placed the effective plan key in the legacy fallback store | Fresh copied home on `a374ab638`: disconnect reported `failed to decrypt secrets file` and the plan remained connected | `6f337693c` makes provider deletion best-effort across both stores and writes a non-secret deletion marker when an inaccessible encrypted copy cannot be removed; reads suppress that stale copy until an explicit login clears the marker | Unit regression corrupts the vault, proves effective deletion, and proves explicit relogin restores the key. Live final binary PASS: disconnect changed `/providers` to `wallet connected · no plan credential`; one-action recovery restored Pro without payment or key disclosure. |
| WTURN-005 | Run 7 / US-4, US-5 | P0 | A post-compaction progress announcement ended the paid turn because the completion classifier returned valid JSON under a provider-renamed property; the strict parser rejected it and the fail-open policy accepted the unfinished response | Exact `6f337693c` binary stopped after 3m07s with “I'll build on … then compose the final report.” Rollout recorded `task_complete`; local logs recorded `missing field decision at line 1 column 23`. Usage settled from 47,506,692 to 49,286,916 with zero reservation and no report. | Completion parsing remains model-first and structured. `db062b429` accepts a one-field object containing a declared enum under a renamed property, or an explicit completion boolean under a recognized completion field. Multiple fields, prose, unrelated booleans, and unknown enums remain invalid and cannot trigger continuation. | Four state tests PASS. Mock regressions prove both renamed-enum and boolean incomplete/complete schemas continue then accept in exactly five requests; unrelated boolean malformed output still cannot trigger paid continuation. Exact binary `2e2493f1…` live PASS: one tool request, one final response, one accepted assessment, 44,792 settled tokens, zero warning, zero extra continuation. |
| WSEC-002 | Post-run audit / US-3, US-4, US-5 | P1 | Wallet-plan account, catalog, receipt, usage, and provider-status reads constructed inconsistent HTTP clients; some lacked the signing path's remote-HTTPS enforcement and bounded timeouts | Read-only paid architecture audit on Run 7; source inspection of the five read paths | One `wallet_http` boundary validates the configured gateway origin and builds every read client with bounded connect/request timeouts plus HTTPS-only redirects for non-loopback origins | Focused HTTP policy tests and wallet TUI suite 31/31 PASS; live exact-binary replay required |
| WAUTH-001 | Post-run audit / US-4, US-7 | P1 | Legacy `provider_auth.json` replacement used truncate-and-write, so a process failure could corrupt fallback credentials | Read-only paid architecture audit; `save_provider_auth` source inspection | Write a unique restrictive temporary file, sync it, atomically rename it over the destination, then sync the containing directory on Unix | New atomic replacement regression PASS; `codex-login` 159/161 with the same two inherited vault timeouts reproduced on exact base at 158/160; live exact-binary replay required |
| WWALLET-001 | Post-run audit / US-1, US-10 | P1 | Wallet create and restore checked existence before persistence without cross-process serialization, allowing two windows to race and replace one seed with another | Read-only paid architecture audit; `Wallet::create` and `Wallet::restore` source inspection | Serialize create/restore with an operating-system file lock held across existence check, machine-secret creation, and persistence | Concurrent short-passcode regression proves exactly one winner, one `AlreadyExists`, surviving address equality, and winner unlock; full wallet suite 8/8 PASS; live two-process replay required |
| WTEST-002 | Qualification infrastructure | P1 | Two slow vault/keyring tests exceed the local nextest 60-second budget, preventing an honest all-green `codex-login` claim | Repaired tip: 159/161; detached exact base `db062b429`: 158/160 with the same `provider_api_key_login_is_provider_scoped_and_not_primary_auth` and `delete_all_provider_keys_clears_provider_entries` timeouts | Classified as inherited test-budget debt only after exact-base reproduction; no product code exception or skipped assertion added | Base checkout started at 156 GB free, reused the shared target, was removed immediately after proof, and ended at 153 GB free |
| WUX-018 | Renewed signup / US-1, US-2, US-7 | P1 | `/wallet` rendered the currently linked plan credential's usage, receipt, and paid period directly beneath a newly created wallet address without identifying the purchasing wallet or separating account-level plan state | Frozen binary `e7f74ad3…` at 94×57: new address `2YYwro8…dBhbB5` immediately appeared with the older Pro account's 49,398,438-token usage and latest receipt; no new payment occurred | `ae8de0e34` partitions a fetched plan by its server-returned `walletAddress`: a mismatch is visibly labeled as a linked plan purchased by another shortened wallet, cannot produce the local receipt or upgrade path, and leaves the current wallet eligible for a new purchase | PASS: two generalized identity/action regressions and 33/33 wallet TUI tests; exact binary `18040eeb…` at 94×57 rendered the mismatch as `Purchased by 4RBjjHA…hNxSGn · not this local wallet`, while the matching paid wallet rendered normal Pro ownership with 0 used / 50,000,000 weekly tokens; disposable copied home reclaimed after proof |
| WUX-019 | Renewed Run 2 / US-1, US-8 | P1 | Successful restore reused the create-completion event without operation identity, so the transcript falsely said `Created Solana wallet` after the user selected Restore; the error branch likewise always said `Wallet creation failed` | Exact `ae8de0e34` binary at 94×57 restored `2YYwro8…dBhbB5` from recovery material into a fresh home and loaded the correct balances/plan, but emitted the wrong operation verb | `WalletCreateFinished` now carries an explicit `WalletPersistenceOperation::{Create,Restore}` from the initiating flow through event dispatch; success and failure copy derive from that operation rather than an input phrase | Focused create/restore identity regression PASS; wallet TUI suite 34/34 and `cargo check -p codex-tui` PASS; rebuilt-binary live restore replay required |

Append findings only. A concrete phrase or input is evidence, never the repair.
